{{- if .Values.networkPolicies.enabled }}
{{/*
Allows the host pod to do its job:
  - INGRESS from runner pods (so they can connect to the host's IPC port)
    and from inside the host namespace (cluster traffic / ingress).
  - EGRESS to:
      - postgres (port 5432, in-cluster or external)
      - k8s API (443/6443, scoped via the host SA's Role to runner CRUD)
      - DNS (53)
      - HTTPS (443) — Anthropic API + future channel integrations
      - git-server StatefulSet (only when gitServer.enabled +
        workspace.backend=git-protocol — same-namespace, port from
        .Values.gitServer.port). Without this rule the host's
        @ax/workspace-git-server calls would be denied at the CNI layer
        when networkPolicies.enabled=true.
*/}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "ax-next.fullname" . }}-host-network
  namespace: {{ include "ax-next.hostNamespace" . }}
  labels:
    {{- include "ax-next.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "ax-next.selectorLabels" (dict "component" "host" "context" $) | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Runner pods → host (cross-namespace). Runners ONLY reach the IPC
    # back-channel; never the public surface (issue #39).
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ include "ax-next.runnerNamespace" . }}
          podSelector:
            matchLabels:
              ax.io/plane: execution
      ports:
        - port: {{ .Values.host.ports.ipc }}
          protocol: TCP
    # Anyone in the host's own namespace (ingress controller, port-forward).
    # Both ports — operators may forward either the IPC port (debugging
    # the runner back-channel) or the public surface (the normal /chat
    # case). Production ingress controllers should target public-http.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ include "ax-next.hostNamespace" . }}
      ports:
        - port: {{ .Values.host.ports.ipc }}
          protocol: TCP
        - port: {{ .Values.host.ports.publicHttp }}
          protocol: TCP
  egress:
    # PostgreSQL.
    - ports:
        - port: 5432
          protocol: TCP
    # k8s API server. Both 443 (ClusterIP `kubernetes` service) and 6443
    # (the actual apiserver port — Calico CNI rules don't always match
    # the DNAT'd 443→6443 hop).
    - ports:
        - port: 443
          protocol: TCP
        - port: 6443
          protocol: TCP
    # DNS.
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    {{- if and (eq .Values.workspace.backend "git-protocol") .Values.gitServer.enabled }}
    # Sharded git-server StatefulSet (workspace.backend == git-protocol).
    # Same-namespace, pinned by the git-server-experimental component
    # label. Without this rule the host's @ax/workspace-git-server traffic
    # would be denied at the CNI layer.
    - to:
        - podSelector:
            matchLabels:
              {{- include "ax-next.selectorLabels" (dict "component" "git-server-experimental" "context" $) | nindent 14 }}
      ports:
        - port: {{ .Values.gitServer.port }}
          protocol: TCP
    {{- end }}
{{- end }}
