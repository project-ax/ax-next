---
'@ax/channel-web': minor
---

Scaffold `@ax/channel-web` — a Vite + React 19 + TypeScript SPA with vitest/jsdom for component testing. First slice of the chat-UI-pulled-forward plan; subsequent tasks add design tokens, the mock backend, ported assistant-ui plumbing, and the Tide UI.
