# Patterns

## Patterns (do these)

- `2026-06-01` (TASK-106) — **The TASK-98 deferred "`/admin/connectors` dev MOCK route" follow-up shipped: `packages/channel-web/mock/admin/connectors.ts` (`adminConnectorsMiddleware`), wired into `mock/server.ts`'s handler chain.** KEY path gotcha: connector routes have NO `/api/` prefix (`/admin/connectors[/:id]`) — they mirror the REAL `@ax/connectors` routes the UI hits directly, UNLIKE the sibling `/api/admin/mcp-servers` mock. So `url.startsWith('/admin/connectors')` is the dispatch gate and it does NOT collide with the other admin mocks (those are all `/api/admin/*` or `/api/agents`). Auth is `requireSession` (auth:require-user parity — ANY logged-in user, NOT `requireAdmin`/admin-only like mcp-servers), 401 on no session. OWNER-SCOPING in the flat-file mock `Store` (which keys a collection by `id` only): row `id` = COMPOSITE `${userId}::${connectorId}`, with the connector's own slug + `userId` as separate fields; list filters `r.userId===actor.id` and maps to the wire shape; get/patch/delete resolve by `${actor.id}::${slug}` so a foreign/missing connector naturally 404s (never 403). userId FORCED from session, never the body (validateUpsert never reads `userId`); wire `id` is always the slug (mappers build it from `row.connectorId`, so the composite key + userId never leak). Mock does LIGHTWEIGHT validation (slug `^[a-z0-9][a-z0-9_-]*$`, required name/keyMode/visibility → 400) and stores `capabilities` verbatim — it does NOT re-implement @ax/connectors' full zod cap parse (mock is offline UI parity; the real route owns strict validation). Test mirrors `admin-mcp.test.ts` (real `node:http` server + `authMiddleware`, cookie `mock-session=u2`). The proxy-mode `/settings` forward (TASK-114) is a SEPARATE path — mock mode is offline. No connectors seed added (registry starts empty, create-flow drives the rest).
- `2026-05-31` (TASK-99) — **The agent-centric Settings IA shipped: the channel-web AdminShell "Settings" (user) section is now `Skills · Connectors · Credentials` (was `Connections · Keys`); `AdminTabId` user ids are `'skills' | 'connectors-user' | 'credentials'` (default active = `'skills'`). The single calm source badge is `components/SourceBadge.tsx` — `<SourceBadge source="catalog"|"private"/>` renders one `<Badge variant="secondary">Catalog</Badge>` for catalog items, NOTHING for private (absence IS the private signal).** Source is DERIVED, no new wire field: `skillSource(scope)` maps `'global'→catalog`, `'user'→private`; `connectorSource({visibility, defaultAttached?})` is catalog iff `defaultAttached===true || visibility==='shared'`. **GOTCHA: the metadata-only `ConnectorSummary` (what `/admin/connectors` LIST returns, in both @ax/connectors AND channel-web `lib/connectors.ts`) does NOT carry `defaultAttached` — only the full `Connector` does — so `connectorSource`'s `defaultAttached` is OPTIONAL; the user connector list keys the badge off `visibility` alone.** The user Connectors tab + admin connector registry both reach `/admin/connectors`, which is `auth:require-user`-gated + OWNER-SCOPED (despite the `/admin/` prefix, TASK-98) — so a non-admin lists/CRUDs their OWN connectors there; NO new `/settings/connectors` BFF endpoint was added. Admin connector default-on: the `ConnectorRegistry` form gained a `defaultAttached` Checkbox (the field already rode the upsert wire from TASK-97; flagging it flows the connector into every agent via `connectors:list-defaults`) — the connector half of the admin Catalog (skills' half is `CatalogTab` `setSkillDefaultAttached`). **The "My Skills" content was extracted into `components/skills/UserSkillsPanelBody.tsx` (one source of truth); `UserSkillsPanel` (modal) wraps it, `SkillsTab` renders it inline with `active` — keep this when touching either.** `ConnectionsTab.tsx` + its test were DELETED (skills-merge view superseded by the Skills tab + orchestrator union); its Allowed-sites (host-grants, TASK-54) card was FOLDED into `ConnectorsTab` (agent switcher + `getAllowedSites`/`revokeAllowedSite` preserved). `CredentialsTab` is a thin re-export of `KeysTab` (Credentials carry NO source badge — reach derives from attachment). The "Submit to catalog" share verb is the SANCTIONED catalog wording (card-mandated); "no catalog language" for solo users applies to the source-BADGE/scope language, not the share affordance. **TEST GOTCHA: a fresh test that drives AdminShell must stub `/settings/skills` + `/settings/skills/authored` (Skills is the default tab now, was Connections→`/api/chat/agents`); the admin connector-registry nav LABEL is now "Connector catalog" (disambiguates from the user "Connectors" tab — both would otherwise collide as duplicate accessible names).** Channel-web tsconfig type-checks `__tests__` (TASK-92), so run the FULL `pnpm -F @ax/channel-web build` to surface fixture type errors.

- `2026-05-31` (TASK-104) — **The DURABLE fix for the postgres-testcontainer teardown `57P01` flake (the TASK-94/mistakes-2026-05-31 family) shipped: `stopPostgresContainer(container)` from `@ax/test-harness`.** Every postgres-testcontainer suite now calls it in `afterAll` in place of `await container.stop()` (105 files, 106 call sites). It guards the `stop()` window with a temporary `uncaughtException`+`unhandledRejection` handler that swallows ONLY the benign 57P01 ("terminating connection due to administrator command") and re-emits anything else, then waits one macrotask tick (the socket error surfaces a tick AFTER `stop()` resolves) and detaches. **So: a NEW postgres-testcontainer test MUST use `stopPostgresContainer(container)`, not raw `container.stop()`** — raw stop() with any still-open pool reds the suite as `Tests N passed / Errors 1 error → exit 1` even though every assertion passed. Reproduced + verified locally: control (raw stop, open pool) = exit 1; helper = exit 0. The per-package `afterEach` pool-draining stays (it shrinks the window) but the helper is the deterministic backstop. NOTE: the database-postgres PLUGIN pool has its own `pool.on('error')` that LOGS the 57P01 (`msg:"database_postgres_pool_error"`) rather than crashing — that log is benign/handled (suite stays green); it shows up when a harness boots `createDatabasePostgresPlugin` and doesn't `shutdown()` the plugin before stop (e.g. `memory-strata-index-postgres/contract.test.ts`'s factory) — separate noise, NOT the uncaught flake, left as a followup.
- `2026-06-01` (TASK-105) — **TASK-104's left-as-followup handled-57P01 log noise is now FIXED: `memory-strata-index-postgres/src/__tests__/contract.test.ts`'s factory captures the per-iteration `dbPlugin` and `await dbPlugin.shutdown?.()` in `teardown`.** Why it existed: `runIndexContract` calls the backend factory once PER TEST (in `beforeEach`), and that factory does `createDatabasePostgresPlugin().init()` each time → a fresh `pg.Pool` per test. The factory never shut those plugins down, so all per-test pools stayed open until `stopPostgresContainer` stopped the container in `afterAll`; each orphaned pool's `pool.on('error')` then LOGGED a benign 57P01 (`msg:"database_postgres_pool_error"`) — **handled** noise, suite stayed green (`10 passed`), distinct from the **uncaught** 57P01 flake TASK-104 root-fixed via `stopPostgresContainer`. Verified locally: BEFORE = 10 `database_postgres_pool_error` lines (one per test), AFTER = 0; both green. **Pattern for any contract/harness test that boots `createDatabasePostgresPlugin` per-iteration: own its lifecycle — `await dbPlugin.shutdown?.()` in the same teardown that the contract already calls.** `shutdown()` is best-effort/idempotent (guards `kysely !== undefined`, `.catch(()=>{})` on destroy) so awaiting it can't throw a fresh `afterEach` error (avoids the TASK-98 whole-file-fails trap). The indexer plugin itself correctly has NO `shutdown()` (it borrows the shared pool); only the db-plugin owns a pool. Test-only change, no production touch, no new hook surface. Docker WAS available this run (testcontainer ran locally).
- `2026-05-31` (TASK-104) — **A freshly-created `git worktree` under `.claude/worktrees/` starts with STALE/incomplete `node_modules` — run `pnpm install --frozen-lockfile` IN THE WORKTREE before building/testing.** Symptoms in this run: (1) `node_modules/.bin/tsc` absent → `pnpm build` fails `tsc: not found` / `ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL`; (2) per-package `node_modules` missing → "Local package.json exists, but node_modules missing"; (3) `@ax/test-harness`/other `@ax/*` workspace deps resolve to the worktree's `packages/*` which have EMPTY `dist/` until you `pnpm build` there. Also a pre-existing hazard surfaced: `testcontainers@11.14.0` symlinks `tmp -> ../../tmp@0.2.5/...` but the lockfile override (`tmp@<0.2.6: '>=0.2.6'`, a CVE pin) only installs `tmp@0.2.6` → dangling symlink → `Cannot find module 'tmp'` breaks EVERY testcontainer test until `pnpm install` repairs the link. Fix sequence for a worktree: `pnpm install --frozen-lockfile` → `pnpm build` → then `pnpm -F <pkg> test`. `--frozen-lockfile` is safe (won't mutate the lockfile).
- `2026-05-31` (TASK-98) — **A channel-web admin UI surface reaches a host plugin's hooks ONLY through a real `/admin/*` HTTP route — never the bus directly (the bus is host-side). To give the connector registry UI a backend, `@ax/connectors` gained a `mountAdminRoutes` opt (mirroring `@ax/mcp-client` + `@ax/teams`) that registers `/admin/connectors[/:id]` routes BRIDGING the existing `connectors:{list,get,upsert,delete}` hooks; wired `mountAdminRoutes: true` in `presets/k8s`.** The route file (`packages/connectors/src/admin-routes.ts`) DUCK-TYPES the http-server's req/res (`RouteRequest`/`RouteResponse`) so it imports only `@ax/core` (I2-clean); every handler is `auth:require-user`-gated; the actor id is FORCED from the session and re-asserted AFTER the body spread (a body-supplied `userId`/`connectorId`/`id` is stripped → no owner/identity forging); 64 KiB body cap (413) + JSON-parse guard (400) + a `handleHookError` that maps the hook's `not-found`→404 / `invalid-payload`→400 and re-throws everything else (never echoes a stack). The `calls` array is built at construction and includes `http:register-route`+`auth:require-user` ONLY when mounted (so the bus surface still loads in an http-server-less context). Routes are unregistered in `shutdown()` (track the unregister callbacks) so a test re-init doesn't trip duplicate-route. TEST it by driving the handlers directly with stub req/res against a REAL store (harness with `database-postgres`+`connectors`) and a stubbed `auth:require-user` service — no need to pull http-server/auth-better into the package devDeps; pins auth-401, CRUD round-trip, cross-tenant 404, 413, 400. **`TestHarness` teardown method is `close()`, NOT `shutdown()`.** Preset gotcha (TASK-91 twin): turning on `mountAdminRoutes` changes the plugin's `manifest.calls`, so `preset.test.ts`'s `connectors!.manifest.calls` assertion must be updated to `['database:get-instance','http:register-route','auth:require-user']`.
- `2026-05-31` (TASK-98) — **`agent.mcpConfigIds` is the load-bearing agent↔backing-service binding (read by session-store, sandbox open-session, mcp-client tool-dispatcher, AND the wildcard sentinel `allowedTools=[] && mcpConfigIds=[]`). The connectors-first-class UI flip does NOT rip it out — there is NO per-agent connector-attachment store yet (TASK-97's connector-union reads the effective set from defaults∪owner's-own; the per-agent attachment is an explicit deferred follow-up). So AgentForm's connector PICKER (a Checkbox list over `/admin/connectors`) WRITES the selected connector ids straight into `mcpConfigIds` — the field stays live, only the UI changes from a raw comma-chip to a named picker.** This is the channel-web HALF of design Phase 4. Removing the chip entirely / adding a new attachment store would be out of scope and would break the live MCP + wildcard paths (half-wired). The standalone `McpServerForm` + its `lib/admin.ts` MCP client helpers (`listMcpServers` etc.) are DELETED (I4 collapse); the dev MOCK server's `/admin/mcp-servers` route is left intact (separate dev harness, out of scope) — a `/admin/connectors` mock route is a deferred dev-convenience follow-up. The McpServerForm `admin-mcp.test.tsx` is replaced by `admin-connectors.test.tsx`; the nav tab `mcp-servers`→`connectors` rename forces updates to THREE tests that assert the nav label (`AdminShell.test.tsx`, `admin-settings.test.tsx`) + the admin-agents edit test (now asserts a pre-checked connector checkbox, not an MCP text input).

- `2026-05-31` (TASK-102) — **The fix for a wall-clock-dependent "timeout fires" test in the #146/TASK-5 load-flake class is `vi.useFakeTimers()` + a test-only injectable seam, NOT a bigger sleep/looser bound.** `@ax/memory-strata`'s I10 test (`plugin.test.ts > "consolidation timeout fires warn log … (I10 bounded)"`) flaked on the push-to-main backstop: it seeded a real inbox file, set `consolidatorTimeoutMs:1`, then used `await setTimeout(100)` + `expect(Date.now()-t0 < 200)` to assert the timeout beat the REAL fs consolidation — under full-suite parallel load the event-loop starves and both wall-clock windows breach. The robust fix: drive the consolidator's clock with fake timers. The consolidator's `runConsolidation` is pure fs with NO injectable delay, so I added a test-only seam `MemoryStrataConfig.testHooks.runConsolidation?(input)` (defaults to the real fn — `const consolidate = cfg.testHooks?.runConsolidation ?? runConsolidation`; production path UNCHANGED). The test then injects a pass that hangs on a `setTimeout(PASS_MS)` we control, calls `debouncer.flush()` (force-fires the debounce), `await vi.advanceTimersByTimeAsync(TIMEOUT_MS+1)` (fires the `raceTimeout` deadline deterministically — PASS_MS never elapses), then asserts the `memory_strata_consolidator_failed` warn log. Dropped the `Date.now()` bound entirely (reaching the assert proves no-crash). Result: 20/20 green under 8-core `yes` saturation (was load-fragile). The in-package precedent is `observer.test.ts`'s "drops the run cleanly on a 30s timeout" (same `useFakeTimers` + `advanceTimersByTimeAsync` shape). Prior commit `b8a2d3ee` had only fixed the SIBLING C4 serialization test (via `onConsolidationSettleReady`), not this I10 one.
- `2026-05-31` (TASK-102) — **Infra-flake to recognize, NOT chase: `@ax/agents` `src/__tests__/migrations.test.ts` reds with `Error: Hook timed out in 10000ms` in a `beforeAll` that does `new PostgreSqlContainer('postgres:16-alpine').start()`.** This reproduces IDENTICALLY in complete isolation with ZERO load (so it's NOT the parallel-contention TASK-5 class — it's a flat testcontainers/Ryuk container-START latency exceeding vitest's default 10s `hookTimeout` in this sandbox), even when the image is present + pullable (`docker images postgres:16-alpine` shows it). The OTHER Postgres-testcontainer packages (`session-postgres`, `eventbus-postgres`, `database-postgres`, `storage-postgres`, `memory-strata-index-postgres`, `credentials-store-db`) all PASS the same run, so it's `migrations.test.ts`'s tight 10s hook budget specifically. `pnpm test` = `pnpm -r run test` BAILS on the first failing package (topological order hit `agents` early, before `memory-strata`); use `pnpm -r --no-bail run test` to get every package to report, then classify. Don't chase it for a memory-strata-only diff (cousin of the git-lfs env-artifact note, mistakes 2026-05-25 TASK-24).
- `2026-05-31` (TASK-92) — **Adding an additive field to the skill manifest that the skills store should surface = parse it from the manifest YAML; do NOT add a column.** The `@ax/skills` store does not persist `capabilities` — it re-parses `manifest_yaml` on read (`_row-mappers.ts parseCapabilities`). The manifest YAML is the single source of truth; capabilities (and now the `connectors[]` soft-dependency reference list, connectors-first-class design Phasing step 1) are DERIVED. So a new manifest field follows: (1) `@ax/skills-parser` `manifest.ts` — add to `ParsedManifest` (always-present, default `[]`), parse a TOP-LEVEL field (sibling of `sourceUrl`, NOT under `capabilities:`), validate with an inline grammar (`connectors` reuse the @ax/connectors store's id rules `/^[a-z0-9][a-z0-9_-]*$/` ≤128, count cap 64 — RE-DECLARED, never imported, I2), add a `ManifestCode`, and REJECT it nested under `capabilities:` (fail-loud, it's not a capability); `build.ts` emits it only when non-empty (round-trip parity). (2) `@ax/skills` `types.ts` — add to `SkillSummary` + `ResolvedSkill` (SkillDetail extends Summary) AND to the zod `SkillSummarySchema`/`ResolvedSkillSchema` (the hook-bus strips keys absent from the `returns` schema — forget it and skills:get/list/resolve silently DROP the field). (3) `_row-mappers.ts` — derive via a `parseConnectors(manifest_yaml)` helper wired into all 6 mappers (global/user × summary/detail/resolved); Detail mappers spread Summary so inherit it. NO migration. The skill capability block stays AUTHORITATIVE during the half-wired window (TASK-100 closes it) — connectors[] is a declared reference only. **GOTCHA (caught by `pnpm build`, NOT a per-package test): `@ax/channel-web`'s tsconfig type-checks its `**/__tests__/*.tsx` fixtures** (unlike TASK-90's note that `@ax/skills` excludes `__tests__` from tsc) — so adding a REQUIRED field to `SkillSummary`/`SkillDetail` breaks ~6 channel-web tsx fixtures (CatalogTab/SkillAttachmentsSection/SkillEditor/UserSkillsPanel `.test.tsx`) with TS2741 'Property X is missing'. `CatalogSkillSummary = SkillSummary & {tier?}` so those literals break too. Fix: add the field to every fixture literal; run the FULL `pnpm build` (not just the changed package) to surface them all — tsc stops at the first errors per file so re-run until clean.

- `2026-05-30` (TASK-87) — **The admin agent-global skill-attach validator (`@ax/agents` `skill-attachments-validation.ts validateNewAttachments`) is now aligned with TASK-86 per-skill namespacing: NO cross-skill `slot-collision` rejection.** Two agent-global attachments declaring the same bare slot (e.g. both `LINEAR_API_KEY`) ATTACH CLEANLY → they resolve to distinct `skill:<id>:<slot>` keys at runtime, no collision. The skill-vs-`agentRequiredCredentialSlots` (trusted/reserved) rejection is ALSO gone — at runtime a skill slot shadowing a trusted bare name is a benign no-op suppression (trusted wins `projectEnvMapToBareNames`), not a fatal collision, so rejecting at attach time was stricter-than-runtime. The `agentRequiredCredentialSlots` PARAM is KEPT as a documented forward-compat seam (Phase-1.5 `requiredCredentials`), now unused-for-rejection (eslint-disable `no-unused-vars` on it). This mirrors the per-user validator (`@ax/skills attachment-validation.ts`), which TASK-86 already stripped. **The genuine-validation half: as the collision check relaxed, a declared slot now flows STRAIGHT into the per-skill namespace, so the validator now hard-rejects a malformed declared slot id (`invalid-slot`) against an INLINE copy of the parser's `SLOT_RE = /^[A-Z][A-Z0-9_]{0,63}$/` — slot values are untrusted (a manifest may be model-authored); never silently namespace `skill:<id>:<garbage>`.** The `ValidationResult.code` union dropped `slot-collision` and gained `invalid-slot`; `admin-routes.ts` consumes `.code`/`.message` OPAQUELY (no exhaustive switch) so the union change is safe. To pin "distinct `skill:<id>:<slot>` keys" in the @ax/agents test, MIRROR the format string inline — do NOT import `skillCredentialEnvName` from @ax/chat-orchestrator (invariant #2). NOTE the stale TASK-86 line below ("admin validator … left as-is") is now SUPERSEDED by this row.

- `2026-05-30` (TASK-86) — **Per-skill credential-slot namespacing: how the bare-env-name requirement is satisfiable despite a flat sandbox env.** The credential-proxy substitution is VALUE-based (`@ax/credential-proxy registry.ts replaceAll` swaps `ax-cred:<hex>` wherever it appears in egress, regardless of which env-var name carried it). So the env-var NAME in the sandbox is just a placeholder vehicle. That lets you DECOUPLE: (1) the host-side credential map handed to `proxy:open-session` keys SKILL slots by `skill:<id>:<slot>` (collision-free — two skills wanting `LINEAR_API_KEY` get two distinct keys → two distinct placeholders), AGENT/trusted base creds stay BARE; (2) the proxy returns a namespaced envMap; (3) the orchestrator PROJECTS it back to BARE env-var names (`credential-namespace.ts projectEnvMapToBareNames`, trusted-wins + first-skill-wins) before threading into `proxyConfig.envMap` (the flat sandbox env). The skill reads `$LINEAR_API_KEY`. Skills share ONE flat sandbox env (no per-skill process/env scope), so two skills sharing a bare slot still collapse to ONE value there — IRREDUCIBLE; per-skill git egress stays correct because `buildGitCredentialEnv` (@ax/sandbox-protocol) uses each skill's OWN placeholder, threaded as optional `InstalledSkillSchema.credentials[].placeholder` (back-compat: falls back to `envMap[slot]`). SECURITY: a skill can't hijack a trusted bare credential — the trusted bare name always wins the env projection (a benign no-op suppression, NOT the old fatal `skill-slot-collision` terminate that locked the user out of ALL chat). **GOTCHA (caught by CI, not local): the env projection MUST be NON-DESTRUCTIVE.** `proxy:open-session`'s `envMap` can carry NON-credential entries — the preset-k8s test-proxy feeds the stub-runner script as `AX_TEST_STUB_SCRIPT` THROUGH the envMap (`presets/k8s/.../acceptance.test.ts` ~L143). A projection that REBUILDS the map from scratch (only trusted creds + known skill slots) DROPS those passthrough vars → `stub-runner: missing required env: AX_TEST_STUB_SCRIPT` → `sandbox-exit-before-chat-end` → all 3 preset acceptance canaries go `terminated` not `complete`. Fix: passthrough EVERY entry whose KEY is a valid bare env-var name (`^[A-Za-z_][A-Za-z0-9_]*$`) verbatim — that naturally excludes the namespaced `skill:<id>:<slot>` keys (they contain `:`), which get re-projected to their bare slot. Pin it with a unit test (non-credential var survives the projection). The orchestrator credential loop + `foldAuthoredSkillCaps` (`authored-egress.ts`) no longer terminate on a bare-name clash; the per-turn `skill-slot-collision` outcome reason is GONE. Slot-collision is still NOT checked at per-user/per-skill attach time (`skills/attachment-validation.ts`) — that's correct now (coexistence). The ADMIN agent-global attach validator (`agents/skill-attachments-validation.ts validateNewAttachments`) STILL rejects colliding agent-global attachments at attach time — a separate admin-UX gate, left as-is (follow-up to align it with namespacing if desired).

- `2026-05-30` (TASK-85) — **To surface per-(user,agent) authored skills on a PER-USER UI surface (no agent selector), aggregate across the caller's OWN personal agents in a route, don't add an agent dimension to the UI.** The "My Skills" panel (`UserSkillsPanel` / `lib/user-skills.ts` / `@ax/skills` `settings-routes.ts`) reads catalog skills via `GET /settings/skills`; authored skills (`skills_v1_authored`, keyed `(ownerUserId, agentId)`) are surfaced via a NEW `GET /settings/skills/authored`: `agents:list-for-user({userId: actor.id})` → filter `ownerType === 'user' && ownerId === actor.id` (the I5 boundary — never trust the agent list to be pre-filtered) → per-agent `skills:list-authored({ownerUserId: actor.id, agentId})` → keep `active`+`pending`, DROP `quarantined` (a flagged draft is not a user-facing "installed" skill) → tag each listing with `agentId`. Return a SUMMARY shape (`AuthoredSkillListing {skillId, agentId, description, status}`) — no manifest/body bytes (that's the editor's job, not a listing). **`skills:list-authored` needs NO hasService guard here — it's registered by `@ax/skills` itself, the same plugin that owns the route, so it's always present.** New literal route `/settings/skills/authored` can't collide with the `/settings/skills/:id` pattern: the http-server router (`packages/http-server/src/router.ts`) checks its exact-match Map (O(1)) BEFORE the per-method pattern scan, so a pure-literal path always wins regardless of registration order.
- `2026-05-30` (TASK-85) — **`@ax/skills` → `agents:*` soft calls must be bare `bus.hasService`-guarded and NOT declared in the manifest `calls`/`optionalCalls` — declaring even an `optionalCalls` edge forms a skills↔agents call-graph CYCLE that boot's cycle detection rejects** (because `@ax/agents` already declares `optionalCalls: skills:list-authored`/`skills:approved-caps-list`, the reverse direction). This is the SAME reason the pre-existing `agents:any-attached-to-skill` soft dep in `skills:delete` is undeclared (documented in `packages/skills/src/plugin.ts`'s manifest comment). `optionalCalls` is non-fatal-when-absent BUT "still a real call-graph edge when the producer IS present" — so it does NOT escape cycle detection. Rule of thumb: when plugin A already optionally-calls plugin B, A↔B back-calls from B must stay manifest-undeclared (hasService only).
- `2026-05-30` (TASK-84) — **The SSE `GET /api/chat/stream/:reqId` 404 on cold-respawn gated turns is a reqId-LOOKUP 404, NOT a missing-route 404 — the route is registered at HOST BOOT in `channel-web/src/server/plugin.ts` `init()` (not per-turn), so the router always matches; the 404 is `conversations:get-by-req-id` → not-found (`sse.ts` step 2) when the per-reqId bind hasn't landed at the microsecond the browser races over after the POST's 202.** `routes-chat.ts` already does a server-side early-bind (`conversations:bind-session`, AWAITED before the 202) — that narrows the window but can't close it (single-replica Postgres read-after-write lag, a best-effort bind that silently fell through, OR a host pod still coming up during a cold respawn). TASK-82 made the approval CARD durable (replays on reconnect) but the 404→manual-retry symptom persisted; **TASK-84 fixed the symptom client-side: bounded backoff/retry on the SSE OPEN in `AxChatTransport.openSseStream` (transport.ts), retrying on TRANSIENT statuses (404/425/429/502/503/504) or a network throw, 4 attempts × [150,400,900]ms, fail-fast on a real client error (401/403/400/413), abort stops the loop.** KEY safety reasoning: the SSE GET is IDEMPOTENT — it only REPLAYS the server's bounded per-reqId chunk buffer; only the POST starts/duplicates a turn — so re-opening the GET is safe, unlike a `regenerate()` re-POST (which the transport deliberately never auto-fires). This is the safe consuming half of TASK-23's same-reqId loss-free reconnect primitive. The fix is CLIENT-ONLY (one file, transport.ts) — no server/preset/chart/hook change, so no boundary review and security-checklist doesn't trigger (the stream auth/routing boundary is untouched; the same already-authenticated same-Origin GET is just re-issued, J9 ACL re-enforced each attempt). The base `HttpChatTransport.reconnectToStream` SDK hook is a SEPARATE page-reload-resume path AX doesn't wire (no stream-id wire) — out of scope for the initial-open race.

- `2026-05-30` (TASK-75) — **The resume transcript is now the `conversations_v1_transcripts` ROW store, read/written via the `conversations:get-transcript` hook + the store helpers `getTranscriptBytes`/`replaceTranscript`/`appendTranscriptLines`/`getTranscriptMaxSeq`/`getTranscriptPrefixHash` (TASK-67). To rewrite a session's resume transcript host-side (drop a turn, redact, etc.): `bytes = store.getTranscriptBytes(conversationId)` (rows ORDER BY seq joined `line+'\n'` — byte-identical to the on-disk SDK jsonl) → operate on the bytes (e.g. the pure `dropTurnFromJsonl(bytes, turnId)` in conversations/store.ts) → split back with the canonical jsonl split (`text.split('\n')`, drop the trailing empty element — same as `@ax/ipc-core` session-transcript `splitJsonlBytes`) → `store.replaceTranscript(conversationId, lines)` (DELETE+re-INSERT seq 1..N in one txn).** Do NOT reach for `workspace:list`/`read`/`apply` over `.claude/projects/**/<sid>.jsonl` — that's the pre-TASK-67 git path; `.claude/projects/` is gitignored so the glob returns empty and any rewrite SILENTLY no-ops. Seed transcript rows in tests via `bus.call('conversations:replace-transcript', ctx({userId}), {conversationId, lines})` then assert via `conversations:get-transcript`. No CAS/version arg needed — the store is single-writer-per-session and the host append guard (`fromSeq`+`prefixHash` → `resync-required`) is the fail-safe net; `getTranscriptPrefixHash` is content+order-based so the seq renumbering after a `replaceTranscript` is hash-invisible to a later resume. **The pre-TASK-67 git-jsonl read/write is fully retired from `@ax/conversations` — drop-turn was the last `workspace:*` caller, so the plugin no longer declares `workspace:list`/`read`/`apply` in its manifest `calls`.**
- `2026-05-30` (TASK-70) — **After TASK-66/67, `conversations:get` redisplay reads ONLY the display event log (`store.listEvents` → `projectEventTurns` → `reconstructAttachmentBlocks`); the legacy transcript-via-git read (`workspace:list`/`read` glob of `.claude/projects/**/<sid>.jsonl` + `parseJsonlToTurns`) is GONE.** Any test that seeds a conversation transcript by writing the runner jsonl via `workspace:apply` (path `.claude/projects/-permanent/<sid>.jsonl`, `parent:null`) + binding `runner_session_id` will now read back EMPTY turns and fail. The migration (used across `@ax/conversations` get-reconstructs-attachments, `preset-k8s` acceptance canaries, `@ax/channel-web` routes-chat + acceptance-e2e): seed via `bus.call('conversations:append-event', ctx({conversationId}), { conversationId, kind:'turn', role, payload:{ blocks } })` — one call per turn, keyed by conversationId (no runner_session_id bind needed). A raw-string user `content` maps to `[{type:'text',text}]`. The thinking/redacted_thinking filter + attachment-chip reconstruction run on the event-log read path unchanged. **The blast radius reaches TRANSITIVE dependents (`@ax/channel-web` is a conversations dependent) — a per-package local sweep misses them; CI's affected-packages run is what catches it. Run the full affected set or at least every dependent of a plugin whose read contract you change.**
- `2026-05-30` (TASK-70) — **Post-LFS commit path (for the TASK-72 walk):** a pure chat turn produces an EMPTY `/permanent` git diff and is SKIPPED — `.claude/projects/` is gitignored (TASK-67) and the transcript ships as DB rows, so `commitTurnAndBundle` stages nothing → `git diff --cached --quiet` clean → `baseline..main` empty → returns null → commit-notify skipped (trace: `[commit-trace] per-turn commitTurnAndBundle → EMPTY (no staged diff; commit-notify SKIPPED)`). Only identity/Pattern-A writes still commit/bundle, and those still go through `commitNotifyWithResync` + `resyncBaselineAndReplay` on a concurrent-writer advance (NOT removed by TASK-70 — see mistakes.md). The resume jsonl lives at `<workspaceRoot>/.claude/projects/<slug>/<sid>.jsonl` via the `scaffoldSdkProjectsSymlink` redirect (gitignored, load-bearing for path-locality not git capture). `git-lfs` is gone from the sandbox image; `git check-attr filter .ax/uploads/x` → `filter: unspecified` (nothing tracks `.ax/**`).
- `2026-05-30` (TASK-70) — **workspace-git-server LFS server fully deleted (lfs.ts + 4 routes + .lfs provisioning + the LFS-only PUT method → PUT now 405s).** PUT/PATCH against `/repos/<id>` changed 400 invalid_workspace_id → 405 unsupported_method (the only valid PUT route was lfs-storage-upload). `vitest.config.ts testTimeout` is now 30s (was the 5s default — TASK-73 flake class; the package does real-git bundle work). If you re-add any non-GET/POST/DELETE route here, re-open the method gate in `listener.ts` `handle()`.
- `2026-05-30` (TASK-74) — **Skill authoring (out-of-git Part D) is now ONE chokepoint: the agent writes a bundle into `/ephemeral/skill-draft/<id>/`, calls the `skill_propose` sandbox tool → runner executor reads+validates+ships the `skill.propose` IPC action → host `skills:propose` hook gates+stores it as a `skills_v1_authored` row (+ blob). The old `.ax/draft-skills` git workspace + `listAuthoredBundles` workspace scan are GONE.** How the pieces compose (for the TASK-72 walk "author + propose a skill, see it next turn"): (1) the `skill_propose` ToolDescriptor is in `@ax/tool-skill-propose`, registered host-side via `tool:register`, GATED on open mode (`AX_ALLOW_USER_INSTALLED_SKILLS=true`) in the k8s preset — a wildcard-scope agent sees it via the catalog wildcard, a non-wildcard tenant agent would need it injected into `allowedTools` (FOLLOW-UP, same shape as TASK-51's broker injection). (2) The hybrid gate (`propose-gate.ts` `classifyProposal`): clean-scan ∧ origin=authored ∧ zero-cap → `active`; any cap OR non-authored → `pending`; scan hit → `quarantined`. (3) The `skills:scan` SERVICE hook is the validator-skill veto's new home (moved OFF `workspace:pre-apply`, which now only does the SDK-config hard veto). (4) A proposed skill is visible NEXT turn, not this one (spawn-time discovery, §D6): the host fires `skills:proposed` (notify), the orchestrator's subscriber marks `ctx.sessionId` dirty → next turn re-spawns (replaced the old `workspace:applied` `.ax/draft-skills` trigger). The runner system-prompt's `skillAuthoringNote` tells the model this. (5) `agents:resolve-authored-skills` (the orchestrator projection) + `agents:list-authored-skills` (admin) now read `skills:list-authored` (DB), omit `quarantined`, project `active`+`pending` with `proposalDelta` (proposal − approved-caps) driving the TASK-35 approval card. Scope (ownerUserId, agentId) is host-derived from the runner's session ctx — NEVER on the `skill.propose` wire (a runner can't author into a foreign agent); `origin` is wire-fixed to `'authored'` (can't claim a more-trusted provenance). KNOWN residual (deliberate, preserves the merged lazy-approval card flow): a `pending` skill's SKILL.md BODY projects (caps-stripped, zero live reach) so its delta can fire the card — design §D3's "no bytes project until approved" is NOT fully enforced (FOLLOW-UP: fully hide pending bodies would require a different card-firing path). Bundle EXTRA files ride `files[]` INLINE in the JSON envelope (validateBundleFiles caps ≤16 files/≤512 KiB, well under the 4 MiB frame) — NOT a separate blob.put channel like artifact_publish (artifact bytes are unbounded; skill files aren't). To author a skill end-to-end in a test, boot `database-postgres + blob-store-fs + validator-skill + skills + agents + http/auth stubs` and call `skills:propose` directly (see `presets/k8s/.../acceptance.test.ts` TASK-74 canaries) — far leaner than the retired git-commit-pipeline harness.

- `2026-05-30` (TASK-76) — **Two TASK-74 follow-ups in the line above are now RESOLVED; the skill-authoring gate is fully realized.** (1) **The pending→active flip exists:** approving a gated skill's caps at the card flips its `skills_v1_authored` row via the new `skills:authored-activate` SERVICE hook (store `activate()` does a status-guarded `UPDATE … SET status='active' WHERE status='pending'` — idempotent + never un-quarantines; returns `{activated}` from `Number(res.numUpdatedRows ?? 0n) > 0`, the established kysely UPDATE-count idiom). The orchestrator's `applyAuthoredCapabilityGrant` calls it right after `skills:approved-caps-set`, hasService-gated (NOT in the manifest `calls` — same conditional-peer convention as `skills:approved-caps-set`). (2) **§D3 "no bytes project" IS now enforced:** `agents:resolve-authored-skills` threads the gate `status` ('active'|'pending'; quarantined still omitted) through, and the orchestrator splits the authored list — `activeAuthoredDraftSkills = authoredDraftSkills.filter(s => (s.status ?? 'active') === 'active')` feeds BOTH `foldAuthoredSkillCaps` (caps) AND the spawn union (body bytes + the same-id shadow set), while the FULL `authoredDraftSkills` list still feeds the approval-card path (`buildAuthoredCardPayload` reads `description`+`proposalDelta`, NEVER `bodyMd` — that's why a pending skill can stay invisible to the model yet still fire its card). KEY subtlety: the same-id shadow set (`authoredIds`) must be the ACTIVE ids, not the full list — else a pending draft sharing an id with a real default/explicit attachment would blank out the attachment without projecting itself. The orchestrator's `AuthoredResolvedSkillForOrch.status` is `?optional` (back-compat default 'active') but the agents-side `AuthoredResolvedSkill.status` is REQUIRED. (3) **Non-wildcard `skill_propose`:** added `skill_propose` to the orchestrator's `ALWAYS_ON_BROKER_TOOLS` (`withBrokerDefaults`) so a non-wildcard tenant agent gets it injected at session-open — a wildcard agent already sees it via the catalog wildcard. Tool visibility isn't a grant; the host `skills:propose` gate is the boundary. Also: `stripCapabilitiesFromFrontmatter` in `@ax/validator-skill` was dead (the agents projection caps-strips via `buildSkillManifestYaml(EMPTY_CAPABILITIES)`) and is deleted.
- `2026-05-30` (TASK-73) — **`@ax/workspace-git-core` reads of git OBJECTS must go through the real `git` binary (`cat-file blob`, `ls-tree`), NEVER isomorphic-git (`git.readBlob`/`git.listFiles`) — iso-git's `FileSystem.read()` adapter (v1.37.5, index.cjs:5038) swallows EVERY read error (ENOENT/EAGAIN/EMFILE/partial) into `null`, and `loadPackIndex`→`GitPackIndex.fromIdx({idx:null})`→`new BufferCursor(null).slice(4)` then throws `TypeError: Cannot read properties of null (reading 'slice')`.** This is a LOAD-INDUCED flake: it only fires when an `.idx` read transiently fails under the full parallel `pnpm test` gate (fd/CPU starvation across ~59 packages); solo it's invisible (0/20). `readSnapshotAt` already dodged it; TASK-73 moved the last three object-read sites (`readBlobBytes`, `workspace:read`, `workspace:list`) over too. Real `git` reads its packs atomically in a CHILD process whose fd table is independent of ours, so the parent-side transient read fault can't reach it. Deterministic repro/guard: `vi.spyOn(nodeFs.promises,'readFile')` to throw a one-shot `EAGAIN` on the first `.idx` read, then drive the hook — iso-git throws the null-slice; a real-git path is immune (the child never touches the mocked parent `fs`). Build a packfile-backed bare repo the way apply-bundle does (seed loose baseline → `git fetch` a bundle → objects land in `objects/pack/*.pack`+`.idx`) so the read actually hits `readObjectPacked`. Map `cat-file`'s not-found stderr (`does not exist in`, `exists on disk, but not in`, `: bad file`) and `ls-tree`'s `not a tree object` to the discriminated `{found:false}`/`{paths:[]}`, re-throwing anything else (never silently turn corruption into "absent"). `resolveRef`/`writeBlob`/`writeTree`/`commit`/`init` stay on iso-git — they don't read `objects/pack/*.idx`. **NB for TASK-67/TASK-70 (they rework this bundle/resume path):** the bundle round-trip in `bundle-hooks.test.ts` is LIVE on main and reds the CI backstop two ways — null-slice (now fixed) AND a 5s-testTimeout under load (now `testTimeout:30_000` in both workspace-git*/vitest.config.ts, since the suite spawns ~20 real-git processes per test). If you delete this path, delete `bundle-hooks.test.ts` + `null-slice-race.test.ts` with it.
- `2026-05-30` (TASK-71) — **New workspace package that adds a genuinely NEW third-party dependency (vs. TASK-65's all-existing-deps case): the "splice importer block only, no new snapshot entries" recipe does NOT apply — you DO need the dep's transitive tree in the lockfile.** Recipe: (1) scaffold the package (`package.json` with the new dep EXACT-pinned, `tsconfig.json` extends base + `references:[{path:"../core"}]`, `vitest.config.ts`, add to ROOT `tsconfig.json` references). (2) Run a plain `pnpm install` (NOT frozen) — pnpm adds your importer block + the new dep's `@scope/*` snapshot entries; `git diff pnpm-lock.yaml` should show ONLY your importer block + that one tree (no unrelated peer churn — if you see jose/zod peer flips, those are pre-existing warnings, not lockfile edits). (3) PROVE it: `pnpm install --frozen-lockfile` (CI's frozen gate) + `pnpm audit --audit-level moderate` (CI's audit gate, ci.yml line ~65) must both pass on the pinned range. (4) For @aws-sdk/client-s3 specifically: 0 install lifecycle hooks across its 32-package @aws-sdk/@smithy tree (verified), official Amazon maintainers, audit-clean. Scan a new dep's tree for install hooks with a node one-liner over `node_modules/.pnpm/@scope+*/node_modules/*/package.json`.
- `2026-05-30` (TASK-71) — **Wiring a NEW default-on plugin into the k8s preset breaks every `bootstrap()`-booting preset acceptance test whose config can't satisfy the plugin's `init()`.** blob-store-fs's `ensureRoot()` mkdir's its `root`; the preset's default fs-root for the `git-protocol` workspace backend is the prod path `/var/lib/ax-next/blobs` (a chart-mounted PVC in prod, EACCES in the test sandbox). The acceptance tests (`presets/k8s/src/__tests__/acceptance.test.ts`, `multi-tenant-acceptance.test.ts`, `prod-bootstrap.test.ts`) boot the REAL preset via `bootstrap({bus, plugins})` — so a new plugin's init runs for real. Fix: give each preset-booting test config an explicit field pointing at its per-test `tmp` (e.g. `blob: { backend: 'fs', root: path.join(tmp,'blobs') }`); keep the prod default correct. This is the exact "whole-branch gate catches a repo-wide break the per-package test missed" case — run the FULL package test (`pnpm -F @ax/preset-k8s test`), not just your new test file, before the PR. `acceptance.test.ts` has ~10 presetConfig blocks at two indent levels; a `replace_all` on the shared `sandbox: { namespace: 'ax-next', image: 'ax-next/agent:stub' },` anchor line (do BOTH 8- and 10-space variants) inserts the field everywhere at once.
- `2026-05-30` (TASK-71) — **A SECOND backend behind an existing storage-agnostic hook (the I1/I2 two-backend pattern, e.g. blob-store-fs → blob-store-s3, storage-sqlite → storage-postgres) RE-DECLARES the hook's I/O types + zod `returns` schemas, it does NOT import them from the sibling backend (that'd be a cross-plugin runtime import → I2 violation; eslint `no-restricted-imports`).** Each backend keeps a structurally-identical copy co-located (the ARCH-13 per-registration drift guard), plus its own canary that boots through `createTestHarness({plugins})` + round-trips the hook. The preset selects exactly ONE backend per deployment (discriminated `K8sXConfig` union + an `env`-reading `xConfigFromEnv` helper + a `validateXBackend` `_helpers.tpl` guard that `fail`s `helm template` on a misconfig). Add the new backend to: root `tsconfig.json` references, the preset `package.json` deps + `tsconfig.json` references, and the preset.test.ts "expected production plugin set" canary (the default arm). A registrar plugin importing only `@ax/core`/`zod`/a third-party SDK needs NO eslint allowlist entry.
- `2026-05-30` (TASK-71) — **Two CI security-scanner false-positive/real-positive traps a new S3 backend hits, neither caught by local build/test/lint — verify with the actual scanners before pushing.** (1) **gitleaks `generic-api-key`** fires on a NON-test source line `const keyPrefix = env.AX_BLOB_S3_KEY_PREFIX;` — the `key`-ish identifier + high-entropy uppercase env-var NAME trips it (entropy 4.08). The repo's `.gitleaks.toml` path-allowlists `__tests__/`+`*.test.ts` (so test fixtures with `secretAccessKey:'minio-secret'` are FINE), but plain `src/*.ts` is scanned. Fix: inline `// gitleaks:allow` **ON THE MATCHED LINE** (a comment on the line above does NOT work — gitleaks checks the matched line's own text). Reproduce locally: `/tmp/gitleaks git --log-opts="main..HEAD" -c .gitleaks.toml` (download the v8 binary; CI uses `git`-mode on the commit range, source-only — `dist/` is gitignored so don't worry about `dir`-scan dist hits). (2) **CodeQL `js/polynomial-redos` (high)** fires on `str.replace(/\/+$/, '')` (trailing-slash strip) — the anchored `+`-quantified regex backtracks on a many-slash string. Fix: replace with a non-regex reverse char-scan (`while(end>0 && s.charCodeAt(end-1)===47) end--`). CodeQL's `Analyze (js-ts)` STEP passing ≠ clean — the separate `CodeQL` CHECK fails the PR on new high alerts; fetch them via `gh api repos/<o>/<r>/code-scanning/alerts?ref=refs/pull/<n>/head&state=open`. Both are config/security fixes the unit suite can't see; add a regression test for the ReDoS (assert a 100k-slash input returns fast).
- `2026-05-30` (TASK-71) — **MinIO-in-kind for S3 dev parity: a ~90-line hand-rolled Deployment(emptyDir)+Service(ClusterIP)+Secret(lookup-stable random password)+post-install mc-bucket-create Job beats the Bitnami MinIO subchart** (no extra chart dep, no Bitnami flaky-index dependency). Gate everything on `minio.enabled` (default false; kind-dev-values flips it true alongside `blob.backend: s3`). The host deployment wires the MinIO Secret's root-user/password into AX_BLOB_S3_ACCESS_KEY_ID/_SECRET_ACCESS_KEY via `secretKeyRef` (never a literal) and the endpoint via a `ax-next.minioEndpointUrl` helper; the GCS PROD path sets `blob.s3.endpoint=https://storage.googleapis.com` + OMITS the static keys entirely (SDK default provider chain = Workload Identity, no static keys in the tree). Chart render tests live in `deploy/charts/ax-next/__tests__/` (filter `@ax/chart-tests`), gated by `resolveHelmGate`/`AX_REQUIRE_HELM` — copy render.test.ts's helmTemplate/helmTemplateExpectFailure/helmRepoSync helpers; the CI helm-render lane runs them with `AX_REQUIRE_HELM=1`. Helm hook Jobs (`helm.sh/hook: post-install`) are EXCLUDED from `helm template` output, so assert on Deployment/Service/Secret, not the Job, in render tests.

- `2026-05-30` (TASK-79) — **The `skill_propose` manifest contract is `parseSkillManifest` (`@ax/skills-parser/src/manifest.ts`) and NOTHING else — align docs to the parser, never the reverse.** The canonical frontmatter is `name:` (lowercase slug `^[a-z][a-z0-9-]{0,63}$`, the parser maps it → `ParsedManifest.id`), `version:` a non-negative INTEGER (NOT semver; defaults to 0), and capabilities nested under a `capabilities:` MAPPING (`allowedHosts`/`credentials`/`mcpServers`/`packages`). The TASK-74 tool description in `@ax/tool-skill-propose/src/descriptor.ts` originally told the model to write `id` + a SEMVER `version` + TOP-LEVEL cap keys — both wrong: `id` → `invalid-name` ("malformed" + the 120s retry/timeout the TASK-72 walk hit). **The security bite (TASK-79): the parser read caps ONLY from `doc['capabilities']`, so top-level `allowedHosts`/`credentials` were SILENTLY DROPPED → the proposal parsed to ZERO caps → the hybrid gate (`classifyProposal`, `propose-gate.ts`) saw origin=authored + zero-cap → `active`. A cap-bearing Linear skill installed as a zero-cap ACTIVE skill with NO approval card — the capability-LOSS bypass.** The gate itself was correct; the bug was upstream (the parser dropping instead of flagging). **Fix lives at the parser (single source of truth): `parseSkillManifest` now hard-rejects (`invalid-manifest`) ANY of `allowedHosts`/`credentials`/`mcpServers`/`packages` appearing at the TOP LEVEL, so a misplaced/stripped-cap manifest is a loud structural reject the author must re-draft — never a silent zero-cap active. Defense-in-depth that holds even if the docs drift again.** `buildSkillManifestYaml` (the inverse) already always nests under `capabilities:`, so the round-trip + every existing fixture (authored-skills/promote/admin tests all use `capabilities:\n  allowedHosts:`) stays green; the only top-level-caps manifest in the tree is the TASK-79 security repro that expects rejection. To prove the bypass pre-fix: stash the parser change, rebuild `@ax/skills-parser`+`@ax/skills`, and the propose-chokepoint test returns `{skillId:'linear',status:'active'}` for the top-level-caps manifest (it should reject). The runner executor's split-error wording ("frontmatter fence (id/description/version)") was also corrected to `name/description/version`.
- `2026-05-25` — **For a guard test that must run on EVERY PR regardless of which package changed (e.g. a Dockerfile-content/repo-root-file assertion that isn't owned by any pnpm package), put it in `scripts/__tests__/*.test.js`.** CI's PR test step gates package tests behind `pnpm --filter "...[$BASE_SHA]"` (affected + dependents), but it then runs `pnpm test:eslint-rules` and `pnpm test:scripts` (`vitest run --root scripts`) UNCONDITIONALLY (ci.yml lines ~60-61). So a guard in `@ax/agent-claude-sdk-runner` would be SKIPPED on a PR that touches only `container/agent/Dockerfile` (the ARCH-11 path-reach/affected-selection trap), but a guard in `scripts/__tests__/` always runs. Resolve the target file repo-root-relative from `import.meta.url` (`join(dirname(fileURLToPath(import.meta.url)), '..', '..', ...)`), NOT cwd, so it's worktree-safe. The `scripts/` vitest root has NO config — `vitest run --root scripts` picks up `**/*.test.js` by default; existing example `scripts/__tests__/memory-write-target.test.js`. (TASK-19's `dockerfile-home-bin-path.test.js`.)
- `2026-05-25` — **The agent's RUNTIME `$HOME` is `/permanent` (the git-bundled workspace tier), NOT the image's `/home/axagent`.** `agent-claude-sdk-runner/src/main.ts` sets `HOME: env.workspaceRoot` (default `/permanent`, env.ts:105) + `cwd: env.workspaceRoot` on the SDK subprocess. And the SDK's Bash tool runs a **non-interactive** shell whose PATH comes ENTIRELY from the explicit `query({options:{env}})` allowlist — `proxy-startup.ts` forwards `process.env.PATH` and comments that without it the Bash tool's `ls` exits 127 (proof: no rc file is sourced, else a default PATH would exist). Consequences: (1) a `.bashrc` baked into the image is NOT read by the agent's shell — to actually affect the agent's PATH you must prepend to the forwarded env (the `buildToolCacheEnv`/`buildPythonVenvEnv`/`buildHomeBinEnv` layers in main.ts's env literal); (2) "persists between sessions" = under `/permanent` (bundled + restored), so `$HOME/bin` = `/permanent/bin`. The env literal can't reference its own earlier keys, so to layer a new PATH segment relative to the venv bin, compute the venv env in a const BEFORE the `query()` call and feed its `.PATH` (or the base PATH when venv off) into the new layer spread LAST. (TASK-19.) **SECURITY (codex Phase-5): APPEND a model-writable persisted dir to PATH, never PREPEND** — `$HOME=/permanent` is restored across sessions, so a leading entry lets an injected `/permanent/bin/git` (etc.) persistently shadow trusted image/venv tools (I5 violation). The venv bin DOES prepend (correctly — it's seeded + session-ephemeral, and python/pip must win), but `$HOME/bin` appends so trusted bins win on name collisions.
- `2026-04-23` — When adding a new service hook, fill in the boundary-review block in the PR description (alternate impl / leaky field names / subscriber risk / wire surface). If you can't name an alternate impl, it probably shouldn't be a hook yet.
- `2026-04-23` — When scaffolding a plugin that touches fs/net/process/untrusted input, drop a `SECURITY.md` in the package directory at scaffold time, not later. Week 3's `packages/storage-sqlite/SECURITY.md` is the template.
- `2026-04-23` — For ESM "am I the main module?" checks, use `import { pathToFileURL } from 'node:url'` and compare `import.meta.url === pathToFileURL(process.argv[1]).href`. `require.main === module` doesn't exist under ESM.
- `2026-04-29` — When reading a Claude Agent SDK jsonl by sessionId, glob `<HOME>/.claude/projects/*/<sessionId>.jsonl` rather than reconstructing the encoded-cwd directory. The SDK encodes `realpath(cwd)`, not the literal cwd, so any symlink (e.g. macOS `/var` → `/private/var`) breaks reconstruction. SessionId is unique across project dirs.
- `2026-04-29` — When parsing SDK jsonl, whitelist turn-bearing entry types (`user`/`assistant`/`tool`) rather than blacklisting bookkeeping types. The SDK adds new bookkeeping kinds (`queue-operation`, `last-prompt`, `attachment`) over time; a blacklist drifts.
- `2026-05-03` — When a PR wires a new plugin into the k8s preset, the chart-shape test (`deploy/charts/ax-next/__tests__/env-shape.test.ts`) is the contract that catches missed env-var stamping. The test scans `presets/k8s/src/index.ts` for `env.*` reads and asserts both directions against the rendered deployment. Run it; if it fails, the chart's env block is incomplete — fix in the same PR.
- `2026-05-03` — Subcommands that need an HTTP front door should register routes via `http:register-route` against `@ax/http-server`, not spin up their own `http.createServer`. Mirror the channel-web / auth-oidc / teams pattern: a plugin with `manifest.calls: ['http:register-route']`, routes registered in `init()`. Multiple listeners on the same pod cause `EADDRINUSE` collisions and bypass the http-server's CSRF / body-cap subscribers.
- `2026-05-24` — Adding a NEW field to `PluginManifestSchema` must be `.optional()` (NOT `.default([])`) if you don't want to touch every existing in-code manifest. `PluginManifest = z.infer<schema>` uses the schema's *output* type, and a `.default([])` field is **required** in the output type — so all ~20 hand-authored manifest literals fail tsc with "Property X is missing". `.optional()` makes it `X?:` in the inferred type (additive, zero churn); have `bootstrap` treat the absent value as empty (`manifest.optionalCalls?.map(...) ?? []`). This is why `calls`/`registers`/`subscribes` have `.default([])` yet don't error — every manifest already spells them out explicitly.
- `2026-05-24` — `pnpm` is NOT on the orchestrated-agent PATH; only `node` + `corepack` are (under `~/.nvm/versions/node/<v>/bin`). Run pnpm as `corepack pnpm <args>`. But the root `test` script does `pnpm -r run test` internally, and that nested invocation can't find `pnpm` → `sh: 1: pnpm: not found`. Fix: drop a 2-line wrapper (`#!/usr/bin/env bash\nexec corepack pnpm "$@"`) into a tmp dir and prepend it to PATH so nested `pnpm` resolves. (Alternatively `corepack enable --install-directory $(dirname $(which node))` installs a real `pnpm` shim on PATH.)
- `2026-05-24` — When a hook payload schema is duplicated across plugins (and drifting), the sanctioned fix is a pure `@ax/*-protocol` package: zod schemas + inferred types, NO `@ax/core` dep, mirror `packages/workspace-protocol` exactly (package.json/tsconfig/vitest.config), register in root `tsconfig.json` `references`, and add `'!@ax/<name>'` to the `no-restricted-imports` group in `eslint.config.mjs` (plus the doc-comment + trailing message string). Backends `safeParse` the shared schema at their boundary (keep their own `PluginError` wrapping); a constructor-only consumer (the orchestrator) imports the inferred TYPES only (type-only import). Converge drifted variants UP to the stricter one. `@ax/sandbox-protocol` (ARCH-5) joined `@ax/ipc-protocol` / `@ax/workspace-protocol` this way.
- `2026-05-24` — Same `@ax/*-protocol` wiring recipe also applies when relocating **git-vocabulary hook-bus payload TYPES** out of the storage-neutral `@ax/core` kernel (ARCH-3 → `@ax/workspace-bundle-protocol`, holding the 4 optional bundle-wire types `WorkspaceApplyBundle*`/`WorkspaceExportBaselineBundle*`). TWO differences from the zod `*-protocol` packages: (1) it exports **pure TS types, NO zod** — the contract is an in-process `bus.call<In,Out>`/`registerService<In,Out>` generic, not an untrusted wire (the untrusted runner↔host wire is already `@ax/ipc-protocol`, which DOES zod-validate; git backends re-derive `baselineCommit` from their own scratch repo), so zod here would be dead validation; (2) it **DOES depend on `@ax/core`** (type-only) because the bundle types reference neutral `WorkspaceVersion`/`WorkspaceApplyOutput`. Decision rule for git-vocab on the workspace surface: *wire* shape → `@ax/ipc-protocol`; *hook-bus* type → its own git-runner `*-protocol` package; never `@ax/core` (I1 / arch §4.5 forbids sha/branch/bundle/ref/commit there). Optional-hook mechanism untouched — non-git backends register neither hook and never import the package.
- `2026-05-24` — Codex CLI **IS installed + authed in this orchestrated env** (`codex` on the nvm PATH, `~/.codex/auth.json` present, no `OPENAI_API_KEY` needed) — the ARCH-4/5 "codex unavailable" notes were per-run, re-check don't assume. Drive yolo-ship Phase 5 with `node "$(ls -d ~/.claude/plugins/cache/openai-codex/codex/*/ | sort -V | tail -1)scripts/codex-companion.mjs" review --wait --scope branch --base main` from the worktree. The companion's OWN verification shell lacks `corepack`/`pnpm`/`tsc` on PATH; it self-recovers (prepends `~/.nvm/.../bin`, calls `./node_modules/.bin/tsc`/`eslint`) and completes fine.
- `2026-05-24` — **CANONICAL (TASK-7): agents write + commit `.claude/memory/` ONLY in their own worktree/branch copy, never the shared main checkout.** A concurrent yolo-ship agent in the SHARED main checkout once overwrote `.claude/memory/decisions.md`, silently dropping appended rows (it rewrote the file with its own newest entries); parallel auto-ship agents writing the shared checkout race. The branch copy is race-free: memory is tracked so `git worktree add` carries it in, rows land on PR merge via the serialized queue, and a true clash becomes a visible git merge conflict, not a silent drop. This is now the standardized rule in CLAUDE.md "Codex Memory Bootstrap" + the `claude-memory` skill, and it **supersedes** the older decisions-45/52/55/60/67/73 "write decisions.md to the main checkout" guidance (which predated memory being committed, when a fresh worktree's copy was empty — it no longer is). Helper: `scripts/memory-write-target.sh` prints the right dir for your tree and (`--check`) fails if you're in the shared main checkout while linked worktrees exist.
- `2026-05-24` — The env lacks `git lfs`, so `@ax/agent-claude-sdk-runner`'s 24 `git-workspace` tests fail locally with `git: 'lfs' is not a git command` — a local-env gap, NOT a regression (CI has git-lfs). Build the whole workspace before running any package's tests — sibling devDeps resolve via their `dist/` `exports`, so an unbuilt dep fails with `Failed to resolve entry for package "@ax/..."`.
- `2026-05-24` — **Testcontainers tests are UNRUNNABLE locally in the orchestrated env** — `/var/run/docker.sock` is `root:docker` and the agent user (`vpulim`) is NOT in the `docker` group (no passwordless sudo, and `dangerouslyDisableSandbox` doesn't help — it's a host socket permission, not the sandbox). Any test that boots a `PostgreSqlContainer` (preset-k8s `acceptance.test.ts` Phase D/F + ARCH-8 `prod-bootstrap.test.ts`) fails with `Could not find a working container runtime strategy` — a local-env gap, NOT a regression (CI has Docker; these suites are green on main). To verify the parts you DO control without Docker: extract the non-DB logic (config build, plugin filter/splice, manifest shape, duplicate-registrant check, env-loader) into a throwaway `.mjs` run with `node` against the built `dist/` — proves the assembly is well-formed; the actual `bootstrap()`-against-Postgres step is CI's job.
- `2026-05-24` — **SHARED-WORKING-COPY HAZARD: parallel auto-ship agents can all operate on the SAME main checkout `/home/vpulim/dev/ai/ax-next` and stomp each other's `git checkout`.** ARCH-8 created its branch with `git checkout -b` in the main copy; a concurrent ARCH-2 agent then `git checkout`ed ITS branch in the same copy, so HEAD silently moved off ARCH-8's branch and `git diff main...HEAD` showed ARCH-2's files. The fix: as soon as you notice you're sharing the main checkout (or proactively at Phase 0 if `EnterWorktree` errored with "already isolated"), create a DEDICATED `git worktree add .claude/worktrees/<TASK> <branch>` and do ALL remaining work there with absolute paths + `git -C <worktree>`. Your commit is safe on your branch ref regardless of HEAD; verify with `git diff main...<your-branch>` (three-dot, merge-base) NOT `main...HEAD`. Never `git stash`/`commit -a` in the shared copy — you'll capture another agent's files.
- `2026-05-24` — When driving CI green, the GitHub API for a freshly-pushed commit can serve **wildly inconsistent state across replicas**: `gh run view <id> --json status`, the PR `statusCheckRollup`, and even the per-job `/actions/jobs/<id>` endpoint each flapped between `in_progress` and `completed/success` *for the same commit at the same wall-clock time* — a forward replica says done while a lagging one (and `gh pr view --json mergeStateStatus`) still says UNSTABLE. A single "success" read (or even a Monitor that confirms 3-4× on ONE endpoint) is NOT trustworthy; the monitor process and the foreground shell hit different replicas and disagree. Trust the **immutable** signal: a job's `completed_at` timestamp (null until truly done, never reverts) and the test job's own *step* progression (`Test (affected packages + dependents)` → `Test (full suite)` → `pnpm audit`). Cross-check two endpoints and require agreement; treat any disagreement as "not done." Don't post a premature `CI green` heartbeat off a flapping read.
- `2026-05-24` — The CI `test` job runs a 3-step sequence: `Test (affected packages + dependents)` → `Test (full suite)` (often `skipped` when affected-scope already covered it) → `pnpm audit --audit-level moderate`. The whole job took ~6–14 min on `@ax/core`-touching diffs. The CI runner HAS git-lfs, so `agent-claude-sdk-runner` `git-workspace.test.ts` (which `git lfs install`s) passes there even though it fails locally on hosts without git-lfs — that local failure is environmental, not a regression.

- `2026-05-24` — **TASK-12: the sandbox `git` binary does NOT trust the credential-proxy MITM CA via `NODE_EXTRA_CA_CERTS` / `SSL_CERT_FILE`.** Those two only steer Node's TLS (the SDK's undici fetch). The model's `git clone` runs the in-image `git` (libcurl/OpenSSL) via the Bash tool, which verifies the proxy cert against **`GIT_SSL_CAINFO`** (or `http.sslCAInfo` config) — pointed at the SAME CA path. k8s: stamp `GIT_SSL_CAINFO=/var/run/ax/proxy-ca/ca.crt` in `sandbox-k8s/pod-spec.ts` `proxyEnv` (alongside the two Node vars). subprocess: stamp `GIT_SSL_CAINFO=<caPath>` in `sandbox-subprocess/open-session.ts` (the per-session `ax-mitm-ca.pem`). It rides into the SDK subprocess automatically via proxy-startup's `GIT_` prefix allowlist (`ENV_ALLOWLIST_PREFIXES`) — no extra forwarding code needed, but add a regression test pinning the forward. Symptom when missing: `git clone` over the proxy dies `SSL certificate problem: unable to get local issuer certificate` (the CLI-1 walk-fail). The `echo-stub.mjs` test fixture must echo any new env key you assert on (it emits an explicit allowlist, absent keys read `undefined` not `null`).
- `2026-05-24` — **TASK-12 unsolved (followup): credential non-injection on git's Basic header.** With the CA fixed, the CLI-1 walk still 403'd from gitlab and the proxy audit showed `credentialInjected:false` on `GET /info/refs`. The framer's Basic-password substitution is correct in isolation (`request-framer.ts` `transformBasicAuthHead` round-trips `oauth2:ax-cred:<hex>` → real value, fully tested), so the bug is UPSTREAM of the pure function — most likely (a) git never sent the placeholder (no credential helper / URL userinfo wired into the Bash-tool git env), (b) the placeholder git sent wasn't registered for that session in `SharedCredentialRegistry` (so `replaceAll` is a no-op), or (c) a chunk-boundary case. Distinguishing needs a LIVE decrypted-bytes trace against the kind cluster (a worktree can't reproduce post-TLS-terminate bytes). A `.skip`'d pin lives in `credential-proxy/src/__tests__/request-framer.test.ts` ("TASK-12 part2"). To chase: `k8s-acceptance-loop` the git-clone walk, capture the proxy's `event.http-egress` audit, and check whether the request's Basic blob even contains an `ax-cred:` token before blaming the framer.

- `2026-05-24` — **ARCH-13 (returns-schema long tail): for a live-handle hook, the `.passthrough()` schema must NOT model the function field with `z.function()` — that WRAPS it in a validating proxy and breaks `returned === original` identity.** Modeling `http:register-route`'s `{ unregister(): void }` as `z.object({ unregister: z.function() }).passthrough()` made `.parse(out).unregister` a `[Function anonymous]` clone, not the same object — the drift-guard `expect(parsed.unregister).toBe(unregister)` failed. The correct posture (exactly what ARCH-6's `OpenSessionResultSchema` does for the live `handle`) is `z.object({ <serializable fields only> }).passthrough()` — model ONLY the storage-agnostic serializable keys and let the capability object ride through UNTOUCHED by reference. For `http:register-route` (output is ONLY the handle, no serializable field) that's a bare `z.object({}).passthrough()`. A capability object is not a data contract; don't try to validate it. Same applies to `eventbus:subscribe`'s `{ unsubscribe: () => void }`.

- `2026-05-25` — **ARCH-11: to keep TWO independent validators in sync without coupling them (a host zod schema + a sandbox-side hand-rolled re-validator that MUST NOT import the host package), use a SHARED GOLDEN-VECTORS JSON FIXTURE + a drift test on EACH side, not a cross-import.** The fixture is data (`{vectors:[{desc,core,schema:'accept'|'reject',runner:'accept'|'reject',value}]}`), so neither package imports the other. The schema-side test (in the package that owns the fixture) maps `Schema.safeParse` over the vectors; the runner-side test imports its own pure validator and reaches the fixture **read-only by repo-root-relative path** (`path.join(__dirname,'..','..','..','..','packages',...)` — the worktree-safe idiom, NOT cwd). Each vector declares an expected verdict PER SIDE so a legitimate asymmetry (the last-gate re-validator being STRICTER — defense in depth) is modeled without hiding drift; tag the security-critical vectors `core:true` and assert `schema===runner` for those. Two discipline points: (1) compute the expected verdicts by RUNNING both validators over the fixture in a throwaway `.mjs` BEFORE writing assertions (don't hand-wave them), and mutation-test the guard (break each validator, confirm the right side fails) to prove it actually detects drift; (2) add an independence self-check that greps the runner's `src` for an actual `import/require/from '@ax/<host-pkg>'` (anchored regex, NOT a bare mention — a cross-reference COMMENT naming the package is fine; exclude the guard file itself).
- `2026-05-25` — **ARCH-11: a path-reach test across packages has NO pnpm dependency edge, so CI's affected-package PR selection (`pnpm --filter "...[$BASE_SHA]"`) WON'T run it when only the OTHER package changes.** The runner-side drift test reads the sandbox-protocol fixture by path (deliberately no dep edge), so a PR touching only `packages/sandbox-protocol` selected sandbox-protocol but NOT the runner → the runner half of the guard would only run post-merge on main. Codex caught it. Fix WITHOUT a dep edge (which would defeat the independence): in the PR test step, `git diff --name-only "$BASE_SHA"...HEAD | grep -Eq '^packages/<other>/'` and, when matched, append `--filter @ax/<this-pkg>` to BOTH the affected-package list-probe and the run. Build the extra filter as a bash ARRAY (`extra=(--filter @ax/...)`, expand `"${extra[@]}"`) so shellcheck/actionlint stay clean (a bare `$extra` trips SC2086). Validate the workflow with the downloadable `actionlint` (the nested-worktree `pnpm lint` can't gate a YAML-only change). General rule: whenever a test relies on a *file-path reach* rather than a package import, ask "would `...[BASE_SHA]` select this test's package on a PR that changes only the reached file?" — if not, add a path-scoped CI inclusion.

- `2026-05-26` — **TASK-37: when you add a hook to a plugin's HARD `manifest.calls`, EVERY test that boots that plugin via `bootstrap` needs a registrant for the new hook — including the container-backed e2e suites, not just the obvious unit test.** Adding `'proxy:add-host'` to `@ax/channel-web`'s `calls` made bootstrap's `verifyCalls` throw `'@ax/channel-web' declares calls:['proxy:add-host'] but no plugin registers it` in FIVE places: the plugin manifest-shape test (`plugin.test.ts`), the channel-web container e2e suites (`routes-chat.test.ts`, `acceptance-e2e.test.ts`, `stream-e2e.test.ts` — each has its OWN local `mock-chat-run` stub), AND two hand-crafted preset canaries in `presets/k8s/src/__tests__/acceptance.test.ts` that boot `createChannelWebServerPlugin()` without `@ax/credential-proxy`. The fix in each: add the hook to the stub plugin's `registers` + a no-op `bus.registerService(hook, name, async () => ({...}))`. **Find them ALL up front:** `grep -rln "create<ThatPlugin>" packages/<pkg>/src/__tests__/ presets/*/src/__tests__/` then `grep -c "<new-hook>"` each to confirm coverage before re-running. `routes-attachments.test.ts` did NOT need it — it calls `registerAttachmentsRoutes(...)` directly (not the full plugin), so `verifyCalls` never runs over channel-web's manifest. Also update the manifest-shape `toEqual` assertion (the one that lists the full `calls` array). The preset's `preset.test.ts` "every calls entry is satisfied" derives the sets DYNAMICALLY from the real preset (which wires both the caller AND registrant), so it needs NO edit — only the hand-crafted/PLUGINS_TO_DROP lists do.
- `2026-05-26` — **TASK-37: to prove a credential-proxy LIVE-egress change end-to-end through the real listener, open the session with BOTH a restrictive `allowlist` AND `allowedIPs:['127.0.0.1']`.** The `allowlist` is the gate your change toggles; `allowedIPs` is the test-only escape hatch from the SEPARATE private-IP SSRF block (loopback is a private IP). Without `allowedIPs`, a retry that PASSES the widened allowlist still 403s on the SSRF gate, so `expect(status).not.toBe(403)` fails and masks your real result. Mirror `attribution.canary.test.ts` / `listener-http.test.ts` (which all pair `allowlist:new Set(['127.0.0.1'])` with `allowedIPs:new Set(['127.0.0.1'])`). Drive the proxied request with undici `ProxyAgent({uri, proxyTunnel:false, token:'Basic '+base64('ax:'+proxyAuthToken)})` — the same per-session-token pattern TASK-52 established. The plugin's `sessions` Map is module-internal (no test seam), so assert the WIDENING via the canary's retry reaching the upstream (status 200), not via `sessions.get(...).allowlist.has(...)`.
- `2026-05-26` — **TASK-37: widening a shipped subscriber-hook payload (`chat:permission-request`) to a discriminated union means adding the discriminant `kind` at EVERY hop AND updating the producer + its tests.** TASK-35 shipped `PermissionRequest` with no `kind`. Adding `{kind:'skill'} | {kind:'host'}` required: the producer (`skill-broker/request-capability.ts` — add `kind:'skill'` to the fired card), its unit test (`toEqual` now includes `kind`), the e2e canary that captures the card (`toMatchObject` — forgiving, but add `kind` for honesty), the channel-web server type + SSE branch, the client store + transport SseFrame type, and the React card. The SSE/transport RUNTIME forwarding is type-agnostic (opaque `permissionRequest` passthrough), so `vitest` passes even with stale types — `tsc --build` (the real gate, via `pnpm build`) is what catches a hop that didn't get the union. Always `pnpm -F <pkg> build` after a cross-hop type change, not just the package test.

## Anti-Patterns (don't do these)

- `2026-04-23` — Don't put backend-specific field names in hook payloads (`sha`, `bucket`, `pod_name`, `table`, `rowid`). If the name only makes sense for one impl, rename before subscribers depend on it.
- `2026-04-23` — Don't land "we'll wire it later" infrastructure. Either a live caller reaches it in the same PR or it doesn't merge. Symmetric API pairs (`get`/`set`) with a documented near-term consumer are the narrow exception.
- `2026-05-24` — Promoting a hook from absent to a REQUIRED `manifest.calls` entry can break canary acceptance harnesses that deliberately DROP the producer plugin. ARCH-2 expanded `@ax/ipc-server`/`@ax/ipc-http` required `calls` (spread from `@ax/ipc-core`'s `DISPATCHER_DEPENDENCIES`) to include `workspace:read` + `conversations:store-runner-session`. `presets/k8s/src/__tests__/acceptance.test.ts` + `multi-tenant-acceptance.test.ts` build "preset-equivalent" sets via `createK8sPlugins(cfg)` then `.filter(p => !PLUGINS_TO_DROP.has(name))` — dropping `@ax/workspace-git` (local backend) and `@ax/conversations` to stay postgres-free — so `bootstrap`'s `verifyCalls` then fails `'@ax/ipc-server' declares calls:['workspace:read'] but no plugin registers it`. Fix: a tiny no-op `createDispatcherDepsStubPlugin(hooks[])` registered alongside `createIpcServerPlugin()`, passing ONLY the missing subset (git-protocol backend keeps `@ax/workspace-git-server` which still registers `workspace:read`, so those lists only need the `conversations:store-runner-session` stub; local-backend lists need both). Passing a hook a kept plugin already registers trips `checkDuplicateRegisters`' `duplicate-service`. The real preset (`prod-bootstrap`/`preset.test.ts`) is fine — it keeps the real producers. When you widen a transport/plugin's required calls, grep the preset canaries for `PLUGINS_TO_DROP` + the new producer and stub the gap in the SAME PR.
- `2026-05-24` — This env has NO Docker/testcontainers: `presets/k8s` tests that `new PostgreSqlContainer(...).start()` fail locally with `Could not find a working container runtime strategy` (acceptance Phase D / attachments / artifact_publish / Task 21, all of `prod-bootstrap.test.ts`). Like the git-lfs gap, this is environmental, not a regression — CI has Docker. To verify a preset change locally, run the non-testcontainer canaries by name (`-t "boots a preset-equivalent"`, `-t "Phase 3 canary: workspace.commit-notify"`, the whole `multi-tenant-acceptance.test.ts`, and `preset.test.ts` static wiring) and confirm the failures that remain are ONLY `container runtime` errors.
- `2026-05-24` — **Digest-pinning an image dep (CLI-3): use the multi-arch INDEX digest, not a per-platform manifest digest.** `docker buildx imagetools inspect <ref>` prints `Digest: sha256:…` = the OCI index digest the tag points to → pin `image:tag@sha256:<index>` (keeps multi-arch + keeps the human-readable tag). `docker manifest inspect` (without `--verbose`) lists the *inner per-platform* manifest digests — pinning one of those would lock the image to a single arch. `docker manifest inspect --verbose` does expose the top-level `Descriptor.digest` (= the index), but `buildx imagetools inspect` is the clean one-liner. Daemon NOT required for either (they query the registry over HTTPS), so they work even when `docker run`/`build` is blocked (vpulim isn't in the `docker` group in this sandbox; no passwordless sudo to the socket).
- `2026-05-24` — **Resolving exact installable Debian apt versions without a container (CLI-3):** pull the signed binary Packages index directly — `curl -s https://deb.debian.org/debian/dists/bookworm/main/binary-amd64/Packages.gz | gunzip | awk '/^Package: (pkg)$/{p=$2}/^Version: /{if(p){print p": "$2;p=""}}'`. Gives the version actually in the `bookworm/main` pocket (what `apt-get install pkg=ver` would resolve). Caveat: a hard `=version` apt pin ROTS — when a point release supersedes it, the old `.deb` leaves the pocket and the build breaks. **TASK-9 closed this gap:** the `docker-build` lane in `ci.yml` now builds both `container/agent` and `container/git-server` images (buildx, push:false/load:false, path-gated to `container/**` + the workspace graph) on every change that touches them, so a rotted apt pin turns CI red instead of failing silently. Bump the pins in the Dockerfile when that happens. The credentialed-CLI security note (`docs/plans/2026-05-22-...-security-note.md`) is a point-in-time record of THAT PR's state; don't rewrite its "digest-pin is a tracked TODO" line when the follow-up lands — it's history, not a live spec.
- `2026-05-24` — ARCH-6 `returns`-schema rollout recipe (followed the credentials precedent): a hook's `returns` zod schema is a **per-registration in-process shape assertion** (NOT an inter-plugin wire contract — that's `@ax/ipc-protocol`), so it co-locates with the registering plugin's I/O interfaces. For shapes already in storage-neutral `@ax/core` (workspace read/list), the schema lives in core and all 3 backends import it (git-core, git-server client, test-harness mock). For two-backend hooks (session inmemory/postgres), each backend carries its OWN structurally-identical copy — same boundary cost as the duplicated I/O interfaces (I2). EVERY schema pairs with a **drift-guard test** (`return-schemas.test.ts`): round-trip a fully-populated INTERFACE-TYPED value through `.parse` and assert `toEqual` — a new required field fails at compile, a new optional one fails at runtime (the schema strips it → toEqual diverges). The credentials precedent (`CredentialsResolveOutputSchema` + its drift test) is the template.
- `2026-05-24` — ARCH-6: hooks that return a **live capability handle** (functions/Promises, e.g. `sandbox:open-session`'s `{runnerEndpoint, handle:{kill,exited}}`) MUST use a `.passthrough()` schema asserting only the serializable storage-agnostic field(s) — a strict `z.object` STRIPS the handle (hook-bus.ts:141-147 strips undeclared keys) and silently breaks the lifecycle. The `OpenSessionResultSchema` lives in `@ax/sandbox-protocol` (both backends already dep it); test it by asserting the handle survives by reference (`expect(parsed.handle).toBe(handle)`). Same trap for the workspace `apply`/`diff` delta (`changes[].contentBefore?/contentAfter?` are lazy fns) — those were DEFERRED, not schema'd, because read/list (IPC-reachable, clean data) were the priority subset and apply/diff need coordinated contract-test verification across backends.
- `2026-05-24` — ARCH-6: a zod schema used as a runtime VALUE (e.g. attaching `OpenSessionResultSchema` to `registerService({returns})`) makes `zod` a real dependency — plugins that previously had no zod dep (session-inmemory, session-postgres, sandbox-subprocess, sandbox-k8s) need `"zod": "^3.23.8"` added to package.json `dependencies` (NOT just devDeps); a bare `import type {ZodType} from 'zod'` ALSO needs zod resolvable, so even the type-only cast import forces the dep. Standard range across the repo is `^3.23.8` (core/agents/conversations/credentials all pin it). Casting the schema to `ZodType<O>` (`as unknown as ZodType<O>`) bridges zod's inferred `|undefined`/`|null`/passthrough shapes to the interface where `exactOptionalPropertyTypes` blocks direct assignability — the drift-guard test is the real enforcement.
- `2026-05-24` (TASK-11) — **The runner's "anything to ship this turn?" decision keys off the COMMIT RANGE `baseline..main`, NOT the working-tree staged diff.** `commitTurnAndBundle` did `git add -A` → `git diff --cached --quiet` → return null on a clean staged tree, conflating "no uncommitted changes" with "nothing to ship". They DIVERGE after `resyncBaselineAndReplay`: the rebase commits the turn onto the advanced baseline + re-pins `baseline`, leaving a CLEAN tree but a real commit on `baseline..main`. Returning null there made `commitNotifyWithResync` treat it as "turn absorbed ⇒ accepted" and silently drop the turn. Fix: gate on `git rev-list --count refs/heads/baseline..main` (null only when genuinely empty — empty turn OR a replay whose commit became empty because its diff was already in the advanced baseline = a TRUE absorb). The unit "absorbed" test still passes (it mocks `commitTurnAndBundle`'s return); the real-git gate is covered in `git-workspace.test.ts` ("re-sync replay; baseline..main non-empty").
- `2026-05-24` (TASK-11) — **Waiting for the SDK's post-`result` jsonl flush: target the turn's LAST `SDKAssistantMessage.uuid`, NOT "any new assistant line".** The SDK writes the final assistant line AFTER yielding `result`; a TOOL turn first writes an intermediate `tool_use` assistant line DURING the turn, so "wait until the last assistant uuid differs from the pre-turn one" short-circuits on the tool_use line → the per-turn commit fires before the closing text lands → closing text dropped (`[user, tool_use, tool_result]`). The runner sees every assistant message in-band: record the latest `msg.uuid` per turn (reset at the `result` boundary), poll the jsonl for THAT exact uuid (`waitForTranscriptUuid`). `SDKAssistantMessage.uuid` == the jsonl line `.uuid` by construction (the jsonl IS the SDK's transcript). The opt-in `AX_COMMIT_TRACE` runner trace (forwarded host→runner via `open-session` extraEnv) is the tool for diagnosing this class — it logs which uuid the flush waits for + every commit/resync outcome.

- `2026-05-26` — **TASK-35: re-probe the env each run — Docker WAS available this run (testcontainers ran fine), git-lfs was NOT.** Memory's older notes said "no Docker/testcontainers locally"; that did NOT hold this run — `docker info` succeeded and the `@ax/skills` skill-install canary (PostgreSqlContainer) + the new chat:permission-request card canary both ran green locally (193/193 skills, 2/2 broker-canary). git-lfs is still absent, so `@ax/agent-claude-sdk-runner` `git-workspace.test.ts` would still fail the full `pnpm test` (environmental, not a regression — CI has it). Practical gate for a diff that touches N packages: build the whole workspace (`pnpm build` = tsc refs, the real cross-package type gate), `pnpm lint` (clean — the nested-worktree tsconfigRootDir phantom did NOT reproduce), then run each TOUCHED package's full suite in isolation (`pnpm -F @ax/<pkg> test`) rather than the full `pnpm test` (which drags in the git-lfs failures of untouched packages). Confirm `git diff --name-only main...HEAD | sed 's#/src/.*##' | sort -u` = exactly the packages you ran.

- `2026-05-26` — **TASK-39: the broker's loose tool-arg filters MUST match the downstream `parseSkillManifest` grammar, or a valid-looking arg is silently dropped then rejected with an opaque error.** The JIT impl-plan sketched the broker's slot filter as `/^[A-Za-z0-9_]{1,64}$/` (lowercase-permitting) and even used `slots: ['api_key']` in its examples — but `@ax/skills-parser`'s `parseSkillManifest` requires `capabilities.credentials` slots match `/^[A-Z][A-Z0-9_]{0,63}$/` (SCREAMING_SNAKE) and throws `invalid-slot` otherwise. If the early broker filter is LOOSER than the authority, a slot like `api_key` passes the filter, rides into `buildSkillManifestYaml`, and `skills:upsert` rejects the whole install with a slot the model never saw on the card. Fix: tighten the broker's `SLOT_RE` to the SAME SCREAMING_SNAKE grammar as the authority so the friendly early filter and the real validator agree. General rule: when a thin host tool re-validates model input for an "early friendly error" ahead of a downstream authority, the early regex must be a SUBSET-or-equal of the authority's, never a superset. (HOST_RE is fine — it's the same hostname grammar both sides use.)
- `2026-05-26` — **TASK-39: a `workspace:apply` that DELETES (retire-the-draft) must pass the CURRENT workspace version as `parent`, not `null` — both the mock (`mock-workspace.ts`) and the git backend (`workspace-git-core/impl.ts:796`) enforce optimistic-concurrency CAS (`input.parent !== currentVersion → parent-mismatch`).** The JIT impl-plan sketched the retire as `parent: null`, which only passes on an EMPTY workspace; a seeded draft makes `latest` non-null → `parent-mismatch`. `workspace:read`'s output carries `version` (the snapshot it read from — core/workspace.ts:86 says "pass it back as `parent` on a subsequent `workspace:apply`"), so capture it in the bundle read and pass it to the retire apply. Note: an intervening `skills:upsert` writes the SKILL STORE (Postgres), NOT the workspace, so the read-time version is still the correct retire parent — no re-read needed for correctness.
- `2026-05-26` — **TASK-39 / the JIT canary + agents authored-skill tests use the REAL Postgres testcontainer + `createMockWorkspacePlugin()` (or raw `workspace:*` service stubs), NOT the in-memory `createTestHarness({ services })` stubs the impl-plan code sketches show.** `agents:install-authored-skill` resolves the owner via `localStore.getById(agentId)`, which needs a real agents DB row (seed via the `agents:create` hook, then for a TEAM agent also stub `teams:is-member`). The impl-plan's pseudo `seedPersonalAgent` + bare workspace stubs can't satisfy that. Mirror `promote-authored-skills.test.ts` / `authored-skills.test.ts`. Docker WAS available this run (testcontainers ran); git-lfs was NOT (the 25 `agent-claude-sdk-runner` git-workspace failures are the known env gap, not a regression).

- `2026-05-28` (P2b) — **An IPC host handler's error STATUS drives the sandbox `IpcClient.callBinary`/`call` retry behavior — pick it for the failure's nature, not by reflex.** `classifyRetry` (ipc-protocol/src/ipc-client.ts) buckets: `IpcRequestError.status >= 500` → RETRYABLE (small finite `MAX_5XX_RETRIES=3` cap); `4xx` (incl. 409) → NOT retried (thrown on the FIRST attempt); transient connection errnos → wall-clock-bounded retry. The `parseErrorEnvelope` path turns any host non-2xx into an `IpcRequestError` carrying that status, so the host's choice of 409-vs-500 directly decides retry-vs-fail-fast. The `workspace.export-baseline-bundle` handler's `parent-mismatch` (head moved AGAIN between commit-notify returning `actualParent` and this fetch) was sanitized to 500 → callBinary reissued the SAME stale fetch 3× (each rebuilding the large bundle on the git-server backend) before the runner's bounded re-sync loop re-asked commit-notify — amplifying the very stall the resync work fixes. Fix: map `parent-mismatch` PluginError → `hookRejected('conflict')` (409, sanitized) so it fails fast; EVERY other failure (no backend, real git error, transient hiccup) stays 500 (retryable). The runner-side fetch-catch in `commit-notify-resync.ts` is STATUS-AGNOSTIC (catches any thrown error → re-enters the bounded loop), so no runner change was needed. Test the retry classification directly: a 409 callBinary response must be fetched EXACTLY ONCE (`attempts===1`) vs a 5xx which retries — flip 409→500 to confirm the test discriminates.
- `2026-05-26` (TASK-44) — **New postgres-backed plugin → also add it to `PLUGINS_TO_DROP` in BOTH `presets/k8s/src/__tests__/acceptance.test.ts` and `multi-tenant-acceptance.test.ts`.** Those canaries call `createK8sPlugins(cfg)` then filter out the postgres/k8s/http plugins and run with sqlite/in-memory substitutes — but they do NOT register a `database:get-instance` stub. Any plugin whose `init` calls `database:get-instance` will throw `no plugin registered for service hook 'database:get-instance'` at boot if it survives the filter. Each test file has its OWN local `PLUGINS_TO_DROP` set (not shared/imported despite the "shared with" comment). The new plugin's static wiring + reachability stay pinned in `preset.test.ts` + the package canary.
- `2026-05-26` (TASK-44) — **`exactOptionalPropertyTypes: true` is on (tsconfig.base).** An optional field `agentId?: string` CANNOT be assigned an explicit `undefined`. Returning `{ added: true, agentId: sess.agentId }` where `sess.agentId: string | undefined` fails `tsc --build`. Fix: build the object conditionally — `sess.agentId !== undefined ? { added: true, agentId: sess.agentId } : { added: true }` — so the key is omitted, not set to undefined. `pnpm build` (full tsc project refs) catches this; per-package vitest does not.
- `2026-05-26` — **TASK-43: a cross-package canary in package A that imports workspace packages B/C resolves B/C to their `dist/`, NOT `src/`.** The TASK-43 vault canary lives in `@ax/skills` and imports `@ax/skill-broker` + `@ax/chat-orchestrator`; vitest transpiles `@ax/skills` (the package-under-test) from `src`, but the imported workspace packages load from their built `dist`. So after editing a SOURCE file in B that a test in A imports, you MUST `pnpm build` (whole workspace, or at least `pnpm -F @ax/B build`) BEFORE running A's test — otherwise A's test exercises STALE B behavior and fails confusingly (my card lacked `account`/`haveExisting` because the broker dist predated my Task 6 edit). Same-package tests don't need it. This is the dist twin of the TASK-34 isolated-build lesson. Quick tell: a cross-package assertion fails for a field you KNOW you added in another package's source → rebuild that package's dist first.
- `2026-05-26` — **TASK-43: the right home for a JIT cross-plugin canary that needs the REAL skills catalog is `@ax/skills` `src/__tests__/e2e/skill-install.canary.test.ts` (PostgreSqlContainer), extending the existing TASK-36 grant block.** `@ax/skills` needs a real `database:get-instance` (Postgres) — a canary in `presets/k8s` bootstrapping `createSkillsPlugin()` without a Postgres plugin fails `bootstrap` ("no plugin registered for service hook 'database:get-instance'"). And `@ax/credential-proxy`'s reactive-wall canary uses a STUB credentials plugin + is proxy-focused — not a skills/broker home. But note `@ax/skills` does NOT dep `@ax/credentials`, so the canary CAN'T resolve real secrets — assert the binding ref reaches `proxy:open-session` (capture fake) and cover the credential reuse+revoke property separately in `@ax/credentials` `vault.test.ts` (real credentials stack: storage-sqlite + credentials-store-db + credentials). Two tests, full P8 coverage, no cross-package devDep expansion.

- `2026-05-26` (TASK-55) — **The vendored Claude Code CLI inside `@anthropic-ai/claude-agent-sdk` 0.2.119 phones home to `datadoghq.com` (telemetry) + an error-reporting channel; disable it via env in the runner's `query({options:{env}})` literal, NOT an egress allowlist entry.** The datadog egress is `https://http-intake.logs.us5.datadoghq.com/api/v2/logs`, emitted by the platform `claude` binary (NOT the SDK's `sdk.mjs`/`bridge.mjs` — those only carry the env-flag helpers; the binary lives in `node_modules/.pnpm/@anthropic-ai+claude-agent-sdk-linux-x64@*/.../claude`). Verified gate chain in the binary: `initializeDatadog` bails (`datadogEnabled=false`) when `rU()` is true; `rU()=USE_BEDROCK||USE_VERTEX||USE_FOUNDRY||trafficMode()!=="default"`; `trafficMode()` returns `"essential-traffic"` for `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`, `"no-telemetry"` for `DISABLE_TELEMETRY`/`DO_NOT_TRACK`. So ANY of those flags skips datadog init entirely. The umbrella flag Anthropic's own docs use (devcontainer.json example) is `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1`; `DISABLE_TELEMETRY=1` + `DISABLE_ERROR_REPORTING=1` are the per-channel switches. **How to find the real flag for any future SDK behavior: `strings -n 6 <the-platform-binary> | grep -E 'CLAUDE_CODE_[A-Z_]*|DISABLE_[A-Z_]*'`, then extract the gating fn bodies with `grep -oaE 'function NAME\(\)\{[^}]{0,200}\}'` — the SDK `.mjs` bundle's `Uf()`/`UK()` are minified twins of the binary's `ha6()`/`c9()`. Don't guess the flag.**
- `2026-05-26` (TASK-55) — **Pattern for injecting fixed env into the SDK subprocess: a pure `build<Thing>Env(): Record<string,string>` helper module + its own unit test, spread into the `main.ts` `query({options:{env}})` literal.** Established by `tty-hint-env.ts` (overridable default → spread FIRST, before `...proxyStartup.anthropicEnv`) and `tool-cache-env.ts`/`home-bin-env.ts`/`python-venv.ts`. For a NON-overridable security floor (like telemetry-disable), spread AFTER `...proxyStartup.anthropicEnv` so it wins on conflict. The strongest regression test is in `main.test.ts` (it already mocks `query` via `queryMock=vi.fn()` and drives `main()` end-to-end with mocked IPC) — extend the existing `queryArg.options.env` assertion block (search `FORCE_COLOR`/`buildTtyHintEnv`), don't write a standalone harness. Verify no later spread collides with your keys (the floor claim): the later helpers set PATH/UV_CACHE_DIR/XDG_CACHE_HOME/VIRTUAL_ENV — disjoint from telemetry vars. Also confirm the keys aren't in proxy-startup's `ENV_ALLOWLIST` (they aren't), so a host-forwarded env can't carry them.

- `2026-05-29` — **`.msg-body` (chat message body) carries `white-space: pre-wrap` to preserve newlines in plain-text turns — but message text is markdown-rendered through `MarkdownText`/`.aui-md`, and react-markdown emits a literal newline TEXT NODE between every block element (`<p>`, `<li>`, …). With pre-wrap inherited into `.aui-md`, each of those structural newlines renders as a full line-height BLANK LINE — most visibly a ~23px gap between every `<li>` and ~32px (9px margin + blank line) between paragraphs.** Symptom: "too much vertical space between list items." Fix is NOT a per-list margin tweak — reset the markdown container's whitespace: `.msg-body .aui-md { white-space: normal }`, then restore the intended block rhythm (`.aui-md :is(ul,ol){margin:.6em 0}` / `.aui-md li{margin-bottom:.6em}` to match `.msg-body p`'s 0.6em). Code blocks are safe — `DefaultPre` renders a bare `<pre>`, whose own UA `white-space: pre` is not inherited and survives. Also note: `@tailwindcss/typography` is NOT installed and `plugins:[animate]` only, so every `prose-*`/`prose` class on `MarkdownText` is INERT — list/heading/code styling falls back to bare HTML + Preflight (`ol,ul{list-style:none;margin:0;padding:0}`) + the global `*{margin:0;padding:0}` reset. Verified by compiling index.css via `pnpm exec tailwindcss -c tailwind.config.ts -i src/index.css -o /tmp/x.css` and measuring `getBoundingClientRect` gaps in a Playwright file-served harness (file:// is blocked → serve over http).
- `2026-05-29` — **Brand-color glow effects: use `hsl(var(--primary))`, NOT `var(--accent)`.** In `@ax/channel-web` the shadcn `--accent` token is a near-neutral GRAY (`240 5% 96%` light / `240 3% 9%` dark — the hover-bg accent), so an "accent glow" built on it reads as no color. `--primary` (`211 100% 44%`) is the actual brand color. Also: tokens are stored as HSL COMPONENTS (`H S% L%`), so they must be wrapped — `color-mix(in oklch, hsl(var(--primary)) 22%, transparent)`, never bare `var(--primary)`. `color-mix` is already used in `.composer-fade`, so it's a safe primitive here.
- `2026-05-30` (TASK-65) — **Scaffolding a NEW workspace plugin: the minimal CI-clean recipe.** (1) `package.json` (mirror storage-sqlite: `@ax/core`+`zod` runtime, `@ax/test-harness`+`@types/node`+`typescript`+`vitest` dev; `files:["dist","SECURITY.md"]`), `tsconfig.json` (extends base, `references:[{path:"../core"}]`), `vitest.config.ts`, `SECURITY.md`. (2) Add `{ "path": "packages/<name>" }` to ROOT `tsconfig.json` references. (3) **The pnpm worktree has NO `node_modules`** — install with the corepack wrapper (`printf '#!/usr/bin/env bash\nexec corepack pnpm "$@"\n' > /tmp/pnpm-bin/pnpm; chmod +x; PATH=/tmp/pnpm-bin:$PATH`). (4) `pnpm install --frozen-lockfile` FAILS on a new package (`ERR_PNPM_OUTDATED_LOCKFILE`) — but a full `--no-frozen-lockfile` install can drag in UNRELATED peer-dep churn (this run: `@better-auth/core` jose `5.10.0→6.2.2`) that muddies the PR + risks CI's frozen check. **Cleaner: `git checkout pnpm-lock.yaml` to revert, then SPLICE only your importer block in by hand** — importers are alphabetical (`blob-store-fs` between `auth-better` and `channel-web`); the block lists `specifier`+`version` per dep, and for an all-existing-deps package every `version` (link:../core, link:../test-harness, zod 3.25.76, typescript 6.0.3, @types/node 25.6.0, the long vitest pin) already appears elsewhere in the lockfile, so NO new snapshot entries are needed. Re-run `pnpm install --frozen-lockfile` to PROVE the spliced lockfile is internally consistent (CI runs frozen). Net lockfile diff = one importer block. (5) A registrar plugin needs NO entry in eslint's `no-restricted-imports` allowlist (it imports only `@ax/core`/`zod`/`@ax/test-harness`).
- `2026-05-30` (TASK-68) — **The REQUEST-direction binary IPC channel + blob.* actions now EXIST (out-of-git Part C).** `ipc-protocol`'s `IpcClient` gained `callBinaryUpload(action,bytes:Buffer)` — POSTs raw `application/octet-stream` (mirror of the response-direction `callBinary`, which now also handles `blob.get`); a socket-level error handler (`req.on('socket',s=>s.on('error',...))`) was REQUIRED to stop a mid-upload reset crashing the runner (and the test fake-server's EPIPE). Host side: `ipc-core` added `readRawBody(req,maxBytes)` + a `BINARY_ACTIONS` dispatch branch (raw body, NOT readJsonBody) for `blob.put` (100 MiB cap), plus JSON actions `blob.get`/`artifact.publish`/`attachments.list`; the POST content-type gate moved into a SHARED `checkContentType(method,path,ct)` in ipc-core (octet-stream for blob.put, JSON else) used by BOTH `ipc-server` + `ipc-http` listeners — when you add a binary action, update that one function, not two inline gates. New service hooks went to `DISPATCHER_DEPENDENCIES.optionalCalls` (blob:put/blob:get/artifacts:publish-blob/attachments:list-for-conversation) so the CLI single-session boot doesn't fail verifyCalls. **For TASK-70 (delete LFS):** the attachment-via-git path (`attachments:commit→workspace:apply`) is ALREADY gone (commit now does blob:put + an `attachments_v1_files` row); the LFS server/`git lfs install`/`.lfs` provisioning are still present and are TASK-70's scope. **For TASK-72 (walk):** uploads materialize to `<ephemeralRoot>/uploads/<conv>/<turn>/<file>` at session start (runner `materialize-uploads.ts` via `attachments.list`+`blob.get`); the pre-tool-use re-root now maps `.ax/uploads/<rest>`→`<ephemeralRoot>/uploads/<rest>` (drops `.ax/`); artifacts publish to `/ephemeral/artifacts/**` (or `/permanent/workspace/**` Pattern A) and are durable at `artifact_publish` RETURN (not turn-end). Download (`GET /api/files`) resolves path→row→blob:get (digest re-verified). `AX_BLOB_STORE_ROOT` env (chart-stamped, local backend, sibling PVC dir) configures the fs store root; default `/var/lib/ax/blobs` EACCES'es in CI so every preset/host-boot test that keeps `@ax/blob-store-fs` MUST pass a tmpdir root (or drop the plugin where attachments is dropped — add it to PLUGINS_TO_DROP). New-package-in-preset wiring (dep+tsconfig ref+lockfile splice) followed the TASK-65 recipe cleanly (one importer block, no peer churn).
- `2026-05-30` (TASK-65) — **The content-addressed blob substrate (`blob:*` + @ax/blob-store-fs) exists as of TASK-65** (out-of-git design Part A, Phase 1). Host-side service hooks `blob:put({bytes:Uint8Array})→{sha256,size}` / `blob:get({sha256})→{bytes}|{found:false}` / `blob:stat({sha256})→{size}|{found:false}` / `blob:delete({sha256})→{}`. fs backend stores at `<root>/<sha[0:2]>/<sha[2:4]>/<sha>`, atomic temp-then-rename, **digest re-verified on read** (corrupt→throws, never returns bad bytes), sha keys regex-gated `^[a-f0-9]{64}$` (no traversal), put idempotent, delete ENOENT-no-op. Bytes ride the bus as RAW `Uint8Array` (NOT base64) so the eventual IPC `callBinary` octet-stream wire is additive. **NOT YET DONE (followups for the epic):** the IPC `blob.*` binary action + `callBinary` REQUEST-direction channel (today's callBinary is response-direction only) — belongs with the first runner-side caller (Part C: artifact_publish/attachments→blob:put); `@ax/blob-store-s3` backend; wiring @ax/blob-store-fs into `presets/k8s` (no host consumer until Part C/D); reference-counted GC for blob:delete (lives with consumers' rows). When Part B/C/D cards land, they build ON this hook — don't re-invent a store.

- `2026-05-30` (TASK-67) — **The resume jsonl is now OUT OF GIT, stored as opaque row-per-line in `conversations_v1_transcripts` (conversation_id, seq, line) owned by @ax/conversations.** The runner ships the per-turn DELTA at the result boundary (`transcript-delta.ts` `shipTranscriptDelta`) via `session.append-transcript({fromSeq, prefixHash, lines})` → host `conversations:append-transcript`, REPLACING `commitTurnAndBundle` of the jsonl. `commitTurnAndBundle` SURVIVES for NON-transcript /permanent state (identity, Pattern A) — the jsonl is now `.claude/projects/`-gitignored (`WORKSPACE_GITIGNORE_ENTRIES`). **For TASK-70 (LFS delete / Phase 5 git-pipeline thinning):** the transcript no longer rides the git bundle path AT ALL, so the commit/bundle fires only when real agent-state changed (usually empty on a chat turn → commitTurnAndBundle returns null → commit-notify skipped). The `workspace.commit-notify` + bundle round-trip in `agent-claude-sdk-runner` is now exercised ONLY by identity/Pattern-A diffs; TASK-73's `bundle-hooks` deflake stays relevant for THOSE, but the transcript-loss class of bug it guarded is mooted for the common chat path. **For TASK-72 (walk):** resume now = host joins rows ORDER BY seq → bytes (`getTranscriptBytes`, each line `\n`-terminated) → runner writes them to `$CLAUDE_CONFIG_DIR/projects/<slug>/<sid>.jsonl` BEFORE `query({resume})`; F2a guard = host `max(seq) > 0` (not a file scan). A multi-turn resume walk should verify the rebuilt jsonl round-trips and the SDK warm-resumes.
- `2026-05-30` (TASK-67) — **Resume-WRITE needs the SDK's EXACT project-dir slug (the dir doesn't exist yet on a fresh-pod resume, so you can't readdir-walk it like turn-end-uuid does).** The vendored Claude SDK (0.2.119, `node_modules/.pnpm/@anthropic-ai+claude-agent-sdk*/.../sdk.mjs`) encodes `realpath(cwd).replace(/[^a-zA-Z0-9]/g,'-')`, truncated to 200 chars + `-${djb2hash}` when longer (`var P0=200`; hash = `kB`/`gE`: `h=(h<<5)-h+charCode|0` then `Math.abs(h).toString(36)`). `transcript-delta.ts` `encodeProjectSlug` ports it byte-for-byte. `/permanent` (prod cwd, 10 chars) never hits the cap. DON'T hand-wave the slug — `grep -oaE 'replace\(/\[\^a-zA-Z0-9\]/g,\"-\"\)' sdk.mjs` and extract the gating fns; a mismatch = silent resume failure (SDK can't find the jsonl it just asked to resume).
- `2026-05-30` (TASK-67) — **The B3 structural divergence detector (out-of-git Part B3) is HOST-SIDE, in the `conversations:append-transcript` hook (after the rows land), comparing COLLAPSED turn-role runs of the display log (TASK-66 `listEvents` kind='turn') vs the resume rows (`getTranscriptRoles`, parse `type` per line: assistant→assistant, user→user, user-with-tool_result-block→tool, bookkeeping→null).** Compare only the COMMON PREFIX (the two stores are written by SEPARATE IPC calls so one routinely lags a turn in flight — a length diff is NOT divergence; only a prefix-position role mismatch is). Collapse consecutive same-role runs FIRST (the SDK jsonl writes intermediate tool_use assistant lines + coalesces, while the display log folds a turn to one role event). Fire = loud `logger.error('transcript_display_divergence')`, NEVER a throw (it must not block the append ack). Install the ALARM only — NO derivation escalation (design says YAGNI until it rings).
- `2026-05-30` (TASK-67) — **Delta-ship integrity: the runner hashes `sha256(fileBytes[0..sentOffset))` (raw on-disk prefix) and the host's `getTranscriptPrefixHash` hashes each stored line + a trailing `\n` — these AGREE because each SDK jsonl line is `\n`-terminated and `sentOffset` always sits AFTER a complete line's terminator (`splitCompleteLines` holds back a trailing partial).** `getTranscriptBytes` reconstructs `line + '\n'` per row (NOT `join('\n')` — that drops the final terminator and breaks round-trip). The zero-new-lines turn sends an EMPTY-`lines` append as a PREFIX-INTEGRITY PROBE (host re-checks prefixHash → resync-required on an in-place rewrite) rather than a silent noop — closes the no-omission gap for a same-length/shrink prefix rewrite. Thread `sentOffset`=`splitCompleteLines(buf).consumed` (not raw `buf.length`) everywhere so offset and seq stay consistent.
- `2026-05-30` (TASK-69) — **Skill bundle EXTRA files moved off the TASK-40 git-tree backing onto the shared `blob:*` store (out-of-git Part D2).** `@ax/skills/src/bundle-store.ts` (isomorphic-git bare repo) is GONE; `blob-bundle-store.ts` replaces it: canonical SORTED-by-path JSON serialize → `blob:put` (one object), read = `blob:get` + extract-boundary `validateBundleFiles` re-validation (I2). `BlobBundleStore` keeps the ctx-free `writeTree(files)`/`readTree(sha)` shape (drop-in), with the bus + a stable store-owned `initCtx` captured at construction — CORRECT because `blob:*` is purely content-addressed (no ownership scope, ctx carries only the logger). The `skills_v1_skills.bundle_tree_sha` column now holds a blob **sha256 (64-hex, was a 40-hex git oid)** — column name kept (additive-only migration; storage detail, never in a hook payload). `@ax/skills` now HARD-deps `blob:put`+`blob:get` in `calls` (init edge); preset dropped `AX_SKILLS_BUNDLE_ROOT`/`config.skills.bundleStore` and `isomorphic-git` left the skills package.json (still used by workspace-git-core, so it stays in the lock — splice ONLY the skills importer block). **Test fallout (the load-bearing gotcha):** EVERY harness that boots `createSkillsPlugin()` now fails `bootstrap`'s `verifyCalls` unless it supplies blob:put/blob:get — added a shared `mockBlobStoreServices()` to `@ax/test-harness` (content-addressed in-process, bytes as Uint8Array; pass the SAME instance across two harnesses to simulate a durable store surviving a restart) and spread it into the skills canary/admin/settings/plugin/catalog tests, `@ax/agents` promote, `@ax/channel-web` connections-mirror, and the preset Task-21 canary (which uses the REAL `createBlobStoreFsPlugin({root:tmpdir})`). `createInMemoryBundleStore()` is the bus-free default for skills store unit tests. **CI flake amplifier:** touching `@ax/test-harness` makes CI's "affected packages + dependents" set ≈ the whole repo → ~50 testcontainer suites run in parallel → the slowest sequential-insert test (`@ax/host-grants` 256-host-cap, 257 awaited pg inserts) consistently blows its default 5s timeout (3 re-runs failed on DIFFERENT pkgs: conversations→host-grants→host-grants). Fix = bump that one throughput test to `30_000`ms, not chase. **Still TODO for TASK-69 (the card's headline, returned as followups):** `skill_propose` IPC action (JSON envelope in ipc-protocol + bytes via `callBinaryUpload`→blob.put, mirroring artifact_publish) + a `skills:propose` host hook; `/ephemeral/skill-draft/` authoring + runner tool/executor + the validator-skill veto fired at the propose chokepoint (a `skills:scan`-style hook, NOT workspace:pre-apply); the hybrid materialization gate at propose time (clean+authored+zero-cap→active; any cap or imported/attached→pending+approval-card TASK-35; scan hit→quarantined) writing one skills_v1_skills row with origin/status/scan_verdict; swap `agents:resolve-authored-skills` to read DB rows + blob:get and RETIRE the `.ax/draft-skills/` workspace-draft projection (`listAuthoredBundles`) + the validator-skill workspace:pre-apply SKILL.md branch; re-spawn-on-next-turn + system-prompt "available next turn" guidance.
- `2026-05-30` (TASK-77) — **`tool_result.content` has TWO on-the-wire shapes (`ToolResultBlockSchema` = `string | (TextBlock|ImageBlock)[]`) and any code that parses a tool result's JSON MUST handle BOTH.** The runner persists an MCP/SDK tool result (artifact_publish, skill_propose, any host/sandbox-MCP tool) as the ARRAY form `[{type:'text', text:<json>}]` — `agent-claude-sdk-runner/src/main.ts` narrows the SDK echo to that array (~L1248-1268) — NOT the bare string. The TASK-72 walk's published-artifact `GET /api/files` 404 was `attachments/src/handlers.ts` `checkPathScope` matching only the `typeof content === 'string'` branch, so the artifact path was never found → `forbidden`. Fix = a tiny `toolResultTextCandidates(block)` that yields `[content]` for the string form and the `text` of each `{type:'text'}` entry for the array form, then run the SAME strict `JSON.parse` + exact `path === candidatePath` match over each candidate (don't widen the ACL — only the parse surface). The reference impl for the array→text flatten already existed CLIENT-side: `channel-web/src/lib/history-adapter.ts` `flattenToolResultContent` (so reload worked, masking the bug on the live path). Client twins that also needed the array unwrap: `MarkdownText.tsx` `parseArtifactsFromThread` (was `typeof result==='string' ? JSON.parse : result` → array fell through as a non-matching object → dead "unknown artifact" pill) and `ToolUse.tsx` `ArtifactPublishTool` (was `JSON.stringify(p.result)` then parse → array re-stringified, no `.path` → degraded to raw tool panel). Tell: a published-artifact link/chip works on RELOAD but 404s / shows "unknown artifact" on the LIVE turn (or vice-versa) ⇒ one of the parse sites is string-only. Test each by feeding the ARRAY shape, not the string the older tests use.
- `2026-05-30` (TASK-78) — **An uploaded file must MATERIALIZE at the SAME absolute path the system prompt advertises, and re-materialize on warm-runner rebind.** TASK-68 originally wrote uploads to `<ephemeralRoot>/uploads/<rest>` (a DIFFERENT root, `.ax/` dropped) while `system-prompt.ts` `workspaceNote` tells the model to open `<workspaceRoot>/.ax/uploads/…` — so an agent that followed the prompt (or `cat`'d via Bash, whose command string is NOT re-rooted by `pre-tool-use.ts` — only `file_path`/`path`/`notebook_path` keys are) found nothing. Fix: materialize at the advertised path `<workspaceRoot>/.ax/uploads/<conv>/<turn>/<file>` (runner `materialize-uploads.ts` now takes `workspaceRoot`, keeps `.ax/`; the pre-tool-use re-root dropped its `uploadsRoot`/ephemeral branch and always targets `<workspaceRoot>/.ax/uploads/<rest>`), and gitignore `.ax/uploads/` in `scaffoldWorkspaceGitignore` so the blob-store-backed bytes do NOT round-trip into the commit/bundle (that would re-create the git-era duplication TASK-68 removed — blob store is the single SoT). **Warm-rebind:** the runner is ONE persistent `query()` per session (single conversation, conversationId fixed at boot); `materializeUploads` ran ONLY at boot. A warm runner (idle-keepalive, orchestrator routes the next turn into the live sandbox via `session:queue-work` — NO re-spawn) reused for a later turn that brings a NEW upload never re-materialized it. Fix: factor a `materializeUploadsForConversation()` closure called at boot AND in the `userMessages()` generator whenever the inbox entry carries an `attachment` content block. **Stale residue:** `/permanent` persists across sessions, so a prior conversation's uploads could linger; each materialize now `fs.rm`-wipes `<workspaceRoot>/.ax/uploads/` first then writes the FULL current set from `attachments.list` (scoped to this conv) — idempotent + additive across turns, no cross-conversation leak. The `workspaceReader` (attachment-translation byte fetch) now resolves `.ax/uploads/...` to the materialized file under workspaceRoot (was gated on `ephemeralRoot`), so uploads no longer route through `workspace.read`. **Test gotcha:** `main.test.ts` tests with a bound conversation now call `attachments.list` (best-effort, non-fatal if the per-test mock throws → harmless stderr noise); the two attachment-translation tests had to write a real materialized file under a temp `AX_WORKSPACE_ROOT` (was mocking `workspace.read`).
- `2026-05-30` (TASK-80) — **A helm Job/Pod that runs a CLI as `runAsUser:1000` (non-root, no HOME) must point that CLI's config dir at a world-writable path or it crashloops at startup before doing any work.** The minio `mkbucket` Job (`deploy/charts/ax-next/templates/minio/bucket-job.yaml`) ran `mc` as UID 1000 with no HOME → `mc` tried to write its alias config to the default `~/.mc` (resolves to `/.mc` for a homeless UID) → died with `mkdir /.mc: permission denied` and OnFailure-restarted into CrashLoopBackOff. Fix = a single container env `MC_CONFIG_DIR=/tmp/.mc` (`/tmp` is world-writable). The MinIO *server* Deployment dodged this with `fsGroup:1000` on an emptyDir mount, but a Job with no volume needs the env approach. **Verifying a helm HOOK Job:** hooks (`helm.sh/hook`) DO render under plain `helm template` (only `--no-hooks` strips them), so chart-render tests can `docs.find(d=>d.kind==='Job' && name.endsWith('-mkbucket'))` and assert the env — no special flag needed (the older blob-backend.test.ts comment claiming hooks are excluded is wrong). For a true end-to-end kind check WITHOUT disturbing a live release: `helm template <liveRelease> ... --show-only templates/minio/bucket-job.yaml`, then strip the `helm.sh/hook*` annotations + rename to a `task<N>-verify-` name so `kubectl apply` runs it as a plain Job immediately against the running MinIO; poll `.status.succeeded`, capture logs, delete the verify Job. A negative control (same Job minus the env) reproduces the exact `mkdir /.mc` crash and proves the regression test discriminates.

- `2026-05-30` (TASK-82) — **Any per-turn SSE frame that must survive a (re)connect needs a boot-level "fill" subscriber + a replay-on-open step in `createSseHandler` — the per-connection live subscriber alone is NOT durable.** channel-web's `ChunkBuffer` (single-replica, replica-local — same J7 boundary as chunks) is the one host-side replay store: it already buffers chunks/phase/turn-error this way, and TASK-82 added the JIT approval card (`chat:permission-request`). The card was live-subscriber-ONLY, so on a gated turn — where the runner pod is COLD-spawned and the browser's `GET /api/chat/stream/:reqId` races that boot (it even 404s: `chat-flow SSE open failed: 404`) — the card fired before the subscriber attached and was lost; the orchestrator's per-conversation `upfrontCardsByConv` dedup (orchestrator.ts ~1957) then suppressed re-emission, so the pending cap-skill was permanently un-approvable. Fix shape: (1) `ChunkBuffer.appendPermissionCard(key, card)` + `tailPermissionCards(conversationId)`/`tailHostCards(reqId)` — skill cards keyed by conversationId (the SSE skill match key; the broker fires with the REAL conversationId but a FRESH reqId), host cards keyed by the routing reqId on the payload (the SSE host match key). These live in SEPARATE maps the IDLE_TTL sweep does NOT touch (a pending approval legitimately outlives the turn — minutes), de-dupe by skillId/host (re-proposal replaces in place), and are capped per-conversation. (2) A `createPermissionCardFillSubscriber` wired at boot next to the turn-error fill. (3) Replay block in `createSseHandler` right after the turn-error replay — NON-terminal (replay + keep the stream open, unlike turn-error which closes); strip the routing reqId from host cards before the browser sees it (same as the live host subscriber). (4) Evict on resolve: thread narrow `onCardResolved(conversationId, skillId)`/`onConversationDeleted(conversationId)` callbacks through `registerChatRoutes` → `createChatRouteHandlers` deps (NOT the buffer itself — keeps routes-chat decoupled from chunk-buffer), fire `onCardResolved` in BOTH permission-decision success paths (authored + catalog grant) and `onConversationDeleted` on delete. The fix is general (host cards too), not skills-only. Security: the card payload is PUBLIC manifest data (hostnames, slot names, opaque sessionId) — no secret ever rides the frame or the buffer (the key POSTs straight to the credential store, design §10), so buffering adds no secret-exposure surface, and the gate stays strict (the buffer only re-DELIVERS the card; approval still goes through the unchanged permission-decision grant). TDD proof that the regression test discriminates: python-splice the replay block out of sse.ts → the 3 "replays …" tests RED, restore → GREEN.

- `2026-05-31` (TASK-88) — **The SSE-open retry (`openSseStream` in `packages/channel-web/src/lib/transport.ts`) is budget-driven, not fixed-count.** TASK-84 first added a bounded retry (4 attempts / [150,400,900]ms ≈ 1.45s) on `GET /api/chat/stream/:reqId` for the cold-respawn 404 race, but 1.45s is too short for a genuine cold runner pod spawn (the channel-web early-bind in `routes-chat.ts` is best-effort; when it doesn't hold, `active_req_id` only lands AFTER the orchestrator's `sandbox:open-session` — seconds later), so the user still hit the manual CONNECTION_LOST banner. TASK-88 replaced the fixed count with a **wall-clock budget**: capped exponential backoff (`SSE_OPEN_BACKOFF_BASE_MS=250` doubling, capped at `SSE_OPEN_BACKOFF_CAP_MS=2000`) under `SSE_OPEN_TOTAL_BUDGET_MS=30_000`, terminating when the next clamped wait would overshoot the deadline. SAFE because the GET is idempotent — it only replays a bounded per-reqId buffer; only POST starts a turn — so re-opening it far longer than 1.45s never duplicates a turn (unlike a `regenerate()` re-POST, which FAULTA-5 forbids automating). Retry set unchanged (404/425/429/5xx + network throw retryable; 401/403/400/413 fail fast on the first attempt). TERMINAL throw differs by reason (review fix): a spent budget on a RETRYABLE failure throws `new Error(CONNECTION_LOST)` (the established FAULTA-5 sentinel) so the runtime's onError shows the user-facing "Connection lost. Retry to continue." banner + manual-retry button — the card's "fall back to the existing CONNECTION_LOST banner"; a fail-fast NON-retryable status keeps its verbatim `chat-flow SSE open failed: <status>` (a real client error, not a connection loss). A `failFast` flag set only on the non-retryable break distinguishes them. During the wait it surfaces `agentStatusActions.show(PHASE_LABELS['sandbox-starting'])` ("Starting sandbox…") set LAZILY (only after the first retryable failure, so the happy-path first-open never flashes it); the label transitions naturally once the stream opens or on the terminal throw → banner. **Testing budget-driven retry with fake timers needs `vi.setSystemTime(0)` AND `vi.useFakeTimers()` together** — the loop budgets on `Date.now()` deltas, so the system clock must advance with the fake timers or the wall-clock test (404-clears-after-10s) won't model elapsed time. Open-ended `for(;;)` loops with a deadline check (vs a counted loop) make the budget self-documenting. OUT OF SCOPE / follow-up: hardening the server early-bind to be authoritative (retry the bind / 503 the POST) so `active_req_id` is guaranteed before the 202.

- `2026-05-31` (TASK-89) — **The server early-bind of `active_req_id` in `channel-web/src/server/routes-chat.ts` `postMessage` is now AUTHORITATIVE — it closes the cold-respawn 404 at the SOURCE (the server twin of TASK-88's client retry).** The old early-bind was a single best-effort `bus.call('conversations:bind-session')` wrapped in `if (hasService) { try …catch → warn chat_bind_session_failed; fall through }` → on a miss the 202 went out for a reqId whose `GET /api/chat/stream/:reqId` could only resolve seconds later (after the orchestrator's post-`sandbox:open-session` bind). Replaced with an exported helper `bindActiveReqIdAuthoritative(bus, ctx, {conversationId, sessionId, reqId}): Promise<boolean>` — a deadline-bounded retry loop (`BIND_TOTAL_BUDGET_MS=400`, capped-exp backoff 25→150ms, same `for(;;)`+`Date.now()` deadline shape as TASK-88's openSseStream), returns `true` on first/eventual success, `false` when the budget is spent OR `hasService` is false. The call site 503s the POST (`{error:'bind-unavailable'}`) on `false` BEFORE dispatching `agent:invoke`, so a 503 starts NO turn (no FAULTA-5 duplicate-turn hazard) and the client retries the whole turn via the existing CONNECTION_LOST banner (transport.ts throws on any non-OK POST → onError → banner+manual-retry; NOT auto-re-POST). Happy path = exactly ONE bind call, 202, zero added latency. `hasService` convention KEPT (the hook is NOT a manifest `call` — same as the orchestrator's own bind site; the k8s preset always co-deploys @ax/conversations so `false` is unreachable in prod). 400ms absorbs the realistic transient miss (single-replica read-after-write lag, brief DB blip right after the row was created/fetched in the SAME request) without hanging a genuinely-missing row. `chat_bind_session_failed` telemetry preserved (now with an `attempt` count). **Testing the retry/never-establishes cases:** the live-container route harness boots the REAL @ax/conversations which always binds first try — so the transient/never cases live in a SEPARATE unit test (`bind-active-req-id.test.ts`) driving the exported helper against a real `HookBus` with a programmable `conversations:bind-session` (real timers — the whole 400ms budget runs sub-second), plus full-stack route tests that `vi.spyOn(harness.bus,'call').mockImplementation` to reject ONLY `conversations:bind-session` (forward all else to the real impl) for the 503 case, and assert `bindCalls===1` on the happy path (deterministic — `chatRunMockPlugin`'s agent:invoke mock does NOT itself call bind-session, so no race on the fire-and-forget orchestrator bind). This RESOLVES TASK-88's "OUT OF SCOPE / follow-up" item above and reduces how often the 30s client SSE-open budget is even exercised.

- `2026-05-31` (TASK-90) — **Test files are NOT type-checked anywhere in ax-next's gate.** Every package `tsconfig.json` `exclude`s `src/**/__tests__/**`, `pnpm build` is `tsc --build` (so it skips them), vitest runs via esbuild (strips types, no type-check), and `eslint.config.mjs` uses `tseslint.configs.recommended` (NOT `recommendedTypeChecked` — no `projectService`/`project`, so no type-aware lint). Consequence: a **type-only** regression test (e.g. asserting an exported `type`/`interface` exists and is structurally X) is NOT load-bearing — if the export vanishes, nothing in `pnpm build && pnpm test && pnpm lint` goes red. To genuinely guard a type/export-only change, make a NON-test `src` file depend on the type (it IS type-checked by `tsc --build`); then the build is the guard. Verified by temporarily renaming an export and watching `pnpm build` go red on the `src` consumers (not on the test). The vitest "type test" still has value as executable documentation of the contract, just don't mistake it for the enforcement.

- `2026-05-31` (TASK-93) — **Extending an existing compound-PK approval store to a new subject type = ADDITIVE polymorphic-subject column, NOT a fork or a rename.** TASK-93 widened `@ax/skills`'s `skills_v1_approved_caps` wall from skill-scoped to also connector-scoped. Pattern that worked: (1) add `connector_id TEXT NOT NULL DEFAULT ''` and extend the PK to `(owner,agent,skill_id,connector_id,cap_kind,cap_value)`; the **empty-string sentinel `''`** on the unused side keeps the column inside the PK (Postgres PK columns are NOT NULL — a nullable "one-of" column can't be in the PK and would also permit duplicate rows). This is purely additive: existing skill rows get `connector_id=''`, connector rows get `skill_id=''`, and existing `WHERE skill_id=X` queries still match because every connector row carries `skill_id=''`. (2) The migration header BANS column rename (destructive → side-table), so `(subject_kind,subject_id)` is out — the additive column is the smaller back-compatible change. (3) **Migration PK-swap for a pre-existing table must be idempotent:** `ALTER TABLE … ADD COLUMN IF NOT EXISTS` then a `DO $$ … IF NOT EXISTS (SELECT 1 FROM information_schema.key_column_usage WHERE constraint_name='…_pkey' AND column_name='connector_id') THEN DROP CONSTRAINT IF EXISTS … ; ADD CONSTRAINT … PRIMARY KEY(…) END IF $$` — the column-presence guard makes a fresh 6-col table AND a re-run both no-ops. Name the PK constraint explicitly (`CONSTRAINT skills_v1_approved_caps_pkey PRIMARY KEY (…)`) so the DROP/ADD is deterministic. (4) **Store API = discriminated subject ref** `ApprovedCapSubject = {skillId} | {connectorId}` intersected onto the input (`input & ApprovedCapSubject`); a `subjectColumns()` helper normalizes to `(skill_id, connector_id)` with the sentinel, and EVERY where-clause filters BOTH columns so a skill `linear` grant and a connector `linear` grant never collide. (5) **Hook wire input keeps both as `skillId?`/`connectorId?` optional** and a handler-side `resolveApprovedCapSubject()` enforces exactly-one (`invalid-payload` PluginError on both/neither; an empty-string id counts as absent). Existing `{skillId}` callers (orchestrator apply-authored-grant, agents resolve-authored-caps) are unchanged and `pnpm build` (tsc, which DOES type-check src consumers) is the back-compat guard. **Test isolation note:** `plugin.test.ts`'s `afterEach` DROPs `skills_v1_approved_caps`, so each `makeHarness()` re-migrates a fresh table — tests reusing `u1` don't bleed. **Env note:** a yolo-ship agent dispatched by auto-ship runs AS a subagent and has NO `Task`/Agent-dispatch tool, so the Phase-5 `ax-code-reviewer` subagent can't be spawned from inside — fall back to a disciplined self-review of `git diff main...HEAD` + the `security-review`/`security-checklist` skills. **CI flake seen:** `@ax/agent-claude-sdk-runner`'s `flush-workspace-host.e2e.test.ts` times out (5000ms) under parallel `pnpm -r test` load but passes in ~1.3s in isolation — unrelated to skills changes.

- `2026-05-31` (TASK-91) — **New @ax/connectors plugin (connectors-first-class epic, design Phase 1) landed: `connectors_v1_connectors` Postgres table + 5 storage/mechanism-agnostic service hooks `connectors:{list,get,upsert,delete,resolve}`.** Shape: `Connector = { id, name, description, usageNote, keyMode:'personal'|'workspace', visibility:'private'|'shared', capabilities }`. `capabilities` is the neutral `Capabilities` shape TYPE-imported from `@ax/skills-parser` (the ONLY @ax/* import; zod schema re-declared LOCALLY per invariant #2 — no runtime cross-plugin edge). **The backing-mechanism vocabulary (transport/command/stdio/url/mcpServers/packages/allowedHosts/credentials) lives ONLY inside the opaque `capabilities` JSONB spec — NEVER as a first-class hook field.** A `leak-guard.test.ts` walks each return schema's zod `.shape` (skipping the `capabilities` subtree) and asserts none of those leaky names appear at top level — boundary-review-as-a-test. **Wired into k8s preset ONLY** (step 9b in `presets/k8s/src/index.ts`), NOT the local CLI (Postgres-free; would crash bootstrap on `database:get-instance` — same wall as skills/routines/conversations). **The full set of preset test touchpoints for a new postgres plugin (all required or the WHOLE-PRESET test goes red, per-file misses them):** (1) `preset.test.ts` "expected production plugin set" `.toEqual([...sorted...])` — add the name; (2) `preset.test.ts` add a "loads X and registers [...]" reachability `it`; (3) `PLUGINS_TO_DROP` in BOTH `acceptance.test.ts` AND `multi-tenant-acceptance.test.ts` (each has its OWN local set); (4) root `tsconfig.json` references + preset `package.json` workspace dep. The "every calls entry is satisfied" test derives dynamically — no edit. **`connectors_v1_` added to the eslint `no-bare-tenant-tables` TENANT_TABLE_PREFIXES** (+ a valid/invalid case pair in `eslint-rules/__tests__/`); connectors are owner-scoped via `scope.ts` (`scopedConnectors(db,{userId})` = `owner_user_id` + `deleted_at IS NULL`). **zod-vs-exactOptionalPropertyTypes:** `Capabilities`' `.optional()` fields infer `T|undefined` which won't `satisfies z.ZodType<Capabilities>` — cast `as unknown as ZodType<X>` (same as @ax/skills return schemas) and rely on a runtime round-trip test as the drift guard. **`created` flag semantic for upsert = "no LIVE row existed"**: resurrecting a tombstoned row reports `created:true` (it's invisible to its owner, so re-connect == create). **Half-wired window OPEN by design (sanctioned):** the store exists, nothing routes through it yet (skill capability block stays authoritative); NOT an invariant-#3 violation because the plugin is fully registered+tested+reachable — only the CONSUMER lands in later epic phases (authoring `install_authored_connector` + approval-wall extension; orchestrator union; UI/IA reorg; skill-capability migration).

- `2026-05-31` (TASK-94) — **Agent-authored connector authoring = the connectors-side mirror of the authored-SKILL flow, NOT a fork.** Shape that worked (host data plane only): (1) `@ax/connectors` gains a DEDICATED `connectors_v1_authored` draft table keyed `(owner_user_id, agent_id, connector_id)` (status pending|active, name, usage_note, key_mode, capability_proposal JSONB) — distinct from TASK-91's `connectors_v1_connectors` LIVE registry, exactly as `skills_v1_authored` is distinct from `skills_v1_skills`. The draft carries `agent_id` (the live connectors table has none) because the approved-caps wall is per-(owner,agent,subject). (2) Four hooks: `connectors:{install-authored, list-authored, activate-authored, clear-authored}`. `install-authored` (= the `install_authored_connector` entry) takes FLAT args `{connectorId,name,hosts[],slots[],packages?,mcpServers?,usageNote?,keyMode}`, assembles+validates a canonical `Capabilities` proposal, persists status='pending'. (3) **ZERO-REACH invariant: `connectors:resolve` reads ONLY the live table, so a pending draft is never resolvable** — pin with a test (`resolve` rejects not-found for a pending-only connectorId). (4) Orchestrator fires ONE `chat:permission-request {kind:'connector', connectorId, name, hosts, slots, packages, authored:true}` card at session-open, deduped per (conversation, connectorId, shown-surface) via `upfrontConnectorCardsByConv` — the exact twin of the authored-skill card block + `connector-card.ts` mirrors `authored-card.ts`. (5) `agent:apply-authored-connector-grant` mirrors `applyAuthoredCapabilityGrant`: re-resolve draft via `connectors:list-authored` (unknown→not-authored), apply the `shown` TOCTOU intersection, write rows via **`skills:approved-caps-set` with `{connectorId}` subject (the TASK-93 wall — DO NOT fork)**, flip active via `connectors:activate-authored`, then cred→re-spawn / host-pkg→live-widen. mcp grants DEFERRED (the wall rejects kind:'mcp'; card excludes mcp). **Slot names re-validated against `SLOT_RE` at the install boundary** (untrusted model output, I5 defense-in-depth) — added `validateSlotName` to connectors `store.ts`. **Scope split (sibling cards):** the sandbox `install_authored_connector` tool executor + IPC action = TASK-95 (the hook's caller; adding the IPC handler here would be half-wired with no runner caller); the connector PermissionCard React UI + channel-web `postPermissionDecision` connector branch = TASK-99; resolving an ACTIVE connector's reach into the sandbox = TASK-97. The grant hook is NOT half-wired in the harmful sense — the card fires from the running orchestrator + the hook is bus-reachable + tested; TASK-99 wires the HTTP route. **Preset touchpoint:** `preset.test.ts` asserts the connectors `registers` array with EXACT `toEqual` — add the four new hooks there (the acceptance/multi-tenant PLUGINS_TO_DROP sets don't enumerate hooks, so no change). **REBASE GOTCHA (this run):** auto-ship merged TASK-96 (+TASK-103) ahead of me while I worked off a stale base (forked at TASK-92); TASK-96 rewrote the SAME @ax/connectors files (`types.ts` added `credentialPlan`/`requiresSharedKeyConsent` to ResolveOutput, `plugin.ts`, `index.ts`, `leak-guard.test.ts`). Always `git fetch origin main && git rebase origin/main` BEFORE opening the PR — conflicts were union-merges (index.ts exports, plugin.ts resolve comment, decisions.md top entry); my zero-reach test still holds because resolve throws not-found before reaching deriveCredentialPlan. **Env:** yolo-ship subagent has no Task tool → Phase-5 = disciplined self-review of `git diff origin/main` + security-checklist (confirmed works again).

## Connector authoring tool: connector_propose (host-executed) — TASK-95
- The model-facing connector-authoring tool is `connector_propose` (@ax/tool-connector-propose), a HOST-executed tool (`executesIn: 'host'`) — mirror of @ax/skill-broker's `request_capability`, NOT @ax/tool-skill-propose's sandbox-executor+IPC pattern. Connector args are pure structured JSON the model emits inline (no /ephemeral draft dir to read), so no sandbox executor / IPC action is needed.
- It registers BOTH the descriptor (tool:register) AND `tool:execute:connector_propose`, reads (userId, agentId) from the host-trusted tool ctx (the IPC server stamps these from the bearer-token session — listener.ts:144), and calls the `connectors:install-authored` hook (TASK-94). The hook persists a PENDING draft (zero reach); the orchestrator fires the one approval card at next session-open.
- Host-tool scope-stamping pattern (request_capability + skill.propose handler): NEVER read owner/agent from model input; read from ctx. Reject the `ipc-server` placeholder (unbound session) before calling the hook — `connectors:install-authored`'s requireScope only checks non-empty, so the guard belongs in the tool.
- Added to `ALWAYS_ON_BROKER_TOOLS` in chat-orchestrator/orchestrator.ts so non-wildcard tenant agents see it (mirror of TASK-76's skill_propose addition). Multiple orchestrator.test.ts arrays (toEqual on the always-on set + withBrokerDefaults BROKER const) must be updated together.
- Builtins (ax-skill-creator + ax-connector-creator) both load via loadBuiltinSkills() in presets/k8s/src/builtin-skills/index.ts, gated on `allowUserInstalledSkills`. The preset build's cpSync copies any *.md under src/builtin-skills recursively — a new builtin dir needs NO build-script change.

## GOTCHA: ax-skill-creator builtin was stale (pre-TASK-95)
- Before TASK-95, presets/k8s/.../ax-skill-creator/SKILL.md taught `install_authored_skill({skillId,hosts,slots,packages})` + `.ax/draft-skills/` — but that tool was DELETED (commit 4d3d0bc8) and replaced by `skill_propose({path})` writing to `/ephemeral/skill-draft/<id>/` with a `capabilities:` block in frontmatter (TASK-74). The live runner system-prompt (skillAuthoringNote) uses skill_propose. TASK-95 narrowed ax-skill-creator to know-how AND fixed it to the skill_propose reality + connectors[] reference.

## 2026-05-31 (TASK-111) — Skill→connector cap-resolution bridge
- **Where skill connectors[] become reach:** `chat-orchestrator/src/orchestrator.ts`, right after the TASK-97 `effectiveConnectors` resolution (~line 1819). Collect `new Set(unionedSkills.flatMap(s => s.connectors ?? []))`, dedup against the effective-set ids, resolve via the new `resolveSkillReferencedConnectors(bus, ctx, ids, alreadyResolved)` in `connector-union.ts`, then `[...effectiveConnectors, ...skillReferenced]` feeds the SINGLE existing `foldConnectorCaps` call. No second fold path (invariant #4).
- **`skills:resolve` ALREADY returns `connectors`** (via `_row-mappers.ts` `parseConnectors(manifest_yaml)` — derived-on-read, no stored column). The orchestrator's `ResolvedSkillForOrch` mirror just dropped it; adding `connectors?: string[]` (optional, `?? []`) surfaces it. `AuthoredResolvedSkillForOrch` inherits it; the agents authored-skill projection may not supply it (degrades to []).
- **request_capability connector folding** (`skill-broker/src/tools/request-capability.ts`): `skills:get` → `SkillDetail.connectors`; resolve each via `connectors:resolve` (hasService-gated, best-effort, re-validate id with `CONNECTOR_ID_RE`), union hosts/slot-names/packages into the EXISTING `kind:'skill'` card (deduped). The TASK-93 wall keys connectorId on its own — DON'T fork the wall, just widen the card surface. Add `connectors:resolve` to broker `optionalCalls`.
- **Security property to preserve:** a model-authored skill can only NAME a connector id (parser-validated opaque slug); reach comes from `connectors:resolve`, which reads ONLY the live (human-approved/curated) table — a pending authored connector draft is never resolved, so naming an unapproved connector grants ZERO reach. This is what keeps the bridge safe without the capability block being removed yet.
- **TASK-100 (removes the cap block + reject-validator) is now unblocked** — both paths (skill.capabilities + skill.connectors→connector) materialize in parallel during this half-wired window.

## Adding a new `chat:permission-request` card kind — the FULL channel-web boundary list (TASK-112)

A permission card `kind` is NOT just a React render branch. When TASK-37 added `kind:'host'` and TASK-112 added `kind:'connector'`, each had to touch the SAME set of channel-web boundaries. Miss one and the card either crashes, can't be approved, or can't be replayed after a cold-boot SSE reconnect (the TASK-82 hazard — every gated turn cold-spawns, and the SSE GET races the boot). Checklist for the next card kind:

1. **The `PermissionRequest` union, re-declared at THREE channel-web spots** (I2 — no shared import): `lib/permission-card-store.ts`, `lib/transport.ts` (the `SseFrame` inner `permissionRequest` type), `server/types.ts`. Plus the host producer's own local copy (e.g. chat-orchestrator `connector-card.ts`).
2. **`components/PermissionCard.tsx`** — a render branch (own `data-testid`) + an approve/grant handler. The skill+connector branches share a `renderReach(hosts, slots, packages)` helper so the design language can't drift (invariant #6).
3. **The decision/grant route** (`server/routes-chat.ts`) + its zod schema (`wire/chat.ts`) — IF the card POSTs a decision. The skill+connector subjects are `skillId` XOR `connectorId` on one `PermissionDecisionRequest` (a `.refine`), routed to different grant hooks.
4. **The SSE pipeline** (`server/sse.ts`): the LIVE per-connection subscriber (the conversationId-matched fall-through already handled connector cards — that's why TASK-94 "saw it in the stream body") BUT the boot-level **buffer-fill subscriber `createPermissionCardFillSubscriber` silently drops any kind it doesn't branch on** → no replay → un-approvable after a reconnect. Conversation-matched cards (skill, connector) buffer keyed by `ctx.conversationId`; reqId-matched cards (host) key by `payload.reqId`.
5. **`server/chunk-buffer.ts`** — `appendPermissionCard` dedup + `evictPermissionCard` must branch on the new kind's SUBJECT id. Conversation-keyed cards share the `skillCards` map + the conversationId-keyed replay/eviction; `evictPermissionCard(conversationId, subjectId)` filters by `skillId` OR `connectorId` (exact-string, namespaces don't collide → one id arg suffices).

Symptom if you only do (2): `TypeError: Cannot read properties of undefined (reading 'length')` when the skill default branch reads `request.description` on a card kind that has no description.

- `2026-06-01` (TASK-113) — **The TASK-94 authored-connector approval flow GATED reach but never PROJECTED it — the gap the TASK-101 walk hit.** `applyAuthoredConnectorGrant` wrote approved-caps wall rows (`skills:approved-caps-set` w/ connectorId) + flipped the draft `pending→active`, but a connector's reach is FOLDED into the sandbox by `resolveEffectiveConnectors → foldConnectorCaps`, which reads ONLY the curated registry `connectors_v1_connectors` (so do all 3 UI surfaces: Connectors tab, admin catalog, AgentForm attach). The authored draft activating in `connectors_v1_authored` is invisible to all of them → approved connector = npm 403 + reactive wall + invisible/unattachable. **The wall ROWS are a gate, not a reach source — don't confuse "approved-caps row written" with "connector usable".** Fix (path A, invariant #4): on approval, PROMOTE into the registry via `connectors:upsert` (the same hook TASK-100's migration + cap-migration.ts use), carrying the **`shown`-narrowed APPROVED** sets (`approvedHosts/approvedCreds/approvedNpm/approvedPypi` already computed for the wall rows) + `proposal.mcpServers` + `keyMode/usageNote/name` from the resolved draft, `visibility:'private'`, owner-scoped to `input.userId`. Then the EXISTING registry reads pick it up with ZERO new code (`resolveEffectiveConnectors` reads the owner's `connectors:list` private items → folds). The authored row stays draft/proposal staging (flipped active = audit only; nothing reads it for active reach/UI) → no second read path → invariant #4 holds. **Order: upsert BEFORE the activate flip** so a promotion failure leaves the draft re-approvable. hasService-guard `connectors:upsert` (back-compat w/ a preset that strips @ax/connectors). Mirror the `ConnectorsUpsertInput` shape structurally (I2 — no @ax/connectors import), like the existing `ConnectorsListAuthoredOutput` mirror. Cred-ref consistency for free: promoted slots carry the proposal's `account`, fold derives `account:<slot.account ?? connectorId>` identically → same vault row the JIT stored. Security: promoted caps == human-gated approved caps (never the full proposal — the `shown` TOCTOU narrowing flows through); `connectors:upsert` re-validates against CapabilitiesSchema; promotion runs only inside the human grant (pending draft never promoted → zero reach).
- `2026-06-01` (TASK-113 secondary) — **The single-slot `permission-card-store` (channel-web) last-write-wins clobbers the upfront connector connect card with a same-turn reactive `host` egress-wall frame on a WARM turn** (user sees "npm 403" wall instead of "Connect <service>"). Fix in `permissionCardActions.show()`: drop an incoming `kind:'host'` frame when the current card is `kind:'connector'` (connector wins; a `connector` frame still replaces anything). Client-store fix, NOT server-side — the wall fires legitimately; the connect card is just the actionable root cause. Server-side card *eviction* (routes-chat-card-eviction.test) is a separate concern (clears after a grant decision) and is untouched.

## Adding a new bus `calls` entry to a plugin → update presets/k8s/src/__tests__/preset.test.ts (2026-06-01, TASK-108)

When a plugin gains a new `calls: [...]` entry (e.g. the connectors Test probe added
`credentials:list` to `@ax/connectors` under `mountAdminRoutes`), TWO things in
`presets/k8s/src/__tests__/preset.test.ts` matter:

1. There's a per-plugin `expect(<plugin>!.manifest.calls).toEqual([...])` pin (search the
   plugin name). It's an EXACT-array assertion — a new call reds it with a `+ "your:hook"`
   diff. Update that array (it's the human-readable edge pin).
2. There's a dynamic `every "calls" entry is satisfied by some plugin's "registers"` test
   (×2 — base + titles-enabled). This is the invariant-#3 guard: it FAILS if your new call
   isn't registered by ANY loaded plugin in the preset. If it stays green, your call is
   wired end-to-end (here `credentials:list` is registered by `@ax/credentials`, already in
   the preset, loaded before connectors). If it reds, you added a half-wired call — either
   the providing plugin isn't in the preset or you typo'd the hook name.

The package-local `packages/<plugin>/src/__tests__/plugin.test.ts` `calls` assertion
usually constructs the plugin WITHOUT `mountAdminRoutes`, so a mount-only conditional call
(`if (mountAdminRoutes) calls.push(...)`) does NOT affect it — only the preset test sees
the mounted manifest. Don't forget the preset test just because the package test is green.

## 2026-06-01 — Per-agent attachment store pattern (skill_attachments → connector_attachments)

- `@ax/agents` owns per-agent attachment stores as JSONB columns on `agents_v1_agents`,
  written by a dedicated `agents:set-<thing>-attachments` hook + a
  `PATCH /admin/agents/:id/<thing>-attachments` admin route, NEVER via
  `agents:create`/`agents:update`. The orchestrator reads the column off the
  resolved `agent` (it's on `agents:resolve` output + `ResolveOutputSchema`).
  TASK-107 added `connector_attachments` (a plain `string[]` of connector-id slugs)
  mirroring `skill_attachments` exactly — store method `setConnectorAttachments`,
  validator `validateConnectorAttachmentIds` (slug/dedup/count, exported for the route).
  To add a new attachment store: copy ALL of: migration column (NOT NULL DEFAULT '[]'),
  `AgentsRow` field, `rowToAgent` corrupt-row guard + return, `create` insert, the
  THREE `.returning([...])` column lists (create/update/setSkillAttachments — use
  replace_all), store interface+impl, hook registration + manifest `registers[]` entry
  (+ the plugin.test.ts manifest snapshot!), route handler + route registration +
  `serializeAgent` field, the `AgentSchema`/`Agent` type, and EVERY test `Agent`/
  `AdminAgent`/`SerializedAgent` fixture (acl.test, return-schemas.test,
  admin-routes.test SerializedAgent iface, admin-agents.test.tsx sampleAgent,
  SkillAttachmentsSection.test mockPatch, channel-web lib/admin AdminAgent + a
  `patchAgent<Thing>Attachments` wire helper). tsc --build catches the fixtures.

- Connector attachments do NOT participate in the wildcard sentinel
  (`allowedTools=[] && mcpConfigIds=[]`) — same as skill_attachments. `mcpConfigIds`
  is the HOST tool-dispatcher MCP catalog binding (`filterByAgentScope` →
  `mcp.<configId>.<tool>`), an ORTHOGONAL mechanism from a connector's MCP servers
  (which reach the agent as sandbox `.mcp.json` installed-entries via foldConnectorCaps).
  TASK-98's stopgap that stuffed connector ids into mcpConfigIds mis-typed them as host
  MCP config ids; TASK-107 reverted that — the new store routes through the owner-scoped
  `connectors:resolve` gate, a net security tightening.

## 2026-06-01 — yolo-ship in an isolated worktree: ALWAYS prefix the worktree path

- auto-ship dispatches with `isolation: worktree`; the Bash cwd resets to the SHARED
  main checkout between calls, and Edit/Write/Read track state per EXACT absolute path.
  Reading `packages/.../foo.ts` (shared path) then `Edit`-ing it FAILS with "Edit the
  worktree copy instead" / "File has not been read yet". Fix: prefix EVERY Read/Edit/Write
  with the worktree root (`.claude/worktrees/<id>/packages/...`). The worktree starts with
  NO node_modules — run `pnpm install --frozen-lockfile` + `pnpm build` (full workspace,
  so `@ax/test-harness` etc. have dist) before any `pnpm test`, or tests fail with
  "Failed to resolve entry for package @ax/test-harness".
