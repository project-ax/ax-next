# TASK-519 — patterns worth reusing (2026-09-25)

- **The facts observer fires at `chat:end`, which is the orchestrator's IDLE REAP (~5 min after the last turn), not turn end.** A walk step "profile after one chat" must wait for the reap; poll host logs for `memory_observer_run` with a `ts` after your turn (a `--since` window overlaps older events — filter on ts).
- **The memory preset needs THREE credentials**: `provider:vertex` (embed), `provider:cohere` (rerank), and `provider:openrouter` (the observer's extraction model, `DEFAULT_MEMORY_OPS_MODEL`). Missing OpenRouter → `memory_no_llm_credential` at error, nothing extracted, UI silent.
- **Vertex/Cohere have no product write path** (TASK-523). Seed with an in-pod node script that bootstraps database-postgres + storage-postgres + credentials-store-db + credentials and calls `credentials:set {scope:'global', ownerId:null, kind:'api-key'}`; secret on stdin. Resolve packages via `createRequire(require.resolve('@ax/preset-k8s'))` from `/opt/ax-next/host/package.json`. The host pod's `/tmp` is wiped on restart — re-copy the script.
- **zsh `echo -` prints nothing** — use `printf -- '-'` when a script treats "-" as a sentinel.
- **`POST /admin/credentials/providers/:id/validate` wants the key BASE64** (`{key: base64}`); a raw key decodes to garbage and surfaces as "We could not reach OpenRouter".
- **A new agent is in BOOTSTRAP mode and gets NO `system-prompt:augment`** (no memory block, no Rules) until it writes IDENTITY/SOUL, deletes BOOTSTRAP.md, AND the session reaps (next spawn). Graduate the agent before testing the injected block (TASK-524).
- **`helm upgrade --reuse-values` on an old release + newer chart fails with nil-pointer on new keys** (`memory.facts.storage`); helm 3.13 lacks `--reset-then-reuse-values`. Dump `helm get values -o yaml` to a scratch file (it holds secrets) and pass it with `-f` before the overlay.
- **Read `facts.db` in the host pod with Node's built-in `node:sqlite`** (`DatabaseSync(..., {readOnly:true})`) — `select` from `memory_facts_v1` works; counting every table fails on the `vec0` virtual table.
- **The workspace memory HTTP route takes only `{query, history}`** — `limit`/`activeOnly` → `invalid-memory-request`.
- **Theme is `localStorage['ax-theme']` → `html[data-theme]`**, not prefers-color-scheme. Measure contrast by compositing through a 1×1 canvas (handles oklch + alpha); script at `.playwright-mcp/contrast.js` pattern.
