## 2026-09-19 — TASK-441 a server-refused Undo stayed offered and pressable

- `2026-09-19` (TASK-441) — **A REFUSAL VERDICT THAT LIVES ONLY IN A SIBLING FIELD IS A REFUSAL NOBODY RENDERS.** `POST /decisions/:id/undo` answers `{ decision, undone }`. On a refusal `@ax/decisions`' `machine.ts` hands back the stored row UNCHANGED — correct, a refusal must not write anything — and `toWireDecision` derives `undoable` from `consumedAt` / `replayedAt` / `replayClaimedAt` **with no clock**. When the refusal is the 10s window closing SERVER-side all three are still null, so the very response that refused answered `undoable: true`, and both renderers kept drawing an Undo button that could not work. The fix is not a new rule: it is writing the verdict the response already carries onto the field the renderers read. **When a response carries a verdict in one field and a state in another, check the state field can actually express that verdict.**
- `2026-09-19` (TASK-441) — **THE NON-VACUOUS SEAM FOR A "UI STILL OFFERS X" BUG IS THE UNFIXED SERVER'S PAYLOAD IN, A PRESSABLE CONTROL OUT.** A component test handed a pre-narrowed row proves nothing — the mock IS the fix. The test that fails for the right reason mounts `useDecisionQueue` wired to the real `DecisionRow` exactly as `TodayView` wires it, mocks the undo POST with the row a *pre-fix host* sends (`undoable: true`, `undone: false`), and asserts `queryAllByRole('button', {name:/undo/i, hidden: true})` is empty. `hidden: true` is the load-bearing option: it catches hidden-but-focusable, so the assertion is "cannot be reached", not "cannot be seen". Measured red: `expected [ <button/> ] to have a length of +0 but got 1`.
- `2026-09-19` (TASK-441) — **TWO NARROWING ENFORCEMENT POINTS BEAT ONE WHEN THE WALK RAN ON THE OLD HOST.** Route (`undone: false` ⇒ `undoable: false`) and client (`useDecisionQueue`'s undo records the same on the row it keeps). Neither re-derives the rule; each fails CLOSED (true→false only, only on `undone: false`), and `applyPolledRow`'s existing "still inside its own window" guard then drops every later poll for that row, so nothing can turn it back on. The client half is what holds against a host predating the route fix. Spread the row only behind the existing `isRecord` check — spreading `42` would smuggle `{ undoable: false }` past `act`'s non-record rejection as a valid-looking row.
- `2026-09-19` (TASK-441) — **`packages/channel-web` local gate: 5 suites fail as `Hook timed out in 120000ms` with 0 assertion failures** (`acceptance-e2e`, `connections-mirror`, `routes-attachments`, `routes-chat`, `stream-e2e`) and `packages/decisions` 2 more — all `PostgreSqlContainer` `beforeAll`s, Docker down on this host. No expected/received diff + wall-clock == declared timeout = infra. 2620 channel-web tests still pass.
