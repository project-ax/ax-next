# Patterns — TASK-561

- **Team `displayName` is now clean at the source**, like agent `displayName` (TASK-558):
  `@ax/teams` `store.ts` refuses `REWRITES_THE_SURFACE` at write and fences older rows on
  read in `rowToTeam` (`fenceStoredDisplayName`, fallback `'Untitled team'`). Readers in
  channel-web should NOT add their own fence for it.
- **The old `/\S/` check in a label door lets an all-zero-width name through**: `\S`
  matches U+200B. A `REWRITES_THE_SURFACE` door closes it; worth checking in any other
  label validator that only tests for "at least one non-whitespace char".
