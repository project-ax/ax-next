# Patterns — TASK-562

| Date | Pattern | Where |
|---|---|---|
| 2026-09-26 | Text-safety character classes (bidi / zero-width / controls) come from `@ax/core/surface-text` — never write a `[‪-‮...]` literal. `scripts/__tests__/surface-text-single-owner.test.js` fails any production file that names a bidi control. Widen the class there, once. | packages/core/src/surface-text.ts |
| 2026-09-26 | A consumer test that feeds U+2028/U+2029 through a fence that later collapses `\s+` is VACUOUS: JS `\s` already matches both. Prove the separators on a path with no `\s` collapse (a `.test()` write door, `fenceBody`, a scanner). | packages/agent-activity/src/__tests__/derive.test.ts |
