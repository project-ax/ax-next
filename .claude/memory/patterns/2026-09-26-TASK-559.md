## Patterns

- `2026-09-26` (TASK-559) — **A test spy subscribed after boot runs LAST, behind every plugin subscriber registered at boot, and `HookBus.fire` awaits each one in turn.** In `routes-chat.test.ts` 4b the spy sat behind `@ax/conversations`' `chat:turn-error` subscriber, which does a Postgres insert. Measured 24-52ms from POST to spy on an idle laptop, against a 50ms post-202 sleep, so CI flaked (run 36240494701). Before blaming a recent change to the fire path, list who else subscribes to the hook in the harness and what they do. Here the card blamed #725's `fireChatEvent`, which is the orchestrator's helper, and the orchestrator is not booted in that harness at all.
- `2026-09-26` (TASK-559) — **To reproduce a "subscriber ran late" flake, subscribe a stalling subscriber AHEAD of the spy.** It stands in for a slow real subscriber, needs no product mutation, and gives CI's exact failure deterministically. Worth keeping as a committed regression test (4b', 250ms stall), since it reddens against the old fixed sleep.
