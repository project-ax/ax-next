# Patterns — TASK-569

| Date | Pattern | Where |
|---|---|---|
| 2026-09-26 | Two turns CAN overlap on one conversation (a hold approved while the held reply still streams: held reqId A running, continuation reqId B bound). Any `chat:turn-end` subscriber that acts per-turn must key on `payload.reqId` (the runner stamps the inbox entry's reqId), never on `ctx.conversationId` alone — `ctx.reqId` is restamped by the IPC listener and useless. | packages/channel-web/src/server/sse.ts (4c) |
