- **2026-09-26 (TASK-555): supersedes TASK-551's pattern note that ipc-core's runner-reported chat:end is the one fire that cannot be bounded.** It is now bounded (`RUNNER_CHAT_END_SUBSCRIBER_TIMEOUT_MS`, 30 s). The TASK-514 trap is a TEST-shape problem, not a product one: a witness that counts a later-registered subscriber the instant the raced caller returns is testing microtask order. Count after settle: `vi.waitFor(>=1)` → `handle.shutdown()` → `toHaveLength(1)` (`expectChatEndOncePerTurn` in `presets/k8s/src/__tests__/acceptance.test.ts`). Every `chat:end` fire in the tree is now bounded.
- **2026-09-26 (TASK-555): to regression-test "a hung subscriber AHEAD of plugin X", put a hanger plugin before X in the plugins array** — bootstrap preserves array order among plugins that don't depend on each other, so its `init` subscribes first. See `packages/ipc-core/src/__tests__/runner-chat-end-bound.test.ts`.
