- **2026-09-26 (TASK-514): `HookBus.fire`'s microtask timing is load-bearing — don't wrap the unbounded path.** Routing every subscriber through a `.then().finally()` + async-helper chain added a few microtask hops per subscriber, and `presets/k8s` acceptance (`observedChatEndReqIds` toHaveLength(1)) went red: `agent:invoke`'s caller resumed after the orchestrator's `chat:end` subscriber resolved its waiter but BEFORE a later-registered `chat:end` subscriber ran. The Docker-less local run hid it among ~140 DOCKER_HOST failures — run the suite with `DOCKER_HOST=unix:///var/run/docker.sock` (OrbStack symlink) before trusting a local gate on a `@ax/core` change. Fix was to keep the old inline `await sub.handler()` path for unbounded fires and use the wrapper only when a finite `subscriberTimeoutMs` is passed.
- **2026-09-26 (TASK-514): to bound a subscriber hook, pass `bus.fire(hook, ctx, payload, { subscriberTimeoutMs })` at the fire site.** Per-subscriber; a subscriber over the bound is skipped (`hook_subscriber_timed_out`), not cancelled. Only `chat:start` uses it so far (`CHAT_START_SUBSCRIBER_TIMEOUT_MS`, 60 s).
