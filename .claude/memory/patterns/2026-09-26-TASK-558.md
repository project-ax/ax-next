# Patterns — TASK-558

- **Agent `displayName` is now clean at the source.** `@ax/agents` `store.ts` refuses
  `DISPLAY_NAME_FORBIDDEN` at write and fences older rows on read in `rowToAgent`
  (`fenceStoredDisplayName`). The TASK-480 decisions shard row saying the rail's
  displayName "stays unfenced" was true when written and is superseded. Readers in
  channel-web should NOT add their own fence for it.
- **Canonical "rewrites the surface" class** (one rule, spelled per plugin because of
  invariant 2): `[\u0000-\u001F\u007F-\u009F؜​-‏‪-‮⁦-⁩﻿]`.
  Copies: channel-web `lib/fence-line.ts` REWRITES_THE_SURFACE, agent-activity
  `derive.ts`, decisions `freshness.ts`, agents `store.ts` DISPLAY_NAME_FORBIDDEN.
  agent-runner-core `decision-turn.ts` is a WIDER variant (adds U+2028/9, U+2060-4).
  Grep for `202A-‮` to find them all before adding another.
- **Write door rejects, read path fences** is the shape for a stored user label that
  predates its validation: reject new bad input loudly; fence old rows in the one
  row-mapper every read funnels through; no migration.
