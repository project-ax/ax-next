# Mistakes

One entry per mistake. Graduates to `## Resolved` after two subsequent sessions avoid it. Never deleted.

- `2026-08-24` (TASK-316) — **A reviewer subagent dispatched into MY OWN worktree shares my git index, so the `git checkout <ref> -- <path>` I asked it to run silently landed in MY next commit and reverted the fix.** I told the reviewer to verify the guard's RED claim with `git checkout origin/main -- deploy/charts/ax-next` and then restore. That command writes the index as well as the worktree. Minutes later I staged two unrelated files by name, saw a clean-looking `git status --short` (its checkout had not happened yet), and committed — sweeping in its revert of three test files AND `vitest.config.ts`, which removed `globalSetup`. CI went red and I very nearly recorded a FALSE root cause from it: the failing assertions read "worker never saw the env marker", which looked like proof that vitest workers do not inherit the parent's environment, when in fact nothing had stamped the marker because globalSetup had been deleted out of the config. **Two rules. (1) Give a reviewer read-only instructions — Read/grep, and `git show <ref>:<path>` / `git diff <ref> -- <path>` for comparisons, never `checkout`/`restore`/`stash`/`add`. Do the mutating verification yourself, before dispatching. (2) `git status` immediately before `git commit` is not enough under concurrency — diff the commit you just made against the last known-good one (`git diff <good> HEAD --name-only`) and confirm the file list is exactly what you intended.** The tell that saved it: a post-fix measurement re-run reported 3x `repo add --force-update`, a number that could not exist in the tree I thought I had. Re-measure after every commit, not just once — the number is a checksum on the tree.
- `2026-08-24` (TASK-316) — **`someOutput.includes(SOME_URL)` to decide "is this URL known?" fails CodeQL (`js/incomplete-url-substring-sanitization`, HIGH) and is usually a real bug too — it is one of the 9 required checks, so it reddens the PR.** I used `(helmRepoList.stdout ?? '').includes('https://charts.bitnami.com/bitnami')` to decide whether the repo was registered; `https://evil.example/charts.bitnami.com/bitnami` satisfies that. Fixing it properly also fixed a functional bug I had not seen: **parse the structured output and compare the URL field exactly** (`url.replace(/\/+$/, '') === URL`, trailing slash tolerated), then act on the record you matched — helm resolves `Chart.yaml`'s `repository:` by URL, not by name, so a machine that registered the same URL under a different name is a WORKING setup that a hard-coded `helm repo update bitnami` would have broken. Rule: when a CLI offers `-o json`, parse it; a substring test over CLI text is both a CodeQL failure and a latent wrong answer. Note the check is named `CodeQL` in `gh pr checks` and can transiently read `fail` while `Analyze (javascript-typescript)` is still `IN_PROGRESS` — confirm with `gh api repos/.../code-scanning/alerts?ref=refs/pull/<n>/head` before concluding either way.
- `2026-08-24` (TASK-316) — **A source-scanning guard that strips BLOCK comments before LINE comments will silently mis-scan real code, because a `/*` inside a `//` comment opens a bogus block.** My drift guard filtered chart test files by "does it contain a `spawnSync(`", and `deploy/charts/ax-next/__tests__/env-shape.test.ts` dropped out of the scan entirely. Cause: the file has a line comment reading `walks /setup/* after install`, whose `/*` paired with the next real `*/` 57 lines later and swallowed the file's only `spawnSync`. **Strip whole-line `//` comments FIRST, then block comments** — and note the failure mode was silent-and-green-looking in the direction that matters: the guard's primary assertions all passed while it was policing one fewer file than it thought. The only reason I caught it was an over-guard asserting the scan still finds the specific files it exists to police. Every regex-over-source guard needs that assertion.
- `2026-08-24` (TASK-316) — **`Hook timed out in 30000ms` on a suite that shells out is a contention signal, not a slowness signal — resist the urge to reach for `hookTimeout`.** The parent card (TASK-302) was first diagnosed as a container hook-budget problem; measurement showed the hooks start no container at all. The timeout was three parallel copies of one `beforeAll` racing on a shared helm cache with a retry loop feeding the race. Raising the timeout would only have let all three racers retry longer. **Before tuning any timeout, count the concurrent writers to whatever shared resource the hook touches** (`~/.cache/**`, a lockfile, a build output directory). A PATH shim that logs every invocation of the spawned binary answers this in one run and gives a before/after number for the PR.
- `2026-08-22` (TASK-253, review F1) — **A catch block that returns a specific user-facing CAUSE must test for that cause, not assume it from where it sits.** I absorbed `claimForApproval`'s throw and answered "an identical request from this agent is already approved" — true only for pg `23505`, but returned for every error the UPDATE could raise. The dangerous cases are the SELF-CLEARING ones (deadlock `40P01`, `lock_timeout`, `statement_timeout` under eviction/failover/load): the row re-reads clean, so the user gets a confident, false explanation whose whole effect is to stop them retrying. A hard outage happens to be safe (the re-read throws too → 500), which is exactly why the transient case survives review by looking handled. **Rule: when a catch turns an exception into prose, gate on an identified cause and rethrow the rest — an unrecognised fault must surface as a fault.** Put the classification in the layer that owns the backend (here the store translating `23505` into a typed `DuplicateAuthorisationError`), not in the caller sniffing `err.code`, or the driver's vocabulary leaks upward and every alternate impl inherits a SQLSTATE contract. Test it against the real database with a `BEFORE UPDATE` trigger doing `RAISE EXCEPTION ... USING ERRCODE = '40P01'` — a real driver error, no mocking of the layer under test.
- `2026-08-22` (TASK-253) — **Never restore a vacuity-check mutation with `git checkout -- <path>` while the implementation is still UNCOMMITTED: it restores to HEAD and silently deletes the whole feature from that file.** I scripted "revert one guard → run tests → restore", and the restore threw away every uncommitted change in `store.ts` — the new store method, its doc comment and the column mapping. The tell was the NEXT case failing with `anchor not found`, plus `git status` no longer listing the file. **Commit the implementation FIRST, then run mutation/vacuity checks**; `git checkout --` is only safe once HEAD contains the code you want back.
- `2026-08-22` (TASK-253) — **A vacuity check that reverts ONE copy of a deliberately duplicated predicate proves nothing.** `@ax/decisions`' store re-states its `WHERE` predicates on both the SELECT and the UPDATE of a select-then-update claim (deliberate: the row can move in between). Removing just the SELECT copy left every test green and I nearly recorded three guards as "load-bearing" on that evidence. Mutate BOTH copies together — the pair is the guard. Related: a predicate can also be logically implied by a neighbour (`replay_claimed_at IS NOT NULL` is implied by `replay_claimed_at <= $cutoff`, since `NULL <= x` is unknown), in which case no test can catch its removal and it should be deleted rather than left as a guard nothing pins.
- `2026-06-29` (TASK-193) — **A FLOATING `undici: ^7.x` dep + an UNBOUNDED pnpm override (`undici@>=7.0.0 <7.28.0: ">=7.28.0"`) silently floated `@ax/credential-proxy` + `@ax/credential-proxy-bridge` (and transitively `testcontainers@^7.24.5`) onto undici 8.5.0, which REMOVED the old request-handler interface — 8.x's `assertRequestHandler` rejects the handler that Node's BUNDLED undici (`process.versions.undici` = 7.18.2) builds for the global `fetch`, throwing `InvalidArgumentError: invalid onRequestStart method` (`UND_ERR_INVALID_ARG`). This broke 28 cross-package tests that exercise `fetch(url, { dispatcher: <standalone-undici Agent/ProxyAgent> })`. It went unnoticed ~12 days because PR CI runs only AFFECTED-package tests; only the push-to-main FULL suite ran credential-proxy, so main was red while every PR went green.** KEY INVARIANT (the root cause, now a guard test `undici-handler-compat.test.ts`): when code hands a STANDALONE-undici dispatcher to the GLOBAL (bundled-undici) `fetch`, the two undici copies must share a MAJOR version or the handler contract mismatches. The override's replacement `">=7.28.0"` had no upper bound, so `^7.x` resolved to the newest matching = 8.5.0 (undici has NO 7.29+; 7.28.0 is the last 7.x). FIX: pin both packages to exact `7.28.0` (drop caret; 7.28.0 is the CVE-patched 7.x floor AND doesn't match the override key so it's untouched) AND cap the override replacement to `">=7.28.0 <8.0.0"` so no `^7.x` consumer (incl. testcontainers, used by ~20 pkgs' Postgres suites) can float into 8.x again. After: undici 8.5.0 is GONE from the lockfile; everything 7.x → 7.28.0, runner stays on its own 6.x override. **Next time you see `invalid onRequestStart method` / `UND_ERR_INVALID_ARG` from `fetch failed`: it's a dual-undici major mismatch — check `process.versions.undici` vs the standalone `require('undici/package.json').version`, and check the root `pnpm.overrides` for an unbounded `>=X` replacement that jumped a major. Pin exact + cap the override `<nextMajor.0.0`.**
- `2026-06-29` (TASK-192) — **A test that spawns a long-lived child process and intentionally leaves a request pending (to assert a guard) will throw an UNHANDLED REJECTION at teardown that fails the whole vitest run with exit 1 — even though every test reports passed.** I had a "rejects a concurrent call" test that fired `rerank()` against a fake worker that never replies (`void first`), then `close()`d it. `close()` SIGTERMs the worker; the `exit` handler rejects the still-pending promise; nothing awaits it → `⎯ Unhandled Rejection ⎯`, `Errors 1 error`, vitest exit 1 with "352 passed | 0 failed". SIGNAL: `Tests N passed` but exit code 1 + an "Unhandled Errors" / "Unhandled Rejection" block in the output. FIX: attach the assertion (`const settled = expect(first).rejects.toThrow(/exited|in flight/)`) to the pending promise BEFORE triggering teardown, then `await settled` after `close()` — so the rejection is handled. PREVENTION: when a test creates a promise it deliberately won't await to completion, give it a `.catch`/`expect(...).rejects` handler up front; and ALWAYS check the vitest EXIT CODE, not just the "N passed" line (CPU contention from a parallel CPU-bound job — e.g. a local cross-encoder run — also flakes timing-sensitive consolidator/reindex timeout tests; re-run on a quiet box before believing a failure).

- `2026-06-08` (TASK-164) — **auto-ship's serialized merge queue can HIJACK your assigned worktree mid-ship: it checked out `main` IN my worktree, ff-pulled it, then UNREGISTERED my worktree (left an empty dir) and relocated my branch to a NEW worktree (`git worktree list` is authoritative). My `git -C <old-worktree-path> checkout <branch>` then resolved that empty path to the MAIN checkout (`git rev-parse --show-toplevel` returned the main root) and SWITCHED MAIN onto my branch.** And the Edit tool stays pinned to the original (now-empty) worktree path, so edits 404. SIGNALS: `ls <your-worktree>` nearly empty (no package.json/node_modules), `show-toplevel` != your worktree, `git worktree list` shows your branch on a different `agent-*` dir (often detached+locked). RECOVERY: find where the branch lives, confirm the rebased commit is intact, make remaining fixes via `python3`/`sed` on the live working-tree path (NOT the Edit tool), build+test via `(cd <real-checkout> && pnpm --filter …)`, commit on the branch, push `--force-with-lease`, THEN restore main (`git -C <main> switch --detach origin/main`). Prevention: re-confirm `show-toplevel` EACH time before a git op; never `git checkout <branch>` against a path that might be main.

- `2026-06-08` (TASK-164) — **`gh pr checks <n>` DEDUPES check runs by name and can report a check as "pass" while a DUPLICATE run (from an earlier push) is still in_progress for the actual HEAD — false-green.** Two pushes (code, then docs-only memory) each triggered CI, leaving two runs per check; a monitor on `gh pr checks --json name,bucket` saw the COMPLETED copies and signaled settled while `gh api repos/<org>/<repo>/commits/<HEAD-sha>/check-runs` still showed `test`+`docker-build` in_progress for that SHA. LESSON: verify green via the HEAD-SHA-scoped check-runs API, not `gh pr checks` — zero `queued|in_progress` AND every conclusion `success` for the current HEAD. Fold memory into the code commit; a trailing docs-only push doubles every check run and creates the ambiguity.

- `2026-06-03` (TASK-152) — **The TASK-102 `migrations.test.ts` testcontainer container-START flake (`Error: Hook timed out in 10000ms` in a `beforeAll` doing `new PostgreSqlContainer(...).start()`) now also hits `@ax/skills`, not just `@ax/agents`** — same exact signature (`migrations.test.ts:67:1`, `Hook timed out in 10000ms`, `20 passed | 1 failed`), a flat Ryuk/testcontainer start latency exceeding vitest's default 10s `hookTimeout` under CI load. Hit my PR #314 `test` job even though my diff touches ONLY `@ax/sandbox-subprocess` (which passed 6/6, incl. compose.test 22/22) — `@ax/skills` was pulled into CI's affected set as a transitive dependent. **TELL it's this flake: the failing file is one your diff didn't touch, the file is `migrations.test.ts`, the single failure is in a `beforeAll`/hook (not an assertion), and the message is the 10s hook timeout on a PostgreSqlContainer start.** Action that's correct (per orchestrator + memory): `gh run rerun <run-id> --failed` — do NOT add a hookTimeout bump to `@ax/skills` from a sandbox-subprocess PR (not my bug; Bug-Fix-Policy is about bugs I introduce). Diagnosis path that worked while the run was still in_progress (so `--log-failed` 404s): `gh api repos/<org>/<repo>/actions/jobs/<jobId>/logs > /tmp/x.log` (per-JOB logs ARE available once the job completes, even mid-run) then grep for the FAIL line + confirm my own packages show `N passed`. A durable fix (raise `@ax/skills` migrations hookTimeout, like other testcontainer suites) is a separate infra card, not a feature-PR concern.

- `2026-05-31` (TASK-100) — **In an isolated worktree, the Bash tool's cwd defaults to the SHARED main checkout, NOT the worktree — so `cd /home/vpulim/dev/ai/ax-next && pnpm build` builds the WRONG tree, and `git rm <file>` stages a deletion ON MAIN.** My Edit/Write calls correctly hit the worktree (the harness pins them there + errors if I target the shared path), but my Bash commands silently operated on the shared checkout: an early `pnpm --filter @ax/skills-parser build` reported success while the dist stayed STALE (it rebuilt the shared tree's unchanged source), and a `git rm` of two obsolete test files staged them for deletion on `main`. Caught it when the parser dist still showed the old `capabilities` field after a "successful" build. **Fix that worked: set `WT=<worktree-abs-path>` once and prefix EVERYTHING with it — `git -C "$WT" …`, `(cd "$WT" && pnpm …)`, and `"$WT/packages/…"` for greps. Undo shared-checkout damage with `git -C /home/vpulim/dev/ai/ax-next restore --staged <f> && git -C … checkout -- <f>`.** Verify early: `git -C "$WT" rev-parse --show-toplevel` must equal the worktree, and after the first build grep the dist `.d.ts` for the change you made. Also run `pnpm install --frozen-lockfile` IN the worktree first — a fresh worktree has no `node_modules`/`@ax` links until you do.

- `2026-05-31` (TASK-98) — **`TestHarness` (from `@ax/test-harness`) is torn down with `harness.close()`, NOT `harness.shutdown()`.** I wrote an `afterEach` calling `h.shutdown()` (the per-PLUGIN lifecycle method name); `TestHarness` only exposes `close(opts?)` (it INTERNALLY calls each plugin's `shutdown()` in reverse order). All 9 of my new tests went red with `TypeError: h.shutdown is not a function` — but the failure surfaced in `afterEach`, so EVERY test in the file reported failed even though the bodies were fine; the real signal was the single `TypeError` line, not the 9 assertions. **Next time, before writing harness teardown, grep the interface: `grep -n "close\|shutdown" packages/test-harness/src/harness.ts` — the public method is `close`. And when every test in a fresh file fails identically, read the teardown error FIRST (an `afterEach` throw fails the whole file).**

- `2026-05-30` (TASK-87) — **When driving CI to terminal, a SINGLE read of `gh pr checks` / `gh run list` / the REST check-runs API / even the GraphQL `statusCheckRollup.state` can FLAP between terminal and in_progress on the SAME head SHA — GitHub serves it from multiple replicas with read-after-write lag, and a stale replica can stay frozen for many minutes (its run `updated_at` stuck at the run's START time) while fresh replicas already report success.** Symptoms I hit on PR #245: `gh pr checks --json state` falsely read ALL-terminal once (its `.state` field doesn't reliably reflect in_progress) → a naive monitor exited early; then the REST check-runs API returned DUPLICATE rows for `test`/`docker-build`/`Analyze(js-ts)` (one `completed/success`, one `in_progress/null`) because TWO workflow runs (CI + 2× CodeQL) both contribute jobs to the SHA; then run-level + GraphQL reads oscillated SUCCESS↔PENDING for ~minutes after the jobs genuinely finished. **Don't trust one read, and don't trust `mergeStateStatus=UNSTABLE` alone (it lags too).** What worked: a Monitor loop that (a) breaks IMMEDIATELY on any `FAILURE|ERROR|TIMED_OUT|CANCELLED|ACTION_REQUIRED` (so a real red never waits), and (b) only declares green after **N consecutive fully-settled reads** (I used 4 on GraphQL `statusCheckRollup.state==SUCCESS`; also viable: CI-run jobs all `completed_at != null` + run top-level `completed/success`, N in a row). The consecutive-stable gate rides out the oscillation; the immediate-fail break means the extra patience costs nothing on a genuine failure. Also: the foreground-`sleep` block applies to backgrounded Bash loops too (a `for…sleep 20` background poll got killed after 1 iteration) — use the **Monitor** tool for the wait loop, not Bash `run_in_background` with `sleep`. Across DOZENS of reads, no check EVER reported a failure conclusion — every value was `SUCCESS` or a stale empty/in_progress placeholder; that pattern (only-success-or-stale, never-fail) is itself strong evidence of green-but-lagging.

- `2026-05-30` (TASK-74) — **When RETIRING a behavior (e.g. the `.ax/draft-skills` workspace authoring path), grep for EVERY test file that SEEDS through it, not just the one whose hook you changed — a sibling test file seeded via the old path and CI caught it after I'd already opened the PR.** I re-backed `agents:resolve-authored-skills`/`agents:list-authored-skills` onto the DB and rewrote `authored-skills.test.ts` (the hook's own test) + `orchestrator.test.ts` + the preset canaries — but `promote-authored-skills.test.ts` (a DIFFERENT @ax/agents test file, for the admin promote ROUTE) seeded drafts via `workspace:apply('.ax/draft-skills/<id>/SKILL.md')` and asserted the promote route found them; once the reader stopped scanning the workspace, all 4 of its seeding tests 404'd. Local `pnpm test` would have caught it (it runs the whole package) but I'd only run the specific test files I edited. **Next time: after changing how a hook is BACKED, `grep -rln "<hook-name>" packages/ --include="*.test.ts"` to enumerate ALL consumer tests, and run the FULL affected PACKAGE suite (`pnpm -F @ax/<pkg> test`), not just `vitest run <the-file-I-touched>`, before opening the PR.** The fix was mechanical (a `seedAuthored` helper calling `skills:propose` instead of `seedFile` calling `workspace:apply`), but it cost a red-CI round-trip. Note: the CI `test` job runs "affected packages + dependents" so a retirement that touches `@ax/agents` pulls in every @ax/agents test file — exactly the ones a narrow local run skips.

- `2026-05-25` (TASK-24) — **The full `pnpm test` gate can show ~25 RED tests that are a MISSING-BINARY env artifact, not your diff: `agent-claude-sdk-runner`'s `git-workspace.test.ts` fails every case with `git lfs install --local failed: git: 'lfs' is not a git command` when `git-lfs` isn't installed in the sandbox** (it isn't, and `apt-get install git-lfs` needs root we don't have). `git-workspace.ts` materialization runs `git lfs install --local`; with no lfs binary every materialize-dependent test throws at setup. CI has git-lfs so these pass there. **Next time, before treating a full-suite RED as a blocker: (1) `grep` the failure message — `'lfs' is not a git command` (or any `command not found` / missing-binary) is environmental; (2) confirm the failing FILE is one your diff didn't touch (`git diff --name-only main...HEAD`); (3) re-run the package EXCLUDING that file (`pnpm exec vitest run --exclude '**/git-workspace.test.ts'`) to prove the rest is green; (4) confirm your OWN touched packages are fully green in isolation.** Don't chase a missing-binary failure in code you didn't change — same posture as the 2026-05-24 tsconfigRootDir phantom. (Also note: the tsconfigRootDir lint phantom did NOT reproduce this run — `pnpm lint` ran clean from the worktree — so re-probe, don't assume it's present.)

- `2026-05-25` (TASK-24) — **The Edit tool's first edit to a worktree file landed in the PRIMARY checkout instead, even though I passed the full worktree path** (`.claude/worktrees/agent-*/packages/ipc-protocol/.../ipc-client.test.ts`): vitest then ran the STALE worktree copy (my 4 new tests "passed" because they weren't there — only 11 of 15 ran). This is the FAULTA-5 file-edit twin recurring. Root-caused by grep-counting a unique marker string in BOTH paths: worktree=0, primary=1. **Fix that WORKED: revert the primary (`git -C <primary> checkout -- <file>`), then Read the WORKTREE path FRESH immediately before re-Editing it — after the fresh Read the Edit landed in the worktree (verified worktree=1, primary=0 clean).** So the reliable recipe when an edit may have gone astray: (a) after EVERY first edit of a file, grep-count a unique marker in BOTH the worktree and primary paths; (b) if it's in the primary, `git -C <primary> checkout -- <file>` then Read-then-Edit the worktree path; (c) a vitest run that shows FEWER tests than you added is the tell that you edited/are running the wrong tree.

- `2026-05-25` (TASK-5) — **An isolated per-package timing sweep UNDER-REPORTS the #146-class "near-5s-default flake" — those flakes are load-induced, so a test that's fast solo can breach 5s only when the full suite saturates the runner.** Auditing for near-boundary tests, I ran each package's suite solo with `--reporter=json` and ranked per-test `duration`; that found 8 tests >2.5s. But the real CI flake mode is parallel contention: when I ran the actual gate (`pnpm test` = all packages' testcontainers + suites at once), a 9th test — `workspace-git-server argv-injection.test.ts` `len-64k REST GET` — that runs in **233ms solo** timed out at **5004ms** under load (its 222-case battery of real HTTP fetches starves for CPU/socket when ~59 packages run concurrently). This is the same reason the original #146 test passed locally yet flaked on CI. **Next time auditing test-timeout robustness (or any "is this test near a deadline?" question): the full-suite `pnpm test` run IS the ground-truth detector — solo per-package timing is a first pass only. Always re-run the whole gate after the fix and trust a full-suite timeout over a fast solo number.**

- `2026-05-24` (TASK-9) — **CLOBBERED the shared `main` checkout by running git ops with `cd`/`git -C` pointed at the PRIMARY repo root instead of my worktree.** The Bash tool's cwd resets to the primary checkout (`/home/vpulim/dev/ai/ax-next`) between calls; the worktree branch was created fine via `git switch -c` but every later `cd /home/vpulim/dev/ai/ax-next && git …` (including the COMMIT) ran against the primary checkout, which was on `main` — so my commit landed on the shared `main`, exactly the clobber the orchestration warns about. Recovered by: `git -C <worktree> merge --ff-only <my-sha>` to move the commit onto my branch, then `git -C <primary> reset --hard <pre-clobber-sha>` to restore `main`. **Next time, in an orchestrated worktree: NEVER `cd`/`git -C` to the primary root for writes. Always `git -C "$WT"` (or `cd "$WT" && …` in the SAME compound command) where `$WT` is the worktree path from `git rev-parse --show-toplevel` at Phase 0. Confirm `git -C "$WT" branch --show-current` shows your task branch (not `main`) BEFORE every commit.**
- `2026-05-24` (TASK-9) — **`pnpm lint`/typecheck fail with `No tsconfigRootDir was set, and multiple candidate TSConfigRootDirs are present` when run from a worktree nested INSIDE the repo** (auto-ship's `.claude/worktrees/agent-*` lives inside the primary checkout, so typescript-eslint finds two `tsconfig.json` candidate roots and refuses). It is an ENV artifact of the worktree location, NOT a code defect — proven by stashing all changes and re-running on the clean baseline (identical ~2048 parsing-errors). CI checks out to a clean single-root dir via `actions/checkout`, so it never reproduces. When your diff touches zero `.ts/.tsx/.js/.mjs` files (e.g. a CI-YAML-only change), the lint gate over your diff is effectively N/A — validate the workflow with `actionlint` (downloadable via `rhysd/actionlint/scripts/download-actionlint.bash`) + a YAML parse instead, and let CI's own lint job be the real gate. Don't chase the 2000 phantom errors.
- `2026-04-23` — **Shebang alone isn't enough for `pnpm exec` / direct invocation.** `@ax/cli` `dist/main.js` had `#!/usr/bin/env node` but was not executable; `pnpm exec ax-next` failed. Fix: `chmod +x dist/main.js` in the postbuild script (commit 3488654). Next time: when scaffolding a CLI package, add the chmod to postbuild at scaffold time.
- `2026-04-23` — **`require.main === module` doesn't work in ESM.** `@ax/cli` main-module guard misfired under ESM. Fix: use `pathToFileURL(process.argv[1]).href === import.meta.url` (commit 87246cc). Next time: any ESM entry-point pattern uses `pathToFileURL`, not CJS idioms.
- `2026-04-23` — **kysely `^0.27.4` pulled GHSA-wmrf / GHSA-8cpq advisories.** Bumped to `^0.28.14` post-merge (commit 957288b). Next time: before pinning a new dep, run `pnpm audit` or check `npm view <pkg> --json` for advisories on the target version range, not just latest.
- `2026-05-03` — **PR #37 wired plugins into the preset but didn't update the chart's env-var stamping; tests didn't catch it because they sidestep the production listener layout.** `@ax/http-server` + `@ax/auth-oidc` + `@ax/teams` + `@ax/agents` + `@ax/channel-web` shipped into the preset, but `deployment.yaml` never set `AX_HTTP_HOST` / `AX_HTTP_PORT` / `AX_HTTP_COOKIE_KEY` / `AX_DEV_BOOTSTRAP_TOKEN`. Three plugins also collided on port 8080 (`EADDRINUSE`). Fix in PR #40: `serve` registers routes via `http:register-route` (no third listener); chart-shape test scans `presets/k8s/src/index.ts` for `env.*` reads + asserts every required var is in the deployment + every deployment env is read somewhere. Caught and removed an orphan `K8S_IMAGE_PULL_POLICY` env on the way. Next time: when wiring a new plugin into the preset, the chart-shape test is the contract — if it would catch the gap, ship it in the same PR.
- `2026-05-24` (TASK-11) — **A CORRECT timing fix deterministically EXPOSED a latent bug that flaky timing had been masking.** Fix #1 (per-turn commit now waits for the closing-text line) left the working tree CLEAN after the commit; that flipped the resync re-bundle from "git add -A sweeps leftover dirty content → non-empty → shipped" (lucky pre-fix) to "clean tree → `commitTurnAndBundle` returns null → resync mis-reads as 'absorbed' → turn dropped" (deterministic). Verifying ONLY fix #1's isolated scenario (lone tool turn → 4 turns ✓) passed, but the FULL original scenario (tool → attachment → reload) then FAILED worse than before. **Next time: after any fix that changes runner/commit TIMING or working-tree cleanliness, re-run the COMPLETE end-to-end scenario (not just the isolated repro) on the cluster before declaring done — a timing fix can surface a downstream latent bug. The `AX_COMMIT_TRACE` trace + ground-truth `git log` in the runner pod are how you catch it (the trace said "rebundle=EMPTY(absorbed)" while `git log` showed the commit sitting on `main`).**
- `2026-05-24` — **Don't hand the model a URI scheme (`ax-file://…`) to disambiguate a file path — it strips the scheme itself and may mistake it for a web URL.** The bug: uploaded attachments are referenced by the workspace-relative path `.ax/uploads/…`; because it starts with a dot, the model reads it as a home dotfile and `Read /home/<user>/.ax/uploads/…` → "File does not exist" (the file is under the runner workspace root `/permanent/.ax/uploads/…`). Tried wrapping the path in `ax-file://` (resolved by a PreToolUse rewrite). Cluster walk DISPROVED it: the jsonl showed the model received `ax-file://.ax/uploads/…`, **stripped the scheme**, home-rooted the bare path anyway (`Read /home/user/.ax/uploads/…` still failed), and even ran `ToolSearch select:web_extract` (mistook the URI for a web resource). The deterministic fix instead keys on the **`.ax/uploads/` attachment namespace** (our own convention, which the model can't strip): a PreToolUse `resolveAttachmentPaths` re-roots the structured path fields (`file_path`/`path`/`notebook_path`) whose value references the `.ax/uploads/` segment to `<workspaceRoot>/.ax/uploads/…`, BEFORE the host `tool:pre-call` so the host adjudicates the real path (and refusing any `..` segment). (An earlier draft rewrote EVERY string field via a `\S*` regex — Codex flagged that it mangled free-text fields like Edit `old_string` and quoted command paths, so it's path-fields-only now.) Verified on kind: the model's `Read /home/user/.ax/uploads/x.pdf` was rewritten to `/permanent/...` and succeeded on the first try (tool_result "PDF file read: /permanent/..."). Two reusable facts: (a) **the SDK PreToolUse hook can REWRITE tool input** via `hookSpecificOutput.updatedInput` (works for builtins like Read); (b) **only a live cluster reveals how the model handles a path/scheme** — Playwright masks file dialogs AND you can't unit-test model path-resolution behavior, so a model-facing path change MUST be walked on kind, not just unit-tested.
- `2026-05-26` (TASK-53) — **Recurrence of the worktree-leak (TASK-9 twin), this time via ABSOLUTE-PATH file writes, not git ops.** auto-ship dispatched me already-cd'd into my worktree (`git rev-parse --show-toplevel` at Phase 0 = the worktree ✓). But I then did all my Edit/Write/Read with absolute paths under `/home/vpulim/dev/ai/ax-next/packages/...` — the PRIMARY checkout root — so EVERY code edit + the plan file landed in the shared main checkout, not my worktree. The tell: late in Phase 5, `cd /home/vpulim/dev/ai/ax-next && git rev-parse --show-toplevel` returned the PRIMARY root (the Bash cwd had reset to it), and `ls <worktree>/packages/.../coldstart.ts` → "No such file"; the file was at `<primary>/packages/.../coldstart.ts`. Main was still on `main` with my edits as UNCOMMITTED working-tree changes (no commit clobber — caught before commit). **Recovery that worked: (1) `git -C <primary> diff -- <paths> > /tmp/x.patch` for tracked edits; (2) `git -C <worktree> apply /tmp/x.patch`; (3) `cp` the new untracked files into the worktree; (4) `git -C <primary> restore <tracked> && rm <new>` to clean main; (5) `pnpm install` in the worktree (it had NO node_modules — that's why my earlier tests "passed": they ran against the primary checkout where the edits actually were!); (6) rebuild+retest in the worktree.** **Next time, in an orchestrated worktree: capture `WT=$(git rev-parse --show-toplevel)` at Phase 0 and address EVERY Edit/Write/Read AND every `ls`/grep at `$WT/...`, never the bare repo root — the absolute path `/home/vpulim/dev/ai/ax-next/...` is the PRIMARY checkout and is a trap. A fast guard: after the first file write, `ls "$WT/<that file>"` — if it's missing, you wrote to the primary.** Also: a worktree carried in by `git worktree add` has NO `node_modules` until you `pnpm install` in it, so a green test run that used the repo-root cwd is testing the WRONG tree.
- `2026-05-30` (TASK-75) — **When re-pointing a handler off a now-dead `calls` capability, the bootstrap `verifyCalls` guard (`@ax/core/bootstrap.ts:287`) makes a "harness without that service" the cleanest pre-fix repro AND forces the manifest cleanup.** drop-turn used to `bus.call('workspace:list'…)`; my TASK-75 test harness omits the `workspace:*` services, so PRE-fix the harness threw `plugin '@ax/conversations' declares calls:['workspace:list'] but no plugin registers it` at `createTestHarness` — failing ALL cases (Bug-Fix-Policy "fails before" satisfied) and proving the manifest `calls` removal is MANDATORY, not optional: once the handler stops calling the hook you MUST drop it from `calls` or every harness lacking a registrar reds. (The *production* no-op is subtler — there `workspace:list` IS registered by the git plugin and returns `{paths:[]}` because `.claude/projects/` is gitignored, so the `if(paths.length===0) return` no-ops silently; verified by code-read + TASK-70 decisions.md:12.) **Next time you cut a hook call from a handler: grep the plugin for OTHER callers of that hook before deciding; if it was the last one, remove it from the manifest `calls` + the now-unused type imports in the SAME diff, and update the manifest-assertion test (`plugin.test.ts`) — a stale `calls` entry is both an over-grant (I5) and a bootstrap red for any harness without the registrar.**
- `2026-05-30` (TASK-75) — **A jsonl test-fixture helper that DEFAULTS every line to the same `message.id` silently breaks any test that drops an EARLIER turn and expects a LATER one to survive — `dropTurnFromJsonl` coalesce-drops every later assistant line sharing the dropped line's `message.id`.** My `jsonlLine({uuid})` helper defaulted `message:{id:'m1',…}`, so a 2-line fixture (t1,t2) both had `m1`; dropping t1 also dropped t2 → transcript empty, byte-round-trip assertion got `''`. This is CORRECT impl behavior (the SDK coalesces a logical assistant response across consecutive same-`message.id` lines; parse.ts:29), not a bug — the FIXTURE was wrong. **Next time building jsonl fixtures for drop/coalesce logic: give each distinct logical turn its OWN `message.id` unless you're explicitly testing the coalesce case (then assert the whole `message.id` group disappears together). The "drops most recent" case is immune because there are no later lines to coalesce.**
- `2026-05-30` (TASK-81) — **`pnpm --filter @ax/<pkg> exec vitest …` run from the PRIMARY repo root silently tests the PRIMARY checkout, not your worktree — and a fresh worktree has NO `node_modules` so the worktree's own vitest binary isn't even there.** Symptom this session: I ran `pnpm --filter @ax/channel-web exec vitest run <4 files>` from `/home/vpulim/dev/ai/ax-next` (the Bash cwd resets there between calls) — it ran only 3 of the 4 files and never found my brand-new `tool-name.test.ts`, because `--filter` resolved channel-web in the MAIN checkout (which lacks my new files). The new file was on disk in the worktree (`git status` showed it `??`). **Root cause + fix: a `git worktree add` tree has no `node_modules` until you `pnpm install --frozen-lockfile` IN it (fast — symlinks from the shared store; the `ax-git-server`/`ax-claude-sdk-runner` bin ENOENT warnings are just pre-build dist-absent, harmless). Then run the package's OWN binary from inside the worktree: `cd "$WT/packages/channel-web" && ./node_modules/.bin/vitest run …`. NEVER `pnpm --filter` from the repo root in a worktree session.** Also: the full channel-web suite shows ~20 RED files with `Failed to resolve entry for package "@ax/core" / "@ax/credentials" / "@ax/skills/manifest" / "@ax/test-harness"` until you `pnpm build` (tsc --build) so the workspace packages' `dist/` exports exist — those reds are an unbuilt-dist env artifact (all 527→721 actual assertions pass once built), NOT your diff. Same posture as the TASK-24 missing-binary and TASK-53 wrong-tree entries: grep the failure (`Failed to resolve entry`/`command not found`) before treating a full-suite red as a blocker.
## Resolved

- `2026-05-16` (**Resolved 2026-05-24**) — **Walked Phase C MANUAL-ACCEPTANCE expecting the runner→host workspace sync chain to be live; it wasn't in main at the time.** Spent two chat rounds asking the agent to write `.ax/routines/notify.md`, watched the runner pod spawn + exit cleanly, saw `routines_v1_definitions` stay at zero rows. Root cause (as of 2026-05-16): Default Agent had `workspace_ref=NULL` and the chat orchestrator deferred `workspaceRef` consumption; no plugin in the active preset wired session-close → bundle export → workspace apply. **Resolved 2026-05-24:** the workspace bundle-apply chain shipped (workspace Phase 2/3 + the bundle-wire PRs) — `presets/k8s/src/index.ts` now registers the six-hook bundle-aware contract (`workspace:apply-bundle` + `workspace:export-baseline-bundle`) plus commit-notify + materialize, and the Phase C MANUAL-ACCEPTANCE walk completed 2026-05-16. The session-close → apply gap no longer exists. **Lesson still holds:** before walking any MANUAL-ACCEPTANCE scenario that depends on `workspace:applied` firing from an agent-written file, grep `presets/k8s/src/index.ts` for `commit-notify` / `bundle` / `workspace:apply` wiring first — if nothing connects session-close to apply, the scenario fails regardless of what your PR shipped.

- `2026-05-25` (FAULTA-5) — **Edited the PRIMARY checkout instead of the worktree by passing the primary path to Read/Edit.** The dispatch put my cwd in the worktree (`.claude/worktrees/agent-*`), but I read+edited `/home/vpulim/dev/ai/ax-next/packages/...` (the SHARED main checkout, on `main`) because that's the path the env-info "Working directory" snapshot and habit suggested. My two Edits "succeeded" but landed on the shared `main` working tree (dirtying it under parallel agents); the worktree copy was untouched, so vitest run against the worktree still showed the OLD test. Caught it by `grep`-comparing the two paths. Recovered with `git -C <primary> checkout -- <file>`. **Next time, in an orchestrated worktree: every Read/Edit/Write file_path MUST be prefixed with the worktree root (`$(git rev-parse --show-toplevel)` captured at Phase 0), NEVER `/home/vpulim/dev/ai/ax-next/...`.** This is the file-edit twin of the 2026-05-24 `git -C` mistake — same shared-main hazard, different tool. Sanity-check before editing: `grep` the line you're about to change in BOTH paths; if the primary already has your change, you edited the wrong tree.

- `2026-05-26` (TASK-34) — **Added `@ax/skill-broker` to `presets/k8s/package.json` `dependencies` + a VALUE import in `presets/k8s/src/index.ts`, but FORGOT the matching `{ "path": "../../packages/skill-broker" }` in `presets/k8s/tsconfig.json` `references`.** The ROOT `pnpm build`/`pnpm typecheck` (`tsc --build`) PASSED locally because the root `tsconfig.json` references `packages/skill-broker` directly — so the project graph resolved. But CI's `test` job (`pnpm typecheck` step) and the `docker-build` job (`pnpm --filter @ax/cli build`) FAILED with `TS2307: Cannot find module '@ax/skill-broker'`, because a package's OWN `tsc --build` resolves its imports through ITS tsconfig `references`, NOT the root's — and `@ax/cli` → `@ax/preset-k8s` → (needs) `@ax/skill-broker`. **The root build is NOT a proxy for the isolated per-package build.** A value import of a workspace `@ax/*` pkg needs BOTH a package.json dep AND a tsconfig reference; a TYPE-ONLY import (e.g. channel-web's `import type {Destination} from '@ax/credentials'`) needs only the dep. **Next time wiring a new plugin into a preset: add the tsconfig `references` entry in the SAME edit as the package.json dep, and verify with an ISOLATED `pnpm -F @ax/cli build` (clean its dist + tsbuildinfo first) — not just the root `pnpm build`.** Regression guard added: `scripts/__tests__/tsconfig-references.test.js` asserts every package's tsconfig references each `@ax/*` runtime dep that builds with tsc (runs unconditionally via `pnpm test:scripts`; mutation-tested to fail on the exact omission).

## 2026-05-26 — worktree `pnpm test` needs node_modules install + git-lfs is absent locally (TASK-41)
- A fresh auto-ship worktree has NO `node_modules` (worktrees don't share the primary checkout's). `pnpm -F @ax/skills test` fails with `sh: 1: vitest: not found` until you run `pnpm install --frozen-lockfile` in the worktree, then `pnpm build` (workspace `dist/` must exist or `@ax/test-harness`/`@ax/skills-parser` fail to resolve). Do this BEFORE the first test run.
- vitest file filter is positional: `pnpm exec vitest run src/__tests__/x.test.ts` (NOT `pnpm test -- src/...` — the `--` arg does NOT filter, it runs the whole package suite).
- `pnpm test` (whole monorepo) shows 25 failures in `@ax/agent-claude-sdk-runner` `git-workspace.test.ts` — ALL `git lfs install --local failed: git: 'lfs' is not a git command`. This is ENVIRONMENTAL (git-lfs not installed in the agent sandbox), pre-existing on main, unrelated to any skills/host-side change. CI has git-lfs. Don't chase it; verify your own package suite is green (`pnpm -F @ax/<pkg> test`) and that your diff doesn't touch the runner.

- `2026-05-26` (TASK-44) — **Worktree-isolation leak: file-editing tools (Write/Edit/Read) resolve the absolute path EXACTLY as given; Bash cwd is the worktree but the tools are not auto-rooted there.** I hardcoded `/home/vpulim/dev/ai/ax-next/packages/...` (the SHARED main checkout) from CLAUDE.md/memory examples and my first batch of writes landed on `main` in the shared checkout, NOT my worktree branch. Caught it via `git -C <worktree> status` showing nothing changed while `git -C <main> status` showed `?? packages/host-grants/` + ` M tsconfig.json`. Reverted main cleanly (`git checkout tsconfig.json && rm -rf packages/host-grants`) and re-did everything under the worktree prefix `/home/vpulim/dev/ai/ax-next/.claude/worktrees/<id>/...`. **Rule: in an isolated-worktree dispatch, EVERY Write/Edit/Read path must start with the worktree root, never the bare repo root.** Confirm with `git rev-parse --show-toplevel` and prefix accordingly.

- `2026-05-30` (TASK-65) — **Writing a test that needs a NUL byte in a string literal via the Write tool put a LITERAL `\0` byte into the source file** (`const withNul = 'a'.repeat(63) + '<NUL>'`), which made git classify the whole `.test.ts` as BINARY (`Bin 0 -> 6884 bytes` in `--stat`, `file` reports `data`) — it would render as an unreadable binary blob in the PR/review and CI diff. The Codex review didn't catch it (it reads via `sed`), and the package tests PASSED (the NUL is a valid runtime char). Caught it via `git diff --stat` showing `Bin`. **Fix: never embed a real control char in source — use the explicit escape `'\x00'` (visible ASCII, same runtime value).** Repaired with `perl -0777 -i -pe "s/\\+ '\\x00';/+ '\\\\x00';/g"` then re-ran tests (still 39/39) and amended. **Next time, after writing any test that constructs strings with control bytes (NUL/ESC/etc.), run `file <path>` or `git diff --stat` — if it says `data`/`Bin`, you wrote a raw control char; swap it for `\xNN`/`\uNNNN`.** General tell: a source file showing as `Bin 0 -> N bytes` in `git diff --stat` is never right for `.ts`/`.js`.

- `2026-05-31` (TASK-91) — **A new package referenced by `presets/k8s/src/index.ts` needs its project reference in BOTH the root `tsconfig.json` AND `presets/k8s/tsconfig.json` `references`.** I added connectors to the root tsconfig (so `pnpm build` was green) but FORGOT the preset's own tsconfig. The root build masked it — CI's `docker-build` (agent image runs `pnpm --filter @ax/preset-k8s build`) and the `test` job both build via the preset's OWN tsconfig, where `tsc --build` won't build the un-referenced connectors project → `presets/k8s/src/index.ts: error TS2307: Cannot find module '@ax/connectors'`. **Repro/guard: clean dist + run the ISOLATED `pnpm --filter @ax/preset-k8s build` (the exact CI/Docker invocation) BEFORE pushing** — the root `pnpm build` does NOT catch a missing preset-tsconfig reference because the root tsconfig builds the dep first. Same trap applies to package.json workspace dep (needed too, but it was the tsconfig reference that bit). Checklist for wiring a new pkg into the preset now has FOUR build-graph touchpoints: (1) preset package.json dep, (2) preset tsconfig.json reference, (3) root tsconfig.json reference, (4) the package's own tsconfig references its deps.

- `2026-05-31` (TASK-94) — **CI `test` job can go RED on a postgres-testcontainer TEARDOWN race that is NOT a real test failure and NOT in your changed package.** Seen on PR #257: `@ax/agents/migrations.test.ts` reported `12 passed (12) / 168 passed (168)` but `1 error` → job exit 1. The error is an UNCAUGHT exception during shutdown: `terminating connection due to administrator command` (pg code `57P01`) — a pooled connection was still open when the testcontainer was stopped by the suite's `afterAll`/`afterEach`. Vitest counts an uncaught exception during teardown as a suite failure even though every assertion passed. This is DISTINCT from the hook-timeout flake TASK-103 fixed (that raised `hookTimeout`; this is a connection-open-at-shutdown race). It amplifies under parallel `pnpm -r test` load (~12 agents container suites + the whole repo). **Tell:** the failing package is one you didn't touch, the suite shows `N passed / 1 error`, and the error mentions `57P01` / `administrator command` / `terminating connection`. **Diagnosis:** run the named test file in ISOLATION locally (`cd packages/<pkg> && npx vitest run src/__tests__/<file>.test.ts`) — if it's green, it's the teardown flake. **Action:** re-run the failed CI job (`gh run rerun <run-id> --failed`), don't chase a fix in your PR (Bug Fix Policy is about bugs YOU introduced). The channel-web `routes-chat.test.ts` `afterAll` container-stop TIMEOUT (>10s) is the same family. A durable fix (close pools before `container.stop()`, or swallow `57P01` in teardown) is a separate infra card, not a feature-PR concern.

## worktree agent: `cd /home/vpulim/dev/ai/ax-next && ...` switches to the SHARED checkout
- In an isolated-worktree agent, Bash cwd DEFAULTS to the worktree, but prefixing a command with `cd /home/vpulim/dev/ai/ax-next` (the primary checkout root) silently runs it against the SHARED main checkout — which may have stale/unbuilt dist, the OLD version of files you edited, and no new packages/lockfile entries. This caused: (1) a full `pnpm -r run test` that showed 50+ phantom failures (the shared checkout had unbuilt @ax/tool-skill-propose dist), and (2) `grep` reading the stale shared-checkout SKILL.md instead of my narrowed worktree version.
- FIX: run gate/grep/ls commands WITHOUT a leading `cd` (default cwd is the worktree), or use absolute worktree paths (`/home/.../.claude/worktrees/<id>/...`), or `pnpm -C <worktree>`. Never `cd` to the primary checkout root for any operation that reads/writes files or runs tests.

## 2026-05-31 (TASK-111) — `pnpm test` from worktree: two non-issues to expect
- `pnpm` Bash calls default-cwd to the agent's WORKTREE; an explicit `cd /home/vpulim/dev/ai/ax-next` jumps to the SHARED checkout (whose node_modules may be stale/half-installed → spurious `Cannot find package '@ax/core'` for e.g. @ax/tool-connector-propose). Never `cd` to the shared root for build/test; run from the worktree (no cd, or `git -C <worktree>`).
- `@ax/memory-strata`'s `test/bench/__tests__/configs.test.ts > Config C (BM25 + dense + RRF)` is a 5000ms-timeout-prone BENCHMARK test that flakes under full-suite parallel load; it passes in isolation (`pnpm --filter @ax/memory-strata test`). Not a real failure — re-run the single package to confirm before chasing it.

- `2026-06-02` `active` — **TASK-139: my fix for one review finding INTRODUCED a worse one.** Codex round-1 flagged "read-before-size-check" (P2); I fixed it with `stat()` + `isFile()` + size before `readFile`. Codex round-2 caught that `stat()` FOLLOWS symlinks, so my "fix" created a P1 secret-leak: an agent-writable `.ax/SOUL.md -> /proc/self/environ` passes `isFile()` and `readFile` injects the runner's env/IPC token into the LLM system prompt. The right primitive was `lstat` (rejects the symlink itself). Lesson: when adding a pre-read guard on an AGENT-WRITABLE / untrusted path, default to `lstat` not `stat` — `stat` follows links and any follow-symlink read of an attacker-controlled path whose bytes reach the model (or a log, or another trust sink) is a secret-exfil primitive. Also: re-run the reviewer after a security-relevant "fix" — the second round is where the fix's own hole surfaced. And TEST the dangerous case explicitly (symlink-to-regular-file-outside-dir), not just the benign one (symlink-to-dir), or the gap hides.

## 2026-07-01 — plan drifted from design; the plan's own test caught one drift, review caught the other
- On feat/memory-search-snippets the implementation plan drifted from the approved design in two places: (1) design implied reusing "the existing tsquery" — under real Postgres that's `plainto_tsquery` AND-semantics, which deterministically fails the plan's own conformance test (body has "graduated", query "degree graduated"); (2) design §3 specified `ts_headline(... StartSel=, StopSel=)` (no markers emitted) but the plan used default `<b>` markers + a mapper strip-regex — the regex also eats a literal `<b>` in body text (follow-up card).
- Lesson: when a plan embeds literal SQL/code, diff it against the design's version before dispatching implementers; "the plan has complete code" ≠ "the plan's code matches the design". A conformance test that runs on BOTH backends (shared kit) is what turned drift (1) into a red test instead of a shipped divergence.
- Also: `snippet()`/`ts_headline` differ on truncation markers and matching semantics per engine — any cross-backend text function needs an explicit parity case in the shared kit, not per-backend unit tests.

## 2026-07-07 — rollup tests: salienceMaxFraction:1 lets the shared date token form a spurious class
When a rollup test seeds docs via `writeNewDoc` with DATED facts (`(2026-…) …`) and sets `salienceMaxFraction: 1` to make a tiny homogeneous corpus qualify (the trick the `doctors` test used), the shared year token `2026` appears in 100% of dated facts → `detectClasses` groups all docs into a spurious `2026s` class (df ≥ K, salience filter disabled). The `doctors` test got away with salience 1 only because it used `mkDoc` with summary-ONLY docs (no dated facts). Fix: don't reach for salience 1 with real dated docs — add distinct-token FILLERS so the target class stays a minority under the DEFAULT 0.4 salience, which is exactly what drops the `2026`/`user`/`day` generics. Salience filtering is load-bearing precisely for these date/number tokens.

## 2026-07-08 — e2e bench SILENTLY dropped every question's FINAL-session facts (0ms-debounce race)
The `@ax/memory-strata` e2e driver (`test/bench/e2e-driver.ts`) set `consolidatorDebounceMs: 0`, thinking "consolidate immediately." But `chat:end` fires TWO independent subscribers — Observer (fire-and-forget, writes inbox) and Consolidator (`debouncer.schedule`). With a 0ms window the pass is `setTimeout(0)` — a macrotask that fires DURING the driver's `await settleObserver` (the Observer's real LLM call is a macrotask that yields), i.e. BEFORE that session's facts are written. So each session's facts get consolidated by the NEXT session's pass; the FINAL session has no next pass → its high-confidence facts strand in the inbox, invisible to `memory_search` → false-refusal on any question whose gold fact is in a late session. Depressed the LongMemEval e2e score across the board (true score is higher than measured). **Tell:** a fact is a well-formed inbox observation (conf ≥0.7) but there's no doc for it and search can't find it; re-running `runConsolidation` on the kept workspace promotes it with zero code change. **Why the existing driver test missed it:** its durable fact was in session 0 (swept up by session 1) AND used an instant stub — no macrotask latency, so the race never triggered. A regression test for a latency-dependent race MUST simulate latency (`await new Promise(r=>setTimeout(r,25))` in the stub) and put the fact in the LAST session. **Fix:** `debounceMs 0 → 10*60_000` so the ingest loop's explicit `flush()` (already after settleObserver) is the sole race-free driver. Full writeup: `docs/plans/2026-07-08-memory-strata-postrollup-e2e-analysis.md`. Production note: the shipped plugin has the same 2-subscriber structure — "ask immediately after telling" may strand the newest turn's facts until the next turn (self-heals); a possible ask-time drain guard is a separate card.

## 2026-08-19 — `pnpm build` does NOT type-check test files, so wire-shape drift only surfaces in the full suite
PR 2 of the provider-agnostic runner sequence added a required `runner` field to `AgentConfig`
and turned `ChatOrchestratorConfig.runnerBinary` (a string) into `runnerBinaries` (a map). Nine
subagents each ran `pnpm build` **and** their own package's suite, all green — and the branch was
still broken in three packages nobody owned. Every package `tsconfig.json` carries
`"exclude": ["src/__tests__/**"]`, so `tsc --build` never sees a single test file. A test fixture
that constructs `agentConfig` without `runner`, or calls `createChatOrchestratorPlugin({ runnerBinary })`
with the deleted scalar key, compiles clean forever and fails only when that suite actually runs.

Three distinct drift classes surfaced, each in a package that did not change:
1. **`agentConfig` literals missing `runner`** → `PluginError: 'owner.agentConfig.runner' must be a
   non-empty string` from `session-inmemory`'s `validateOwner`. Hit `@ax/ipc-server`, `@ax/ipc-core`,
   `@ax/mcp-client`. 62 literals across 14 files repo-wide.
2. **A bare model id passed to `agents:create`** → `model 'claude-opus-4-7' is not in the allow-list`,
   in `@ax/skills`' install canary.
3. **The stale `runnerBinary` scalar** in `@ax/skills`' orchestrator config — silently ignored, so the
   agent's runner id resolved against an EMPTY map and every turn came back `terminated` instead of
   `complete`. The most expensive one to read: the symptom names neither `runner` nor `runnerBinary`.

`pnpm -r run test` is **fail-fast** (`ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL`), so each round exposed only
the first broken package. Three full-suite rounds were needed. Also: piping it (`pnpm test | tail`)
swallows the exit code — the first round LOOKED like exit 0 while `@ax/ipc-server` was red. Redirect
to a file and echo `$?` instead.

**Next time you change a shape that crosses packages:** (a) budget for a full `pnpm test`, not just the
owning packages' suites — per-package green means nothing here; (b) grep for *structural* consumers in
`**/__tests__/**` before dispatching, because tsc will not; (c) a shape scan beats a keyword grep — walk
`agentConfig: {` blocks by brace depth and check for the new key, rather than `grep -L runner` (the word
"runner" appears in prose in most of these files, so the keyword filter hid `ipc-core` entirely).

- `2026-08-19` (PR 3, aisdk runner) — **A test HARNESS fake with the wrong response shape silently disabled a whole
  code path across an entire e2e suite, and announced itself only as a stderr line that read like benign noise.**
  The `attachments.list` fake returned `{ attachments: [] }`; `AttachmentsListResponseSchema` is `{ files: [...] }`.
  `materializeUploads` parses defensively and swallows its own failures (correct — a bad upload must not abort a
  session), so every parity row logged `runner: attachments.list failed; skipping upload materialization` and the
  upload/attachment path never executed ANYWHERE. Every test still passed. Worse: the same line appears in the
  claude-sdk runner's suite output for the same reason, so I had already scrolled past it once without connecting it.
  Caught only by an adversarial reviewer reading the stderr rather than the assertions.
  **Guard:** when a fake stands in for a schema-validated response, build it from the SCHEMA (import it and
  `Schema.parse(fake)` in the harness, or copy the shape from the schema definition) — never from memory of the field
  names. And treat recurring stderr in a green suite as a finding, not as scenery: a swallowed-error code path is
  exactly where a fake's shape error hides. Corollary for reviewers: read the suite's stderr, not just its verdict.

- `2026-08-19` (PR 3) — **Reintroduced a regex class this repo had ALREADY solved, and then wrote a guard for it that
  could not fail.** `composeInstructions` used `.replace(/\s+$/, '')` on the composed system prompt (which concatenates
  agent-authored `.ax/` files, so: uncontrolled input). CodeQL caught it as `js/polynomial-redos`, HIGH. `governed-paths.ts`
  already avoids the identical shape for trailing slashes, with a comment naming the same rule — so the prior art was one
  grep away and I didn't look.
  Worse, my first regression test used `'x' + '\t'.repeat(200000)` — an all-whitespace TAIL, which the regex handles in
  0ms. The pathological shape is a whitespace RUN FOLLOWED BY a non-whitespace char (`'\t'.repeat(60000) + 'x'`): `$`
  never matches, so the engine retries from every position. Measured 1830ms vs 0ms for `trimEnd()`.
  **Guards:** (a) before writing any `/<something>+$/` or `/^<something>+/` trim, use the native `trimEnd`/`trimStart`
  or a `while (s.endsWith(c))` loop — this repo treats that as the house style, and CodeQL enforces it; (b) a ReDoS
  regression test MUST use a non-matching-suffix input, or it proves nothing. Both times this session that a guard of
  mine looked strong and wasn't, the tell was the same: I never ran it against the mutation it was supposed to catch.

- `2026-08-20` — **A test ran a 71-project workspace build from inside a vitest worker, and the resulting OOM was
  misread as a vitest bug for weeks.** `packages/cli/src/__tests__/e2e.test.ts`'s `beforeAll` called
  `pnpm --filter '@ax/cli...' build` — which includes channel-web's `vite build` (rollup + minify, the heaviest step
  in the repo) — while pnpm concurrently ran other packages' suites and several testcontainer Postgres containers on
  a 4-vCPU/16 GB `ubuntu-latest` runner. A worker got OOM-killed, so CI failed with `Worker exited unexpectedly` /
  `Timeout terminating forks worker`, **zero failing assertions**, and a DIFFERENT file blamed each run. It reddened
  `main` (on a docs-only commit) plus PRs #399/#400/#402 before anyone traced it; the standing note said "raise/limit
  `poolOptions.forks.maxForks`", i.e. we were about to tune the symptom.
  **The tell that cracked it:** the repeat-offender file (`credentials-wiring.test.ts`) was a SIBLING of the
  build-spawning file — a concurrent fork of the very vitest instance detonating the build. When a flake keeps
  blaming one package, look at what its OTHER test files do, not at the blamed file.
  **Also worth noting:** the in-test build was never even load-bearing for imports — a missing `@ax/test-harness`
  dist fails at import time, before `beforeAll` runs. It only ever produced `dist/main.js` for a `node` spawn, which
  the pipeline's `pnpm typecheck` (`tsc --build`) already emits.
  **Guards:** never build the workspace from a test under CI (`scripts/__tests__/no-workspace-build-in-tests.test.js`
  is a tripwire for it); and when triaging a zero-assertion CI failure, check for heavy work spawned BY the suite
  before blaming the runner.

- `2026-08-20` — **The aisdk runner could not boot in the container at all, and 220+ green tests could not see it.**
  `packages/agent-aisdk-runner/src/tools/builtins.ts` imported `glob` from `node:fs/promises` for the Glob/Grep
  built-ins. That API landed in **Node 22**; `container/agent/Dockerfile` pins **Node 20**. So the runner died at
  module load with a `SyntaxError`, the host logged `sandbox-terminated` → `sandbox-open-failed`, and EVERY turn on
  an `aisdk`-pinned agent failed with no explanation. The `claude-sdk` runner was fine in the same image, which made
  it look environmental rather than runner-specific.
  **Why nothing caught it:** the repo's `engines.node` is `>=24` and the whole suite — including the parity e2e that
  drives the real `runRunner` shell — runs on the DEVELOPER's Node, never on the container's. The docker build even
  printed `WARN Unsupported engine: wanted {"node":">=24.0.0"} (current: v20.20.2)` on every build; nobody read it.
  **The general shape:** the runner executes on a different Node than the one that tests it. Any API newer than the
  Dockerfile's pin compiles, type-checks, unit-tests and e2e-tests green, then dies in the pod.
  **Guard:** `packages/agent-aisdk-runner/src/__tests__/node-floor.test.ts` reads the pinned major straight out of
  the Dockerfile and fails on a runner-side import of anything newer. Bump the base image and entries drop off the
  deny-list by themselves.

- `2026-08-20` — **"Cross-runner resume demotes to fresh" was true in one direction only; the other direction
  bricked the conversation.** Switching an agent `aisdk` → `claude-sdk` handed
  `agent-claude-sdk-runner`'s `JsonlTranscriptSource` the aisdk runner's transcript. It adopted the bytes
  unconditionally, the shell's F2a guard read "bytes present ⟹ resumable", and `query({ resume })` exited 1 with
  "No conversation found with session ID". Every later turn crashed identically — reproduced 5/5.
  **The seam already had the fix in it:** `TranscriptSource.write` may return `'unusable'`, which
  `restoreTranscriptForResume` turns into "no resumable transcript" and F2a turns into a clean fresh start. The aisdk
  source used it; this one never did, behind a comment asserting "any bytes the host store returns for a session
  this runner wrote are, by construction, in the shape the SDK reads back" — a premise that stopped being true the
  moment an agent could change runner.
  **Lesson:** when a comment justifies skipping a check by naming an invariant, re-read whether that invariant still
  holds after the feature that made it conditional. And a documented behaviour is a claim to TEST, not to trust —
  both of this walk's real bugs were found by exercising something the docs said already worked.

- `2026-08-20` — **The "fork-pool flake" was never one bug, and #404 only fixed the first one.** The standing note said
  RESOLVED; `main` was red on most runs anyway, with a DIFFERENT zero-assertion cause: a better-sqlite3 native
  destructor running after the Node environment is torn down —
  `Assertion failed: (env) != nullptr` in `Statement::~Statement()` / `Database::~Database()`. SIGABRT, so vitest's
  forks pool reports `Worker exited unexpectedly` and blames whichever test file the dying worker held. **A different
  file each run** (`cli/credentials-wiring`, `memory-strata` bench smoke,
  `credentials-admin-routes/destination-handlers`) — which is exactly why it kept reading as a vitest flake.
  Probabilistic, not a version bump: Node 24.19.0 was on the last GREEN main run too.
  **Three wrong turns worth remembering**, all from reasoning instead of measuring:
  1. Assumed my PR caused it. It did not — three pre-branch `main` runs carry the identical signature. Check `main`
     before debugging your own diff.
  2. "Pinning handles for the process lifetime is a leak" → switched the exit-close registry to `WeakRef`. CI put the
     abort straight back on the next run. Keeping the handle REACHABLE is most of the fix: an unreachable unclosed
     handle is GC-finalized on V8's schedule, possibly during isolate disposal. Strong refs, deliberately.
  3. Read one green-ish run as proof the weak version was the culprit. It is probabilistic — a single run proves
     nothing either way. Compare signatures across several.
  **What actually fixes it:** better-sqlite3 **13.x deletes `AddEnvironmentCleanupHook`** (verified by unpacking the
  tarballs — absent in 13.0.3, present in 11.10.0 AND 12.11.1, so 12.x is not an escape hatch). It needs Node >=22;
  `container/agent/Dockerfile` pins Node 20 and the image ships better-sqlite3, so it is blocked on bumping the
  container — the SAME Node-20-vs-Node-24 split that made the aisdk runner unbootable. Card filed.
  **Partial mitigation that did land:** close tracked Databases on `process.exit` (kills the `Database::~Database()`
  variant, not the `Statement` one), fix the bench's real leak, and raise `teardownTimeout` where a native-module
  worker overran vitest's 10s default — that message only LOGS, the stop is still awaited, and the log is what
  reddens an otherwise all-passing run.

- `2026-08-20` — **`pnpm test` at the repo root reported exit code 0 after running 7 of ~75 packages, and I nearly took that as a green gate.**
  The root script is `pnpm -r run test`, which **bails at the first failing package**
  (`ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL`) and leaves every package after it in the topological
  order simply *unrun*. There is no "skipped" line and no count — the output just ends, and the
  summary of whichever packages did run looks entirely healthy. A `| tail` on the output also
  masks the real exit status (see the older note in
  `project_local_suite_preexisting_failures.md` about `pipestatus`).
  **What it looked like:** seven packages, all green, a plausible-looking tail. **What it was:**
  ~9% of the suite, with the packages this branch actually changed mostly unexecuted.
  **Guard:** for a pre-PR gate always run `pnpm -r --no-bail run test`, and *count* the
  `Test Files` lines against the number of packages with a `test` script
  (`grep -l '"test"' packages/*/package.json presets/*/package.json deploy/charts/*/package.json | wc -l`).
  If those two numbers disagree, the gate did not run — regardless of the exit code.
  This is the cousin of [[feedback_run_tsc_alongside_vitest]]: there, a green vitest hid what only
  tsc catches; here, a green-looking run hid the packages that never ran at all.

- `2026-08-20` — **A guard that reads `MAP[userControlledKey]` and then checks `=== undefined` does not
  guard anything for ~8 keys.** In PR 4 the aisdk runner resolved its provider with a bare
  `PROVIDERS[provider]`, where `provider` is the first segment of `agentConfig.model`. Plain-object
  index access walks the prototype chain, so `constructor`, `__proto__`, `toString`, `valueOf`,
  `hasOwnProperty`, `isPrototypeOf`, `propertyIsEnumerable` and `toLocaleString` all return
  something non-`undefined` — `PROVIDERS['constructor']` is the `Object` constructor. The
  `entry === undefined` branch never fired, execution fell through to the *credential* check, and
  the operator got an error about an env var named `undefined` instead of about the provider they
  actually typed.
  It failed closed, but only because the credential check happened to run before `entry.create`
  was dereferenced — reorder those two and `Object.create` is a real callable.
  **Found by the security-checklist walk, not by 244 passing tests**, because every test used a
  realistic provider name. **Guard:** any lookup keyed by outside input goes through an
  own-property check (`Object.prototype.hasOwnProperty.call`), and its test table includes the
  Object.prototype key names — not just a nonsense string like `'notaprovider'`, which *does*
  return undefined and therefore proves nothing.
- `2026-08-20` (TASK-217) — **A raw NUL byte in a source file makes `grep`/`rg` return NOTHING for that file, silently.** `packages/memory-strata/src/map.ts` carried a literal NUL in `hashFacts`' separator, so `file` classified it as `data` and grep treated it as binary — suppressing ALL matches with no error and no warning. I grepped for `filterSensitive` in that file, got zero hits, and was one step from concluding the import was missing; it was sitting at line 43. SIGNAL: `grep -c ""` on the file prints nothing and `file <path>` says `data`. FIX: write the `\u0000` escape, never the raw byte — the runtime string is byte-identical, so hashes and fixtures don't change (verified: same sha256, so the densify cache stayed valid). DETECT across the repo with perl, not grep (grep cannot report on what it will not read): `git ls-files -z | xargs -0 perl -ne 'if(/\x00/){print "$ARGV\n"; close ARGV}'`. PREVENTION: when a grep of a SPECIFIC file returns empty but you have reason to think the symbol is there, check `file` before trusting the empty result. Five tracked files were affected (TASK-218). NOTE the same trap bites when WRITING: a `gh project item-create --body` or a heredoc containing a raw NUL is rejected for "control characters" — build such strings programmatically instead of typing the escape inline.
- `2026-08-20` (TASK-217) — **"Known flaky test" is a claim that expires; re-derive it before repeating it.** The `@ax/conversations` seq-race test was recorded in memory (and repeated by me during TASK-216) as a CPU-contention flake. It is not: `APPEND_MAX_RETRIES = 8` while the test drives `N = 20` concurrent appends, so an unlucky append can lose the seq race up to 19 times and the `duplicate key` error escapes the retry loop. That is a product race with a real correctness cliff (dropped conversation events under a burst), filed as TASK-220. SIGNAL that it was not infra: an unhandled `duplicate key` error with a product stack (`store.ts:820`), not a worker crash with zero assertions — exactly the distinction `feedback_flaky_test_may_be_a_real_bug` draws. PREVENTION: before writing a failure off as a known flake, run it in isolation SEVERAL times and read the actual error; "passes on a quiet box" is consistent with a real race, not evidence against one.

- `2026-08-20` (TASK-217) — **A persisted `cd` from a previous Bash call made me commit to the SHARED main checkout, twice.** The Bash tool keeps its working directory between calls. I ran `cd /Users/vpulim/dev/ai/ax-next && gh project item-create ...` for a board write; the next call was a bare `git add -A && git commit`, which landed on `main` in the primary checkout — the one thing CLAUDE.md forbids — and `git add -A` also swept in two of the user's untracked working files. Recovered with `git reset --mixed HEAD~1` + `git checkout -- <memory file>` (nothing was pushed; the untracked files returned to untracked). Then it happened AGAIN a few calls later with `git rebase origin/main`, which rebased the main checkout instead of my branch (harmless — it was a pure fast-forward — but the same root cause). SIGNAL: `git rev-parse --show-toplevel` is the primary checkout, or `git branch --show-current` says `main` when you expect a task branch. PREVENTION: put an ABSOLUTE `cd <worktree>` at the front of EVERY git-touching Bash call — never rely on inherited cwd — and never chain a board/`gh` call that cds elsewhere with a later git call. Announcing "I'll use absolute paths from now on" is not a fix; the next call must actually carry the cd.
- `2026-08-20` (TASK-221) — **A guard that validates only the TYPE of a bound lets an out-of-range value bypass a `>=` cap entirely.** `loadCache` accepted `gatedAttempts` on `typeof === 'number'`. A NEGATIVE value (corrupt/hand-edited sidecar) then never satisfied `attempts >= CAP`, so the retry cap silently never engaged and the doc re-paid an LLM call every pass forever — reintroducing the exact unbounded cost the cap was added to prevent. Note the near misses that hid it: NaN/Infinity self-neutralize (`JSON.stringify` writes them as `null`, failing the typeof check) and huge POSITIVE values cap immediately, so negatives were the one malformed shape that degraded unsafely. PREVENTION: when a persisted number feeds a `>=`/`<=` bound, validate the RANGE (`Number.isInteger(x) && x >= 0`), not just the type — a type check is not a bounds check. Caught by a reviewer, not by me.
- `2026-08-20` (TASK-218) — **git decides a diff is binary from EITHER side of the pair, so fixing a file's binary-ness still renders as `Bin N -> M bytes`.** The four NUL-byte files diffed as byte counts even though the change was one character each, because the OLD blob carried the NUL. On a PR whose entire purpose was "make these files readable by ordinary tools", the diff itself was unreadable. FIX: a `.gitattributes` marking source extensions as `diff` (text) — the PR then renders normally. Check this whenever a change removes NULs/BOMs/encoding oddities from a tracked file.
- `2026-08-20` (PR 6) — **`MockLanguageModelV4`'s `finish` chunk takes the V4 PROVIDER usage shape, and the flat one is accepted silently.** Writing `usage: { inputTokens: 90_000, outputTokens: 1, totalTokens: 2 }` (what `parity.e2e.test.ts` uses, harmlessly, because it never reads usage back) maps through `asLanguageModelUsage` — which reads `usage.inputTokens.total` — to `StepResult.usage.inputTokens === undefined`. No error, no warning. My first compaction e2e "passed the SDK a step with 90k reported" and actually passed it `undefined`, so the compactor silently took its estimator fallback and the assertion failed for a reason three levels away from the cause. Correct shape: `usage: { inputTokens: { total: N }, outputTokens: { total: 1 } }`. SIGNAL: log `steps.at(-1)?.usage` inside `prepareStep` — `{ inputTokenDetails: {}, outputTokenDetails: {} }` with no `inputTokens` key means the chunk shape is wrong, not that the SDK failed to report. PREVENTION: when a mocked SDK value flows somewhere you assert on, assert the value ARRIVED at least once, or the mock's shape is untested.
- `2026-08-20` (PR 6) — **A truncation marker makes truncation non-idempotent unless it is also a sentinel.** `prepareStep`'s returned `messages` CARRY FORWARD to later steps, so any rewrite there runs over its own output every step. The first mask left `head(500) + "\n\n[N more characters were dropped…]"` — which is longer than the 500-char budget, so the next step masked the masked value, and the step after that masked that, eating the surviving head one marker at a time. Caught by an idempotence test written on the suspicion, not by anything the feature does in one step. FIX: end the note with a fixed sentence and skip any value that already ends with it. PREVENTION: every `prepareStep` transform needs an `f(f(x)) === f(x)` test — the carry-forward makes idempotence a correctness property, not a nicety.
- `2026-08-21` (PR 7) — **A mocked model that only implements `doStream` makes an LLM-calling code path fail SILENTLY when that path swallows failures.** Rung 3's summarizer goes through `generateText` → `doGenerate`; the turn loop goes through `agent.stream()` → `doStream`. The first compaction e2e used the existing `modelReplaying` helper (streaming only), so the summarizer call threw, the ladder did what §7 tells it to do — mark the attempt failed and fall through — and the test's real assertions were about the turn, which still passed. A green test that proved nothing. FIX: `modelReplayingWithSummarizer` in `parity.e2e.test.ts`, with a `doGenerate` that records what it was sent, plus an explicit `expect(summarizerPrompts).toHaveLength(1)` BEFORE the outcome assertions. PREVENTION: when the code under test is allowed to swallow a failure, the test must assert the thing HAPPENED, not only that the outcome looks right — "fell through gracefully" and "worked" are indistinguishable from the outside, which is the whole point of the fallback.
- `2026-08-21` (PR 7) — **A test fake for a store must model each writer's SEMANTICS, not just record the call.** `parity.e2e.test.ts`'s `callBinaryUpload` fake pushed every body onto `shippedTranscript` and returned `appended`. That is right for `session.append-transcript` and wrong for `session.replace-transcript`, which REPLACES the store — so a compacted transcript would have read back as the original long one with a summary stuck on the end, and the "the stored transcript shrank" assertion would have failed while the product was correct. Fixed by branching on the action. PREVENTION: when a fake stands in for a store with more than one writer, check that the fake's model of each writer matches the real handler before trusting a test that reads the store back.

- `2026-08-21` (TASK-222 / AW-1) — **A plan's file list tells you WHICH files to open, never WHAT ROLE they play — verify the role, and don't let a tidy brace-expansion smuggle in a wrong member.** The AW-1 plan listed `packages/validator-{skill,routine,identity,service}/` as "workspace-write vetoes". I opened all four, saw four validator packages that all reject things, and wrote them up as four `workspace:pre-apply` subscribers. Three are; **`validator-service` is not** — it declares `subscribes: []` and registers a `services:validate` SERVICE hook called at session open, never seeing a `FileChange`. The brace expansion made a wrong member look like a typo-proof set. Same review pass caught three more claims I had inherited or inferred rather than verified: (a) I wrote "the zod mirrors are `.strict()`" citing two schemas that were the wrong ones — the actual `CapabilitiesSchema` is plain `z.object`, so the failure mode is a SILENTLY STRIPPED key, the opposite of a gate; (b) I counted the prototype's fixture rows as 22 by eyeballing, actual 19 — `grep -c` would have taken two seconds; (c) I asserted a connector table was "never read" when `connectors:list-authored` reads it in production to build the very approval card I documented two rows below. **Rule: for every claim of the form "X is a Y", "X is never Z", or a count, run the command that proves it (`grep -c`, `grep -rn 'subscribes'`, open the schema) — reading nearby code that is CONSISTENT with the claim is not evidence for it.** Also: a docs-only PR gets no `ax-code-reviewer` pass under yolo-ship, so for a document whose whole value is factual accuracy, dispatch an explicit fact-check agent that re-opens every `file:line` — mine found 12 issues, 7 substantive, after I had already spot-checked ~25 citations myself and found them clean. Spot-checking finds drift; it does not find claims you never tested.
- `2026-08-21` (TASK-223/AW-2) — **`HookBus.fire()` did not pass a subscriber's rejection object through; it RECONSTRUCTED one (`{rejected: true, reason, source}`), silently dropping every field the subscriber added.** That is invisible while `Rejection` is the only shape anyone returns, and it becomes a real bug the moment a subtype carries payload: `Hold extends Rejection` with a `.hold` field, so `isHold()` in the `tool.pre-call` handler would NEVER have matched and every hold would have flattened into a plain deny — the exact outcome `hold` exists to prevent. Found only because the handler's own test kept failing after the handler code was provably correct. FIX: `fire()` re-attaches `.hold` when `isHold(result)`. Covering test in `packages/core/src/__tests__/hook-bus.test.ts` (`preserves the .hold field through a hold rejection`). **Next time:** when you add a subtype of a value the hook bus carries, check that the bus does not rebuild it on the way out — grep the bus for object literals over the type, not just for the type name.

- `2026-08-21` (TASK-223/AW-2) — **`@ax/ipc-server` resolves `@ax/ipc-core` through its package.json `exports` (dist), NOT through source**, so `packages/ipc-core/src/__tests__/dispatcher.test.ts` — which drives a real socket via `createListener` from `@ax/ipc-server` — kept exercising the OLD compiled handler while the edited `.ts` sat right there looking correct. Editing an ipc-core handler covered by those dispatcher tests requires `pnpm --filter @ax/ipc-protocol build && pnpm --filter @ax/ipc-core build && pnpm --filter @ax/ipc-server build` before the test sees the change. Vitest's live transform only applies to relative source imports, not to cross-package `exports` resolution.

- `2026-08-21` (TASK-224/AW-3) — **`expect(array).toContain('substring')` is EXACT ELEMENT equality, not substring matching — the plan's own TDD tests would have failed against the plan's own implementation.** AW-3's plan supplied both the lint tests and the lint code; the tests asserted `toContain('must not start with "to"')` while the code pushed `'must not start with "to" — the clause is a bare infinitive'`. Pasting the plan verbatim yields a red suite for a reason that has nothing to do with the feature, and "fixing" it by trimming the error messages to match would strip the half of each message that tells the author what to do instead. Use `expect(errs).toEqual(expect.arrayContaining([expect.stringContaining(...)]))` when the array holds human-readable prose. **General rule: a plan's code is a draft, not a fixture — run it before assuming a red test means you mis-transcribed it.** Two other AW-3 plan artifacts were also wrong on contact: a `TOOL_IDENT` regex that required TWO underscores and so let every real single-underscore tool name (`web_search`, `request_capability`) through clean, and a seed `capability` clause ("ask you a multiple-choice question") that failed the very lint the same plan specified, because `ask` is a verdict word.

- `2026-08-21` (TASK-224/AW-3) — **A whole-word check written as `padded.includes(` ${word} `)` is evaded by ordinary punctuation, and on a security gate that is a real hole.** The capability lint's verdict-word check space-padded the clause and probed for ` never `, which correctly avoids the "candelabra" ≠ "can" false positive AND silently lets `"never, ever delete anything"`, `"reply first-class post"` and `"it can/cannot run"` through — comma, hyphen and slash are not spaces. Filed under `allow`, the first renders as `✓ Can never, ever delete anything — on its own`, which is exactly the frame contradiction design §4.3.1 claims the authored/generated split makes *unexpressible*. Every one of the 14 seeded clauses was clean, so nothing in the suite went red; a reviewer found it by probing the regex directly. FIX: `new RegExp('\\b' + escaped + '\\b', 'i')` per word — `\b` treats every non-word character as a boundary. It now over-rejects a hyphenated "first-class", which is the correct direction to be wrong in. PREVENTION: **whenever "whole word" matters, use `\b`, never space-padding** — and when the thing being matched is a security claim, write the evasion cases (word next to `,` `-` `/` `(` ) as tests BEFORE trusting the check. Corollary: a lint whose only inputs are the clean rows you just authored proves nothing about what it rejects.

## 2026-08-21 (TASK-227) — a host-initiated bus call under the wrong user reads as "nothing there"

`deliverResolution` originally read the conversation under the APPROVING request's
ctx. `decisions:approve` takes its `userId` from the INPUT, not from `ctx`, so the
two are not guaranteed to name the same person — and `conversations:get-metadata`
pre-filters on `(conversationId, userId)` and answers `not-found` for anyone else.

The failure mode is what makes it worth writing down: the lookup returning nothing
is **indistinguishable from "the session is gone"**, which is a legitimate,
expected, silently-handled outcome on this path. The delivery would have been
skipped on every approval, forever, with no error and no log that read as wrong.

Every test passed the same user for both, so nothing caught it. The fix is the rule
TASK-226 already wrote for the host replay — build the ctx from the DECISION
(`replayContext(decision)`), never from the request that triggered the work. The
general shape: **when a host-initiated bus call happens on someone's say-so, the
say-so identifies WHO CLICKED, not WHOSE WORK IT IS.** And when the wrong identity
degrades into a legitimate no-op rather than an error, the test has to use two
different users or it proves nothing — vacuity-check it by reverting the fix.
- `2026-08-22` (TASK-233/AW-12) — **I wrote a RAW NUL byte into a `.ts` file by typing the character where I meant the escape, and every tool downstream went quiet about it.** `decoded.includes('<NUL>')` looked correct in the editor; `git`/`file` classified the file as `data`, and `grep` then matched NOTHING in it — not the NUL, not any other line — so "grep found no problems" was worthless. Caught by `file <path>` reporting `data` instead of `Java source ... UTF-8 text`. Two follow-on facts: a `perl -0pi -e 's/.../.../'` fix will not match either, because the pattern you type has the same problem; and the same failure recurs with the OTHER invisibles (I later wrote raw U+202E / U+200B into the TEST file, which is the exact Trojan-source hazard the code under test removes). PREVENTION: never type an invisible character — generate the `\uXXXX` escape programmatically (`chr(92) + 'u' + format(cp,'04X')`), and before committing run BOTH `file` on every touched source file and a codepoint sweep for C0/C1 + U+200B-200F + U+202A-202E + U+2066-2069 + U+FEFF. The card's "do not write raw NUL bytes" warning is not hypothetical; I hit it twice in one task.
- `2026-08-22` (TASK-233/AW-12) — **`packages/channel-web` has component tests in TWO directories and I only found one.** `src/__tests__/*.test.tsx` is the one every earlier note mentions; `src/components/workspace/__tests__/*.test.tsx` is where the whole agent-workspace surface's tests live (`AgentView`, `AgentRail`, `ActivityFeed`, `WorkspaceShell`, `TodayView`, `AgentMemory`, `DecisionRow`, `HomeComposer`). Removing a field from `AgentDetail` compiled clean against the first directory and broke two fixtures in the second — surfaced only by `pnpm build`, because vitest's include glob covers both but `tsc` is what type-checks the fixtures. Grep `src/**/__tests__` when changing a shared type, not `src/__tests__`.
- `2026-08-22` (TASK-233/AW-12) — **Adding a hook to `manifest.calls` breaks every harness that boots the plugin through `bootstrap()`.** `workspace:list` became a hard call and `plugin.test.ts` went from green to 17 failures at boot (`verifyCalls`), plus one more in the manifest-surface assertion. The fix is three lines in the mock plugin (add it to `registers`, register a no-op) and one in the expected `calls` array — but the failure presents as unrelated SSE/attachments/settings routes 404ing, because the whole plugin failed to mount. When a `calls` addition turns a suite red everywhere at once, look at the boot, not the routes.
- `2026-08-22` (TASK-247) — **A subagent that hangs reads as "no findings" on an automated merge path, and yolo-ship's own text told builders not to worry about it.** Phase 5 ended with "because a subagent runs async, there's no Bash timeout to set and no hang to babysit" — so when `ax-code-reviewer` hung on 6 of 6 large cards in one auto-ship run, builders pinged a few times, fell back to self-review, and reported the branch reviewed. Three real blocking bugs rode through that gap (unfenced agent-authored text on a user surface; a "Pick another time" control wired to *dismiss*; a permissions rail claiming reach a restricted agent lacks) — none of them CI-visible. The trigger is NOT diff size and NOT the agent type: the orchestrator's own independently-dispatched `ax-code-reviewer` runs on the SAME large diffs (#432/#435/#436/#437) returned in 13–17 min, while builder-spawned ones deep in a long session hung 40+ min. PREVENTION: treat every subagent dispatch as fallible — note the dispatch time, set an explicit deadline (25 min here), ping at most once, then ONE fresh re-dispatch with a minimal self-contained prompt, then fail LOUDLY. `reviewer: clean` may only mean a reviewer RETURNED; a reviewer that never returned is `hung`, and auto-ship now blocks the merge on it pending its own independent pass. The general lesson: any gate whose failure mode is silence must carry a deadline and a distinct "did not run" value — "it came back empty" and "it never came back" cannot share a representation.
- `2026-08-22` (TASK-277) — **A stored field that names a CHANNEL was read as a liveness signal, and the wrong answer degraded into a legitimate-looking no-op.** `decisions:approve` routed on `decision.attendance`, which `attendanceFor` sets to `attended` for `origin === 'web'` at hold time and never revisits — so a web conversation stayed `attended` hours after its runner was reaped. The approval took the attended branch, the row was claimed `executed`, no replay was scheduled, `deliverResolution` found no session, logged `decision_delivery_skipped_no_session`, and **its return value was discarded**. The row read executed, the call never ran, nothing told the user. Two shapes worth carrying: (1) a field captured at time T answers a question about time T — if the code needs to know something about NOW, it has to read NOW, and a stored capability (`could an agent ever be here`) must be ANDed with a live read (`is one here`), never substituted for it; (2) an ignored return from a function whose failure is by-design silent is a bug with no symptom — `deliverResolution` documents that it never throws, which is exactly why discarding its result loses the only signal there was. Also: an EXISTING canary test (`an attended decision whose session is gone degrades into the unattended path`) had pinned the buggy behaviour in prose, asserting the executor was never called and calling it design §3.3's no-special-case degradation. A confidently-commented test is not evidence the behaviour is right. Prove execution by the host executor's invocation count; a row reading `executed` is the false signal itself.
- `2026-08-23` (TASK-298) — **A bug filed from a symptom named the wrong line, and the "fix" would have shipped the bug.** The card blamed `board_batch`'s zsh breakage on `\$it$i:ID!`. `:I` is not a zsh modifier; that line is byte-identical under both shells. The corrupting sites were the two alias prefixes `a$i:update…`, where `:u` is the *upcase* modifier and zsh **eats the `u`** — `a1pdateProjectV2ItemFieldValue`, which GitHub rejects as `undefinedField`, which `2>/dev/null` reduced to the word `FAILED`. A builder following the card literally would have braced the innocent line, watched the symptom persist, and had no better information than before. Two tells that the diagnosis was guessed rather than measured: (a) nobody had run the two forms side by side (`zsh -c 'i=abc; echo ${i}:u'` settles it in one second), and (b) the *generated* run-state copy on disk had already been hand-patched at 150/152 and NOT at 147 — the artifact disagreed with the card and the artifact was right. PREVENTION: for any "X breaks under shell Y" claim, run the minimal reproduction per candidate line before writing the fix, and diff any hand-patched generated copy against the template it came from — the patch marks the real site. Related: the same run's card claimed a `kill -0` liveness check existed to be fixed; exhaustive grep found no such check anywhere, only a doc *warning against* adding one.
- `2026-08-23` (TASK-298) — **Four separate auto-ship defects were each regenerated, byte for byte, at every run start, because the code lived in a markdown code block that nothing tested.** `.claude/auto-ship-board.sh` and `-progress.sh` are gitignored run state written from heredocs in `.claude/skills/auto-ship/references/github-project.md`. Operators kept fixing the *generated* copies mid-run (`a${i}:` braced by hand; `--limit` raised to 700 by hand) and the template kept re-emitting the broken form on the next run — so the same four bugs were "fixed" repeatedly and never once stayed fixed. Skill docs that emit executable code are code, and this repo type-checks and lints none of it. PREVENTION: when a doc code-block is the source of a runtime artifact, put a tracked test on the DOC (extract the heredoc, run it, assert on it) — `scripts/__tests__/autoship-skill-shell-hazards.test.js` does this and fails 7 assertions against the pre-fix doc. Second habit worth keeping: when you hand-patch a generated file during a run, immediately open the template — that patch is a bug report with the line number already in it.
- `2026-08-23` (TASK-298) — **A merge that SUCCEEDS exiting non-zero is worse than one that fails, and we shipped that for months.** `gh pr merge <n> --squash --delete-branch` merges, then deletes the local branch; against a branch checked out in an agent worktree the local delete fails and `gh` propagates exit 1 — *after* main already moved. Since auto-ship dispatches every builder with `isolation: "worktree"`, this fired on 2/2 merges and reads to the orchestrator as "merge failed", which invites retrying a merge that already landed. The general shape: **never let a cleanup step decide the exit code of the operation it follows.** Assert the operation positively (`gh pr view --json state` = `MERGED` → echo a required `MERGE-OK #<n>` token) and let cleanup warn non-fatally. Also do not gate worktree cleanup on `kill -0` of the lock pid: that pid is the Claude Code *session*, shared by every agent worktree and alive until the session ends, so it always says "in use".
- `2026-08-23` (TASK-241) — **A board card's acceptance criterion offered a security regression as one of its two options, because the card's central fact expired the day after it was written.** The card said `skills:approved-caps-list` has no production caller and accepted "or the hook and its tests are deleted." True on 2026-08-21; **false from 2026-08-22**, when TASK-235/#439 shipped the workspace rail as its reader (`routes-workspace.ts:2523`) — the very thing `decisions.md` had predicted one row above the caveat ("AW-14 would be its first reader"). Taking the delete option would have reddened `skills/src/plugin.ts` + the k8s `preset.test.ts` (registration is pinned) **and** silently made the permissions rail understate agent reach, the H4 direction the same decisions row forbids. Two habits: (1) a card citing a "no caller / dead code / unused" fact is a **measurement with a timestamp**, not a standing truth — re-run the grep against current `main` before acting, and treat "delete it" acceptance options on anything security-adjacent as requiring positive proof of no caller *today*; (2) when a decision row records a prediction ("X would be its first reader"), the card filed alongside it is the *other half* of that entry — closing the prediction without closing the card leaves a live instruction to undo the work that fulfilled it. The real residue was tiny: one dead `optionalCalls` degradation in `@ax/agents` describing behaviour TASK-100 removed, and five stale "no production caller" sentences across `docs/plans/2026-08-21-policy-condition-inventory.md`, `context.md`, and `decisions.md`. Correcting the sentences mattered more than the code — they are what made the card wrong, and leaving them lets the next session re-derive the same false conclusion.
- `2026-08-24` (TASK-310) — **The fifth auto-ship doc defect of the same family, and the one that proves a broken loop can hide inside a "best-effort" log line.** `github-project.md` §4's forward-learning loop was `for id in $IDS; do append_learnings "$id" …; done`. bash word-splits an unquoted `$VAR`; **zsh does not** (that is what `${=VAR}` exists for), and the Bash tool runs zsh — so it iterated **once** with every node id concatenated, `append_learnings` read a bogus id, and it printed `learnings: skip (read)` — the line the operator actually saw; note the helper has a second best-effort exit (`skip (not a draft-issue card)`) reachable from the same bad id depending on whether GitHub answers with a GraphQL error or a null node, and nobody has pinned which. Either way it is a **transient-looking** path, so an orchestrator correctly reads it as "rate-limit blip, ignore" instead of "your loop is broken": observed live at 3 output lines where 12 were expected, caught only because the count was visibly wrong. Note the **asymmetry** that makes this easy to mis-audit: an unquoted *command substitution* in a `for` list DOES split under zsh (measured 3 iterations in both shells), so §7's `for b in $(git branch --list …)` was never affected — establish which form you have before "fixing" both. Fix is `printf '%s\n' "$VAR" | while IFS= read -r x`, identical under both shells; `${=IDS}` is zsh-only and "run it under bash" is not available (the orchestrator cannot pick its shell). Two general shapes: **a degradation path that swallows a *logic* error is worse than no degradation** — if a helper's failure message can only mean "transient", it must not also be reachable from a malformed argument; and when a helper prints one line per call, the doc should tell the reader the expected line COUNT, because that is the only thing that made this visible. Guard: `scripts/__tests__/autoship-skill-shell-hazards.test.js` now scans every tracked `.claude/skills/**/*.md` for `for x in $VAR` (non-comment lines only, so a doc can still warn about the broken form) and asserts the 1-vs-3 iteration difference directly.

## A shared zod row schema is a trap: `returns` strips, and tsc does not see it

**Seen:** TASK-251 (2026-08-23). The obvious fix for "hook X leaks a storage `id`" was
to delete `id` from the row schema. That schema was shared with a *sibling* hook whose
consumer reads `id` for a React key.

**Why it is silent:** `HookBus.call` returns `returns.safeParse(result).data`,
and a zod object strips undeclared keys. So the sibling hook stops delivering the field
at runtime, while every type still says it is there — the HTTP hop that carries it is an
untyped `get<T>`, so `pnpm build` stays green and the only symptom is
`key={undefined}` in a table.

**The rule:** before narrowing any `*Schema` used in a `returns:`, grep for every other
schema that composes it. Narrow with a *derived* schema (`Base.omit({ field: true })`)
for the one hook, and leave the shared base alone. Then write the test as a **pair** —
one assertion that the field is gone from the narrowed hook, one that it is still
present on the sibling. The second assertion is the one that catches the mistake.

## A comment sweep must re-measure the card, including the card's own measurement

**Seen:** TASK-243 (2026-08-23). The card named one stale comment at
`workspace-commit-notify.ts:289-290`. Three things were wrong with that: `:289-290` is
not a comment (it is the `bus.fire` generic body), the stale text it described had
already been deleted by this card's own stated parent (TASK-240, `d309a5d8`), and the
comment that *does* discuss rejecters says "three plugins" and is true today. The real
work was a sweep the pre-flight measurement found: **nine false claims in one file**,
eight needing a rewrite.

**The pattern, now twice confirmed** (TASK-295 claimed 2 and found 9; TASK-243 claimed 1
that did not exist and found 9): a card filed from a symptom carries a *guessed*
`file:line`, and the guess is wrong often enough that "verify the named defect exists"
has to be step one, not an afterthought. Same shape as TASK-298's `board_batch` card.

**And the pre-flight measurement is itself a claim to check.** TASK-243's brief asserted
"there is **no** `__tests__` file for this handler under
`packages/ipc-core/src/__tests__/`" and built an explanation on it ("that absence is
*why* nine comments rotted here"). The path is right and the conclusion is wrong: the
tests live one directory down, at
`packages/ipc-core/src/handlers/__tests__/workspace-commit-notify.test.ts` (599 lines,
13 cases, plus `workspace-commit-notify-core-resync.test.ts`, which adds 1). **`packages/*/src/__tests__/`
is not the only test root in this repo** — the same trap as channel-web's two component-test
directories. Glob `src/**/__tests__` before concluding a file is unpinned; a follow-up
card was filed on that false absence.

**Why comments rot here specifically:** tests pin *behaviour*, and none of them read the
prose. Six of the nine claims described control flow the tests exercise correctly — the
tests were green the whole time the comments were lying. There is no mechanical guard for
this, so the mitigations are (a) sweep a file's whole prose when you touch one line of
it, and (b) make the header point at its test pins so the next reader can check it
cheaply.

**A comment fix that introduces a new false claim is a net loss.** Verify each rewritten
assertion against the code you cite, and hand the reviewer the list of new claims to
fact-check rather than asking it to look for typos.

- `2026-08-24` (TASK-262) — **I argued a human-facing label should stay understated because extending it "risks the 120-char cap", without reading what the cap does.** `@ax/decisions`' `fenceLine` (`packages/decisions/src/freshness.ts:200-207`) truncates with an ellipsis; it does not drop the label. The reviewer pushed back on the same decision for the right reason (a "checked against: the catalog entry" clause misleads when a CONNECTOR is what moved) and I reversed it. **Lesson: before citing a limit as a reason not to do the honest thing, read the code that enforces the limit.** A cap that degrades gracefully is not an argument.

- `2026-08-24` (TASK-262) — **My first pass digested credential slot NAMES only, which reproduced the bug the card was about one level down.** The card's whole point is "reach moves under a stable id"; for an OAuth slot the reach IS the scopes, so `read` → `read,write` under slot `OAUTH` still would not have tripped the guard. I had even written the membership rule down ("resolve returns it and it grants reach") and then applied it to the top level only. **When you state a membership rule in a comment, walk every nested level of the wire shape against it before you ship** — the reviewer found this, not me.
