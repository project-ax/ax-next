# Project context

Confirmed facts about ax-next. Not opinions.

- `2026-04-23` — Only `@ax/cli` and `presets/**` (the meta-packages that wire plugins together) may import sibling plugins; enforced by `eslint.config.mjs` `no-restricted-imports` overrides. All other inter-plugin communication goes through the hook bus. (Updated 2026-05-24: `presets/**` is on the allow-list too, not just the CLI.)
- `2026-04-23` — Storage hook payload contract is `{ key: string, value: Uint8Array }` — opaque bytes, no backend vocab. Any `table`/`bucket`/`rowid` field would leak.
- `2026-04-23` — pnpm 10 requires `pnpm.onlyBuiltDependencies` allowlist for packages with postinstall scripts (e.g. `better-sqlite3` prebuild-install). Without the allowlist, the build silently skips the postinstall and the package fails to load at runtime.
- `2026-04-23` — E2E tests invoke the CLI as `node dist/main.js <args>`, not `pnpm exec ax-next`. Shebang + `chmod +x` are present but tests use node directly to avoid pnpm bin-link flakiness.
- `2026-04-23` — Week 3 shipped as PR #2 (merged). Week 1–2 kernel shipped as PR #1.
- `2026-04-29` — Claude Agent SDK 0.2.119 encodes the project-dir name as `realpath(cwd)` with `/` → `-`, NOT the literal cwd string. On macOS `/var/...` becomes `-private-var-...`. Don't reconstruct the path; glob `<HOME>/.claude/projects/*/<sessionId>.jsonl`.
- `2026-04-29` — SDK 0.2.119 with `settingSources: []` still writes outside `<HOME>/.claude/projects/`: `<HOME>/.claude.json` (~20KB cache: GrowthBook flags, anon userID, migrationVersion, model-cost cache), `<HOME>/.claude/backups/.claude.json.backup.<ts>` (accumulates per spawn), `<HOME>/.claude/policy-limits.json`. None are secrets, but workspace persistence should ignore everything outside `.claude/projects/`.
- `2026-04-29` — SDK 0.2.119 jsonl entry types include non-turn records: `queue-operation`, `last-prompt`, `attachment` (e.g. `skill_listing` injected by the SDK even with `settingSources: []`). Any transcript parser must whitelist `user`/`assistant`/`tool`, not blacklist. (Updated 2026-05-24: the once-planned `runner:read-transcript` hook never shipped — Phase C was superseded by the 2026-05-01 workspace redesign. Transcripts are now read by `conversations:get` globbing committed `.claude/projects/*/<sessionId>.jsonl`. Stale code-comments still mention the hook in `packages/conversations/src/{types,store}.ts`.)
- `2026-04-29` — ax-next has no production data and never will need a schema migration that preserves rows. Existing `_v1` table suffixes (`conversations_v1_*`, `audit_v1_*`) are stable identifiers, NOT version pointers. ALTER TABLE in place is correct for every schema change; no v1→v2 split, ever. (See decisions.md 2026-04-29 entry.)
- `2026-05-03` — Host-pod listener layout (issue #39, PR #40): `@ax/ipc-http` on containerPort 8080 (runner-pod back-channel; Service :80) AND `@ax/http-server` on containerPort 9090 (public — `/chat`, `/health`, `/admin/*`, `/auth/*`, `/api/chat/*`; Service :9090). `serve` registers `/chat` + `/health` against http-server via `http:register-route`; no third listener. Chart values: `host.ports.ipc` / `host.ports.publicHttp` / `host.ipcServicePort` / `host.publicHttpServicePort`.
- `2026-05-03` — POST `/chat` (and any other state-changing route on the public surface) is gated by `@ax/http-server`'s CSRF subscriber. Headless callers must send `X-Requested-With: ax-admin` OR an Origin in `AX_HTTP_ALLOWED_ORIGINS`. The chart sets `AX_HTTP_ALLOW_NO_ORIGINS=1` to silence the empty-allow-list warning when no Origin allow-list is configured.
- `2026-06-29` — `@ax/memory-strata` SHIPPED vs DESIGNED (audited): shipped & wired in BOTH CLI + k8s presets (~310 tests) = hot-tier markdown inject (`system/{agent,user,session}.md` via `inject.ts`, 1500-tok cap) + LLM Observer (fact-extract on `chat:end` → inbox) + SQLite-FTS5 / Postgres BM25 index (`memory-strata-index-{sqlite,postgres}`) + Consolidator (decay→cluster→dedup→promote @conf≥0.7→quarantine→`recent.md`). NOT in runtime (design-only or bench-only): `system/map.md`, the retrieval orchestrator, dual-sourcing, `reflect`/mental-models, warm-tier/promoter, supersession/evidence/trend. Vectors intentionally OUT (lost the spike). Corrects the stale auto-memory note claiming "no code yet / plugin does not exist".
- `2026-06-29` — Strata bench harness = `packages/memory-strata/test/bench/` (`cli.ts`; configs `a-bm25`,`b-rerank`,`c-rrf`,`d-map`,`e-map-fts`; `corpora/longmemeval-s.ts` downloads the REAL `xiaowu0162/longmemeval-cleaned` dataset, handles `_abs` abstention). It benchmarks RETRIEVAL CONFIGS with a generic agent (Sonnet) + judge (Grok) — NOT the shipped plugin end-to-end. Results: `docs/plans/2026-05-13-memory-strata-vector-spike-report.md` (vectors+reranker OUT) and `…-phase-3c-config-d-report.md` (orchestrator+map IN, n=500).
- `2026-07-30` — LongMemEval-S is ordered in **type blocks** (single-session-user 70 → multi-session 133 → single-session-preference 30 → temporal-reasoning 132 → knowledge-update 69 → **single-session-assistant 54**). The first `single-session-assistant` question is at sample position **434**, so `--sample 100` (and `--sample 400`) contain ZERO of them and cannot measure an assistant-recall lever at all. Use `bench --mode e2e --types <question_type>` / `--ids <id,...>` (added 2026-07-29, `test/bench/e2e-select.ts`) — it filters BEFORE slicing. A targeted 54-question run is ~$25/~8h vs ~$224/~3d for the full 500.
- `2026-07-30` — Bench wall-clock is dominated by **haystack ingest, not question count**: each LongMemEval-S sample carries ~135 sessions, each an Observer LLM round-trip. After assistant-content extraction landed, throughput was ~9 min/question (~10x slower than the pre-change estimate). Budget wall-clock separately from dollars; `--cap` must be set above projected spend or the run aborts and silently shrinks the denominator.
- `2026-07-30` — `single-session-assistant` went **25.9% (14/54) → 87.0% (47/54)** via assistant-content extraction (`factType: 'answer'`), taking it from the corpus's sole structural outlier to its strongest type. Design/plan: `docs/plans/2026-07-29-assistant-content-extraction-{design,plan}.md`; result appended to `docs/plans/2026-07-08-memory-strata-postrollup-e2e-analysis.md`.
- `2026-08-02` — Assistant-content extraction FINAL (n=500, `bm25-full-assistant.jsonl`, $221.16): overall **66.2% → 75.8% (+9.6pp)** on the 488 questions both runs scored (76.0% on all 500). Both gates pass (multi-session 69.2% ≥65%; correct-refusal 88.0% ≥83%). **No type regressed.** single-session-assistant 25.9% → 83.3%; **temporal-reasoning 68.9% → 78.0% (+12 questions) — an UNFORECAST gain on a type this lever never targeted.** The temporal mechanism (temporal questions turning on assistant-supplied dates/durations/timelines) is an untested hypothesis, not a finding — a kept-workspace repro on 2–3 temporal flips is the cheapest way to settle it.
- `2026-08-02` — Long bench runs get SIGTERM'd (exit 143) by something outside the bench, repeatedly. `--resume` makes this cheap (a kill costs only the in-flight question), so the fix is a supervisor loop that relaunches the identical command until the JSONL hits the target row count — see `scratchpad/bench-supervisor.sh` pattern. **Never launch a multi-hour bench as a harness-tracked background task**; macOS has no `setsid`, so `nohup <cmd> &` from a foreground call is what survives. Budget **wall-clock separately from dollars**: n=500 is ~2.5 days (~9 min/question), while cost tracked its estimate within 2%.
- `2026-08-19` (PR 3, `@ax/agent-aisdk-runner`, PR #400) — The **second runner** ships: `ai@7`'s `ToolLoopAgent`, selectable per-agent via `agents.runner`, loaded in the CLI + k8s preset + container image. Facts worth carrying into PR 4–7:
  - **`ai@7` is pinned EXACTLY** (`7.0.70`, `@ai-sdk/anthropic@4.0.40`), matching how `@anthropic-ai/claude-agent-sdk` is pinned. `ai` now exists at TWO majors in the workspace — `channel-web` on v6 (browser) and this runner on v7 (Node sandbox). Deliberate; they never interoperate. `dependency-skew.test.ts` fails if a workspace-wide `ai` override appears.
  - **`TranscriptSource` is byte-shaped now**: `read(): Promise<Buffer|null>` / `write(): Promise<'accepted'|'unusable'>`. `'unusable'` IS the cross-runner demotion — it routes into the existing F2a branch, so core has no second branch for it.
  - **The aisdk transcript is ndjson with a header line** `{"v":1,"runner":"aisdk"}` and per-line `{role, uuid, message}`. The envelope is NOT cosmetic: `dropTurnFromJsonl` needs top-level `uuid`, `roleOfJsonlLine` (the B3 detector) needs `role`. Restored lines are re-emitted VERBATIM (`raw`) because `modelMessageSchema` strips unknown keys — re-serializing would force a whole-file resync every resume.
  - **`agentConfig.runner`'s real reader is a boot self-check**, not the demotion driver (the orchestrator comment predicted otherwise and was corrected). The transcript header drives demotion.
  - **The runner has NO `postbuild` script**, on purpose: `tsc --build` never runs a referenced package's npm scripts (the #397 trap). Its `dist/main.js` reaches the image because `@ax/cli` + `@ax/preset-k8s` declare it as a PROD dependency — `pnpm deploy --prod` copies the prod closure. Guarded by preset-k8s's `image-asset-packaging.test.ts`.
  - **Compaction landed in PR 6 (#411)** as `src/compaction/`, wired to `prepareStep`: mask stale tool outputs → prune old tool call/result pairs → a `ContextWindowExceededError` ceiling. Rungs 1-2 are SEND-SITE ONLY — the transcript keeps every message, because its bytes are the host's `prefixHash`/resume source of truth. **PR 7 added rung 3 (summarize), and it is the deliberate exception**: it costs a model call, which cannot be recomputed for free the way a mask or a prune can, so it rewrites the transcript and publishes the rewrite. It runs at the TURN BOUNDARY (`compactor.turn()` from main.ts, before `agent.stream()`), never inside `prepareStep` — mid-tool-loop there is a live tool pair and a signed thinking block to splice around, and no quiescent moment to persist. Remaining non-parity: `TodoWrite`, cross-runner resume, SDK setting sources, and skill-declared in-sandbox MCP servers.
  - **§8 acceptance walk RE-RUN 2026-08-21 against `main` @ f2a883e2 — all rows PASS.** On kind `ax-next-dev`, agent pinned `runner=aisdk` (confirmed by admin-API read-back), image rebuilt and the compaction code verified present in the image binary. Covered: chat+Bash in a real pod · sandbox death mid-turn → "agent stopped unexpectedly" + working retry · resume across turns AND cold resume after deleting every sandbox pod · routine-triggered invocation · workspace materialize→commit→bundle across sessions · skills projection · artifact publish · egress block + the "Allow access?" card · uploads. **PR 4's cluster gate is now MET** (grok-4.6 and kimi-k3 each drove a real conversation with a real tool call through OpenRouter). **PR 6's gate is MET**: compaction fired twice on REPORTED usage (`123,061 of 200,000 … (reported)`, 19 messages in / 19 out — rung 1 only, never escalated, never hit the ceiling) and the turn completed normally.
  - **Two "findings" from that walk were driver artifacts, not product bugs** — worth recording because both looked convincing. A duplicated attachment chip (the payload really did carry two `attachment` blocks) and a `removeChild` console error were BOTH caused by mixing Playwright's `chooser.setFiles()` with the MCP's `browser_file_upload` — the MCP intercepts the file chooser, so both handlers fired and the file uploaded twice. A clean single upload produces exactly one chip and no console error. When driving the composer's Attach flow, use the MCP tool ALONE.
  - **Capturing runner-side stderr in a walk: SNAPSHOT, never follow.** Runner pods are reaped within seconds of a turn ending and their logs go with them, so `kubectl logs` read afterwards returns 0 bytes — which looks exactly like "the feature never fired". The technique that works is a loop that every ~2s runs plain `kubectl logs <pod> --all-containers > podlogs/<pod>.log` for every pod: logs only grow, so an overwrite always holds the latest and the file survives the pod. **`kubectl logs -f` started in advance does NOT work** — it errors out on a pod still `PodInitializing`, and a naive follower marks that pod "seen" and never retries, so you capture nothing and think you instrumented it. (Both failure modes cost a full expensive probe run each.)
  - **A runner pod logging 0 bytes is NORMAL for an ordinary turn** — the runner only writes stderr on notable events. So a zero-byte capture does not validate your instrumentation. Validate it on a path that definitely logs and is CHEAP: a cross-runner switch emits `runner: cannot resume transcript … written by another runner` + `runner: rebuilt N message(s) of context`. Confirm the pipeline there before spending on a long context-pressure probe.
  - **Waiting for a turn by grepping the conversation for an expected string is a FALSE-POSITIVE TRAP** — the string is usually in the prompt you just sent ("reply with exactly: X"), so the wait returns instantly against your own user turn. Poll on the last turn's ROLE becoming `assistant` (or on the turn count increasing) instead. This produced two wrong conclusions in one session, one of which briefly looked like a passing test.
  - **The aisdk turn loop must keep its `streamError` check** (added in PR 6). A step that fails AFTER an earlier one succeeded leaves `result.steps` RESOLVED — `NoOutputGeneratedError` fires only when a turn produced nothing at all — so without that check a failed turn ends as a SUCCESS with partial content and no `chat:turn-error`.
- `2026-08-21` (PR 7, compaction rung 3 + the eval bench) — Facts that outlive the PR:
  - **`LoopContext.replaceTranscript()` exists now** (runner-core), backed by an exported `replaceWholeTranscript` in `transcript-delta.ts`. It is the ANNOUNCED-rewrite path design §5 asked for; `resync-required` stays the net for rewrites nobody announced. It is BEST-EFFORT — on failure the ship state is left unadvanced, so the next `endTurn` delta fails its prefix check and resyncs anyway. Anything that rewrites a transcript in place should call it; anything that adds a throw to it is undoing the fallback.
  - **`MemoryTranscriptSource.replace()` preserves entry uuids BY OBJECT IDENTITY.** The uuid is the host's `turnId` (display log, `conversations:drop-turn`), so re-minting orphans live references. Rung 3 passes head and tail through by reference so this holds — a defensive copy anywhere in `spliceSummary`/`planSummarization` silently breaks it, which is why the summarize suite asserts the reference-passing directly.
  - **The tail seam always starts at a `user` message.** It is the one role that never carries a tool result, so snapping the preserved-tail boundary backward to it keeps pairing valid by construction instead of by repair. If there is no `user` message to snap back to (one enormous turn), rung 3 declines rather than splicing.
  - **`generateText` uses `doGenerate`, `agent.stream()` uses `doStream`.** A `MockLanguageModelV4` with only `doStream` makes the summarizer call fail, which the ladder swallows as `model-call-failed` — so a compaction e2e would PASS for the wrong reason. `parity.e2e.test.ts` has `modelReplayingWithSummarizer` for this; its `callBinaryUpload` fake also models `session.replace-transcript` as a REPLACE, not one more append.
  - **The eval is `pnpm --filter @ax/agent-aisdk-runner bench`** (`test/bench/`, opt-in, needs `ANTHROPIC_API_KEY`). It gates on `recallLoss` — the delta against a `verbatim` control arm — not on an absolute score, because a needle the model cannot answer from the FULL transcript was never a compaction failure. With no control arm the gate FAILS rather than falling back. CI runs the harness's own tests (`test/bench/__tests__/`), including the fairness check that no planted fact sits in the preserved region.
- `2026-08-21` (PR 7 §8 walk — rung 3 on kind `ax-next-dev`, all criteria PASS) — Agent `AISDK Walk Agent` (`runner=aisdk`, `anthropic/claude-sonnet-4-6`, 200k window), conversation `cnv_rOsdbJjgC4ebJ252SrL_Kw`. Result: `[aisdk-runner] compaction: summarize — ~188,266 of 200,000 tokens before, ~63,768 after; 124 messages in, 41 out`. Transcript store 124 rows / 654,074 chars → 43 / 210,500. The needle (a host+port stated ONCE at message index 21, inside the summarized middle) came back correct in the UI; early filler was gone from the store, late filler preserved verbatim; 0 console errors, 0 host error/warn lines.
  - **How to make rung 3 fire without paying for 40 real turns: SEED the transcript rows.** ~650 KB of prose across 123 messages in `conversations_v1_transcripts` (db `ax_next`, cols `conversation_id,seq,line`; line 1 is the header `{"v":1,"runner":"aisdk"}`, every other line `{"role","uuid","message"}`). Generate the SQL with dollar-quoting (`$axseed$…$axseed$`) so nothing in the JSON needs escaping, and pipe it with `kubectl exec -i … psql`. Everything after the seed — resume, the summarizer call to the real provider, replace-transcript, the turn — is real. Rung 3's trigger is 0.75 × window measured by the ESTIMATOR (~3.5 chars/token) with calibration 1 on a cold resume, so size the seed off `estimateMessageTokens`, not off a token count.
  - **DELETE the runner pods before the compacting turn.** The idle-keepalive (PR #124) reuses a warm pod, and a warm pod holds its in-memory transcript — it never re-reads the store, so a seeded transcript is silently ignored and rung 3 does not fire. Cold start is the only way the seed is read.
  - **Announced-vs-resync is distinguishable by TIMING, and only by timing.** Both paths call `session.replace-transcript`, so the row count alone cannot tell them apart. The announced path (`ctx.replaceTranscript()` at the turn boundary) shrinks the store BEFORE the reply exists; the resync fallback would shrink it at `endTurn`, going straight to the post-reply count. Observing the intermediate row count mid-turn (here 42 = header + 41, with the assistant reply not yet appended) is what proves the announced path ran.
  - **Cold resume against a COMPACTED transcript was walked separately and passes** — pods deleted, next turn still answered from the summary, and no `starting fresh` line in the runner log. Worth keeping in any future compaction walk: a spliced summary message that failed `decodeTranscript` would demote the session to fresh and silently drop the whole history, and the failure looks identical to "the model forgot".
- `2026-08-21` — **Agent-workspace surface** (`/workspace`). **UPDATED 2026-08-21 by AW-9/TASK-230: no longer a prototype.** The mock backend (`mock/workspace.ts`, `workspace-seed.ts`, `decision-machine.ts`) is DELETED; the surface now stands on `GET /api/workspace/state` / `/agents/:agentId` + `POST /api/workspace/route` (`channel-web/src/server/routes-workspace.ts`) over `agents:list-for-user` / `agents:resolve` / `conversations:*` / `session:is-alive`. Types moved `mock/workspace-types.ts` → `src/lib/workspace-types.ts`. `/workspace` sits INSIDE the real auth + bootstrap gate; the flag is server-supplied via `GET /api/features` ← `ChannelWebServerConfig.agentWorkspacePreview` ← `AX_AGENT_WORKSPACE_PREVIEW` (default OFF), and the workspace routes only mount when it is on. Everything without a producer (decisions, activity, permissions, files, memory, `now`/`counter`) renders an honest EMPTY STATE; there is no "This week" panel at all. The notes below describe the ORIGINAL prototype and are kept for the design facts they carry:
  - **`packages/channel-web` already ships a mock HTTP backend** — `mock/server.ts`, wired into `vite.config.ts` when `AX_BACKEND_URL` is unset, with a disk-backed `Store` and `seed.ts`. `src/lib/agent-store.ts` already imports its `Agent` type from `mock/agents`, so `src → mock` type imports are the established direction. New prototype surfaces belong here, not in a separate Vite app.
  - **`IPC_TIMEOUTS_MS['tool.pre-call'] = 10_000`** and `agent-runner-core/src/tool-policy.ts:70` maps every failure of that call (timeout included) to `{decision:'deny'}`. This is the fact that kills any "block inside pre-call until a human answers" design.
  - **`PreToolVerdict` is `allow | deny` only**; `ToolPreCallResponseSchema` (`ipc-protocol/src/actions.ts:89`) is `allow | reject`. There is no `ask`/`hold` today.
  - **`tool.execute-host`** (`actions.ts:102`) already carries a full `ToolCall {id,name,input}` and is the natural replay seam for a decision approved after its turn ended.
  - **The JIT permission card is the nearest existing prototype of the flow**: `channel-web/src/components/PermissionCard.tsx` + `src/lib/resume-actions.ts` deny → card → `regenerate()` re-issues the turn. Correct for capability GRANTS; wrong for outward ACTIONS, where re-running the model means what ships is not provably what the human read.
  - **`@ax/host-grants` is network hosts only** (`{ownerUserId, agentId, host}`) — it cannot back a "what may this agent do alone" rail. Those are tool-policy semantics.
  - **`@ax/routines` already models the background half**: `TriggerSpec`, `activeHours`, `FireSource = 'tick'|'webhook'|'manual'`, `FireRow` with `status`/`error`/`conversationId`, and `conversation: 'per-fire' | 'shared'`. The refresh's "one current conversation per agent" IS `shared`; the Activity feed IS `FireRow` history.
  - **`channel-web` has no router** — `App.tsx` branches on `window.location.pathname`. A new top-level surface is a `pathname` check returning early AFTER the hooks (rules-of-hooks), with its own effect guard so the auth/bootstrap fetches don't fire behind it.
  - **`inbox-loop.ts` IS the parking mechanism** — `agent-runner-core/src/inbox-loop.ts` already long-polls `session.next-message`, transparently swallows the 30 s ceiling by re-polling on the same cursor (cursor advances only on real delivery), and carries a cumulative idle floor (`idleTimeoutMs`, default 15 min → `{type:'idle-timeout'}`) set longer than the host's own idle window. So the approvals design's "attended park + degrade to the queue" needs no new machinery: the park budget IS `idleTimeoutMs` and the degradation IS `idle-timeout`. Only DELIVERY is new — a fourth variant beside `user-message`/`cancel`/`timeout` in `SessionNextMessageResponseSchema` (`ipc-protocol/src/actions.ts:899`). Size approvals cards accordingly.
  - **There is no Slack channel.** `packages/` has exactly one channel, `channel-web`. Any design that assumes a Slack surface (interactive buttons, a long park budget) is describing something unbuilt — attendance is `web | routine` today.
  - **shadcn install lives at `packages/channel-web`**, base `zinc`, `cssVariables: true`, `iconLibrary: lucide`. `pnpm dlx shadcn@latest add <name> -c packages/channel-web` must run **from the repo root** — running it from inside the package doubles the path (`packages/channel-web/packages/channel-web`) and the CLI ENOENTs. The bundled `shadcn` skill hits this exact error if invoked with the cwd inside the package.

- **AW-1 / TASK-222 — the enforced-policy-condition inventory** (`docs/plans/2026-08-21-policy-condition-inventory.md`, 2026-08-21). Twenty enforcement points opened and recorded. Headline facts for anyone touching tool policy or the permissions rail:
  - **`tool:pre-call` has ZERO production subscribers** — the chain always returns `allow` with the call unmodified. There is no `PolicyRule` type, no rule table, no evaluator. AW-3 creates the first one.
  - **Half the enforcement points (10/20) have no describable user-facing meaning.** Only 3 rows can become `PolicyRule`s, and only **4 conditions** (the `DISABLED_BUILTINS` denies — `WebFetch`, `WebSearch`, `Task`, `AskUserQuestion`, `agent-claude-sdk-runner/src/tool-names.ts:59`) reflect something the repo enforces today. Everything else on the rail is a catalog-allow fact, a §4.3.4 grant row, or a §4.3.3 mechanical MCP row.
  - **Nothing in the repo stops a tool call and waits for a human.** `chat:permission-request` is fire-and-forget; the turn runs to completion and the card is UI. `request_capability`'s card is TURN-SCOPED (no durable row at all). Only `connector_propose` has a durable hold — a `status='pending'` row in `connectors_v1_authored` with zero reach until approved (the strongest real gate in the repo).
  - **A self-authored, clean-scanning skill installs with NO card and NO human** (`skills/src/propose-gate.ts:31-33`: `origin==='authored' → 'active'`). Deliberate post-TASK-100 (a skill is zero-reach instruction text; reach lives on connectors), but it means "install a skill it wrote for itself" is currently unattended.
  - **`executesIn` decides whether a `hold` is replayable.** `request_capability` / `connector_propose` are `host` → `tool.execute-host` replay works. `skill_propose` is `sandbox` → the host CANNOT replay it; attended path only.
  - **The prototype's rail names a tool catalog that does not exist** (`gmail.*`, `slack.*`, `calendar.*`, `travel.*`, `payment.*` in `channel-web/mock/workspace-seed.ts:495`). Real catalog: `memory_search`, `memory_read_section`, `memory_note`, `search_catalog`, `request_capability`, `web_search`, `web_extract`, `artifact_publish`, `skill_propose`, `connector_propose`, `mcp.<serverId>.<tool>`, plus SDK builtins `Bash`/`Read`/`Write`/`Edit`/`Glob`/`Grep`/`Skill`.
  - **`@ax/validator-service` is NOT a workspace-write veto** despite the plan's Step-1 list grouping it with `validator-{skill,routine,identity}`. It declares `subscribes: []` (`validator-service/src/plugin.ts:111`) and registers the `services:validate` SERVICE hook, called at session open from `chat-orchestrator/src/orchestrator.ts:2157` over the folded service list. There are exactly THREE production `workspace:pre-apply` subscribers: validator-skill:126, validator-identity:138, validator-routine:28. And `validator-routine` protects no path — routine writes are allowed; it rejects malformed frontmatter / duplicate webhook `trigger.path` / non-canonical `hmac.secretRef`.
  - **Neither `Capabilities` zod mirror is `.strict()`** (`connectors/src/types.ts:169-177`, `skills-parser/src/capabilities.ts:115-121` are plain `z.object`). An extra key is SILENTLY STRIPPED on read, not rejected — so a field added to one mirror and not the other vanishes with no error. (`.strict()` IS used on neighbours — OAuthSlotSchema:111, ServiceDescriptorSchema:167 — which makes it easy to misread the file.)
  - **A `PolicyRule` keyed on a DISABLED_BUILTINS name can never fire.** `classifySdkToolName` returns `{kind:'disabled'}` with NO `axName` (`tool-names.ts:66-72`) and `pre-tool-use.ts:35-44` denies BEFORE `policy.preToolUse` runs, so the call never reaches `tool:pre-call`. Those four are rail rows, not enforcement — derive them from `DISABLED_BUILTINS` rather than hand-copying names.
  - **Two enforcement points are invisible to everything:** validator `workspace:pre-apply` vetoes send their reason to **stderr only** while triggering `git reset --hard` (`commit-notify-resync.ts:186`) — the agent's turn work is discarded and neither model nor user is told why. (This entry also used to say `skills:approved-caps-list` had no production caller — **false since 2026-08-22**: TASK-235/#439 made the workspace rail its reader, `routes-workspace.ts:2523`, and the rows it reads are written by the connector approval wall through the sibling `skills:approved-caps-set`, `orchestrator.ts:3146` (`-list` itself is read-only). TASK-241 corrected it. Never delete that hook — the rail would silently understate agent reach, the H4 direction `decisions.md` forbids.)
  - **`@ax/tool-policy` (TASK-224/AW-3) is the FIRST `PolicyRule` engine in the repo**, and it still has no production caller: `tool-policy:evaluate` gets one in TASK-225 (`@ax/decisions`' `tool:pre-call` subscriber, the repo's first), `tool-policy:list-capabilities` in TASK-235 (the rail). Loaded in the k8s preset + asserted in `preset.test.ts`; deliberately NOT in `acceptance.test.ts`'s `PLUGINS_TO_DROP`. 14 seeded rules — 4 denies (the rail rows for `DISABLED_BUILTINS`, which can never fire), 3 holds (`request_capability`, `connector_propose`, `skill_propose` — NEW policy, none of them is held today), 7 catalog allows marked `provenance: 'catalog'`.
  - **The four disabled-builtin rows must NOT be derived from `DISABLED_BUILTINS`** — settled by a human ruling in TASK-245, and for a better reason than the one recorded here before. The rail is deliberately **runner-agnostic**: `rules.ts` (sandbox-floor comment) already defines its content as *the ones BOTH runners have*, so the deny rows are a **cross-runner intersection**, not a mirror of the claude-sdk runner's list. Deriving would couple the rail to one runner. (The earlier "top-level side effects, in tests too" claim was **overstated** — `tool-names.ts` has zero imports and is pure. The real import obstacle is reach: the runner's `exports` map is only `"."` and `main.ts` never re-exports the constant.) The intersection is instead held honest by a source-scan guard, `scripts/__tests__/disabled-builtin-rail-drift.test.js`; `rules.test.ts`'s in-package assertion pins the names but reads no runner and is not that guard.
  - **The repo runs 9 required CI checks on a PR, not 11** (measured 2026-08-23 against #460 and #461 — identical sets): `test`, `Analyze` ×3 (CodeQL's per-language matrix legs), `CodeQL`, `semgrep`, `gitleaks`, `helm-render`, `docker-build`. The last two are change-gated and skip-pass when nothing chart- or image-relevant moved. Count them with `gh pr checks <n> | wc -l`, not from memory — an inflated expected count makes a green PR look like it is still missing checks.
