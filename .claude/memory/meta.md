# Meta — self-observations about how I work on this project

Behaviors (not project facts). Name a behavior, name the better alternative. Tag `active` if load-bearing for next session.

- `2026-05-25` `active` — **TASK-24: my first recovery-UX draft (auto-retry the dropped turn) took NINE Codex rounds to converge, and the convergence point was REMOVING the auto-retry entirely — the exact same endpoint FAULTA-5 reached.** Codex peeled back, in order: same-reqId silent resume → lossy on host restart (in-memory buffer + per-reqId seq reset to 1); broad auto-retry → duplicates sendMessages-rejection turns; narrow auto-retry on the dead-turn frame → duplicates post-side-effect turns; then 4 more rounds of subtler holes (inline-regenerate races useChat cleanup, 5xx stretches to 2min, the long retry budget wraps non-idempotent `tool.execute-host`, the body-drain hangs on abort-during-await, an enum-mirror drifts). **Lesson (reinforcing meta 2026-05-25 FAULTA-5): for ANY fault-recovery/retry/resume feature against a stateful non-idempotent server, the DEFAULT correct answer is manual-retry; auto-retry needs turn-level idempotency + durable/seq-stable events that don't exist yet. Next time I hit a "auto-retry the interrupted turn" clarification, lead with that conclusion and the FAULTA-5 precedent in the brainstorm, ship the manual-retry safe subset + the durable-commit fix that ACTUALLY stops the loss, and file the auto-retry as the idempotency-gated follow-up — rather than implementing auto-retry first and letting Codex find the 9 holes.** The durable-commit fix (Task-1's 2-min IPC deadline) is what truly fixed the reported "silently lost turn"; the recovery UX was secondary. Also: when an explicit clarification collides with a hard data-integrity invariant AND the safe subset still fully fixes the reported bug, ship the safe subset + flag the tension in the handoff (don't block, don't ship the unsafe thing).
- `2026-05-25` `active` — **TASK-24: the Edit tool put my FIRST edit of a worktree file into the PRIMARY checkout despite the full worktree path (the FAULTA-5 twin, recurred). The reliable recovery: grep-count a unique marker in BOTH paths, `git -C <primary> checkout -- <file>`, then Read the WORKTREE path FRESH and re-Edit — after a fresh Read the Edit lands in the worktree.** The tell was a vitest run showing FEWER tests than I added (the stale worktree copy ran). After that first correction every subsequent worktree edit landed correctly. Next time: after the very first edit of each file in a worktree task, grep-verify the marker is in the worktree (not the primary) before running tests.
- `2026-05-25` — **TASK-24: I extracted inline logic into exported, unit-testable pure helpers (`writeWithBackpressure`, `readCappedBody`, `shouldAutoRetry`, `lastUserMessageId`) specifically when an integration test proved RACY** (the abort-during-DNS-resolver test couldn't deterministically hit the window through undici→proxy→resolver). Extracting the helper + driving it with a fake EventEmitter gave a deterministic regression test for the exact bug Codex flagged. Lesson: when a fault-path is timing-dependent through real I/O, extract the pure decision into a helper and test THAT — don't ship a flaky integration test (it'll flake CI like the #146/TASK-5 class).

- `2026-04-23` `active` — **I've been skipping the claude-memory write phase on task completion.** Week 3 shipped as PR #2 without any memory write; this bootstrap is retroactive. Better: treat "PR opened" and "tests pass on a significant change" as hard write-phase triggers, not soft hints. At minimum, ask the meta-question ("what did I learn about how I work?") before declaring a task done.
- `2026-04-29` — **I offered the user an option that depended on local fs state without checking that state first.** Asked "ANTHROPIC_API_KEY vs ~/.claude/.credentials.json", user picked the credentials path — and then I discovered macOS keeps creds in Keychain, not on disk. Better: when listing options that hinge on a local file/binary/env-var existing, do the existence check inline before the AskUserQuestion. One extra Bash call upfront beats a wasted question round-trip.
- `2026-05-24` `active` — **In an orchestrated yolo-ship worktree, I wrote a new file with a MAIN-checkout absolute path (`/home/.../ax-next/packages/...`) instead of my worktree path (`/home/.../ax-next/.claude/worktrees/<id>/packages/...`).** It landed as an untracked file in the main checkout — harmless this time (didn't clobber HEAD, git status showed it `??`), but vitest in the worktree then reported "No test files found" because the file wasn't there. Cost a debugging detour. Better: when in a worktree, every Write/Edit target MUST be prefixed with the worktree root; `git rev-parse --show-toplevel` is the worktree path — paste it as the prefix and never hand-type the bare repo root. After any stray write, `mv` it into the worktree and re-check `git -C <main> status` is clean.
- `2026-05-24` `active` — **ARCH-6 (orchestrated yolo-ship): no `Task`/`Explore` subagent tool was available in the env** (ToolSearch returned nothing for them). Did the codebase exploration + per-task implementation INLINE with strict TDD instead of dispatching subagents. Worked fine because the surface was fully mappable up front (one mechanism — `registerService({returns})` — repeated across ~6 plugins) and the credentials precedent gave an exact template. Lesson: when the subagent tools aren't present, don't stall — inline strict-TDD is the documented fallback for fully-mapped mechanical work (decisions 19/38/50/63/74), just watch the context budget and flush to the plan file if it climbs.
- `2026-05-25` `active` — **ARCH-11: the openai-codex companion script was NOT installed this run** (`~/.claude/plugins/cache/openai-codex/...` empty — only `claude-plugins-official` + `skill-codex`), so the yolo-ship Phase-5 `codex-companion.mjs review --wait` invocation had nothing to call. But the `codex` CLI binary WAS on PATH (`codex-cli 0.133.0`) and authed, and it has a first-class `codex exec review --base main` subcommand — non-interactive, whole-branch-vs-main, exactly the Phase-5 review. Used that instead and it found a real P2 (CI affected-package selection gap). Lesson: codex availability varies per run across THREE shapes (companion script present / `codex` CLI present / neither) — probe all before concluding "codex unavailable". `codex exec review --base main` from the worktree is the CLI-only fallback for Phase 5.
- `2026-05-25` `active` — **FAULTA-5: I shipped a first draft (transport emits error / runtime silently regenerates) that LOOKED done + green, but Codex round-by-round peeled back 4 layers of correctness bugs in the recovery path before it was actually right** — P1 (regenerate re-POST DUPLICATES a live server turn) → reworked to same-reqId reconnect → P2 (count-dedup of the bounded 256-chunk replay buffer silently DROPS output when the server outruns it while disconnected; no wire sequence number to align a partial replay) → narrowed to pre-content reconnect → P2 (even pre-content can attach to a truncated buffer) → finally REMOVED the silent reconnect entirely (banner-on-drop + manual retry). Lesson: for **fault-recovery / retry / resume** logic, "tests green + a plausible design" is NOT enough — the failure-mode space (duplicate side-effects, partial replay, abort-vs-error, buffer truncation, mid-line frame split) is where the bugs live, and each Codex round found a real one I'd missed. Run the local Codex review EARLY and treat a recovery feature as not-done until the review is clean, because the naive design (auto-retry the whole turn) is almost always wrong against a stateful server. Also: when a stated requirement ("silent retry first") collides with a hard correctness constraint ("never silently lose/duplicate data") given the current contract, the right move is to ship the loss-free safe subset (banner + manual retry — which fully fixes the bug) and file the enabling infra (a server per-chunk sequence number) as a follow-up — NOT to ship a lossy silent retry and NOT to reflexively bail as blocked. This was self-answerable.
- `2026-05-25` `active` — **FAULTA-5: I Read+Edited the PRIMARY checkout path (`/home/vpulim/dev/ai/ax-next/packages/...`) instead of my worktree TWICE at the start, dirtying shared `main`** (the env "Working directory" snapshot + muscle memory pointed there). Two Edits "succeeded" but landed on the shared checkout; vitest in the worktree showed the OLD file → I caught it by grep-diffing the two paths, reverted the primary with `git -C <primary> checkout --`, and re-applied to the worktree. This is the file-edit twin of the 2026-05-24 `git -C` note — SAME shared-main hazard, different tool, and I repeated it. Lesson: at Phase 0, capture `WT=$(git rev-parse --show-toplevel)` and prefix EVERY Read/Edit/Write file_path with `$WT` — never `/home/vpulim/dev/ai/ax-next/...`. Sanity-check before the first edit of a file: grep the target line in BOTH paths; if the primary already shows your change, you edited the wrong tree.
- `2026-05-25` `active` — **FAULTA-5: I ran the codex-companion.mjs `review --wait` form (it WAS installed this run at `~/.claude/plugins/cache/openai-codex/codex/1.0.4/`) and it worked perfectly across 9 rounds** — non-interactive, whole-branch-vs-main, real findings each time. Confirms the three-shapes probe (meta 2026-05-25 ARCH-11): this run had BOTH the companion script (1.0.4) AND the bare CLI (0.133.0). When the companion is present, `node "${CODEX_ROOT}scripts/codex-companion.mjs" review --wait --scope branch --base main` is the right Phase-5 driver; it self-recovers its own PATH (prepends nvm bin, uses ./node_modules/.bin/tsc). Re-probe every run — don't assume which shape is present.
- `2026-05-25` `active` — **FAULTA-3: I burned ~45min because I ran the codex CLI review as a FREEFORM PROMPT (`codex exec "<long review instructions>"`) instead of the `codex exec review --base main` SUBCOMMAND.** The freeform invocation HUNG (42min elapsed, 0.05s CPU = blocked, zero output) — I killed it and fell back to a manual self-review. THEN I read meta.md and saw the ARCH-11 note (above) that names `codex exec review --base main` as the correct Phase-5 CLI form; re-ran it and it completed cleanly in ~6min with a real verdict ("no actionable introduced bugs"). Lesson: the structured `review` subcommand is purpose-built (loads memory, diffs vs base, emits a verdict) and reliable; a giant freeform `codex exec "<prompt>"` can wedge. ALWAYS use `codex exec review --base main` for Phase 5 — AND read meta.md's `active` codex notes BEFORE the first codex attempt, not after the first one fails. (Also: codex's own run reported 2 proxy-startup.test.ts 5s-timeout flakes — those are the TASK-5/#146 load-induced class under codex's parallel workspace-write sandbox, NOT my diff; codex correctly tagged them "outside this diff" and CI's test job was green.)
- `2026-05-25` `active` — **TASK-25: the local `pnpm test` gate reports ~25 FAILs in `@ax/agent-claude-sdk-runner/src/__tests__/git-workspace.test.ts`, ALL with `git lfs install --local failed (exit=1): git: 'lfs' is not a git command` — a LOCAL ENV artifact (git-lfs not installed in this shell), NOT a code defect.** Confirmed by `git lfs version` → "not a git command" and by the fact my diff touched only `credential-proxy` + a docs file (zero git-lfs relation). The agent Dockerfile + CI install git-lfs, so CI's `test` job is green. Lesson: when the full-suite gate shows failures, GREP the failure signature first — if every failure shares one env-tool-missing message (`git-lfs`, `docker`, a testcontainer image pull) and is confined to packages your diff didn't touch, it's an env artifact: confirm by checking the tool's presence + that your touched packages are independently green (`pnpm --filter <yours> test`), don't chase it. This is the load-induced-flake note's cousin (mistakes 2026-05-25 TASK-5) but for a MISSING binary, not contention.
- `2026-05-25` `active` — **TASK-25 (orchestrated worktree): the worktree had NO `node_modules` on entry — `pnpm <...> test` failed with `vitest: not found` / `ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL spawn ENOENT`.** `git worktree add` carries the tracked files but not the (gitignored) `node_modules`, and tsc-built `dist/` is also absent. Fix at Phase 0/3 boundary: `pnpm install --frozen-lockfile` (≈1.4s, store is shared so it's just linking) THEN `pnpm build` (tsc --build) BEFORE running any cross-package test — `@ax/core` and siblings resolve via `dist/` exports, so an unbuilt tree fails import resolution ("Failed to resolve entry for package @ax/core") even though the test logic is fine. Lesson: in a fresh orchestrated worktree, run `pnpm install --frozen-lockfile && pnpm build` once up front; a "Failed to resolve entry for package @ax/*" error is almost always "not built yet", not a real break.

## 2026-08-23 (session 4, Wave-3 drain) — measuring the cards before building them is now load-bearing, not optional

- **4 of 8 cards were wrong about their own bug** — the same rate as Wave 2 (3 of 6). This is no
  longer an anomaly to note; it is the base rate for cards written during a *previous* session's
  review pass. Two failure shapes recur: (a) the card describes the symptom the reviewer saw, not
  the defect (TASK-239 — the leak was real but the load-bearing bug was a false causal claim in a
  different package); (b) the card's premise was fixed, or was never true, and the card was never
  re-checked (TASK-238's branch is dead behind `disallowedTools`; TASK-274's two factual claims both
  failed on contact with the test file they described).
- **The measurement pass paid for itself twice over and cost ~20 minutes.** Four read-only agents,
  dispatched in parallel before any builder, against a written brief that said "do not paraphrase the
  card; read the code and state what is true." Direct outputs: 4 corrected cards, 2 rulings that
  changed scope, 1 un-carded live defect found (TASK-272's twin), and 3 new cards. Without it,
  TASK-238 would have shipped tests asserting behaviour no production path executes — the exact
  defect TASK-256 was open to fix, one package over.
- **Rewrite the card, do not just act on the measurement.** Struck the false acceptance bullets in
  place (`~~...~~` + why) rather than deleting them, so a later session cannot rebuild them. Two
  cards were retitled because their titles asserted false premises. This is what makes the
  measurement durable instead of session-local.
- **Sequencing discovered by measurement beat the filed dep graph.** TASK-274 was filed dep-free but
  actually depended on a type TASK-276 had not written yet. Setting that edge produced a chain where
  276 deliberately WITHHELD a user-facing sentence ("...Trying again.") because no retry existed to
  back it, and 274 landed the sentence with the mechanism. Neither card asked for that; it fell out
  of ordering them correctly.
- **My own card was wrong within four hours.** TASK-239's builder overrode two of my prescriptions
  with better reasons (required-vs-optional `cause`; keeping a redundant `.parse` inside the `try`
  because moving it out would be fail-OPEN). Accepted both. The measure-the-premise rule applies to
  instructions I wrote this morning, not only to cards inherited from a prior session.
- **Orchestration findings this run** (all first-hand, filed as TASK-298 — then **re-measured
  before building, which corrected three of the four and found two more**; fixed in the skills
  2026-08-23). What survived measurement: `board_batch` mangles its GraphQL alias under zsh; the
  merge-queue one-liner's `--delete-branch` always exits 1 against worktree-isolated builders; and
  the reviewer deadline discards slow-but-healthy reviews (4 and 7.5 min of work delivered ~40 min
  apart, one holding a real blocker). What did NOT survive: the zsh bug is at `a$i:update…`
  (`:u` = upcase), **not** at `\$it$i:ID!` — `:I` is not a modifier, so a builder braced the line
  the card named and would have left the real bug in place; the progress helper *can* be `source`d
  from a worktree by absolute path — the real cause is that it is **gitignored**, so
  `git worktree add` never carries it in and the *relative* form every example taught exits 127;
  and the "25-minute deadline" fix had already merged in `b5041298`. Two more found by the sweep:
  `board_snapshot` read `--limit 200` against a 300+ item board and guarded only `length>0`, and
  the cleanup block used a single `-f` that cannot remove a harness-locked worktree. **The
  generalisable bit: every one of these was regenerated at run start from a doc code-block that no
  test had ever read.** Prefer making them loud over making them quietly correct — and put a real
  guard on the doc (`scripts/__tests__/autoship-skill-shell-hazards.test.js`), because a fix that
  lives only in prose gets re-broken by the next edit.
- **Builders comply when told; they do not infer.** 3 of 3 skipped their handoff (and its
  `reviewer:` field, the merge gate) until the dispatch prompt explicitly said "return at PR-open,
  report `ci: pending`, do not wait on CI." Then 5 of 5 complied. The knowledge was in project
  memory; it was not in the template agents actually read.
