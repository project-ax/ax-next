- `2026-09-21` [active] — I spent too long designing around hypothetical startup shapes before checking the repository. Read the concrete call inventory and timeout configuration early; here the existing starts were uniform, so a small shared wrapper preserved lazy behavior without new global hooks or budget changes.
- `2026-09-21` [active] — Record a completed exact-SHA CI gate immediately and reuse that evidence until the target changes; interleaving a long investigation led me to request the same already-green main run twice.
