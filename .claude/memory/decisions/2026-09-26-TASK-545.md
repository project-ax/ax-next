| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | Supersedes TASK-541's "NOT covered: a feed MOUNTED already failed" row. ActivityFeed now arms its `data-activity-announcer` region one passive effect after mount: on the first commit the failure renders BESIDE the (empty, zero-height) region at the same spot, then `useEffect` sets `armed` and it moves INSIDE. | The hook lives in WorkspaceShell, so tabbing away from "What it did" and back remounts the feed with `error` still set, and the region was born full. Rendering beside-then-inside keeps the region empty for a commit and a paint while a sighted reader sees nothing move, which answers TASK-541's only objection to arming (a one-frame blank). | Hold the failure back until armed: the one-frame blank TASK-541 rejected. `useLayoutEffect` to arm: no paint between empty and full, so assistive tech may coalesce the two. A key or attempt counter: same text re-inserted in one commit. |
| 2026-09-26 | The TASK-545 test records frames from a React `Profiler` `onRender`, not the parent `useLayoutEffect` recorder from #696. | The arm is a state update inside ActivityFeed, which re-renders the feed alone. A parent layout effect never runs for that commit, so it cannot see the empty-then-filled step. `onRender` fires on every commit under it, after the DOM is written. | Parent layout-effect recorder: blind to child-only commits. |
