| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-516: **CORRECTION to `decisions/2026-09-20-TASK-504.md`, row 2** (the `ax-next.bool` row, "Strictly narrowing: no input that rendered OFF can now render ON, which is the only safe direction"). The render claim holds for every gate; the *safety* claim does not. Unrecognised strings (anything but true/false) now resolve to OFF. For the 13 capability gates, OFF withholds reach, so this fails CLOSED. For the 2 enforcement gates, `networkPolicies.enabled` and `ingress.tls`, OFF is the OPEN posture (no NetworkPolicies, no TLS block), and before the helper the same garbage string was truthy and enforced. So on those two gates the helper moved garbage input from safe to open. The same false claim was in `_helpers.tpl` ("fail CLOSED, never open"); rewritten there too. | MEASURED-BY-PROBE 2026-09-26, helm 3.13.2, schema-less copy of the chart: `networkPolicies.enabled` true → 3 NetworkPolicies, `--set-string =maybe` → 1, false → 1. `ingress.tls` true → `tls:` block present, `=maybe` → absent. The installed chart refuses `=maybe` for both at install time (schema), so this only happens with `--skip-schema-validation` plus a non-true/false string. Now pinned in `render.test.ts` ("enforcement gates"), so the corrected comment is tested. Count: 16 booleans in values.yaml = 15 helper-gated (13 capability + 2 enforcement) + `blob.s3.forcePathStyle`, which is a `quote` pass-through and never goes through the helper. The card's "14 of 16" counted that one on the narrowing side. | Make the helper per-gate aware (a `failOpen` argument for enforcement gates): REJECTED, the card asked for no behaviour change, the schema already rejects every non-boolean, and a second knob at 44 call sites is a new way to get it wrong. Leave the comment: REJECTED, "never open" is false and a later agent would skip the check that catches it. |
| 2026-09-26 | TASK-516: closed the "gate with no values.yaml default is invisible" gap with a **test**, not the convention comment the card offered. `render.test.ts` now scans template source (comments stripped) for `include "ax-next.bool" <arg>` and fails if an arg isn't a `.Values.` path or that path isn't a boolean default in values.yaml. | `BOOLEAN_PATHS` is derived from values.yaml, so a gate with no default drops out of all three TASK-504 guards without anyone noticing. A comment can't catch that; a check can. It reads source, so it runs even without helm. | Convention comment only: REJECTED as the sole fix (a guard that prose can satisfy is not a guard); the helper comment points at the test instead. |
