# TASK-435 — decisions

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-20 | Format every workspace timestamp client-side, in the reader's zone; the wire carries an ISO instant only. | Not a judgement call I made — `ActivityEvent`'s own doc comment in `lib/workspace-types.ts` already settled it for the `did` feed ("A display string is a rendering decision, and rendering decisions do not belong on the wire"), after the prototype shipped server-computed `day`/`time`. The chat tab simply never finished that migration. So this is completing an existing decision, not opening a product question — which is why the card was NOT escalated as `blocked`. | Format server-side in a zone the client sends up (moves a rendering decision onto the wire AND needs a zone on every request); label the server's zone on screen ("12:56 AM UTC") — honest but still makes the reader do arithmetic. |
| 2026-09-20 | Scope includes `relativeDay` / `PastConversation.meta`, not just the chat clock the card names. | Same defect, same file, and it is the DATE half — a "yesterday" computed in the host's zone is the worse version of the bug the card opens with. Card explicitly asks for an audit of every workspace timestamp. | Fix only `shortTime` and file the other as a follow-up: leaves a second server-side formatter in the same function's neighbourhood for the next reader to copy. |
| 2026-09-20 | One shared client module `lib/workspace-time.ts` rather than each surface keeping its own formatter. | CLAUDE.md invariant 4. The card is literally "two surfaces disagree"; leaving two formatters means they can silently drift apart again. `ActivityFeed`'s private helpers and the server's `relativeDay` collapse into it. | Leave `ActivityFeed`'s copies alone and add a third formatter for the bubble — how we got here. |
| 2026-09-20 | The live streaming bubble carries `at: ''`, not a synthetic `new Date()`. | The frame has no committed instant until the turn lands; inventing one puts a clock on screen that the reload then changes. `''` reproduces today's behaviour (`time: ''` drew nothing) and the renderer now omits the row entirely rather than drawing an empty div. | `at?: string` optional — more churn at every construction site for the same meaning. |
| 2026-09-20 | The parity test moves `process.env.TZ` mid-test (host `UTC` → reader `America/New_York`) instead of pinning one zone. | Measured: a single-zone test passes against the UNFIXED code, because `shortTime` formats in the same zone the renderer reads back. The disagreement only exists across two zones. Node 24 applies a `process.env.TZ` assignment immediately, to already-constructed `Date` objects included (probed). | `vi.stubGlobal` a fake `Intl` (tests the stub, not the code); assert only wire shape (would not demonstrate the disagreement the card reports). |
| 2026-09-20 | The test does NOT pin a locale, and derives its expectations instead of asserting literal `8:56 PM`. | Production formats with `toLocaleTimeString(undefined, …)` — the reader's locale, correctly. Pinning `en-US` in the test would assert a formatter the code does not call. Parity + "equals the reader's reading" + "differs from the host's reading" are all locale-independent. | Hard-code `'8:56 PM'`: green today, and silently a different test on any runner whose default locale is 24-hour. |
| 2026-09-20 | Reviewer dispatched with no `name` and no `isolation`, worktree path named in the prompt. | yolo-ship Phase 5 dispatch contract — a named teammate's final text is never delivered, which is what every past "hang" was. | — |
