# TASK-511 hover contrast on filled accent controls

| Date | Decision | Rationale | Alternatives |
|------|----------|-----------|--------------|
| 2026-09-25 | Fix the shared `Button`/`Badge` hover, not the consent button: `hover:bg-{primary,destructive}/{90,80}` → new per-theme tokens `--primary-hover` / `--destructive-hover` (`hover:bg-primary-hover`) | Card premise CONFIRMED and widened: the consent button has no override, it is `Button` default. The alpha lets the surface bleed through, and the surface sits on the foreground's side of the accent in BOTH themes, so the hover drifts toward the text. Measured (theme-contrast formula): button primary 4.48 L / 4.32 D, destructive 4.46 / 4.30; badge /80 3.75 / 3.53 and 3.79 / 3.53. A hover must move AWAY from the foreground — darker in light, lighter in dark — which no single alpha expresses | `hover:brightness-*` filter (darkens in dark mode too → worse); `hover:bg-primary/95` (still toward the text, only smaller); className override on the consent site only (siblings stay failing) |
| 2026-09-25 | Values: primary hover 37% L / 62% D, destructive 45% L / 68% D | ~1.2:1 step off the resting fill, about what shadcn's /90 gave visually, while text contrast rises: 6.50 / 6.18 and 6.05 / 6.25 | Larger steps (visually jumpy); smaller (hover barely visible) |
| 2026-09-25 | Guard reads the hover class out of `button.tsx`/`badge.tsx` (scoped to cva's `variant: {}` block — `size` has its own `default:`) and composites any alpha over background/card/popover/muted; plus a pinned row that stock `/90` measures UNDER the floor | Same read-from-source pattern as the ApprovalCard and state-dot sections, so a revert fails with the true number. Proven red: 12/12 hover rows failed on the unfixed components before the component edit | Hard-coded token pairs in the test (would pass over a component revert) |
| 2026-09-25 | Did NOT fix `SessionRow`'s delete menu item (`text-destructive` on `hover:bg-destructive/15`, 4.08 L / 4.22 D on card) — returned as a follow-up | Different treatment (accent text on its own tint, not a filled control), different fix space, outside the card's "primary buttons" scope and in a file siblings may touch | Folding it in here |
| 2026-09-25 | Reviewer (APPROVE on b6baf901) nits left as-is: dead `/90` utilities emitted from the test/comment literals via the `src/**` content glob (~100 B, nothing renders them); the accent-family test is an invariant guard not a regression test (contrast rows already catch a hover pointed at the foreground). SessionRow delete item returned as a handoff follow-up for auto-ship to file | Contorting the test to hide literals from Tailwind costs readability for ~100 B | Excluding `__tests__` from the Tailwind content glob (out of scope) |
