| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-563: bound the pg-init hook Job twice — an in-script readiness deadline (`postgres.embedded.init.readyTimeoutSeconds`, default 300, FATAL naming the host and the knob) plus Job `activeDeadlineSeconds` (`postgres.embedded.init.activeDeadlineSeconds`, default 600) — and `PGCONNECT_TIMEOUT=5` per probe. | The script bound gives the readable message; the Job bound covers what the script cannot: `backoffLimit: 10` restarts (each would re-run a full wait) and a later psql that hangs. PGCONNECT_TIMEOUT stops one black-holed TCP connect from outliving the budget. | Only `activeDeadlineSeconds` — REJECTED: the Job dies with `DeadlineExceeded` and no line saying why. Only the script bound — REJECTED: 10 restarts x 300s. |
| 2026-09-26 | TASK-563: card premise partly refuted — `helm install/upgrade` does NOT hang forever; helm's `--timeout` (default 5m) bounds its wait on hooks. What was unbounded was the Job itself, which kept spinning in the cluster after helm gave up, with only a generic "timed out waiting for the condition". | Helm docs: `--timeout` is "time to wait for any individual Kubernetes operation (like Jobs for hooks)". | — |
| 2026-09-26 | TASK-563: template `fail`s when activeDeadlineSeconds <= readyTimeoutSeconds; values.schema.json types both as integer >= 1 (catches 0, negatives and `--set-string`). | An inverted pair would let Kubernetes kill the Job before the script can explain itself — silent in exactly the case the card exists for. | No ordering guard — rejected, cheap and closes the silent case. |
| 2026-09-26 | TASK-563: implemented inline, not via subagents. | Three chart files + one test file. | subagent per task. |
| 2026-09-26 | TASK-563 review (APPROVE, 3 Minor/Nit, all taken): (1) read the bounds with `hasKey`, not `\| default` — sprig treats 0 as unset, so a schema-skipped 0 rendered as the default; (2) `additionalProperties: false` on `postgres.embedded.init` so a misspelled bound fails instead of silently keeping the default; (3) default readyTimeoutSeconds 300 -> 240, under helm's default 5m --timeout, so the Job's FATAL fires before helm's generic timeout. The earlier "default 300" row above is superseded by this one. | Each is cheap and each closes a silent case on a deadline knob. | Leaving 300 for a round number — rejected: then helm's timeout and the FATAL race. |
