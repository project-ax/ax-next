| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-551: every `chat:end` / `chat:turn-error` / `chat:permission-request` the ORCHESTRATOR fires goes through one `fireChatEvent` helper that passes `subscriberTimeoutMs = CHAT_EVENT_SUBSCRIBER_TIMEOUT_MS` (30 s; config `chatEventSubscriberTimeoutMs`, validated at init via a shared `validSubscriberBound`). | Each of those fires is awaited by something with no other clock: `agent:invoke` (deadline Infinity) awaits the synthesized chat:end and the turn-error before it; the up-front connector card is awaited in turn setup; the egress-wall card and onSessionTerminate's turn-error are awaited inside other subscribers. No subscriber is load-bearing for the outcome (SSE frame, display-event row, activity line, detached memory work), so skip-and-proceed like chat:start. | Per-hook constants (three numbers for one class of subscriber); fail the turn on a timed-out subscriber (turns a slow observer into a user-facing error). |
| 2026-09-26 | TASK-551: 30 s, not chat:start's 60 s. | 2x the 15 s stall watch; healthy work is one frame or one row write. chat:start's 60 s exists for memory-strata's cold-tier fan-out, which none of these do. | 60 s (reuse); 15 s (== stall watch, no runway after the name-yourself line). |
| 2026-09-26 | TASK-551: the runner-reported `chat:end` fired by @ax/ipc-core stays UNBOUNDED. | agent:invoke does not await it (the orchestrator's subscriber resolves the waiter from inside it), so a hang there is already capped at chatTimeoutMs, after which the bounded synthesized fire runs. Bounding it changes fire()'s microtask timing on every successful turn — TASK-514 measured that breaking preset-k8s's chat:end-once witness. Residual cost documented in CHAT_EVENT_SUBSCRIBER_TIMEOUT_MS: a hung subscriber ahead of the orchestrator's turns a finished turn into chat-run-timeout. Returned as a follow-up. | Bound it and loosen the acceptance witness (changes an I1 canary in a card that must keep it green). |
| 2026-09-26 | TASK-551: skill-broker's request_capability card and channel-web's dispatch-failed turn-error left unbounded. | The first is inside a tool service with its own 30 s timeoutMs; the second is fire-and-forget (void .catch), nobody awaits it. Neither can hang a turn forever. | Bound them too (needs per-plugin constants — cross-plugin import is forbidden — for no reachable hang). |
| 2026-09-26 | TASK-551: implemented inline, not via subagents. | One production file + one test file; a subagent round-trip costs more than the change (same call as TASK-512/514). | subagent-driven per yolo-ship default. |
