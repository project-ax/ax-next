| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-568: the augment's `recall` helper warns `memory_augment_degraded_flag_coerced` once per non-string flag per engine answer (three store queries, so a bad engine answer warns up to 3x), bindings `{ agentId, flagType }` only; flagType names `hole`/`null`/`array` explicitly, else `typeof`. Coercion unchanged (human ruling). | Per-answer is the honest "per occurrence": each of the three `facts:recall` answers independently carried the bad flag. Checking in the helper, before the three answers are spread together, is the only place a hole is still a hole — the spread in `buildMemoryBlock` turns it into `undefined`. | Warn once after the merge+dedup — REJECTED: holes are already gone by then and the per-answer count is lost. Log the value — REJECTED by the card (could carry user content). |
| 2026-09-26 | TASK-568: the warn call is wrapped in try/catch. | The ruling's premise is that a bad advisory flag must not fail the turn; a throwing logger on the warning path would do exactly that. Same shape as `exporter.ts`'s `fail()`. | Unguarded — rejected. |
| 2026-09-26 | TASK-568: a present-but-non-array `degraded` in the augment still maps silently to `[]`; NOT changed here. | Out of the card's scope (the ruling is about non-string flags), and the non-array case is a different decision (coerce-to-empty drops the whole signal). Returned as a follow-up. | Warn on it too — deferred to a card so the human can rule. |
| 2026-09-26 | TASK-568: implemented inline rather than via a per-task subagent. | Single ~40-line change in one file plus tests; dispatch overhead exceeded the work. | Subagent-per-task — skipped as disproportionate. |
