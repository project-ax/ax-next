| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-22 | With explicit user approval, narrow TASK-454 to the triage doc guard and correct the stale two-guard claim. | `scripts/__tests__/autoship-triage-title-draft-id.test.js` joins continuations before removing comments; `scripts/__tests__/autoship-ci-run-lookup-full-sha.test.js` has no continuation-joining helper. The original claim in `scripts/__tests__/yolo-ship-review-range-origin-main.test.js` and its repetition in `scripts/__tests__/mutation-restore-protocol.test.js` overstated the affected scope. The CI-run guard stays unchanged; no second baseline failure or mutation result is claimed. | Expanding CI-run structural scanning was offered and not selected. Retain the original card report as explicitly historical below the approved scope correction. |
| 2026-09-22 | Remove whole-line shell comment text before joining continuations, but preserve the physical newline. | Dropping the entire comment line can splice a preceding continued command to the independent command below the comment. Mapping comment text to an empty line prevents both that splice and a backslash-ending comment swallowing the next command. The guard remains a scanner for the documented shell shapes, not a general shell parser. | Filtering whole comment lines; a broad shell-parser rewrite. |
| 2026-09-22 | Run the same nonempty, all-occurrences pipeline assertion against the real triage block and fixed-input probes. | A first matching unpiped call must not hide a later piped call. Both item-edit and GraphQL shapes get regression probes, with genuine continuations, multiple unpiped calls, logical OR, and missing-command checks labeled as compatibility controls. | A separate test-only checker would miss disconnection from the real structural assertions. |
| 2026-09-22 | Use TASK-452's shell-comment/backslash probe shape, not TASK-408's JavaScript block/line-comment syntax. | The transferable lesson is transformation ordering; the grammars differ. Preserve earlier cards' historical mutation counts rather than treating them as current TASK-454 evidence. | Copying the predecessor probe or mutation counts without verifying their applicability. |
