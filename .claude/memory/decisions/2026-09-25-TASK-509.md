# Decisions — TASK-509

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | Fix in `useDecisionQueue.refresh` (merge fresh read with just-resolved receipts), not local retention in `AgentConversation`. | Card's inferred cause was half-right: `GET /api/workspace/decisions` is open-only, but TodayView retains NOTHING locally — it filters the same shared array. It survived only because Today never triggers `queue.refresh()`; the thread does (continuation `onDone`/`onDecisionRaised` → `onChanged`). Fixing the shared queue fixes both surfaces with one rule (invariant 4). | Per-component retention in AgentConversation (card's suggestion) — a second copy of state for the same row, and Today would still lose receipts on any shell refresh. |
| 2026-09-25 | `JUST_RESOLVED_MS` + `isJustResolved` moved to `lib/workspace-types.ts`, shared by TodayView and the queue merge. | One predicate for "which receipts are still shown" and "which receipts survive a re-read"; two copies would drift. | Keep constant in TodayView and duplicate. |
| 2026-09-25 | Open rows missing from a fresh read are still dropped; server copy always wins for listed rows; retained receipts appended after fresh rows. | Fails closed: never shows buttons for a question answered elsewhere. Today renders receipts as their own filtered group, so position vs. fresh rows is immaterial. | Splice at previous index — more code, no visible benefit. |
| 2026-09-25 | Implemented inline rather than via per-task subagents. | 4 small edits + 1 test file; subagent overhead exceeds the work. | subagent-driven-development per task. |
