# Decisions — TASK-473

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | `ConsentAnnouncement` takes a REQUIRED per-renderer `openNote`. `ApprovalCard` passes notice ?? stale sentence (notice-first, `answerKey`'s order); `DecisionRow` passes `null` explicitly. | ApprovalCard's open-branch stale reason/notice are plain `<p>`s with no live voice except the click-armed focus landing; DecisionRow's are shadcn `Alert` (`role="alert"`), so sharing them there would double-speak. Required (not defaulted) so a renderer's silence is a stated decision. | Global open-branch announce (double voice in DecisionRow); strip `role` from DecisionRow's Alerts and route both through the shared region (cleaner mechanism, but rewrites `consent-focus.test.tsx`'s `visibleAlert()` uniqueness guard while sibling TASK-510 is in that file) — filed as a follow-up instead. |
| 2026-09-25 | A card that MOUNTS already stale stays silent; only a change while mounted is announced. | Mount-silence is TASK-442's rule; a region created holding its sentence is the unreliable shape; the hold's arrival is already said politely by `InThreadApprovals` (TASK-275 ruling: polite announce, no focus move). | Announce on mount (page load would read every stale card as news). |
| 2026-09-25 | Double speech on the person's OWN click (focus lands on the stale `<p>` and the alert fires) accepted. | Same named cost the resolved branch already accepts in `ConsentAnnouncement`'s header; suppressing on armed focus couples the region to a focus move that may not carry the words. | Suppress announce when `armForResolution` fired. |
| 2026-09-25 | Collapsed `DecisionRow` going stale passively is still silent (its Alerts render only when `expanded`) — out of scope, handed off as a follow-up. | Fixing it needs a "voiced elsewhere when expanded" rule or the Alert-role move above; either widens past this card and into sibling TASK-510's files. | Fix here. |
| 2026-09-25 | Implemented inline, no per-task subagents. | Three small edits + one test block. | subagent-driven-development. |
