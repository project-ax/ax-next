# TASK-521 benchmark instrumentation

| Date | Decision | Rationale | Alternatives |
|------|----------|-----------|--------------|
| 2026-09-24 | Treat TASK-497's recall-p95 failure as a time-clustered regime, not a random tail, and instrument before any product change | Offline read of run 15c4554d (mtimes as the only wall clock): first 88 questions p95 1,039 ms Sonnet / 1,288 ms GLM with 1 degraded call; last 12 banks, after a ~4 h gap at interruption 2, held 26/27 degraded calls, slower ingestion (5–9 vs 3–4 min/bank), interruptions 2–5, 24/29 observer failures, 42/61 uncertain charges. Same source throughout (identity guard). The 88/12 split was chosen after seeing the data, so it is a hypothesis for the next run, not a pass | Tuning product budgets now; calling the early window a pass |
| 2026-09-24 | Degraded recall latency is bounded by the product's serial budgets (embed 1,500 ms → fusion → rerank 2,000 ms, `packages/memory-facts-sqlite/src/producers.ts`) | All six `ranking` flags landed at 3,505–3,509 ms = 1,500 + 2,000. Once >5% of calls hit a budget, p95 <1,600 ms is unreachable. Whether the target covers degraded mode is an open product decision, NOT made here | A shared recall deadline (a product change) |
| 2026-09-24 | Harness-only change with a new run identity; no paid run | Card scope. `HARNESS_FILES` now includes the trace module, so the TASK-497 manifest (digest `7dbec284…`) cannot resume under this harness | Rewriting or resuming run 15c4554d |
| 2026-09-24 | Time stages by wrapping handlers in the harness's existing `registerService` shim, not by adding product hooks | No hook-surface change, so no boundary review. A handler span ends when the provider actually settles, so a producer that lost the budget race still reports how late it was; the existing drain-before-save puts late spans into the capture | New timing hooks in `@ax/memory-facts-sqlite` |
| 2026-09-24 | Mint the Vertex token asynchronously, only at untimed boundaries (before the preflight, each ingestion session and each answer attempt); `credentials:get` never spawns | The frozen harness spawned `gcloud` synchronously inside `credentials:get`, so on a resumed bank the first timed recall paid for the spawn and blocked the event loop while the embed budget timer ran | Keeping the lazy spawn and subtracting its time afterwards |
| 2026-09-24 | Persist failures as allowlisted `{name, code, status, cause}` only; unknown values become `other`; string codes only | Provider errors can echo request content and headers carry keys. `DOMException` has a legacy numeric `code`, which is dropped | Hashing messages; a regex allowlist for codes |
| 2026-09-24 | Capture the real `Date` at trace-module import | `withCorpusClock` replaces `globalThis.Date` during ingestion and answering; `Date.now()` there is the corpus date. A mutant reading `Date` at call time turns the wall-clock test red | `performance.timeOrigin + now()` (drifts across host sleep, which is one of the things being detected) |
| 2026-09-24 | Implement inline rather than one subagent per plan task | One ~900-line harness already in the orchestrator's context; splitting it would cost more context than it saves | subagent-driven-development per task |
