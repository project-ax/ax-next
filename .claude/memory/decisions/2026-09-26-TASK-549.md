| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-549: raised the docker-preflight test's DEFAULT probe budget (`startConsumer`) from 3_000 to 20_000; the two `hang-*` tests keep their explicit 1_000. Production `DOCKER_PREFLIGHT_TIMEOUT_MS` (45s) untouched. | The 3000ms was a test-owned wall-clock deadline that `testTimeout` cannot reach. 20s stays under the 30s testTimeout, so a real hang still fails with the helper's named message. It is also ~2.2x the worst measured CI subprocess stall in the package (9087ms, TASK-537). Covers every non-timeout test, not just the two that flaked: the healthy/invalid-count tests race the same 3s against two shell spawns. | Budget only the overflow tests (leaves the same race in the other 12 per shell); replace the stub's `exec node` with a shell writer (removes the node startup but not the shell spawn, and still a wall clock); 45s (above testTimeout, so a hang turns into a generic vitest timeout). |
| 2026-09-26 | TASK-549: committed a regression test that preloads a 3500ms `Atomics.wait` into ONLY the stub's node child via NODE_OPTIONS and asserts the failed-probe branch. Mutating the budget back to 3_000 reddens it with the exact CI message. | Bug Fix Policy. Adds ~3.5s per available shell (CI has bash only). | No regression test (policy violation); a longer stall (slower suite, no stronger proof). |
| 2026-09-26 | TASK-549: implemented inline, not via subagents: one test file plus one comment. | A subagent round-trip costs more than the change. | subagent-driven per yolo-ship default. |
