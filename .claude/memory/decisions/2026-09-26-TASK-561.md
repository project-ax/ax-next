# Decisions — TASK-561 (team displayName bidi/control characters)

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | `@ax/teams` `validateDisplayName` REJECTS any `REWRITES_THE_SURFACE` character (imported from `@ax/core/surface-text`) with `invalid-payload`, same message as `@ax/agents`. | Mirrors TASK-558: reject, not strip, so nobody saves a name they did not type. The one canonical class (TASK-562); `@ax/teams` already depends on `@ax/core`, so no cross-plugin import. The admin route already maps `invalid-payload` to 400 with the message. | Import `DISPLAY_NAME_FORBIDDEN` from `@ax/agents` (rejected: cross-plugin import, invariant 2). Strip at write (rejected: silent mutation). |
| 2026-09-26 | Older rows fenced on READ in `rowToTeam` via `fenceStoredDisplayName` (each run becomes one space, collapse `\s+`, trim, empty becomes `'Untitled team'`). No migration. | `rowToTeam` is the single funnel for `getById`, `listForUser` and `create`'s returning row. Same shape as `rowToAgent`. The fallback passes the write door. | Migration (rejected: irreversible; the card says no migration). |
| 2026-09-26 | `fenceStoredDisplayName` is a small local function, not moved into `@ax/core/surface-text`. | It is three lines on top of the shared `replaceSurfaceRewriters` and carries a plugin-specific fallback. The CLASS is shared, which is what invariant 4 and the single-owner guard care about. Two such wrappers now exist (agents, teams); a third would earn a shared `fenceLabel(value, fallback)`. | Add `fenceLabel` to core now (deferred: two callers). |
| 2026-09-26 | Implemented inline, no per-task subagents. | One production file (~40 lines) plus tests in one package. | subagent-driven-development. |
