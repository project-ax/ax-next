# TASK-459 decisions

| Date | Decision | Rationale | Alternatives considered |
|------|----------|-----------|------------------------|
| 2026-09-22 | Reject C0/DEL/C1 controls on fact writes, except TAB/LF in value; cap about/relation/value/slot at 1024/1024/8192/256 UTF-16 units. | Bounds are explicit design limits, not measured corpus maxima. Keep labels/free-text relations generous, allow larger multi-line values, retain Unicode/spacing/punctuation, and never silently normalize or truncate model output. | Unbounded text; a restrictive slug allow-list; silent sanitization. |
| 2026-09-22 | Apply the slot rule to record and reindex before embedding/store work, using private parity copies held by the shared contract. | Reindex is a second slot writer. Runtime imports from the vitest-bearing contract package would violate the existing plugin boundary; the established slots-filter validator already uses parity copies. | A record-only fix; importing runtime validation from the test contract. |
| 2026-09-22 | Do not migrate or sanitize existing rows; newly supplied and replayed write payloads must validate. | Existing rows remain readable and normal explicit operations remain available. Preserve SQLite's legacy supersede NUL-key regression using fixture-only updates and reopen coverage. New NUL reindex slots are now rejected atomically; do not claim its old successful-write scenario remains reachable. | Bulk cleanup; weakening validation to keep an invalid fixture working. |
| 2026-09-22 | Use inert-store real-handler tests for local failing-first proof and leave real backend/legacy tests to CI while TASK-497 measures. | Both registered handlers must reject invalid text before touching a store. The inert-store tests prove that boundary, not persistence; real shared contract cases cover persisted round trips and atomicity. | Adding local Docker/database load to the peer benchmark; mistaking mock-store admission for a storage result. |
