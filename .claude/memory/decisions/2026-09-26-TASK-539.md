| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-539: fix the slow-create focus drop by making AgentView's "Loading…" pane a focusable, id-keyed landing spot (`data-agent-loading=<id>`, second target after the conversation), with AgentView handing focus on to the conversation region when the read lands — only if the keyboard is still unclaimed. | Unbounded for the agent GET (the pane is on screen as soon as the route moves), where a longer window only moves the cliff. The hand-off is required: the pane is replaced, and a removed focused node drops to `<body>`. Measured: removing the hand-off also fails the existing TASK-533 fast-path test, because the pane paints before even an instant read resolves — so most creates now land on the pane first. | Lengthen the window for this path only (bounded, still fails beyond it). Hold the restore open past a pane focus (would need focus-when-ready to know about two-step targets). |
| 2026-09-26 | TASK-539: implemented inline rather than via per-task subagents. | ~3 small files, one coherent change; subagent dispatch would cost more context than it saves. | subagent-driven per task. |
| 2026-09-26 | TASK-539: a slow kickoff SEND (>2s, before the route moves) still leaves focus on `<body>` — out of scope, returned as a follow-up. | The card's acceptance names the agent GET; nothing keyed by the new id is on screen during the send, so only a window change could cover it. | Also lengthen the window here (card said pick one). |
