| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | TASK-543: the post-Undo focus drift comes from a REMOUNT, not a focus restore. After approve, `keepAnsweredApprovals` keeps the receipt ABOVE the continuation. After Undo the row is open again, so the server's next read carries its pointer again, appended AFTER every turn (`routes-workspace.ts` `approvalMessages`). The position-bearing key (`findFieldKey(i, id)`) changes, the card remounts, and `ThreadApproval`'s layout cleanup hands focus to the "Conversation with ..." region. The brief's INFERRED suspect (a `focusFirstWhenReady` restore) was wrong. | Reproduced in jsdom: the new test found a different `approval-question` node after the re-read. | - |
| 2026-09-25 | Fix: `keepAnsweredApprovals` holds EVERY pointer the previous read had in its previous slot (using the fresh copy), whether the fresh read dropped it or still carries it. Only pointers new to the read take the server's tail position. | Also fixes the no-Undo case: a new turn landing above an open card's tail pointer remounted it too. Keeps the card where the reader saw it. | (a) Key approvals by decision id instead of position. Rejected: React would then MOVE the node (insertBefore), and moving a focused element blurs it to `<body>` in browsers. It also breaks the find index's position-uniqueness argument. (b) Change the server to interleave pointers by time. Bigger, changes wire order, not needed. |
| 2026-09-25 | Implemented inline instead of per-task subagents. | About 40 lines in one function plus tests; subagent overhead exceeded the work. | subagent-driven per yolo-ship Phase 3. |
| 2026-09-25 | Corrected the stale "the thread only grows at the end" claims in `thread-find.ts` and `AgentConversation.tsx` (contract rule 5). | That claim is what made a position key look free. The server appends approval pointers after turns, so turns land above them. | Leave the comments, which would re-seed the same bug. |
