| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-559: refuted the card's inferred cause. #725 (`fireChatEvent`) is not on this path: 4b's `chat:turn-error` is channel-web's own unbounded `bus.fire`, and the orchestrator is not booted. #729 only allocates an AbortController on the unbounded path (no extra hop). The latent cause since #656: the spy queues behind `@ax/conversations`' Postgres persist subscriber. No production ordering change, so the fix stays in the test. | Measured by probe: spy at +24..52ms idle; an 80ms stall ahead of the spy reproduced CI's `expected [] to deeply equal [...]`. | Bound or reorder the production fire (nothing is wrong with it). |
| 2026-09-26 | TASK-559: `vi.waitFor` (30s, under the 60s testTimeout) instead of the 50ms sleep, plus a committed 4b' with a 250ms stall ahead of the spy. Left the NEGATIVE sleeps (4c "fires no turn-error", "no agent:invoke" after a 4xx) alone, and the positive `chatRunCaptures` checks too. | A negative sleep can only false-pass, never flake red. `chatRunCaptures` is pushed synchronously inside `bus.call('agent:invoke')` (the handler runs before the call's first await), which happens before `res.status(202)`, so those checks do not race anything. | Convert every sleep in the file (churn with no flake behind it); a deferred promise (a bare timeout on failure instead of the assertion diff). |
| 2026-09-26 | TASK-559: implemented inline, not via subagents. | One test file. A subagent round-trip costs more than the change. | subagent-driven per yolo-ship default. |
