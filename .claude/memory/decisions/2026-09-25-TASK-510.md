# decisions — TASK-510 (2026-09-25)

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | New-agent dialog close restores by identity via `focusFirstWhenReady` (`lib/new-agent-return-focus.ts`: `[data-new-agent-opener]` then `[data-nav-trigger]`), fired from an `App.tsx` effect on `createAgentOpen` true→false. | Card's INFERRED cause confirmed: the bootstrap gate replaces the whole tree, so the row is detached and `use-opener-restore` bails. One effect covers Escape, ✕ and a finished create; first run never sets `createAgentOpen`, so it arms nothing. Reuses TASK-474's mechanism rather than a second one. | Arming a ref in each close callback (two sites, easy to miss one); rendering the dialog as an overlay over the live workspace (bigger change, also changes the create flow's loading screen). |
| 2026-09-25 | Settings entry focuses its `h1` (tabIndex -1, outline-none) from a mount-only `useLayoutEffect` in `AdminShell`. | Heading announces where you are and is the one title on every tab (TASK-446). Layout effect per TASK-451 — no post-commit window. Mount-only so tab switches keep focus on the chosen nav item. Covers the chat shell's Settings too (same `AdminShell`). | First focusable (the back button) — announces the way out, not the place; passive `useEffect` — the TASK-451/478 race shape. |
| 2026-09-25 | Did not mark the chat shell's AgentChip "New agent" entry. | Card scope is the workspace; the chat shell is being retired (workspace-as-sole-interface). In chat mode the restore finds no target and the 2s window expires harmlessly (focus stays on `<body>`, as before). | Mark the AgentChip trigger too — cheap, but out of scope for a surface being retired. |
| 2026-09-25 | Implemented inline instead of per-task subagents. | Three small, tightly coupled edits (~60 prod lines) plus two test files; dispatch overhead would exceed the work. | subagent-driven-development per task. |
