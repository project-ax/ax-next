# Decisions — TASK-480 (fence the learned-memory doc name)

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | Fence the learned-doc `name` in `routes-workspace.ts` `readMemory` with `fenceLine(d.name, FILE_LABEL_MAX_CHARS) ?? LEARNED_DOC_FALLBACK_NAME` ('Untitled note'). | Card premise was partly WRONG: the name is not model output. `@ax/memory-strata` `readLearnedDocs` hardcodes three names ("What it knows about you", ...). The fence is still right: the name crosses a plugin boundary over the bus, and the hook contract does not promise fixed names, so an alternate impl could send anything. It is a no-op for today's names (all short, no bidi), so it costs nothing. | Skip it as not-model-authored (rejected: the boundary is the right place to fence, per fence-line.ts). Drop docs whose name fences to null (rejected: hides a doc whose body is real). |
| 2026-09-26 | Owner-authored `displayName` on the rail stays unfenced in this PR. | It is already length-validated (1-128) at the `@ax/agents` store write. What it lacks is a bidi strip, and that belongs at the single write site, not in one of several readers (invariant 4). Filed as a follow-up. Note: since TASK-257 the rail can be seen by other users authorized on the agent, so this is a cross-user surface. It is not only the owner's own view. | Fence it here (rejected: only one reader gets fixed, and it is out of scope per the card). |
| 2026-09-26 | Did not collapse `FILE_LABEL_MAX_CHARS` and `CONVERSATION_TITLE_MAX_CHARS`. | Card acceptance, and #640's reasoning about masked prefixes. | — |
| 2026-09-26 | Implemented inline, no per-task subagents. | Two-line production change plus one test. Subagent overhead would cost more than the work. | subagent-driven-development. |
| 2026-09-26 | Did not guard against fenced-name collisions (React `key={d.name}` in AgentMemory.tsx). | Today's three names are distinct and well under the cap. A collision needs an alternate impl that emits two names sharing a 119-code-point prefix, or two names that are all invisibles. Recorded as a known limit. | Key by index in AgentMemory (deferred: UI change outside scope). |
