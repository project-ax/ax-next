| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-556 (1): the CLI identity read (`readAxFile`) treats only ENOENT/ENOTDIR as absent; any other lstat/readFile error throws `identity read failed for .ax/<name>` (cause kept), so the CLI chat:start seeds nothing that turn and warns `memory_strata_bootstrap_failed` — the TASK-553 semantics. Tested with a wrapped `node:fs/promises` (vi.mock), not chmod fixtures. | chmod-000 fixtures are a no-op when tests run as root and flaky across platforms; a pass-through wrapper injects the exact errno deterministically. | Permission fixtures; leave CLI as-is (card rejected that). |
| 2026-09-26 | TASK-556: BOTH paths now read identity only when `system/agent.md` is absent or still the exact placeholder (`needsIdentity`). The CLI path used to compose on every turn. | With (1), a persistent EACCES on IDENTITY.md would otherwise fail every chat:start of a warm agent — including seeding a missing user/session/map file — for an agent.md bootstrap never rewrites anyway. Also saves two lstats per turn. | Keep composing every turn (regression for warm agents under a stuck file). |
| 2026-09-26 | TASK-556 (2): repair lives in `bootstrapMemoryTree`: on agent.md EEXIST with a non-empty composed identity, if the file is a REGULAR file whose frontmatter-stripped body is exactly `# Agent\n\n<placeholder>`, it is replaced via temp file + rename with a fresh seed (fresh frontmatter). Returns `repaired: string[]`. | One place serves the tier and CLI paths. Exact-body match means any agent edit makes the file the agent's. lstat + rename: a symlinked agent.md is never followed for the read decision nor written through. Fresh frontmatter = "what it would have been if seeded now"; the placeholder's `created` carries no information worth keeping. | Separate repair function in plugin.ts per path; preserve frontmatter by YAML-parsing (more code, no user value); in-place writeFile (follows symlinks). |
| 2026-09-26 | TASK-556 (3): the dispose-on-throw lives INSIDE `hydrateAgentTier` (try/catch around a new `populateScratch`), not in plugin.ts `handleChatStart` as the card suggested. | The leak is before the function returns, so no caller can fix it; fixing it at the source covers all three callers (chat:start seed, observer, consolidator). | A try in each caller (impossible: they never receive `dispose`). |
| 2026-09-26 | TASK-556: implemented inline, not via subagents: three small source edits and their tests in one package. | Subagent round-trips cost more than the change (same call as TASK-553). | subagent-driven per yolo-ship default. |
| 2026-09-26 | TASK-556 review: took Minor #1 (strengthened the "poisoned, no identity" tier test to assert the identity gate opened, so it fails against the unfixed gate). Declined Nit #2 (`readRegularFile` throws on ENOTDIR where `readAxFile` treats it as absent) and Nit #3 (real→real identity edits still never refresh agent.md). | #2 only fires on a corrupt memory tree (a path component under `permanent/memory/system/` that is a file) and fails closed: warn + retry, no placeholder. Not worth an unreviewed production edit. #3 is pre-existing create-only behaviour, out of scope; inject reads `.ax/` live, so only the cold-start copy lags. | Fix #2 in this PR (needs another review round for a corrupt-tree edge). |
