| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-544: DecisionRow wraps BOTH StateDot call sites (question + receipt) in a local `DotSlot` (`flex w-2 shrink-0 justify-center`), the sidebar roster's slot class verbatim. | Receipts jitter too (TONE_DOT → working 7px / resting 8px / stopped 7px), not only questions. Local wrapper, not a change to StateDot, so STATE_SHAPE stays the only geometry and parseStateDotArms sees no new arm. | Put the slot inside StateDot (changes every call site incl. sidebar, which already slots); fix widths in STATE_SHAPE (would distort the shapes). |
| 2026-09-26 | TASK-544: sr-only `, waiting on you` (stateWord('held').toLowerCase()) after the agent name on NON-stale questions only. | Card item 2 names the non-stale held question. A stale row's visible summary ("Needs another look — things changed…") already states it, and "Stopped" (its dot's state word) would misdescribe it. Receipts' outcome line is the state. | Word on every branch (redundant / wrong word on stale). |
| 2026-09-26 | TASK-544: left the receipt note `pl-[22px]` and expanded body `pl-[46px]` offsets alone. | Out of the card's acceptance (which is about the dot-adjacent text); neither was pixel-aligned before either. | Re-align to the new 20px/40px offsets — cosmetic, unasked. |
| 2026-09-26 | TASK-544: implemented inline rather than via per-task subagents. | ~30-line diff in two files; subagent dispatch overhead exceeds the work. | subagent-driven-development per task. |
| 2026-09-26 | TASK-544 review (APPROVE, 3 minor/nit): deferred #1 (extract a shared `StateDotSlot` into bits.tsx for sidebar + DecisionRow) as a follow-up card; not applied #3 (derive one `dotState` for dot + word) — correct as written, and a post-review production edit would force another ~10-min review round for a robustness-only nit; kept #2's stale-row test as a regression guard against the "word on every branch" alternative, knowing it passes on unfixed code. | Scope + review-loop cost; all three are non-blocking. | Apply #1/#3 now and re-review. |
