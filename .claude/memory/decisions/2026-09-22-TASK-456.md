# TASK-456 decisions

| Date | Decision | Rationale | Alternatives considered |
|------|----------|-----------|------------------------|
| 2026-09-22 | Verify documented standalone Bash pollers by matching ancestry roots from one bounded process snapshot, not raw pgrep rows. | A live poller had parent86056 rather than1; a separately named bounded probe produced three argv matches from one launched root49719 and descendants49721/49722. Roots distinguish one launch with substitutions from two independent launches without stopping the live watcher. | PID1-only filtering misses tool-owned launches; a single PID file misses untracked duplicates; a time intersection can retain slow substitutions. |
| 2026-09-22 | Treat failed or ambiguous process inspection as unknown, with no root output; do not automate cleanup. | Process metadata is a point-in-time observation, not a race-free ownership handle. Fixed ps argv, bounded capture and numeric PID output keep metadata out of executable text and diagnostics. Unsupported flagged/ambiguous launch forms must not read as zero pollers. | Guessing zero from an empty/error response; widening matching until wrapper text becomes a poller. |
| 2026-09-22 | Correct the universal leak claim without retroactively declaring the historical report false. | The August24 log lacks parentage sufficient to reconstruct two independent roots. The watcher itself exits on board change, while a manual/agent-completion wake can leave it alive. The frozen archive in .claude/memory/mistakes.md remains historical; this row supersedes its raw-pgrep count advice. | Editing the frozen archive; restating every-pass leakage as measured fact. |
| 2026-09-22 | The guard executes both the previous inline verifier and the new marked snippet against fixed local stubs. | Restoring the old doc must execute its real pgrep check and expose the count defect, not merely lose a new marker. Preserve the existing reap instructions; no live poller is killed by this task. | Grepping prose; using extractor disappearance as the only regression signal. |
| 2026-09-22 | Support the canonical helper-checkout path with spaces; report other ambiguous spellings as unknown without resolving process-supplied paths. | Node canonicalizes module URLs on macOS while the initial temporary fixture used a logical path alias. Canonicalize only the test-owned fixture directory and pin an ambiguous-other-checkout refusal; production remains a process-only inspection. | Adding filesystem resolution of arbitrary process arguments; silently reporting zero for an unrecognized path. |
| 2026-09-22 | Reject flagged bare-filename launches and nonzero parentage on a PID-zero row before reporting roots. | Independent review found both shapes could return a successful count under b06900a3. Recognize filename tokens after whitespace and validate the PID-zero sentinel; retain valid kernel0-to-0 rows. Harness sanity cases are labeled separately from behavioral regressions. | Silently ignoring an unsupported bare invocation; trusting a sentinel before checking its supplied parent. |
