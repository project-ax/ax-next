# TASK-540 decisions

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | `ConsentAnnouncement` now mounts TWO persistent `sr-only` regions: `data-consent-said="receipt"` (`role="alert"` + `aria-live="assertive"`) and `data-consent-said="open"` (`role="status"` + `aria-live="polite"`). A change to the sentence is routed by the branch the card is in (`outcome !== null` → receipt) and the other region is emptied in the same commit. | TASK-275 ruling: a card changing because of the agent is polite, no focus move. The assertive rationale is the 10s undo window, which only a resolved card has. Two stable nodes keep TASK-442's mounted-empty rule for both voices; flipping `role`/`aria-live` on one node in the same commit that fills it is timing AT handles inconsistently. | One node with a politeness attribute that flips per branch (fragile); keep assertive (the TASK-535 deferral this card resolves). |
| 2026-09-26 | Every open-branch notice — including one set right after the person's own "Move it" press on ApprovalCard — goes polite. A notice on a RESOLVED row (refused undo) stays assertive. | The card says "open-branch announcements (stale, notice) go polite; resolved receipts keep their current politeness". The resolved-row notice answers a press inside the undo window. | Split open-branch notices by whether a click caused them (needs arming state the shared component does not have). |
| 2026-09-26 | Same sentence staying put while the branch flips is not re-announced and not moved between regions. | Only a CHANGE to what the card says is news (TASK-442); moving it would re-read it. | Re-route on branch flip too. |
| 2026-09-26 | Implemented inline (no per-task subagent), no separate plan file. | 1 component + its test file + two test helpers that counted live regions; smaller than a subagent brief. | Subagent-driven per yolo-ship default. |
