# TASK-500 — decisions

| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-26 | Refuted the card's premise: the UserMenu Theme control was not "reachable but arrowless", it was **keyboard-unreachable**. | MEASURED-BY-PROBE on the unfixed component: keyboard-open the menu, press ArrowDown ×5 → focus went `Settings → Sign out` and stayed there; the three theme buttons were never focused. Radix `MenuContent` `preventDefault`s Tab for any keydown inside `[data-radix-menu-content]` (`@radix-ui/react-menu@2.1.16` `dist/index.js` ~line 372) and its roving focus walks only `MenuItem`s. | Take the card literally and add arrow keys inside the group — pointless, focus can never get into it. |
| 2026-09-26 | Did NOT reuse #649's `Segmented` (ToggleGroup) pattern the card asked for; used shadcn `DropdownMenuRadioGroup` + `DropdownMenuRadioItem` instead. | A ToggleGroup inside a menu is still outside the menu's roving set and still behind the Tab block, so it would be unreachable too. `menuitemradio` items join the menu's own roving focus (arrow keys, Enter/Space, typeahead, Home/End for free) and are the WAI-ARIA-valid children of `role="menu"` — a `radiogroup` of `radio`s is not. Already installed; no new primitive. | Segmented/ToggleGroup (unreachable); hand-rolled key handler that pulls focus into the row (fights Radix's roving focus). |
| 2026-09-26 | Stacked the three options vertically instead of keeping the horizontal segmented row. | A menu's arrow keys are Up/Down; a horizontal row reached with ArrowDown teaches the wrong key, and Left/Right do nothing in a vertical root menu. Labels stay visible (C5). Costs ~2 rows of menu height. | Keep the row with menuitemradio items (works, but visually lies about the keys). |
| 2026-09-26 | `onSelect={e => e.preventDefault()}` on each radio item so choosing a theme keeps the menu open. | Preserves the old behaviour (the buttons never closed the menu) and makes try-and-compare possible. Radix composes `onValueChange` with `checkForDefaultPrevented: false`, so the selection still happens — asserted by the Enter test. | Let the menu close on select (the Radix default). |
