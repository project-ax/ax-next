| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | **TASK-542: the workspace thread attaches to the post-approval continuation through chat's own seam, `lib/continuation-actions.ts`, with the "is this continuation ours" guard MOVED INTO the seam (`continueApprovedTurn`).** Both decision queues pass it as `onDecisionApproved`; each thread registers `registerResume(resume, openConversation)` (now returns a disposer). Chat's runtime registers `chat.resumeStream` + `getActiveConversationId`; `AgentView` registers "take the staged id → `streamFrom(id)`" + `() => conversationRef.current`. | Invariant 4 — one rule for WHEN to attach (non-null `streamReqId`, decision's conversation == the thread open at SETTLE time), two renderers for HOW. The seam file was never chat-only in substance and is not on the TASK-360 delete list or the workspace eslint fence, so the workspace can import it. `streamFrom` with no `sent` bubble: the person answered a card, they did not send a message. | Copy the guard into `WorkspaceShell` (rejected: second copy of the rule). Have the shell hand AgentView a "stream this" prop like `pendingReply` (rejected: the shell does not know which conversation AgentView has open; the guard needs it at settle time). Keep the guard in chat's `conversation-decisions.ts` (rejected: chat-only, deleted by TASK-360). |
| 2026-09-26 | `continueApprovedTurn` is QUIET when no thread is registered; `resumeContinuation` keeps its warn. | Approving from Today (no `AgentView` mounted) is ordinary in the workspace; a console warn per approval would be noise. The turn still renders on the next read. | Warn on every Today approval (rejected: noise, and no operator action follows). |
| 2026-09-26 | Implemented inline rather than one subagent per task. | One cohesive ~6-file diff; splitting it would cost more context than it saves. | Subagent-driven per task (skill default). |
