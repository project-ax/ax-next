| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | ActivityFeed re-announces a repeated identical loadMore failure by CLEARING its polite region while a read is in flight (`loading`) and refilling it when that read fails. | `useActivityFeed` keeps `error` set through a retry and sets the same string again, so "render on error !== null" left the region's text unchanged, and a live region whose text does not change announces nothing. Clear-and-refill gives two real commits a network round trip apart. The empty window is also the honest one: while the retry runs, "could not load" is not yet known. | Attempt marker (counter or key remount): re-inserting the same text in one commit is the create-and-fill shape screen readers skip, and a visible "attempt 3" is noise for everyone else. Clearing `error` in the hook: that changes every consumer, when the fix belongs to the one component that owns the announcement. |
| 2026-09-26 | The page-1 failure goes through the same `data-activity-announcer` region, which is now mounted at a fixed child slot in EVERY state, the placeholder included. The page-1 Alert drops `role="alert"` (`role="none"`). | The first page's only prior state is the placeholder, so the region has to exist under the placeholder for "mounted empty before the failure" to hold. Spacing lives on the message, not the region, so an empty region takes no room. | A second region only for page 1: two regions for one feed's failures, and the placeholder state would still need to own one. |
| 2026-09-26 | NOT covered: a feed MOUNTED already failed (the reader opens Activity or "What it did" after the shared hook failed elsewhere). The region is born full, and the failure is page content the reader lands on. | This matches the placeholder's own rationale: navigation is already announced, and the reader reads the page. Arming the region only after mount would cost a one-frame blank on every born-failed mount. Returned as a follow-up rather than silently dropped. | useEffect-armed region (empty on the first commit, filled after mount): adds a visible one-frame flash. |
| 2026-09-26 | Implemented inline instead of per-task subagents. | One component plus its two test files. Dispatch overhead would have exceeded the work. | Subagent per plan task. |
