| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-513: the card's "measure first, maybe close with no code change" branch was NOT taken, because the measurement says the cost is material. MEASURED (in-process git-server + production client + real chat:start tier path, loopback, median of 5): ~80 ms per `workspace:read`, linear in memory size. Warm: 4 files 0.57 s, 29 files 2.5 s, 104 files 8.6 s, 304 files 25.2 s of chat:start blocking time. | The card's "bounded and small in the steady state" was INFERRED from bootstrap writing nothing after turn 1. The cost is in the reads, which cover the whole `memory/**` subtree every turn. Loopback is a floor for the cluster. | Close the card on the write-side argument (refuted by the numbers). |
| 2026-09-26 | TASK-513: fix = chat:start hydrates only the 4 bootstrap seed files (new `only` option on `hydrateAgentTier`, seed list exported from bootstrap.ts), with the identity read only when `system/agent.md` is missing; every read of one hydrate is pinned to the first read's version; workspace-git-server skips its `git fetch` for a pinned version already in the local mirror. | Bootstrap reads nothing but its own seed files, so reading the whole tree is pure waste (O(N) → O(1)). A partial hydrate is safe for `flushAgentTier` because deletes are baseline-minus-scratch and bootstrap only creates. Pinning also closes a torn-read window (each unpinned read could land on a different head). Skipping the fetch is safe because a commit oid names immutable content; a miss falls back to the old fetch. No hook payload changes: `version` is already on the neutral read/list surface. | Move bootstrap off the blocking path (fire-and-forget): rejected, chat:start must land the seed before sandbox:open-session materializes `/agent` on turn 1. A new batched `workspace:read-many` hook: rejected, new hook surface + boundary review for a problem the existing `version` field already solves. Parallelise the reads: rejected, the engine's per-workspace queue serialises them anyway. |
| 2026-09-26 | TASK-513: measured with an ad-hoc probe, not the `hook_call_stalled` logging the card pointed at. | That logging fires only past a stall threshold; a cost curve needs per-call timing at every size. The probe is not committed; its numbers are in the plan doc. | Lower stallWarnMs and read logs (a threshold crossing, not a curve). |
