# TASK-515 decisions

| Date | Decision | Rationale | Alternatives considered |
|------|----------|-----------|------------------------|
| 2026-09-26 | Option (1): `memory:recall` throws `PluginError({code:'invalid-return', plugin:'@ax/memory', hookName:'memory:recall'})` when any `degraded` element is not a string. Type-only check; the flag vocabulary stays open. Static message, element never interpolated. `undefined` → `[]` absence unchanged. | Matches the row guard in `toMemoryStatement` and the existing non-array `degraded` guard (same error shape, same "a signal we cannot read is not one we may hand a caller" reasoning). Throw, not drop (a dropped element is a silently discarded degradation signal) and not coerce (`String(null)` is a flag the engine never raised). | (2) document the array-level stop — leaves `MemoryRecallOutput.degraded: string[]` untrue on a contract violation; drop non-strings; coerce with `String()`. |
| 2026-09-26 | Did NOT reuse TASK-459's text validator (the dispatch hint). | It validates WRITE-door fact text (controls, length caps) and lives in `@ax/memory-facts-contract`, which TASK-459's own decision says must not be a runtime import. An engine-RETURNED diagnostic flag needs a type check, and the local shape to copy is the recall-side `invalid-return` guard. | Importing the contract validator; a second text validator. |
| 2026-09-26 | Left the system-prompt augment's `String()` coercion of `degraded` elements as-is; corrected its comment (and the augment test comment) that attributed the gap to the `memory:recall` passthrough. | The augment calls `facts:recall` directly, never `memory:recall`, so this guard cannot cover it. Whether the augment should also throw is a separate policy call (it would fail the injected block over a junk flag), returned as a follow-up rather than widened into this card. | Making the augment throw too, in this PR. |
| 2026-09-26 | Implemented inline rather than via a per-task subagent. | ~20-line single-guard change with its test; dispatch overhead exceeded the work. Review still goes to ax-code-reviewer. | Subagent-driven per task. |
