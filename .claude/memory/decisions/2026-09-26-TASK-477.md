| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-477: made laneless (no-`Status`) cards visible with option 2 — a report-only `board_laneless <snapshot>` sweep in the §2b board helper, run every loop pass on the pass's `board_snapshot` (§3a), plus a `⚠ CREATED but NOT routed` line on a failed claim→`board_batch` route (§4). rc contract: 0 clean (prints `laneless: 0 of N items`), 1 hits, 2 could-not-sweep. | Catches laneless cards from ANY source (claim race, human add, failed route), not just `claim`. Fails LOUD: re-asserts non-truncation/non-empty on the file itself, requires an explicit path (no stale-cache default), and the quiet verdict always names the count read. No auto-routing: a card this run created gets its missed route finished; any other is reported every pass until a human routes it. | Option 1 (`claim` warns) and option 3 (`claim --lane`) both edit `scripts/board-task-id.sh`, which sibling TASK-472 was editing, and both only cover the `claim` path. Auto-routing to To Do rejected: a card nobody put there appearing in the actionable lane is its own surprise. |
