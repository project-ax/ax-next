| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-462: both timeout guards (`out-of-process-test-timeouts`, `scripts-suite-timeouts`) now PARSE hooks with the TypeScript compiler API via one shared helper, `scripts/hook-timeout-scan.mjs`, instead of the `HOOK_WITH_TIMEOUT` / `UNREADABLE_HOOK_TIMEOUT` / `NAMED_NUMERIC_CONST` regexes. | Brace structure is not a regular language; the lazy regex stopped at any inner `}, x);` (nested hook OR a plain multi-line `setTimeout`) and under-read — fail-OPEN. An AST knows where each hook call ends, and also closes the regex's documented single-line / brace-less-arrow gaps and the `it`-budget misattribution. `typescript` is already a root devDependency (no new package). One helper = one source of truth for both guards, which had copy-pasted regexes. | Make the regex fail closed when a hook body contains a nested `}, x);` (still regex-matching braces; nested templates/strings defeat it). A hand-rolled brace counter (strings/templates/regex literals/comments make it a tokenizer anyway). Fix only the out-of-process guard (the sibling had the same defect). |
| 2026-09-26 | TASK-462: a source with parse diagnostics is reported as unreadable (whole file), and a missing `SourceFile.parseDiagnostics` throws. | A syntax error can hide hooks from any scanner, so trusting the hooks that were seen would under-read. `parseDiagnostics` is internal API; if a TS upgrade removes it, `undefined` must not read as "clean". MEASURED: 0 of 1947 package sources have parse diagnostics today. | Ignore parse errors (fail-open). Use a full `ts.Program` for diagnostics (much slower, needs tsconfig). |
| 2026-09-26 | TASK-462: accepted that 6 in-scope package maxima DROP (to 0) under the parser; changed no config. | Each was an `it(..., N)` budget the regex had run past a bare hook to reach (verified per file). An `it` budget governs that test, not a hook, so it was never what the `hookTimeout >= max` invariant is about. The old "folds a MISATTRIBUTED it budget" test said in its own comment that a correct anchoring would drop it 90_000 -> 0 and it should then be rewritten; it was, with a timed hook after the `it` so a scanner that reads nothing cannot pass. | Keep folding `it` budgets in for conservatism (would need a regex-shaped rule inside the parser, and inflates maxima for no hook). |
| 2026-09-26 | TASK-462: implemented inline, not via per-task subagents. | One helper + two test files, tightly coupled judgement about failure directions; dispatch overhead exceeds the savings. | subagent-driven per task. |
