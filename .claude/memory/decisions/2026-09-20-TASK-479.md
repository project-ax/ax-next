# TASK-479 — the Q2 review gate length-checks HEAD_SHA but not reviewed-sha

| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-20 | RESOLVE the handoff's `reviewed-sha` with `git rev-parse --verify --quiet "<raw>^{commit}"` rather than merely length-checking it. | A length check alone would reject the abbreviations that actually arrive (three of them in one run) and then… what? The orchestrator would still have to resolve them by hand, which is what it was already doing. Asking git to expand is safe; the "never RETYPE a sha" rule is about *reconstructing* one, and `rev-parse` is how you obey it — it either prints 40 characters or it fails. | (a) `[ ${#REVIEWED_SHA} -eq 40 ]` and HALT, symmetric with `HEAD_SHA` — REJECTED: `HEAD_SHA` comes from `headRefOid` and is machine-produced, so a short one is a caller bug; `reviewed-sha` is typed by an agent, so short is the *normal* input and halting on it stalls the queue on a formatting slip. (b) Silently `git rev-parse` with no verification — REJECTED, bare `rev-parse` echoes back any 40-hex-looking string and exits 0 (measured, git 2.52.0). |
| 2026-09-20 | Keep the variable named `REVIEWED_SHA` and introduce `RAW_REVIEWED` for the pre-resolution value. | `autoship-review-gate-reviewed-sha.test.js`'s `RANGE_FROM_REVIEWED` regex pins that the range's left endpoint is spelled from the reviewed sha (`${REVIEWED_SHA}..`). Renaming the resolved variable to `BASE` would have broken a sibling guard that is protecting a real property. | Rename + update the sibling guard — REJECTED, it would weaken a guard to accommodate a cosmetic choice. |
| 2026-09-20 | Ancestry-check the resolved sha against `origin/<branch>` and, when it is not an ancestor, **fail closed to `origin/main`** — warn loudly, widen the delta, do not halt. | This is the only state where the gate can report a *reassuring* wrong answer: a base off the branch can produce an EMPTY delta, and an empty delta is this gate's "a reviewer saw the head". But the state has two causes and they want the same answer. A corrupted handoff naming another branch deserves the whole branch; and the *honest* cause — the builder was reviewed at that sha and then rebased or force-pushed, which this same skill tells it to do when main moves — also deserves the whole branch, because a rebase can carry conflict resolutions no reviewer saw. Halting would stall a SERIALIZED queue on correct builder behaviour, and this gate has already ruled that a non-terminating queue is the worse bug. | (a) HALT — REJECTED per above; an earlier draft of this fix did halt, and the halt fires on the common rebase case. (b) Warn-and-continue from the off-branch base — REJECTED, that is the bug. |
| 2026-09-20 | Fail **closed to origin/main** (not `exit 1`) for missing / `-` / ambiguous / unknown. | Q2's existing contract already says missing means "the whole branch is unreviewed". Extending it to ambiguous/unknown keeps one rule for five states and keeps the queue moving through an independent pass instead of stalling on a typo. Recorded as a mutant (→ 10 red) because "halting is also closed" is the intuitive wrong answer. | `exit 1` for all of them — REJECTED, see above. |
| 2026-09-20 | Guard by EXECUTING the extracted block against a real throwaway git repository, not by scanning the doc. | The pre-fix block said nothing false; it branched on nothing. A scan for "full 40-char sha" passes against prose. The card's own bar — "only real if something fails when the check is removed" — is a behavioural claim, so the guard has to run the shell. Precedent: `autoship-ci-run-lookup-full-sha.test.js` already extracts-and-runs for the same reason. | Extend the sibling text-scan file — REJECTED, different instrument for a different kind of claim; the two now sit side by side. |
| 2026-09-20 | Manufacture the ambiguous-prefix fixture (two real commits sharing 4 hex chars, found by hashing candidate commit bodies in Node, then `git hash-object -w -t commit`). | Ambiguity is one of the two states that makes an abbreviation dangerous, and it cannot be produced on demand by accident. A skipped "ambiguous" case would have been a green that tested nothing. Guarded on `rev-parse --show-object-format == sha1`; on a sha256 repo the test asserts that fact instead of passing silently. | Skip the ambiguous case — REJECTED (vacuity). Assert only on a non-existent sha — REJECTED, it exercises a different git code path. |
| 2026-09-20 | Also make the Q2 block `git fetch origin main "${BRANCH}"`, and HALT if the fetch fails. | Its fail-closed path now ranges from `origin/main`, so a stale `origin/main` would quietly change what "the whole branch is unreviewed" means. More importantly the two stale directions are NOT symmetric: a stale `origin/main` only widens the delta, but a stale `origin/<branch>` is MISSING commits pushed since the last fetch — an under-review hole, the one direction this gate exists to close. | Rely on the merge-queue block's earlier `git fetch origin` — REJECTED, it is a different fenced block an operator may not have run. Fetch without the `||` halt — REJECTED, measured 2 red. |

## Refuted / corrected

- The card body located the `HEAD_SHA` assert in `references/github-project.md`. It is
  not there: the three sites are `auto-ship/SKILL.md` (merge queue), `yolo-ship/SKILL.md`
  Phase 6 and Phase 7. Nothing in the fix depends on this, but the pointer is wrong.
- **The first builder's own mutant table was wrong, and it was wrong in the direction
  that flatters the patch.** It recorded `--verify --quiet "…^{commit}"` → bare
  `rev-parse` as **2 red**, i.e. as a load-bearing line. Re-run on the finished block it
  is **0 red**. The premise held (bare `rev-parse` echoes any 40-hex string back at rc 0,
  measured) but the conclusion did not: the garbage it lets through is rejected one line
  later by `merge-base --is-ancestor`, because a nonexistent object is not an ancestor of
  anything. The two arms overlap and the ancestry arm wins. The flags stay — they buy the
  clean "missing/ambiguous/unknown" message instead of a `merge-base` error on stderr —
  but they are belt-and-braces, exactly like the 40-char assert, and the note now says so.
  The generalisable lesson: a mutant table measured against an INTERMEDIATE state of the
  patch and then carried forward unmodified is evidence of nothing, and it reads as
  evidence of everything. Re-run the table against the head you are shipping.

## Mutation check (the card's "guard, not a sentence" bar)

Re-derived 2026-09-20 by the resuming builder against the shipped head, git 2.52.0,
bash 3.2 + zsh 5.9. Baseline 21 in this file, 28 with the sibling text-scan guard
collected; every mutant still collects 28, so none reddened by shrinking the suite.
Committed clean first; every restore via `git checkout HEAD -- <path>`, tree verified
clean after each.

| Mutant | Red |
| --- | --- |
| Revert the whole Q2 block to its pre-TASK-479 text | 14 (abbrev, `-`, empty, unknown, ambiguous, off-branch, failed-fetch × bash+zsh) |
| Delete only the `merge-base --is-ancestor` arm | 2 (off-branch × 2) |
| Drop the `\|\| { … exit 1; }` guard from the `git fetch` line | 2 (failed-fetch × 2) |
| Fail-closed arm → `exit 1` instead of origin/main | 10 |
| `--verify --quiet "…^{commit}"` → bare `rev-parse` | 0 — see "Refuted", above |
| Same, keeping the `^{commit}` peel | 0 |
| Delete `[ ${#REVIEWED_SHA} -eq 40 ]` | 0 — kept as belt-and-braces, not claimed load-bearing |
| Handoff field line → `reviewed-sha: <sha> \| -` | 1 |
| Builder-prompt bullet → pre-TASK-479 wording | 1 |

## Trap hit while measuring

`git checkout origin/main -- <path>` writes the INDEX as well as the worktree, so the
usual restore — `git checkout -- <path>` — restored the *mutant*, silently, and the next
mutant was applied on top of it. Use `git checkout HEAD -- <path>` for every mutation
restore, and assert `git status --short` is empty between mutants rather than assuming.
