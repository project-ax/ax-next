| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-548: MEASURED both Routines error surfaces (StatusChip `error` arm, RoutinesList banner) at 4.44:1 light on `bg-destructive/10` over AdminShell's `bg-background`; dark 5.71. Swapped both to solid `bg-destructive-soft` (4.57 L / 4.63 D), kept `border-destructive/25`. | Same pairing and same surface (white page) as SessionRow's confirm row (#705); an alpha composites to a different pixel per theme, the solid token clears both. The border is decorative; the chip's word carries the status. | A lower/higher alpha (can't clear both themes); a `dark:` override; drop the fill (loses the error affordance). |
| 2026-09-26 | TASK-548: the banner's ghost Dismiss gets its own row — inherited `text-destructive` on the banner fill at rest, and its `hover:bg-accent`/`hover:text-accent-foreground` pair on hover — via a new `ghostPaint` parser (ghost has no bare ink, so `controlPaint` can't read it). | "Measure against the element's actual painted surface" (TASK-538 learning): at rest the button paints nothing and shows the banner. | Skip the button (unmeasured control inside the fixed surface). |
| 2026-09-26 | TASK-548: added `data-testid="routines-list-error"` AFTER the banner's className so `classNameOf` can find it (its marker search sees className text and the attributes after it, not `role="alert"` before it). Implemented inline rather than via subagent: 3 files, ~150 lines of test. | A marker inside the className would be a styling class masquerading as an id. | Match on a class substring (brittle). |
| 2026-09-26 | TASK-548: left `docs/plans/_2026-05-17-routines-phase-d-mockup.html`'s `bg-destructive/10` chip alone. | Historical mockup, not shipped code; nothing generates cards from it. | Edit the mockup. |
