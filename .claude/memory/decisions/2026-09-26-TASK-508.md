# TASK-508 — decisions

| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-26 | The mutation-restore block restores with `git checkout HEAD -- "$P"`, not the bare form; the staged-mutant fixture flips from "refuses" to "restores, index included". | The bare form restores from the index, so a staged mutant (`git add`, or written with `git checkout <ref> -- <path>`) comes back. The block's post-check already refused that (fail-closed), so the card's "no test covers it" premise was half-refuted: the harm came from the HAND-TYPED bare form that the prose and the dispatch bullet prescribed. HEAD is safe here because the precondition has already established HEAD == what you mutated; a sibling commit mid-window still survives (HEAD moved to it). | Keep the bare form and just document the refusal — leaves every hand-typed restore exposed and makes the block refuse a case it can fix. `git restore --source=HEAD --staged --worktree` — equivalent, but a second spelling for agents to remember. |
| 2026-09-26 | Staged-mutant shape recorded as shape (5), after the clean reviewer (4), in both SKILL.md and the test header. | Keeps the list order and the test header's numbered shapes in agreement; (4) stays "the absence of a failure". | Insert as (4) and renumber — would desync every existing "(4)" reference. |
| 2026-09-26 | Implemented inline rather than via per-task subagents. | Three files, ~100 lines, one tight test-first loop; a subagent per task would cost more context than it saves. | subagent-driven-development per plan task. |
