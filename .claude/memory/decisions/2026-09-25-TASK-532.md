## 2026-09-25 — TASK-532 the live AgentView panel settles an answered hold

- `2026-09-25` (TASK-532) — **The live path CANNOT reuse reload's call-id / tool-name match: the browser never sees `call`.** The orchestrator brief said "reuse that derivation"; the browser `Decision` projection and the `decisionRaised` SSE frame both drop `call` on purpose (model-authored, invariant 5), so there is no call id or tool name to match on client-side. What IS reused is the one producer of `settled` — `settleHolds` — fed a client-built `LiveHolds` from `liveStreamHolds`. *Alternatives:* thread `decisionId` onto the live held `tool-result` frame (REJECTED for this card — the `held` flag originates in both runners + runner-core and crosses IPC; a multi-package wire change for a coarse-vs-exact improvement); link `decisionRaised` → next held result by frame order (REJECTED — `decisions:raised` is fire-and-forget, order not guaranteed); add `call.id` to the browser `Decision` (REJECTED — widens a deliberately minimized wire shape).
- `2026-09-25` (TASK-532) — **Rule: a live hold settles iff the queue read is ok, NO decision in the turn's conversation is open, AND the hold was witnessed (seen waiting while its conversation had an open decision).** The witness is the race guard: a held result reaches the browser before the queue re-read brings its decision, and without it a brand-new hold flashes "no longer waiting for you" (a mutant dropping the witness reddens 3 tests). Over-holds by design — two holds both wait until both are answered — the same direction reload's name match errs in. Known blind spot: two holds raised at once, first answered before the second's decision reaches the queue → second reads settled until that re-read lands.
- `2026-09-25` (TASK-532) — **Implemented inline, not via per-task subagents:** two small pure helpers + one component wiring, exact spec; briefing would have cost more than the edits.
- `2026-09-25` (TASK-532) — **Supersedes the TASK-517 shard's "Scope held to the reload path" row:** the live panel now settles answered holds too (rule above). That row's description of the live panel is historical, not current.
- `2026-09-25` (TASK-532, review) — **CORRECTION to the "Rule" row above: the live over-hold is NOT "the same direction reload's name match errs in" for different tools.** With two different tools held and one answered, reload settles the answered one (its own id and name are gone) while live keeps it waiting until the other is answered or the thread is re-read. Still the permitted direction; pinned by a unit test ("is COARSER than reload"). **Pairing each hold with its preceding `decisionRaised` frame was re-considered and rejected again, on a firmer ground than ordering:** an identical re-held call (TASK-254) fires NO `decisions:raised`, so a hold can have no frame of its own, and a pairing heuristic would attach it to a neighbour's answered decision and settle a live question — the forbidden direction.
