| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-21 | Resolve the Q2 reviewed SHA through `git rev-parse --disambiguate` before peeling a single object to a commit. | Hex-only input can still name a Git branch or tag. Ref lookup can choose the branch head instead of the reviewed commit; object-database lookup avoids that namespace and rejects ambiguous prefixes. Preserve file transport, the hex whitelist, ancestry validation, and the fallback to `origin/main`. | A prefix check after ordinary ref resolution would reject some mismatches but still let a ref influence resolution of an ambiguous prefix. |
