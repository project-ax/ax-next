## 2026-09-19 — TASK-413 the workspace contract makes every backend prove tenant isolation

- `2026-09-19` (TASK-413) — **Closed the TASK-396 deferral above: `runWorkspaceContract` now REQUIRES tenant isolation, and `MockWorkspace` partitions on `ctx.agentId`.** The ~8-e2e-suite fear that motivated the deferral did not materialise: `@ax/agents` and `@ax/skills` stopped calling `workspace:*` when the `.ax/draft-skills` scan was retired (`agents/src/plugin.ts:124`), and the `@ax/ipc-core` + `@ax/channel-web` suites each use ONE agentId per harness. Zero consumer assertions changed. The only edits outside the contract were to `@ax/workspace-git-server`'s **test-only** adapter, which pinned one workspaceId per plugin instance and ignored `ctx` on every hook — i.e. it was itself a pooled backend, and the new property rejects it. | Rejected: gating the property behind an opt-in flag, which is how a ctx-ignoring backend got waved through in the first place.
- `2026-09-19` (TASK-413) — **Dropped the `workspaceIdFor` override from the git-server contract run and the fixed `boot()` workspaceId from the test-only adapter; the contract mints a unique `agentId` per scenario instead.** Both pins existed to keep scenario histories apart on a shared server, and both did it by switching the partition OFF — so the suite ran against a derivation no host ever loads. Scenario isolation now comes from the real derivation, which means the production run exercises the shipped `workspaceIdFor` end to end for the first time. | Rejected: deleting the test-only contract run (loses a whole backend's coverage over a shim that is two integration tests away from being needed).
