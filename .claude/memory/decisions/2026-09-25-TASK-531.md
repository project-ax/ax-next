# TASK-531 SessionRow delete menu item hover contrast

| Date | Decision | Rationale | Alternatives |
|------|----------|-----------|--------------|
| 2026-09-25 | `hover:bg-destructive/15` → `hover:bg-destructive-soft` on SessionRow's "Delete" menu item | Solid per-theme tint already paired with `text-destructive` on error rows and already measured by `ACCENT_SOFT_PAIRS`: 4.57 L / 4.63 D. Light was 4.08 (confirmed, this file's formula). No new token needed | New `--destructive-menu-hover` token (YAGNI — soft already clears); `/10` (4.44 L, still under); dropping the hover fill (loses affordance) |
| 2026-09-25 | Card premise PARTLY refuted: the "4.22 dark" figure (TASK-511 shard row, "on card") is for `--card` under the tint, but the menu paints its own opaque `bg-background`, pure black in dark → `/15` measured 5.43 there. Only light was ever under the floor | Measured by reading the menu's className; the TASK-511 row can't be edited (memory R1), so this row is the correction | — |
| 2026-09-25 | No focus fill added. Acceptance "hover and focus" is met by the guard, which measures the resting ink AND every `hover:/focus:/focus-visible:/active:` fill the item carries, and refuses any other prefix (`dark:hover:`, `group-hover:`) as unmeasured | The Rename sibling has no focus fill either; adding one to Delete only would split the menu's behaviour. Keyboard focus today shows the ring over the resting 5.14 / 6.17 pair | Add `focus-visible:bg-destructive-soft` (inconsistent with Rename; out of scope) |
| 2026-09-25 | Did NOT fix the confirming-delete row (`bg-destructive/10`): its Cancel (`text-muted-foreground`) measures 4.49 L and its Delete (`text-destructive`) 4.44 L, both under — returned as a follow-up | Outside this card's scope (a different element, and it needs its own source-read guard row). The same swap to solid `bg-destructive-soft` measures Cancel 4.63 / Delete 4.57 light, so the follow-up is likely the same one-class fix. (An earlier draft of this row said 4.13 / 4.25 and "soft does not fix Cancel". A throwaway hsl→rgb helper mishandled hues ≥ 120°. The reviewer caught it, and the theme-contrast formula gives the figures above) | Fold in here |
| 2026-09-25 | Guard reads classes off the component (item's className by `data-testid`, the menu's surface by `session-row-menu `), same pattern as TASK-511's cva reader. Proven red before the fix: light row failed at 4.08, solid-fill row failed at alpha 0.15 | A token-pair row would stay green over a component revert | Hard-coded pair |
