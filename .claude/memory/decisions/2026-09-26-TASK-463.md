| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-463: the "partial check set" (CodeQL-only, no ci.yml run) cause is now MEASURED, not inferred: the PR head CONFLICTED with main. Sweep: every PR head in `code-scanning/analyses` (refs/pull/<n>/head) 2026-08-21..09-26 = 666 shas, vs every ci.yml run (actions/workflows/ci.yml/runs) in the window. 49 no-run heads, ALL `git merge-tree --write-tree` rc=1 against the last first-parent origin/main before the analysis time; 611 with-run heads all clean (607 directly, 3 whose run predates the conflicting main commit by 4-20s and are clean vs its parent, #710 in-flight on recheck); 6 unfetchable. | Replaced "[INFERRED] never diagnosed" in auto-ship + yolo-ship gate docs; gate itself unchanged (still fail-closed on runs=0). Added a no-run diagnosis block (auto-ship Merge queue) printing NO-RUN-CONFLICT / NO-RUN-NOT-CONFLICT / NO-RUN-UNDIAGNOSED, executed by `scripts/__tests__/autoship-no-run-conflict-diagnosis.test.js` against real throwaway git repos. | Card asked for `mergeable` as the first discriminator; used merge-tree instead because mergeable reads UNKNOWN on real conflicts (#620) and merge-tree is local/free. Did not open a deliberate throwaway conflicting PR: the 666-head sweep plus the orchestrator's #620 before/after is stronger evidence than one more sample. |
| 2026-09-26 | TASK-463: the converse (runs=0 on a clean/MERGEABLE head) did NOT occur in the sweep, but is possible by construction: ci.yml's `pull_request` filter is `branches: [main]`, so a PR opened against another base, then retargeted with `gh pr edit --base`, has no run (docs/plans/2026-09-18-dem-first-handoff.md #4). The doc keeps that branch as NOT-CONFLICT → check base / close+reopen, never rebase-push. | A rebase push resolves a conflict and nothing else. | Claim conflict is the only cause (not supported). |
