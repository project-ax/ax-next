## 2026-09-25 — TASK-536 the thread's own re-read keeps a just-answered receipt

- `2026-09-25` (TASK-536) — **Fixed on the CLIENT (`AgentView.load` → `keepAnsweredApprovals` in `lib/workspace-decisions.ts`), not by having the server emit pointers for resolved rows.** The server's `approvalMessages` reads `decisions:list` open-only, and it is right not to invent a receipt: on a fresh page load there is none to show, and the card draws from the client queue anyway. *Alternatives:* widen `decisions:list` status + JUST_RESOLVED_MS on the server (REJECTED — a second copy of the retention rule server-side, invariant 4, and a hook-input change for a client-only concern).
- `2026-09-25` (TASK-536) — **Rule: a pointer the fresh thread read dropped comes back iff the queue still holds its row AND it is open or `isJustResolved`.** Retention is delegated to the queue's rows plus the one shared predicate, so thread and Today cannot disagree. Open rows are kept so the TASK-530 undo race (a read issued while resolved landing after Undo) does not unmount the reopened question in the thread either. Scoped to same agent + same conversationId.
- `2026-09-25` (TASK-536) — **Placement is load-bearing, not cosmetic: the kept pointer goes right after its nearest surviving predecessor.** The transcript keys messages by `${index}:${id}` (`findFieldKey`), so appending the receipt after the turn's continuation changes its index → remount → focus to `<body>`, the same symptom. Anchoring keeps index and DOM node.
- `2026-09-25` (TASK-536) — **Retirement focus: `ThreadApproval` wrapper's layout-effect cleanup returns focus to the transcript when a receipt unmounts with focus inside it; the transcript div now carries `data-consent-region`.** One consent-focus test selected "the first `[data-consent-region]`" in AgentConversation and now names the grants region explicitly. *Alternative:* put the unmount rescue in `ApprovalCard` (REJECTED — also rendered by chat's `InThreadApprovals`; scope).
- `2026-09-25` (TASK-536) — **Implemented inline, not via per-task subagents:** ~60 lines across three files with an exact spec.
