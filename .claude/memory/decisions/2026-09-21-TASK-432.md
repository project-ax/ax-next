| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-21 | Keep the one-shot FILE reader in POSIX sh and require positive completion signals from `head` and `base64` through a separate in-memory pipe. | `set -e` does not expose failures before the last pipeline stage. Descriptor 3 preserves payload stdout; descriptor 4 carries only fixed success markers. Accept either marker order, reject missing markers, and check the final `tr` status. The existing host path rejects nonzero pod exits before parsing partial output. | `pipefail` is not portable to the supported shell; scratch files conflict with the read-only root; buffering raw bytes in a shell variable loses binary fidelity. |
| 2026-09-21 | Preserve the cap-plus-one protocol, confinement, and the host-mounted reader's existing error behavior. | Successful empty, binary, exact-cap, and over-cap reads remain compatible. A failed read is an error, not a valid truncated preview. The shared reader already throws after a partial read followed by EIO. | No size preflight: a second filesystem observation races the read and cannot replace the read's completion status. |
