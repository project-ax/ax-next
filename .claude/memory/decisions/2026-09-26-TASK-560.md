| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-560: `readRegularFile` (bootstrap, agent.md) now uses the same `isAbsentError` (ENOENT or ENOTDIR) as the identity reader `readAxFile`, exported from `compose-identity.ts`, for both its `lstat` and its `readFile` (a file deleted between the two is absent, as in `readAxFile`). Any other errno still throws. | ENOTDIR means a path component is a regular file, so the path cannot exist: "absent" is the true answer. It does not paper over the corrupt tree: bootstrap then tries to create `system/agent.md`, and its own `mkdir` rejects where the file stands, so chat:start still fails closed (test pins it). One predicate means the two readers cannot drift. | Keep the throw and document the asymmetry (rejected: two answers to one question, and the throw carried no information the mkdir failure doesn't). Move `isAbsentError` to a new shared module (rejected: one caller each side; compose-identity imports nothing from bootstrap, so no cycle). |
| 2026-09-26 | TASK-560: implemented inline, no per-task subagents. | ~20 lines of code plus four comment rewrites; a subagent would cost more context than it saves. | Subagent per task. |
| 2026-09-26 | TASK-560: the dated TASK-513 plan doc got a one-sentence "Later: TASK-556…" note rather than a rewrite. | It paraphrased the stale "bootstrap only creates" contract; the TASK-543 lesson is to fix paraphrasing prose too, but a dated plan is a record, so annotate rather than rewrite. | Leave it (a later reader re-derives the old contract); rewrite it (falsifies the record). |
