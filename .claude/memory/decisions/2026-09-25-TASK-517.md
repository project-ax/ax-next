## 2026-09-25 — TASK-517 an attended approval the warm agent never takes up

- `2026-09-25` (TASK-517) — **The card's suggested discriminator (`irreversible:false`) was refuted; the replay markers are the real one.** An `executed` row with the call not made is three shapes: `replayDueAt` set (host deferred replay, undo window), `replayClaimedAt` set (host replay in flight), or NEITHER (the attended path handed it to the warm agent; the yes stands at the gate). An attended *irreversible* call is NOT deferred — `plugin.ts` honours the undo window on the host path only — so keying on `irreversible` would have left attended irreversible approvals receipt-less. `receiptFor` now derives `pending-agent` / `PENDING_AGENT_RECEIPT` for "neither", keeps `null` for the two host-held shapes; `listReceiptCandidates` (SQL + fake store) adds `replay_due_at IS NULL AND replay_claimed_at IS NULL`. *Alternatives:* key on `attendance === 'attended'` (REJECTED — TASK-277: attendance is the opening channel, not the route taken); a new outcome word (REJECTED — same situation as a parked row, same sentence).
- `2026-09-25` (TASK-517) — **Settled holds get a fifth step status, `settled`, reading "— no longer waiting for you", no mark, not counted in the header.** Not `done`: an approval the agent never took up did not run and a declined one never will. Not an outcome-specific word ("approved"): the step cannot see which way it went, and expired decisions were not answered by anyone; the receipt in "What it did" carries the outcome.
- `2026-09-25` (TASK-517) — **A hold is live iff an open decision in the SAME conversation matches its call id OR its (mcp-prefix-stripped) tool name; an unknown read (`failed`/`unavailable`) settles nothing.** The name match exists because TASK-254 collapses an identical re-held call onto the first decision row, which keeps the FIRST call's id — matching on id alone would tell someone a live question was settled. Fails toward "waiting" on every uncertainty. The route now reads `decisions:list` before `buildThread` (it already read it after, for the cards) — one read, both uses.
- `2026-09-25` (TASK-517) — **Scope held to the reload path.** The live SSE panel (`AgentView`) still shows a hold as waiting until the thread is re-read; the durable, "forever" state the walk found is the reload one. "Today: nothing is waiting on you" was already TRUE and is unchanged. The rail counter and the feed now agree on the walk's shape (7 vs 6 → 7/7).
- `2026-09-25` (TASK-517) — **Implemented inline, not via per-task subagents:** two small tasks with an exact spec, where briefing a subagent would have cost more context than the edits.
