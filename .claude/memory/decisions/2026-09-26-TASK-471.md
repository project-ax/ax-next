| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-471: the card was largely already shipped by TASK-468 (#632): the per-role table in yolo-ship Phase 4 (a guest does not mutate at all, which is STRICTER than the card's "restore only paths you mutated + record porcelain") and "if you dispatch a reviewer, commit first" in the code-lane brief. This PR adds only the missing halves: the OWNER's runnable review-window open/close blocks (yolo-ship Phase 5), the reviewer's own guest rule in `.claude/agents/ax-code-reviewer.md` (which said nothing about writes and told it to "run builds/tests yourself"), and one clause in templates.md. | The reviewer never reads yolo-ship Phase 4. Its only standing instructions are its agent definition, so a rule that lives only in the builder's skill reaches the reviewer only if a builder retypes it. | Take the card's weaker per-role rule literally (would loosen TASK-468's); drop `Bash` from the reviewer (loses suite runs); `isolation: "worktree"` for reviewers (fresh tree, reviews the wrong thing); a PreToolUse hook (heavy, not earned). |
| 2026-09-26 | TASK-471: a reviewer that needs a mutation proof uses its OWN detached worktree at the given sha (`worktree add --detach` + install/build, ~25s) and removes it, or reports the mutation for the owner to run. Phase 4's "owner is parked" exception is explicitly NOT a licence during a review. | Makes the reviewer the owner of the tree it mutates, so Phase 4's owner rules apply unchanged; the cost is paid only by a reviewer that wants to mutate. | Builder declares itself parked and lets the reviewer mutate in-tree (only works if the builder really is idle for ~40 min, and a crashed reviewer leaves its mutant behind). |
| 2026-09-26 | TASK-471: enforcement is DETECTION, not prevention, and the doc says so. Open refuses a dirty or untracked tree; close refuses a moved HEAD, any residue, and a sha that is not the full 40 characters. Stated blind spots: a mutant written and restored inside the window, gitignored `dist/`, and a reviewer stash (harmless after a clean open). | "Which direction does this fail in": closed, for everything `status`/HEAD can observe. | — |
| 2026-09-26 | TASK-471: rejected review Nit 4 (add a back-reference in the Phase 4 table saying reviewers are carved out of the "owner is parked" exception). | The reviewer def already says it, the standard review dispatch never asserts "parked", and the Phase 4 table was just edited by sibling TASK-550 — a one-clause cross-reference is not worth a conflict window. | Add the clause (low value, conflict risk). |
