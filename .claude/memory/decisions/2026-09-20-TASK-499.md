# TASK-499 — kind-dev renders the legacy chat shell (the chart never stamped AX_AGENT_WORKSPACE_PREVIEW)

| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-20 | Turn the workspace surface on in `deploy/charts/ax-next/kind-dev-values.yaml` via `channelWeb.agentWorkspace: true`. Production default in `values.yaml` stays `false`. | The dev cluster the manual-acceptance walks install against must serve the surface those walks exist to test. Production-off is a real capability boundary, not cosmetics: with the flag off the k8s preset never registers `/api/workspace/*`, so the routes do not exist on the wire (invariant #5). | (a) Flip the `values.yaml` default — REJECTED, that is the capability boundary. (b) A `host.env` entry in kind-dev-values — REJECTED, `values.yaml` documents that setting both mechanisms stamps the env var twice and the winner depends on template order. (c) A `--set` in each runbook — REJECTED, it is the status quo that lost walk runs; a values file every bring-up path already passes is the only mechanism that cannot be forgotten. |
| 2026-09-20 | Guard it by extending the existing `channelWeb.agentWorkspace` describe block in `deploy/charts/ax-next/__tests__/render.test.ts` with a `-f kind-dev-values.yaml` render, asserted with `toEqual([{name, value:'1'}])`. | The two existing cases (default-off, opt-in-once) both stayed green through the entire window in which no shipped values file exercised the opt-in — so the missing assertion is specifically "the values the walks install with". `toEqual` on the whole list also catches the double-stamp hazard, which the `host.env` block already in kind-dev-values makes one careless line away. | A second test file, or an assertion in `env-shape.test.ts` — REJECTED, the topic already has a home and the card asked to extend, not duplicate. |
| 2026-09-20 | Note the flag in `.claude/skills/k8s-acceptance-loop/SKILL.md` §3 rather than in `deploy/MANUAL-ACCEPTANCE.md`. | Both of the skill's helm blocks already pass `-f kind-dev-values.yaml`, so the hazard is only a bespoke values file; that is the skill's audience. MANUAL-ACCEPTANCE's real-cluster block deliberately says *don't* pass kind-dev-values, and a real cluster is production shape — the flag should stay off there. | Editing `deploy/README.md` too — deferred as scope creep; its three install blocks all pass the values file. |

## Verified, not assumed

Every claim in the card body checked against the tree before implementing:

- `values.yaml:505` `agentWorkspace: false`, and `kind-dev-values.yaml` had **no `channelWeb` block at all**. Confirmed.
- **`presets/k8s/src/index.ts` really is the only reader** — but at **lines 1481–1482**, not the `:1480` the card cites. A repo-wide grep for `AX_AGENT_WORKSPACE_PREVIEW` outside `dist/` returns that one read plus a doc comment in `packages/channel-web/src/lib/conversation-decisions.ts`. Off-by-one in the brief, not a second reader.
- `App.tsx:368` gates on `pathRendersWorkspace() && features.agentWorkspacePreview`. Confirmed.
- `GET /api/features` is public and needs no auth (`routes-workspace.ts:118`), so the runbook's curl check works before any login.

## Mutation check (Bug Fix Policy)

Committed clean, flipped `agentWorkspace: true` → `false` in kind-dev-values (exactly the pre-fix state, since unset == the `false` default), ran `AX_REQUIRE_HELM=1 pnpm --filter @ax/chart-tests test`: **1 failed / 127 passed**. The one failure is the new assertion; the production default-off case stayed green. Restored with `git checkout --` from the index, tree clean.

## Gotcha for the next agent

The port-forward in `k8s-acceptance-loop` §4 is **9090**, not 8080. A `curl` example written above that section has to say so, or it is a runbook line that fails when followed.
