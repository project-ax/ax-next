| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-458: pgvector is REQUIRED on the embedded path; the pg-init hook Job now fails the install (`set -euo pipefail`, `ON_ERROR_STOP=1`, explicit FATAL + a `pg_extension` row check) instead of `|| echo non-fatal`. | Probe: `bitnamilegacy/postgresql:17.6.0-debian-12-r4` (digest 926356130b77…) ships pgvector 0.8.0 and `CREATE EXTENSION vector` works on a running container, so nothing needs an escape hatch. | An opt-out value (`requireVector: false`) — REJECTED: a knob with no user; an operator overriding the image to one without pgvector gets a loud install failure naming the image, which is the point. "Embedded has no dense channel" — moot, it has one. |
| 2026-09-26 | TASK-458: the behaviour proof shells out to the Docker CLI (explicit `DOCKER_HOST`, `AX_REQUIRE_DOCKER=1` in helm-render) rather than testcontainers + `startTestContainer`. | It needs `helm template` output, and helm-render is the only lane with helm AND Docker. testcontainers in `@ax/chart-tests` would pull `@ax/test-harness` (a workspace build) into a lane that never builds. The Docker preflight guards scan `packages/`+`presets/` testcontainers constructors only; the CLI honours `DOCKER_HOST` over contexts, and the gate refuses to run without it. | testcontainers via `startTestContainer` + a build step in helm-render — REJECTED as cost with no extra proof. |
| 2026-09-26 | TASK-458: implemented inline, not via subagents. | Two production-ish files (one template, one test) and doc notes; a subagent round-trip costs more than the change. | subagent-driven per yolo-ship default. |
| 2026-09-26 | TASK-458 review (APPROVE, 5 non-blocking): took #1 (skip `docker pull` when the image is resident + 5s/10s backoff) and #4 (derive the server's app username from values.yaml) as a test-only commit. #2 (an overridden non-pgvector image now fails the next `helm upgrade`) is the intended behaviour and goes in the PR body. #3 (the readiness `until` loop has no deadline; pre-existing) is a follow-up. #5 (the `$(psql …)` failure path aborts via `set -e` with psql's own error, no FATAL prefix) left as is: it is loud, and psql's message is the informative part. | Test-only follow-up leaves the reviewed sha standing under yolo-ship's tests/docs carve-out. | `docker login` in helm-render — not taken: needs a new repo secret, which is a human call. |
