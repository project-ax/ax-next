# TASK-497 measurement method

| Date | Decision | Rationale | Alternatives |
|------|----------|-----------|--------------|
| 2026-09-22 | Pin the existing Strata stratified selector's 100 question IDs and corpus SHA-256 before paid work | Avoid resampling, type-block bias, and choosing a best-of subset; numeric gold answers are explicitly stringified as the existing judge interpolation did | Corpus-order prefix; selecting only answer-bearing sessions |
| 2026-09-22 | Reuse Strata's corpus/date/judge helpers and adapt its answer instructions only for the single statement tool | The model chooses and calls the real catalogued `memory_recall`; no direct-retrieval answer harness or extra answer scaffold | Injecting pre-retrieved evidence; changing the grading rubric |
| 2026-09-22 | Use real preset-selected memory, SQLite, workspace and remote embedding plugins, with a fixed personal-agent/credential control-plane fixture | Matches the existing standalone Strata e2e measurement boundary without claiming HTTP, Kubernetes or NFS latency | Booting unrelated control-plane services for every question; substituting memory implementations |
| 2026-09-22 | Ingest each question's full history once through `chat:end`, then run Sonnet and GLM as read-only answer arms over the same bank | Holds memory input fixed across answerers; answers do not feed back into the observer; shared ingestion spend is counted once in the paid total and separately in each standalone estimate | Re-extracting independently per answer arm; recording answers back into memory |
| 2026-09-22 | Simulate corpus dates sequentially and measure latency with `performance.now`; drain raw service promises before advancing time | Product timeouts can return before remote producers stop spending. Async-local cost attribution and draining prevent late calls from being assigned to the next question/arm | Concurrent global fake clocks; measuring latency with the corpus clock |
| 2026-09-22 | User authorized a $25 total cap and loading the primary checkout's `.env`/`.env.walk`; keys stay in runtime memory | `.env.walk` provides the API keys and existing Vertex ADC/project are available. Persist reservations before requests; retain uncertain charges conservatively across restart | Logging or committing credentials; silently resetting spend on resume |
| 2026-09-22 | Cost reporting distinguishes provider-reported OpenRouter USD from published-rate estimates and uncertain upper bounds | Vertex is priced per characters, Cohere per billed search unit, Anthropic per tokens/cache; missing billing information is never zero. OpenRouter price ceilings bound reservation arithmetic without switching the requested model | Treating token-price floors as actual bills; excluding extraction or retrieval spend |
| 2026-09-22 | Freeze answers before judging and bind resume to the committed tree, input artifact, prompt/model configuration and harness digest | A judge retry must not regenerate a different answer; paid runs refuse dirty trees or mixed run identities | Re-answering on judge failure; combining artifacts from different configurations |
