# Decisions — TASK-530

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | Race is a read ISSUED before Undo landing AFTER it (not a read issued after Undo): server `undoDecision` sets status `pending`, which `decisions:list` does list, so any post-undo read carries the row. Fix: `useDecisionQueue` stamps each click-applied row with `readId.current`; a landing read with id <= that stamp keeps the local copy (listed or not). | Scoped by read generation, so it expires on its own: the next read issued after the click applies normally, and an open row answered elsewhere is still dropped. Also closes the approve-direction twin (stale read listing the row OPEN re-showing buttons over a receipt). | Keep reopened open rows for N seconds (time-based; would show buttons for a question answered elsewhere); suppress refresh while a POST is in flight (misses reads already issued before the click). |
| 2026-09-25 | Overrides TASK-509's "server copy always wins for listed rows" only for reads issued before the click. | A pre-click snapshot of that row is strictly older than the click's own server response. | Leave listed rows server-wins (leaves approve-direction flicker). |
| 2026-09-25 | Implemented inline, no per-task subagents. | One function + one ref + tests; subagent overhead exceeds the work. | subagent-driven-development. |
