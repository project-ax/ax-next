| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | TASK-501: `ActivityFeed`'s whole-feed error branch now fires only when `events` is empty; with rows in hand the error renders beneath them ("could not load the older entries") and the Load more button reads "Try again". Fix is component-only. | `useActivityFeed` already keeps page 1 and leaves `nextBefore` unchanged on a failed page, so retry is free; the component was the only thing discarding rows. | Clearing `error` in the hook on loadMore failure (loses the signal); a toast (not in the design language, detaches the failure from where the missing rows would be). |
| 2026-09-25 | The load-more failure is announced through an always-mounted, empty `aria-live="polite"` region (`data-activity-announcer`) in the rows view; the `Alert` inside passes `role="none"` so it is announced once, not twice. | The reader who pressed Load more gets no other sign it failed (focus on the button, rows unchanged), and a region created + filled in one commit announces inconsistently. Polite, because it answers the reader's own action. | Plain `Alert` role=alert inserted with its content (the exact pattern the card warns about); assertive wrapper (interrupts for a non-urgent fault). |
| 2026-09-25 | Skeleton placeholder: announcement NOT wanted — dropped `role="status"`, kept sr-only "Reading the record…" as plain text + `aria-busy`. | It cannot be pre-mounted (fresh mount is born in that state), the tab/page change that caused it is already reported by the SR, and a spoken wait often lands after the rows. Tests assert via text; a new test asserts no live region exists in the placeholder. | Mount region empty first (not reachable for the fresh-mount case; chatter on every switch). |
| 2026-09-25 | Page-1 failure (no rows) keeps the pre-existing inline `Alert role="alert"` inserted with its content — out of this card's scope; returned as a follow-up. | Card scoped the live-region question to the skeleton; changing every branch's announcement shape is its own design call. | Unifying all feed announcements into one persistent region now. |
| 2026-09-25 | Implemented inline with a plan held in this shard rather than per-task subagents / a docs/plans file. | One component + two test files, ~60 lines; subagent + plan-file overhead exceeds the work. | subagent-driven per plan task. |
