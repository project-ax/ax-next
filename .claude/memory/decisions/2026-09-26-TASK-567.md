| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-567 (human ruling: enforce): `out-of-process-test-timeouts` now fails a package that has a BARE `afterAll`/`afterEach` while its effective `hookTimeout` (config value, or vitest's 10_000 default when omitted) is below the largest DECLARED test budget in the package. Package-wide, like the hook rule. A separate maximum (`maxDeclaredTestTimeout`), not folded into `maxDeclaredHookTimeout`. | A bare teardown runs under `hookTimeout`; the cleanup of a long test lands there. Keeping the maxima separate means an explicitly timed teardown keeps exactly its current rule (the card's acceptance), and the TASK-462 fix that stopped crediting hooks with `it` budgets stays intact. | Fold `it` budgets into the hook maximum (would re-introduce the misattribution and demand every TIMED hook config rise too). Per-file scope (the config is per-package, so package-wide is the unit that governs). Count undeclared tests at `testTimeout` (ruling names declared `it()` budgets; not measured as needed). |
| 2026-09-26 | TASK-567: test budgets are read by `scanTestTimeouts` in the shared `scripts/hook-timeout-scan.mjs`: any call whose callee chain roots at `it`/`test`/`describe`/`suite` (through `.only`, `.skipIf(c)(…)`, `.each(t)(…)`), every argument after the first; functions skipped; literal / resolvable const = budget; object literal → its `timeout` key (spread, computed key, method, or unresolvable value = UNREADABLE); anything else UNREADABLE. `scanHookTimeouts` is unchanged in shape (three keys) and now delegates. | Same fail-closed resolver as hooks, one parser for both. MEASURED on the live tree: the only spellings in packages/presets are numeric literal (56), resolvable const (54), `{ timeout: N }` second arg (38 incl. 4 via `it.skipIf`) — 0 unreadable, 0 parse errors after the widened prefilter. `describe` included because `describe(name, { timeout }, fn)` budgets its tests; 0 occur. | A separate scanner file (two parsers of one file). Only `it` (misses `test.concurrent`, `describe` options). Treat an unresolved identifier at arg1 as a function ref and skip it (fail-open for `it('x', fn, T_IMPORTED)`-style budgets). |
| 2026-09-26 | TASK-567: the rule reddened exactly one package, `presets/k8s` (hookTimeout 120_000 < 180_000 declared by `acceptance.test.ts` / `prod-bootstrap.test.ts`, 6 bare teardowns incl. `afterAll(() => pgContainer.stop())`). Fixed by raising `presets/k8s/vitest.config.ts` hookTimeout to 180_000. | One line covers all six teardowns. Timing the teardowns explicitly at 180_000 would redden the existing hook rule (hookTimeout 120_000 < 180_000 declared by a hook), so it would need the same config raise anyway; timing them at <=120_000 satisfies the letter while leaving the teardown below the test it cleans up after. | Explicit teardown timeouts (6 edits, weaker). |
| 2026-09-26 | TASK-567: did NOT extend the rule to secondary vitest configs (`presets/k8s/vitest.config.k8s-e2e.ts`: hookTimeout 60_000, `{ timeout: 180_000 }` tests, bare `afterAll`) or to `scripts-suite-timeouts`. Returned as follow-ups. | The guard reads only `vitest.config.ts` and that is a pre-existing scope limit of both rules; widening the config reader is its own card. The scripts suite was not named by the ruling. | Widen here (scope creep past the ruling). |
| 2026-09-26 | TASK-567: implemented inline, not via per-task subagents. | One helper + one guard + one config line, tightly coupled failure-direction judgement (same call as TASK-462). | subagent-driven per task. |
