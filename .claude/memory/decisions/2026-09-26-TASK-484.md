# TASK-484 — text-state-quiet guard + the "worst surface" figure

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | Added a separate `--state-quiet is a fill, never ink` describe in `theme-contrast.test.ts` that REUSES the `--ink-ghost` scanner (`filesPainting`/`filesMentioning`/`paints`) rather than refactoring both into a table. | The `--ink-ghost` block's prose is load-bearing and the scanner's anti-vacuity is already proven there; a table refactor would churn a heavily-reviewed block for no new coverage. The new block adds only what is class-specific: premise pin (<4.5 on every quiet-text surface, both themes), usage ban, matcher fixtures on this spelling (`hover:` prefix, `/80` suffix, not `bg-`, not `-quieter`), `bg-state-quiet` liveness, raw-mention backstop pinned to `['index.css']`. | Generalise into a `FILL_ONLY_TOKENS` table (more DRY, bigger diff); ban only (no premise pin — would outlive its reason). |
| 2026-09-26 | Mutation-verified: `text-state-quiet` added to `StatusDot.tsx` className -> 4 red (3 theme bans + backstop); a `// text-state-quiet` comment only -> 1 red (backstop). | Bug Fix Policy: prove the guard reddens. | — |
| 2026-09-26 | CORRECTION to `decisions/2026-09-20-TASK-450.md` line 4 (cannot be edited: memory R1 forbids deleting lines). "1.72:1 light, 1.67:1 dark" for the pre-fix `bg-ink-ghost` dots is the PAGE/CARD value, not the worst. Worst is `--primary-soft` (selected sidebar row): **1.50:1 light / 1.56:1 dark**, measured with the test file's own `contrast()`. Fixed in `index.css`, the test's prose (two sites), `bits.tsx`, `StatusDot.tsx`. | Rule 5: the stale line that generated the card ships fixed in the same PR. | Drop the "worst surface" wording instead of correcting it. |
| 2026-09-26 | Also fixed two adjacent prose errors found while there: `StatusDot.tsx` said "under half the floor" (1.72/3 is 57% — over half); `index.css` listed "every surface a dot lands on" as FIVE, omitting the stale decision row `bg-destructive-soft/50` (3.45 light / 3.30 dark) that the test measures as the sixth. | Same completeness-claim defect class the test file warns about. | Leave as out-of-scope. |
| 2026-09-26 | Card said `--state-quiet` as text is "3.66:1 light / 3.41:1 dark". Light worst is actually 3.33:1 on `--muted` (3.66 is the page). Conclusion unchanged (all under 4.5). | Hypothesis-not-finding: refine, cite. | — |
