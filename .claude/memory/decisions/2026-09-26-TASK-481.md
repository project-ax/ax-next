# Decisions — TASK-481

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | Forward learning's audience is the merged card's **family**: same `epic:` ∪ child (`parent: X` / `**parent:** X` / `Follow-up from X`, line-initial) ∪ X's own queued parent ∪ siblings sharing X's parent ∪ cards **named** by `[TASK-n]` in X's `learnings:` text. Implemented as the `ax-forward-learning: family` block in `auto-ship/references/github-project.md` §4. | Card's option 1 (parent edges) plus a "named" edge: TASK-436's builder addressed a lesson to TASK-431 by name, which is the most precise signal there is and costs nothing. Every edge is an explicit marker, so over-broadcast is bounded by construction; each card is emitted once. X's epic/parent are read from X's own body in `$ITEMS`, removing the hand-typed `EPIC=<slug>` (and its `contains()` prefix match: `epic: foo` used to match `epic: foobar`). Measured marker spellings from 40 dispatched bodies: `parent: TASK-n`, `**parent:** TASK-n`, `Follow-up from TASK-n`, always line 1. | (2) touched-path overlap — fuzzy, over-broadcasts, needs PR file lists at merge time. (3) triage assigns `epic:`/`epic: none` — fixes data not query, highest cost, and still misses the parent/named cases. Could be layered on later. |
| 2026-09-26 | No per-card accrual cap and no dedupe inside `append_learnings`. | The card asks for "bound OR de-duplicate": the query emits each card at most once however many edges match, and edges are explicit, so accrual is limited to real relatives. A cap would silently drop real lessons; helper-side dedupe only matters if the merge step re-runs, which nothing does routinely. | Cap at N bullets with a loud `CAPPED` line; exact-line dedupe in the helper. Revisit if a large sibling set (e.g. a walk with 6+ children like TASK-358) is measured drowning a card. |
| 2026-09-26 | Follow-up cards auto-ship files must start with `parent: <reporting TASK-ID>` (+ inherited `epic:`). | The child/sibling edges only work if the marker exists; no body format was prescribed before, and builders happened to write one of three spellings. | Rely on convention — works today by luck. |
