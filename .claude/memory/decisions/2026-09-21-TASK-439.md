| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-21 | Require a non-blank string caller ID only when a grant destination needs the final caller fallback; throw rather than coerce or invent an identity. | The optional service/account routes can already name a destination without that fallback. Changing a supplied ID would change the vault row; the existing submit handlers catch validation failures without sending the unusable destination. | Do not stringify objects, use an empty/placeholder ID, trim-normalize identifiers, or reject an unused fallback when a usable explicit tag exists. |
| 2026-09-21 | Keep destination grammar and ownership enforcement server-side; the new client check is shape validation, not authorization. | The chat card and workspace row both pass literal user scope to `packages/channel-web/src/lib/credentials.ts`. Those writes send a destination object to the settings route, not the local ref helper's output. `packages/credentials-admin-routes/src/destination-routes.ts` authenticates, validates, computes the ref, and forces user scope plus the authenticated actor as owner. | This finding is scoped to the grant-destination paths, not unrelated raw-ref/admin endpoints, arbitrary ID authorization, or atomicity across multiple slot writes. |
