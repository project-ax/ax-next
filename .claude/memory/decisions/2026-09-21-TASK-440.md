| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-21 | Use one optional shape-checked host value for the chat card's display and grant argument; keep the display fallback out of the action path. | A missing/non-string/blank host has no grant action, but the card remains dismissible with an explanation. Accepted nonblank strings are preserved; full host/session validation remains server-side. | Do not re-read the raw request host on click or manufacture a target from placeholder copy. |
| 2026-09-21 | Keep the fix in the live legacy chat card, reusing the shared host-shape guard without changing it. | At this change's base, Composer still mounted the card, the app retained the chat route, and TASK-360 remained dependency-gated. That future deletion will remove this fix; the workspace row has no separate guarded-label/raw-action split to change here. | Scheduled retirement is not the same as unreachable code; no new shared API is needed. |
| 2026-09-21 | Use a controlled mutation of the test-owned request after render to distinguish the displayed host from a later raw read. | The permission store retains its request by reference. This is a counterfactual regression probe, not evidence that current SSE producers routinely mutate frames or a guarantee of whole-request atomicity. | Ordinary valid-host examples alone cannot distinguish the two implementations while their values agree. |
