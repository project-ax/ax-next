| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | TASK-533: a finished create focuses the new agent's conversation region (`[data-agent-conversation=<id>]` on AgentConversation's `role=region` scroller); Escape/✕ keep the opener restore. `onDone` writes the id to a ref before `setCreateAgentOpen(false)`; the close effect branches on it. | The region is already the designed script-landing spot (tabIndex -1, named "Conversation with X", visible focus ring) and is focusable even while the kickoff turn is running; the composer may not be. | Composer (may be held/disabled mid-kickoff); AgentView h1 (not focusable, would need tabIndex). |
| 2026-09-25 | Target is keyed by agent id, and the opener is NOT a fallback in that list. | The returning workspace paints its old route (possibly another agent's conversation) and the sidebar before the kickoff's send moves the route; `focusFirstWhenReady` takes the first present target, so a bare marker would land on the wrong agent and an opener fallback would always win. Failure direction if the view never appears in 2s: `<body>`, same as the Settings restore. | Opener as second target (always wins — rejected); a window-expiry fallback hook in the shared helper (YAGNI). |
| 2026-09-25 | `focusFirst`/`focusFirstWhenReady` accept `FocusTarget = string | finder fn`; the new-agent target compares the id as a value. | Splicing a server-supplied id into an attribute selector is an escaping hazard; jsdom's nwsapi also threw on a correctly-escaped `\"` string. | Hand-rolled CSS string escaping (tried; jsdom threw). |
| 2026-09-25 | Chat shell untouched. | It never renders `data-new-agent-opener` and its composer has `autoFocus`, which claims the keyboard so the restore stands down; the card's bug is workspace-only. | — |
| 2026-09-25 | Implemented inline rather than per-task subagents. | ~40-line change across 4 files; subagent dispatch overhead exceeds the work. | subagent-driven per plan task. |
