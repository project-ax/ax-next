# TASK-469 — decisions

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-19 | Prove the canary's vacuity **locally**, not in CI | The card allowed "if this can only be proved in CI, say so". It cannot only be proved in CI: Docker was alive, a full mutant round is ~6 s on a warm `postgres:16-alpine`, and the restore is a `git checkout --`. CI would have made each round a ~10 min push cycle and given no per-mutant red counts without reading workflow logs. | Push a temporary mutant branch and read CI; deferring the card again until the daemon was "known good" |
| 2026-09-19 | Prove the daemon alive by **starting a container**, not by `docker version` rc=0 | The brief's own warning: a wedged daemon has been seen answering `docker ps` with rc=0 and empty output while `docker info` reported 39 containers. `docker run --rm alpine:3 echo LIVE` pulled an image and executed — alive/dead/wedged distinguished by doing work, not by an exit code. | Trusting the dispatch brief's `MEASURED-BY-PROBE` claim (the brief explicitly said not to) |
| 2026-09-19 | Mutate **13** claims, not just the card's M4 | "A green canary proves nothing until it has been made to go red on purpose" applies per claim, not per suite. One mutant proves one assertion is load-bearing; it says nothing about the other eleven. The extra twelve rounds cost ~90 s total and found two holes the single mutant would have missed. | Running only the M4 mutant the card named |
| 2026-09-19 | Fix the two surviving mutants with **tests**, not production changes | M2/M3 are mutants of correct code — `revoke` really does hard-code `scope: 'user'` and really does name its owner. The defect is an unasserted claim, not a bug. Touching the store would have been a change with no failing test to justify it. | Rewriting `revoke` defensively; filing the gap as a follow-up card |
| 2026-09-19 | Put the ledger in the **canary's header**, not a `docs/plans/` file | The reader who needs to know this green was proved is the person opening the canary — typically to change it. A plan doc is read once, at authoring time, by someone who already knows. It also puts the numbers next to the assertions they describe, so a later edit that deletes an assertion is visibly deleting a row of evidence. | `docs/plans/2026-09-19-...md`; a `.claude/memory/` shard alone |
| 2026-09-19 | Do **not** commit the mutation runner to the repo | It is ~80 lines of scratch that hard-codes this worktree's absolute path and this one suite's claims. The reproduction recipe (mutate → `npx vitest run <file>` → `git checkout --`) is three lines of prose in the header and does not rot. Committing it would make it a thing to maintain and a thing to mistake for a supported tool. | `packages/tool-policy/scripts/mutate.ts`; a repo-wide mutation-testing dependency (Stryker) |
| 2026-09-19 | Report M1 against the hardened 14-test suite, and M2/M3's survival against the suite as #618 left it | Both numbers are true of different trees and only one of each is meaningful. Quoting M2 as "1 red / 13 pass" without saying it SURVIVED 167/167 before this PR would erase the finding the card exists to surface. | Reporting only post-fix counts; only pre-fix counts |
