# Decisions — TASK-535

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | `DecisionRow` now passes `openNote = notice ?? stale sentence` (same as `ApprovalCard`), and its two open-branch `Alert`s drop shadcn's built-in `role="alert"` via `role={undefined}`. | The Alerts mount only when EXPANDED; collapsed (Today default) had no voice. Routing both states through the one already-mounted region gives one voice either way, and also stops an expanded row that MOUNTS stale from announcing on page load. This was the alternative TASK-473 filed as the follow-up. | `openNote={expanded ? null : sentence}` — rejected: toggling the row would re-announce the stale reason on every collapse (pinned by a test). Keep Alert role + region — two voices when expanded. |
| 2026-09-25 | `openNote` does not depend on `expanded`. | Opening/closing is the person's own disclosure, not news. | Toggle-dependent note (see above). |
| 2026-09-25 | Kept the shared region ASSERTIVE (`role="alert"`) for open-branch notes, though the TASK-275 ruling says "announce politely". | #691 (ApprovalCard) already routes its passive open-branch notes through this assertive region under the same ruling; splitting politeness per branch changes the shared component and ApprovalCard while sibling TASK-536 is in that path. The TASK-275 polite line is the HOLD's arrival (`InThreadApprovals` / workspace announcer), which this card does not touch. Returned as a follow-up for a human call. | Second polite span for open-branch notes (widens scope into ApprovalCard). |
| 2026-09-25 | `consent-focus.test.tsx`'s `visibleAlert()` now finds the focus destination by `tabindex="-1"` + sentence, not by role. | The DecisionRow Alerts no longer carry a role; uniqueness is still pinned. | Class selector on `border-destructive/50` (brittle to styling). |
| 2026-09-25 | Implemented inline, no per-task subagents. | One component edit + test updates. | subagent-driven-development. |
