| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-547: cover a kickoff send slower than `RESTORE_WINDOW_MS` by RE-ARMING the finished-create restore when the route moves: `WorkspaceShell` gains `onKickoffRouted(agentId)`, fired after the kickoff's send settles (success and the failure path, which also navigates); `App.tsx` re-arms `refocusNewAgentViewWhenReady(id)` only for the create its close armed (`kickoffFocusFor`). | The window then starts when the new agent's view can actually paint, so the send's duration no longer matters. A longer window for the create path only moves the cliff. Gating on `kickoffFocusFor` keeps first-run creates (which arm nothing, TASK-510) from gaining a focus move. | Lengthen the window for the create path. Arm the restore inside `WorkspaceShell` (would also fire on first run). Move `onKickoffConsumed` after the send (changes an existing callback's meaning). |
| 2026-09-26 | TASK-547: the re-arm checks `keyboardIsClaimed` up front, in a lib helper (`refocusNewAgentViewWhenReady`) rather than inline in App. | `focusFirstWhenReady`'s immediate try does not check the keyboard (it assumes it runs straight after a close); if the view has already painted when the re-arm runs, it would steal from a person. Measured: the App-level "person moved" test passes with the check removed (in jsdom the view paints after the re-arm starts, so the observer's own check yields) — so the check is pinned by a lib unit test with the target pre-painted, which goes red without it. | Inline guard in App (was untestable). |
| 2026-09-26 | TASK-547: loading pane renamed "Loading agent" on every tab rather than named per tab. | The pane renders before the agent read regardless of tab and says only "Loading…"; one accurate name is simpler and true everywhere. | Per-tab names ("Loading activity", …). |
| 2026-09-26 | TASK-547: implemented inline rather than via per-task subagents. | Three small edits across 4 files; dispatch overhead exceeds the savings. | subagent-driven per task. |
