| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-512: `HookBus.fire`'s subscriber-failure log now goes through `reportSubscriberFailure` (hook-bus.ts): try `ctx.logger.error`; if the logger is missing OR its `error` throws, write one structured JSON line (same `hook_subscriber_failed` msg, the ORIGINAL error) to `process.stderr` via `createLogger`; if that write throws too, swallow. It never throws, so the remaining subscribers always run. | Reverses the earlier deliberate "throwing here beats swallowing" choice: that throw skipped every later subscriber and renamed the error, so it did the opposite of surfacing the failure. The stderr fallback keeps the "don't swallow a real subscriber error" intent without letting diagnostics change control flow. | Silent no-op like `warnQuietly` (erases the original error on a partial ctx, which the card forbids); a `HookBus` constructor `fallbackLogger` seam (new API nobody needs; the test spies on stderr); propagate subscriber errors to the caller (a contract change the card rules out; TASK-514 territory). |
| 2026-09-26 | TASK-512: `fire()`'s contract is unchanged: throws stay isolated and are never returned to the caller. No hook payload changed, so no boundary review is needed. | The card says this is a diagnostics fix, not a contract change. | n/a |
| 2026-09-26 | TASK-512: fallback writes to stderr, not stdout (where `createLogger` writes by default). | It is a last-resort diagnostic from a ctx with no logger. Stderr is the process's error channel and cannot interleave with a stdout protocol stream. | stdout via the default writer. |
| 2026-09-26 | TASK-512: implemented inline, not via subagents: one ~40-line function plus tests in one file. | A subagent round-trip costs more than the change. | subagent-driven per yolo-ship default. |
