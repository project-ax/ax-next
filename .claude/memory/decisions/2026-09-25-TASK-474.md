| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-25 | TASK-474: compact Settings-close restores focus to the nav hamburger (`NAV_TRIGGER_ATTR = data-nav-trigger`), as an ordered SECOND target after `SETTINGS_OPENER_ATTR` | Below `md` the only UserMenu lives in a Radix Sheet that is unmounted while closed, so the opener never returns; the hamburger is on screen, focusable, and the door back to the opener. Opener still wins on desktop (hamburger only renders when compact). Both hamburger render sites (WorkspaceShell navTrigger, AgentView's own) are marked, since the URL-driven route can land on either | Reopen the sheet programmatically (surface nobody asked for); first-focusable-in-main (document-order accident); leave it on `<body>` |
| 2026-09-25 | TASK-474: lifted the #628 observer/yield/window mechanism into generic `lib/focus-when-ready.ts` (`focusFirst(selectors)`, `focusFirstWhenReady(selectors)`, `RESTORE_WINDOW_MS`); `settings-return-focus.ts` keeps its API as thin wrappers | Orchestrator asked for a mechanism TASK-510 (new-agent dialog, also inside the nav sheet on compact) can reuse: it passes its own ordered target list, ending in `[data-nav-trigger]` | Keep it Settings-only (510 would copy it); lift consent-focus's preventScroll too (skipped — two call sites, card marked optional) |
| 2026-09-25 | TASK-474: implemented inline rather than one subagent per task | ~4 small edits in one package; subagent overhead exceeded the work | subagent-driven per plan task |
