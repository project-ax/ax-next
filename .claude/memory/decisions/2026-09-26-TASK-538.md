| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-538: SessionRow's confirm-delete row paints solid `bg-destructive-soft` instead of `bg-destructive/10`. Probe (theme-contrast formulas): light Cancel 4.49→4.63, Delete 4.44→4.57; dark Cancel 5.68→4.61, Delete 5.71→4.63. | Brief's figures confirmed. One per-theme token clears both themes; dark loses headroom but stays over 4.5. Same move as TASK-531's menu hover. | Keep `/10` in dark via `dark:` override (shadcn rule: no manual dark: colours); a stronger alpha (fails the other way on the white page); darken `--muted-foreground` (ripples everywhere). |
| 2026-09-26 | TASK-538: new `controlPaint` wraps `menuItemPaint` and adds the one `hover:text-*` ink; the hover fill is measured against the hover ink. Any other prefixed ink (focus:, dark:) throws. | The Delete control swaps fill AND ink on hover (`bg-destructive` + `text-destructive-foreground`); `menuItemPaint` alone would measure red-on-red (1.0) and false-alarm. Mutant `hover:bg-destructive/15` reddens all three themes (1.39). | Extend `menuItemPaint` itself (changes TASK-531's rows' contract). |
| 2026-09-26 | TASK-538: implemented inline, no per-task subagents. | One class swap + one test section. | subagent-driven-development. |
