| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-555: ipc-core's runner-reported `chat:end` is now bounded per subscriber at `RUNNER_CHAT_END_SUBSCRIBER_TIMEOUT_MS` = 30 s (`packages/ipc-core/src/handlers/event-chat-end.ts`). Supersedes TASK-551's "stays UNBOUNDED" row (decisions/2026-09-26-TASK-551.md), which is not edited (R1). | A hung subscriber registered ahead of the orchestrator's kept the waiter-resolving subscriber from running, so a finished turn became `chat-run-timeout` at chatTimeoutMs. Same 30 s as the orchestrator's `CHAT_EVENT_SUBSCRIBER_TIMEOUT_MS`: the same subscriber set, same healthy cost. Its own constant because ipc-core cannot import a plugin's. | Put a shared constant in @ax/core (kernel would own a chat-specific policy number); make it configurable via ipc-server/ipc-http config (two transports, no caller needs a different value). |
| 2026-09-26 | TASK-555: the test seam is an optional 4th param on `fireEventChatEnd` (default = the constant). The dispatcher calls it with 3 args. | Keeps the public ipc-core index unchanged; the regression test imports the handler by relative path and drives a REAL orchestrator (ipc-core gains a `@ax/chat-orchestrator` devDependency). | Export from index (widens API for tests only); a config plumb through both listeners (YAGNI). |
| 2026-09-26 | TASK-555: preset-k8s acceptance's I1 witness waits for the recorder to see chat:end (`vi.waitFor`), then `handle.shutdown()` (idempotent), then asserts exactly 1 — via `expectChatEndOncePerTurn`. In the git-protocol test it moved to the END of the try (step 4) because it shuts the kernel down. | The card: assert the invariant, not microtask timing. Measured on this branch: with the bound and the old read-on-return check, both acceptance tests fail with `expected [] to have a length of 1 but got +0`; with the helper, 18/18 pass. | Poll-then-assert without shutdown (a late duplicate could land after the assert); key on reqId (ipc-core ctx restamps reqId, so equality with the caller's is not the invariant). |
| 2026-09-26 | TASK-555: the card's inherited claim (from the TASK-551 orchestrator comment) that "the runner's POST never gets its reply" is FALSE — the dispatcher writes the 202 before it fires, detached (`void evt.fire(...)`). Corrected in the orchestrator comment. | Contract rule 5: fix the stale prose that generated the card. | n/a |
| 2026-09-26 | TASK-555: implemented inline, not via subagents. | One production line + two test files + comment fixes; a subagent round-trip costs more than the change (same call as TASK-512/514/551). | subagent-driven per yolo-ship default. |
