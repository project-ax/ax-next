# Decisions — TASK-562 (one copy of the surface-rewriting character class)

| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | The canonical class lives in `packages/core/src/surface-text.ts`, exported ONLY via the subpath `@ax/core/surface-text` (not the `@ax/core` root). | Every consumer (all 8 packages, and TASK-561's @ax/teams) already depends on @ax/core, and `!@ax/core` in eslint's crossPluginImports allows its subpaths (probed: `@ax/core/surface-text` passes, `@ax/teams/foo` errors). The file imports nothing, so channel-web's SPA (`lib/fence-line.ts`) bundles it without the hook bus. Precedent: `@ax/skills/manifest`, `@ax/validator-routine/frontmatter`. One import path makes it obvious for TASK-561. | New shared package `@ax/text-fence` (rejected: new eslint allow-list entry, tsconfig refs, package.json edits in 8 packages for 60 lines); root re-export too (rejected: two spellings of one import). |
| 2026-09-26 | API: non-global `REWRITES_THE_SURFACE` (controls + format) and `HIDDEN_FORMAT_CHARS` (format only), plus `replaceSurfaceRewriters(value, replacement=' ')` and `stripSurfaceRewritersFromDocument(text)`. Global run-regexes stay private. | A shared `/g` regex is only safe with `.replace`; one caller using `.test` would inherit another's `lastIndex`. Functions cover every existing use: label fences (' '), freshness token (''), channel-web fenceBody (keeps TAB/LF/CR). | Export the `/g` regexes (rejected: shared mutable lastIndex). |
| 2026-09-26 | Widened exactly to the card: U+2060-U+2064 and U+2028/U+2029. U+206A-U+206F, U+00AD, U+180E, U+FFF9-FFFB and the tag block U+E0000-E007F are NOT added; returned as a follow-up. | Card scope; tag characters need the `u` flag and astral handling across consumers, a behaviour change worth its own review. | Widen to all Cf now (deferred, not rejected). |
| 2026-09-26 | The two validator scanners (skill, identity) now use `HIDDEN_FORMAT_CHARS`, which adds U+061C, U+FEFF, U+2028/9, U+2060-2064 to their obfuscation check. | They were a narrower copy with the same gap. A leading BOM cannot false-positive: both decode with a strict `TextDecoder`, which consumes it. Controls stay out because a document's newlines are structure. Residual: skills:scan concatenates `input.files[].contents` whose decode path was not traced; a BOM-led helper file would now be annotated (accept-but-annotate, not a block). | Leave validators on their own list (rejected: that is the copy the card is removing). |
| 2026-09-26 | `@ax/agents` keeps exporting `DISPLAY_NAME_FORBIDDEN`, now `= REWRITES_THE_SURFACE` (an alias, not a copy). | admin-routes' zod refine and existing tests use the name; alias keeps the diff small. TASK-561 should import from `@ax/core/surface-text` directly, not from @ax/agents (cross-plugin). | Delete the export (churn with no gain). |
| 2026-09-26 | Guard `scripts/__tests__/surface-text-single-owner.test.js` keys on the bidi controls (U+202A-E, U+2066-9) in any spelling (`\u`, `\u{}`, `0x`, literal) in non-test production JS/TS under packages/ and presets/. | Those code points are the unmistakable anchor of this class. Mutation-proved: re-adding a copy to skill-safety-scan.ts reddens it; dropping U+2060-2064 from the owner reddens 11 core tests. | TS-AST scan (rejected: the anchor spelling is lexical; a regex over lines is sufficient and its blind spots are listed in the header). |
| 2026-09-26 | Implemented inline rather than one subagent per task. | Three small mechanical tasks with a fully known file list; subagent handoff cost would exceed the work. | subagent-driven-development (skipped for size). |
