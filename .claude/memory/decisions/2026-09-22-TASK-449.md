| Date | Decision | Rationale | Alternatives |
| --- | --- | --- | --- |
| 2026-09-22 | The user approved a version-pinned Testcontainers patch for the explicit-endpoint contract. | Requiring DOCKER_HOST did not stop the installed SDK from trying another socket after initialization failed. The opt-in strict path uses only explicit configuration, bypasses cached file configuration, and refuses a differently initialized cached client. Normal non-strict behavior is unchanged. | Do not infer no-fallback behavior from an environment requirement or monkey-patch the SDK at runtime. |
| 2026-09-22 | Reject TCP port 2376 without verified TLS and any certificate path without verification. | The installed modem infers HTTPS on port 2376 and can read certificate settings independently of the higher-level SDK strategy. Filtering only the CLI child environment would leave two transport interpretations. | Explicit configuration is a supported contract, not a promise to accept every Docker environment combination. |
| 2026-09-22 | Test the installed patched SDK selection code against daemon-free strategy/client stubs. | A helper-only test cannot detect the SDK falling back after the helper returns. The fixture separately proves strict rejection, cached-client refusal, and unchanged normal fallback behavior. | These source-execution tests do not claim a live daemon or TLS handshake was exercised; CI remains the healthy integration gate. |
| 2026-09-22 | Validate native socket spellings separately from TCP URLs and preserve their raw path in strict SDK selection. | WHATWG URL parsing removes the named-pipe namespace dot and can normalize Unix paths differently from native resolution. Ambiguous path segments and even empty query/fragment delimiters are rejected instead of silently normalized. | Do not fix only the helper: the SDK must use the same native path interpretation. |
| 2026-09-22 | Reject Node's verification-disable override for verified Docker TLS and explicitly enable verification on the strict SDK HTTPS Agent. | The CLI does not obey Node's global TLS override. Tests must cover the real strategy/Agent configuration below the fake Docker client seam, not only inspect propagated environment variables. | No change to non-strict SDK behavior or local machine TLS settings. |
