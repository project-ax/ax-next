| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-553: a THROWN `workspace:read` of `.ax/IDENTITY.md`/`.ax/SOUL.md` now propagates out of `composeIdentityFromTier` (wrapped as `identity read failed for <path>`, `cause` kept). chat:start therefore seeds NOTHING that turn; the existing subscriber catch logs `memory_strata_bootstrap_failed` with `agentId`, and the next turn (agent.md still absent) re-reads. `found:false` and over-cap stay "absent" → placeholder, unchanged. | Chosen of the card's three options: "proceed without seeding". It is the same posture a hydrate failure already has on this path (hydrate throws → no seed), costs one missing-seed turn, and needs no new state. Bootstrap never rewrites agent.md, so the only safe moment to not-write is before the first write. A throw is also the fastest outcome for TASK-514's 60s chat:start bound. | Retry in-turn (adds latency on the blocking path against the 60s bound, and a persistent outage still needs a fallback); seed the other three files but skip agent.md (needs a new bootstrap input for no user-visible gain — inject tolerates missing seeds for one turn); fail the turn visibly (a memory-seed blip should not break a chat). |
| 2026-09-26 | TASK-553: left the host-FS variant (`composeIdentityFromFiles`/`readAxFile`, CLI path) as-is; returned as a follow-up. | It has the same shape for non-ENOENT errors (EACCES/EIO), but the card names the tier path, the FS read is local and rarely transient, and a faithful regression test needs permission fixtures. | Fix both in one PR. |
| 2026-09-26 | TASK-553: implemented inline, not via subagents: one source function, one comment, one test file. | A subagent round-trip costs more than the change. | subagent-driven per yolo-ship default. |
