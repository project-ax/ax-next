| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-554: MEASURED before any change (in-process git-server on 127.0.0.1 + production `createWorkspaceGitServerPlugin` + real `hydrateAgentTier`, warm mirror, median of 5, macOS laptop): full hydrate 2.34 s at 100 docs, 7.05 s at 300 docs, ~23 ms per pinned `workspace:read`. Confirms the card's ~2.7 s / linear premise. | Each pinned read is its own queued engine op spawning 2 git processes (`cat-file -e` + `cat-file blob`), serialised by the per-workspace queue. | — |
| 2026-09-26 | TASK-554: fix = (a) the git-server engine COALESCES concurrent pinned reads of the same workspace+version into ONE queued op that answers them all with a single `git cat-file --batch`; (b) `hydrateAgentTier` issues the reads after the pin concurrently with a bound, so there is something to coalesce. No hook-surface change. | TASK-513 rejected "parallelise the reads" because the per-workspace queue serialises them. That is true of plain parallelism, which is why the engine must batch what arrives while a queued op is in flight. The queue invariant (one mirror op at a time per workspace) is kept: a batch is one op. A pinned commit id names immutable content, so answering N paths from one `cat-file --batch` is identical to N single reads. A batch of one runs the unchanged single-read code. | New `workspace:read-many` hook: rejected, new hook surface + boundary review + every backend must implement it, for a win available behind the existing `version` field. Let pinned reads bypass the queue and run concurrently: rejected, it breaks the one-op-per-mirror invariant that fetch/apply rely on and still spawns 2 processes per file. Only halve spawns (single `cat-file blob`): still linear-serial, misses the acceptance bar. |
| 2026-09-26 | TASK-554: the hydrate still reads serially until the first FOUND read (that read names the pin), then reads the rest concurrently, all pinned. On any read error it stops pulling new paths, waits for every in-flight read + scratch write to settle, then throws the first error. | Keeps TASK-513's one-snapshot rule exactly (the pin comes from the first found read, never guessed). Waiting before rethrow keeps TASK-556's dispose guarantee: an in-flight `mkdir`/`writeFile` finishing after `rm` would re-create the scratch dir and leak it. | Promise.all over all paths: rejected, it rejects on the first error while other writes are still in flight (the leak above). |
| 2026-09-26 | TASK-554 review round 1 (ax-code-reviewer, 27fe6d48): Minor #1 accepted. git echoes a missing name truncated at a NUL, so a NUL path failed every read in its batch. Fixed two ways: NUL paths skip the batch, and ANY batch reply that does not parse falls back to one read each inside the same queue slot. Nit #3 accepted: the hydrate latches its pin only in the serial phase. Minor #2 (most new tests are equivalence guards that pass unfixed) acknowledged, no change. Each file keeps a discriminating test (spawn counts; max in-flight > 1). | The fallback makes batch == single-read hold by construction for byte patterns we have not enumerated, not just for NUL. Test-first: the NUL test went red before the fix. Both new guards were mutation-checked. | Exclude NUL only: rejected. The reviewer could not rule out other echo divergences. |
| 2026-09-26 | TASK-554 review round 2 (ax-code-reviewer, b878915e): APPROVE. Minor #1 applied (test-only): the pin-latch test now uses more paths than the bound, so its reads assertion discriminates too. Mutation-checked: the reads assertion fails first. Minor #2 (the parse fallback is silent) deferred to a follow-up card instead of fixed here. | The engine has no logger in its options today. Threading one through is a production change that would need another review round, for a signal on a path no known git version reaches. Correctness is unaffected. | Add a log now: rejected for this card, see follow-up. |
| 2026-09-26 | TASK-554: implemented inline rather than via per-task subagents. | Two small, tightly specified changes (one function per package); the spec would be as long as the code, and a same-worktree parallel pair risks colliding builds. | Subagent per task (skill default). |
