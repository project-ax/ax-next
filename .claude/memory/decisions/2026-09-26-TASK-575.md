| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-575: `out-of-process-test-timeouts` scores each VITEST CONFIG, not each package. Configs are discovered from package-root filenames (`vitest.config*.ts`, `vitest.*.config.*`, not `vitest.setup.ts`) UNION every `--config`/`-c` in a package.json vitest script. Each config is charged with the source files its `include`/`exclude` select (vitest `configDefaults` when unset; node `path.matchesGlob`, which handles vitest's extglob default). A file NO readable config claims (a shared helper like `k8s-e2e/helpers.ts`) is charged to EVERY config. | Per-config is the unit vitest actually applies `hookTimeout` at; package-wide scoring would have wrongly charged the main config with e2e files it excludes. Unclaimed files over-read, which is the safe direction for `>=` maxima. | Keep package-wide scoring but take the min hookTimeout across configs (reddens main for tests it never runs). Only read script-named configs (misses a config a human runs by hand). |
| 2026-09-26 | TASK-575: fail CLOSED (reported, never skipped) on: a config that throws / is a function / has no `test`; `test.projects`/`workspace`/`root`/`dir`, top-level `root`, non-empty `includeSource`, non-string-array include/exclude, a `!`-negated include; a script whose config value is not a plain path (`$VAR`), does not exist, or that uses `--root`/`--dir`/`--workspace`/`--project`; an unparseable package.json. | Each moves files in/out of a config in a way the mapping does not model; guessing would fail open. None occurs in the tree. Known gap: a vitest reached through a wrapper script (`node run.mjs`) is not seen. | Model projects/workspace (no instance to test against; YAGNI). |
| 2026-09-26 | TASK-575: the widened rule reddened exactly ONE config, `presets/k8s/vitest.config.k8s-e2e.ts` (hookTimeout 60_000 < 180_000, bare cleanup afterAll); raised to 180_000. MEASURED: it is the only non-`vitest.config.ts` vitest config under packages/ and presets/, and the only script `--config`. The optional scripts-suite bare-teardown rule was added too: green (max test budget 30_000 vs hookTimeout 120_000, 21 bare teardowns, 0 unreadable). | Card acceptance; reviewer's optional item was cheap once `scanTestTimeouts` existed. | Time the afterAll explicitly (the hook rule would then need the same config raise). |
