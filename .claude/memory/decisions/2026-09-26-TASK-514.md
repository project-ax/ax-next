| Date | Decision | Rationale | Alternatives |
|---|---|---|---|
| 2026-09-26 | TASK-514: `HookBus.fire` gains an optional 4th arg `{ subscriberTimeoutMs }` bounding EACH subscriber. Opt-in, caller-chosen; omitted/`Infinity` = unbounded exactly as before. | The firer owns a subscriber hook's latency contract the way a registrar owns a service's `timeoutMs`/`stallWarnMs`, so the knob sits where the bound is decided — same shape as #655's per-hook `stallWarnMs`, no second config mechanism. Opt-in keeps every other fire site (chat:end observers that legitimately run long, etc.) unchanged. | A bus-wide default subscriber timeout (would silently start cutting off every long-running subscriber everywhere); per-`subscribe` option (the subscriber would pick its own bound — the party that hangs is the wrong one to trust with it); one total bound for the whole fire (loses per-plugin attribution). |
| 2026-09-26 | TASK-514: at the bound the subscriber is SKIPPED, not failed: warn `hook_subscriber_timed_out {hook, plugin, timeoutMs}`, discard its late result (a late veto included), run the rest. `fire` still never fails because of a subscriber. | Card preferred proceed-with-warning. Neither chat:start subscriber is load-bearing for turn correctness: agent-activity records a status line; memory-strata seeds the memory tree (idempotent, retried next turn). Failing the turn would make a slow observer a user-facing error. | Fail the turn with a chat:turn-error (different product; nothing today justifies it). |
| 2026-09-26 | TASK-514 (Invariant 5): a timed-out subscriber is abandoned, not cancelled — no cancellation exists and AgentContext has no abort signal. Guarantee is narrower: after the bound its RESULT has no say over the fire (it can still mutate the payload in place — documented, not frozen); a late throw is still reported (`hook_subscriber_failed`, `timedOut: true`); its stall watch stays armed so `_stalled` without `_slow` still means "never finished". Its side effects (and a per-turn pending-task leak for a subscriber that hangs every turn) remain possible. | Honest about what JS allows; adding an AbortSignal to AgentContext is a wider surface change for a hypothetical need. | Thread an AbortSignal through ctx (follow-up if a subscriber ever needs to stop work). |
| 2026-09-26 | TASK-514: chat:start bound = `CHAT_START_SUBSCRIBER_TIMEOUT_MS` = 60 s, overridable by orchestrator config `chatStartSubscriberTimeoutMs` (validated at init). | 4x the 15 s stall watch, so a slow subscriber names itself with 45 s of runway; well above memory-strata's healthy bootstrap. Config seam lets tests run in ms. | 30 s (tight for a cold tier hydrate); 120 s (matches service default but doubles the worst-case silent wait). |
| 2026-09-26 | TASK-514: the timeout timer is `unref`'d, like `withTimeout`. | Consistency; a bound must not keep an otherwise-finished process alive. | Ref'd timer. |
| 2026-09-26 | TASK-514: supersedes TASK-498's note (decisions/2026-09-20-TASK-498.md) that chat:start subscribers have no clock. Not edited there (R1 forbids deleting memory lines); the code comments that said so were corrected in this PR. | Contract rule 5. | n/a |
| 2026-09-26 | TASK-514: implemented inline, not via subagents (two small files + tests). | Same call as TASK-512; a subagent round-trip costs more than the change. | subagent-driven per yolo-ship default. |
| 2026-09-26 | TASK-514: review round 2 Nit (orchestrator comment says "two `workspace:apply`s"; steady state is one, two only on a CAS parent-mismatch retry) left as-is. | Conservative direction and the unbounded-sum argument holds on the N reads alone; editing it would put an unreviewed production-file commit after the approved sha. | Reword to "one (two on a CAS retry)". |
