# TASK-519 decisions

| Date | Decision | Rationale | Alternatives considered |
|------|----------|-----------|------------------------|
| 2026-09-25 | `memory:recall` active reads hide a RE-MENTION: an active slot row whose value (case/space/trailing-punct normalized) matches a REPLACED row of the same `(about, slot)` chain while a higher-provenance row is active. A genuinely new value under an old human row stays visible; a FORGOTTEN value doesn't count as replaced; history shows everything | Walk: correct Portland→Seattle, say "Portland" in chat → recall returned Portland as current `[FACT]` beside the human `[UNKNOWN]`. Owner chose re-mention-only after the first fix ("human always wins", same as the profile) broke the canary's genuine-move case (old human Paris, then "moved to Coimbra" must surface) | Human-always-wins (hides real moves said only in chat); show both + tag provenance and let the model decide (model got it right only because the injected block tagged provenance); changing the engine contract (it deliberately returns both) |
| 2026-09-25 | The re-mention check reads each subject's slot chains WHOLE (closed rows included, one engine call per subject, limit 200), not just the retrieved pool | The replaced row and the correction can both sit outside the pool while the stale re-mention ranks inside (test: limit 1 on a query naming the stale value) | Pool-only comparison (fails the limit-1 case) |
| 2026-09-25 | kind memory walk uses an in-cluster kernel-nfsd pod (`deploy/kind/memory-nfs/`) behind a Service with a PINNED ClusterIP, named by IP in `kind-memory-values.yaml`; chart and `presets/k8s` untouched | kubelet mounts `nfs:` from the node, which can't resolve cluster DNS; the OrbStack kernel has `nfsd` built in. A chart test pins overlay IP == Service clusterIP | NFS on the macOS host (needs sudo /etc/exports); nfs-ganesha image (third-party image) |
| 2026-09-25 | The dev NFS export admits ONLY the pod's default gateway (the node's pod-network address), not `*` | kind runs without NetworkPolicy; with `*` any runner could speak NFS to the Service and write every agent's export, skipping its read-only mount. Verified: privileged pod mount → Permission denied, kubelet mount works | `*` (the original — security-checklist caught it); hostIP via downward API (kubelet's mount arrives from 10.244.0.1, not InternalIP) |
| 2026-09-25 | Findings needing a design call filed to Backlog (TASK-523..529), not To Do | Bootstrap-vs-Rules, Vertex auth source, a memory-status surface and an app-wide token change are human calls; To Do would hand them to auto-ship | To Do (auto-ship would decide them) |
