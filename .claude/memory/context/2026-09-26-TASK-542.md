- `2026-09-26` — **The workspace thread streams the post-approval continuation (TASK-542).** `lib/continuation-actions.ts` is the ONE seam for both surfaces: `continueApprovedTurn(decision, streamReqId)` is every decision queue's `onDecisionApproved` (chat's `conversation-decisions.ts`, the workspace's `WorkspaceShell`), and a thread registers `registerResume(resume, openConversation)` — which returns a disposer that only clears its own registration. `AgentView` registers once per mount and streams the staged id through its ordinary `streamFrom` (no user bubble). Approving from Today, with no `AgentView` mounted, attaches nothing and warns nothing. Regression test: `components/workspace/__tests__/WorkspaceShellApproveContinuation.test.tsx` (real shell + queue + view, wire mocked).
