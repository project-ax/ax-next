- 2026-09-26 (TASK-563): the embedded-postgres pg-init hook Job is bounded by `postgres.embedded.init.readyTimeoutSeconds` (300) and `activeDeadlineSeconds` (600, must be larger — template fails otherwise). On a slow cluster (cold image pull of bitnamilegacy/postgresql) raise both, and remember helm's own `--timeout` (default 5m) is a separate, earlier bound on how long helm waits for the hook. Proofs: `deploy/charts/ax-next/__tests__/pgvector-bootstrap.test.ts` (render + a Docker run with no server that must exit 1 with the FATAL inside the bound).
- 2026-09-26 (TASK-563, correction to the row above): the shipped readyTimeoutSeconds default is **240**, not 300 — deliberately under helm's default 5m `--timeout`. `postgres.embedded.init` has `additionalProperties: false` in values.schema.json.
