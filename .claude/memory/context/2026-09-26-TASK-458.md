- 2026-09-26 (TASK-458): the chart's embedded postgres image `bitnamilegacy/postgresql:17.6.0-debian-12-r4` SHIPS pgvector 0.8.0 (measured). The pg-init Job fails the install if it cannot enable it. `deploy/charts/ax-next/__tests__/pgvector-bootstrap.test.ts` runs the rendered Job script against the chart image (must pass) and `postgres:16-alpine` (must fail) in CI's helm-render lane only (needs helm + Docker; `AX_REQUIRE_DOCKER=1`). The repo's `PostgreSqlContainer` lanes are still `postgres:16-alpine` with NO vector — a dense-channel test (TASK-457) needs a pgvector image there (e.g. `pgvector/pgvector:pg17`, added to the ci.yml pre-pull list).
