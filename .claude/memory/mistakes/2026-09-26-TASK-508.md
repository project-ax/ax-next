# TASK-508 — mistakes

- `2026-09-26` (TASK-508) — **A runnable block that fails closed does not protect the agents
  who hand-type its one-line summary.** The mutation-restore block already refused a staged
  mutant (its post-restore `git status` check, pinned by a test), yet TASK-479 still lost 2 of
  9 mutant counts to exactly that shape — because the prose rule, the owner table row and the
  dispatch bullet all showed the BARE `git checkout -- <path>`, and a builder types what the
  prompt shows. When a guarded block exists, the one-liner the prose quotes must be the SAFE
  spelling, and a text check should pin that (here: the dispatch bullet may not contain
  `git checkout -- ` at all). Restore mutants with `git checkout HEAD -- <path>` and assert
  `git status --short` empty between mutants.
