# TASK-471 — mistakes to avoid (2026-09-26)

- **A shape check in front of an equality check was correctness-vacuous, and only mutation showed it.** The review-window close block checks the pasted sha is 40 lowercase hex characters and then that it equals HEAD. Deleting the length check left all 31 tests green, because a prefix can never equal HEAD and the equality check refuses it anyway. **Fix:** such a check exists for its *diagnosis*, not its verdict, so assert the message ("40-character", "is not a sha", and NOT "HEAD moved"). Otherwise the check is untested.
