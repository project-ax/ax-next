## 2026-09-26 — TASK-550 — SUPERSEDES "the CI runner has no zsh" in `mistakes/2026-09-19-TASK-468.md`

- `2026-09-26` (TASK-550) — **The CI test job HAS zsh, and has since 2026-09-20.** The
  TASK-468 shard's bullet *"The CI runner has no zsh … every `zsh` case in
  `scripts/__tests__/` is therefore a local-only result"* was true on 2026-09-19 and went
  stale the next day: TASK-506 (#657) found the same gap from the other side and added
  `sudo apt-get install -y -qq zsh` to the `test` job in `.github/workflows/ci.yml`. The
  runner *image* still ships none; the job installs it. MEASURED from PR #710's CI log
  (run 36226944450): `Setting up zsh (5.9-6ubuntu2)`, then `docker-preflight.test.ts` 40
  tests (it loops over `['bash', 'zsh']` unconditionally, so it cannot pass without zsh) and
  `mutation-restore-protocol.test.js` 70 tests. The claim had also been copied into yolo-ship
  Phase 4, where builders read it as procedure; both it and the test-file header are
  corrected, and `mutation-restore-protocol.test.js` now asserts `SHELLS` is
  `['bash', 'zsh']` whenever `CI` is set (mutant `HAS_ZSH = false` under `CI=1` reddens it).
  Lesson: a MEASURED environment fact carries the date of the measurement and goes stale
  when someone fixes the environment. Pin it with a `CI` presence assertion, which fails the
  day it stops being true, rather than with prose, which does not.
