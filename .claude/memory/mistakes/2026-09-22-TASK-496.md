# TASK-496 mistakes

| Date | Mistake | Cost | Prevention |
|------|---------|------|------------|
| 2026-09-22 | Added a second top-level `memory:` key in values.yaml (the chart already had `memory.orchestratorModel` for Strata) — js-yaml threw and `render.test.ts` went red | One failing chart suite run | Before adding a top-level values key, `grep -n '^<key>:' values.yaml`; YAML duplicate keys fail at parse, not at helm lint |
| 2026-09-22 | Asserted `memVol.nfs.readOnly === true` in the canary — our pod-spec builder never sets `nfs.readOnly` (the k8s `NFSVolumeSource` HAS the field; we just don't populate it). Read-only lives on `volumeMount.readOnly`, which the builder enforces for `role:'memory'` | One canary red cycle + a wrong API claim in the first shard draft | Check what OUR builder emits before asserting on produced objects; "the field doesn't exist" and "we don't set it" are different claims |
| 2026-09-22 | Invented a 180_000 canary beforeAll budget, then inflated package `hookTimeout` to 200_000 to satisfy the timeout guard — review flagged it: agreed budgets are 60_000/120_000 with beforeAll at 120_000 | One review round-trip | Use the agreed test budgets (preset-k8s: 60k/120k) and report an actual timeout if one occurs instead of pre-inflating |
| 2026-09-22 | Left `eventsNamed` imported-but-unused in rules.test.ts and `let`/`bobAgentId` lint failures in canary — caught at the lint gate, not before | Trivial, but an extra pass | Run eslint on new files before the test gate, or keep the lint step first in the gate sequence |
