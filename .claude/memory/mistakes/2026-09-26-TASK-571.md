# Mistakes — TASK-571

| Date | Mistake | Lesson |
|---|---|---|
| 2026-09-26 | channel-web typed a real backend route (`/admin/teams`) with a type from `mock/`, and every test fixture used that mock shape — so the SPA, its mock and its tests agreed with each other and not with the server, and a page that threw on the first real team shipped green. | A SPA wire type for a real route comes from the owning plugin (`import type`), never from `mock/`. A fixture that needs `as unknown as T` is one the compiler cannot check — treat the cast as a smell. Other `mock/`-sourced SPA types remain (`mock/agents` `Agent`), and the mock admin middlewares still listen on `/api/admin/*` while the SPA calls `/admin/*`. |
